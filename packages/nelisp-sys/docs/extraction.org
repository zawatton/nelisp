#+TITLE: nelisp-sys extraction plan and criteria
#+OPTIONS: toc:2

* Purpose

=nelisp-sys= lives in-tree (=packages/nelisp-sys/=) as a nursery while the
semantics stabilize, but is written to be extractable from day one (Doc 130).

* Extraction criteria (Doc 130)

Do not split into a separate repository until all of these hold:

1. README explains usage without referencing unrelated NeLisp internals.
2. =nelisp-sys-compile= can compile at least one source file to an object.
3. At least one C ABI function can be called from a C harness.
4. Struct layout tests pass for at least linux/x86_64.
5. The adapter has fewer than ~20 public adapter calls.
6. No package file except =nelisp-sys-adapter-nelisp.el= calls private
   compiler/linker functions.
7. Package tests can be run as a named subset.
8. The language and ABI docs are package-local.
9. The memory-safety docs are package-local.
10. Safe code rejects raw pointer deref outside =sys:unsafe=.
11. Move-only resources reject use-after-move and double-consumption.
12. Lexical borrow tests pass for shared/mutable borrow MVP.
13. =:alloc none= tests reject heap allocation.
14. The build flow can be described as a CLI contract.
15. The desired release cadence differs from NeLisp core.

* Current status (2026-05-28)

All 15 extraction criteria hold:

| # | Criterion | Evidence |
|---+-----------+----------|
| 1 | README without unrelated internals | =README.org= |
| 2 | compile a source to an object | =nelisp-sys-compile-object=; backend e2e test emits =nl_add.o= |
| 3 | a C ABI fn callable from a C harness | backend e2e: C harness returns 42 |
| 4 | struct layout tests (linux/x86_64) | =nelisp-sys-abi-layout-test= |
| 5 | adapter < ~20 public calls | rehearsal: 4 public adapter calls |
| 6 | only the adapter calls private backend | =nelisp-sys-extraction-test= (no leaks) |
| 7 | tests runnable as a named subset | =packages/nelisp-sys/test/nelisp-sys*-test.el= glob |
| 8 | language + ABI docs package-local | =docs/language.org=, =docs/abi.org= |
| 9 | memory-safety docs package-local | =docs/memory-safety.org= |
| 10 | safe code rejects raw deref outside unsafe | =nelisp-sys-unsafe-test= (E-SYS-UNSAFE-001) |
| 11 | move-only rejects use-after-move/double | =nelisp-sys-ownership-test= (E-SYS-OWN-001) |
| 12 | lexical borrow tests pass | =nelisp-sys-borrow-test= (E-SYS-BORROW-001/002) |
| 13 | =:alloc none= rejects heap alloc | =nelisp-sys-unsafe-test= (E-SYS-ALLOC-001) |
| 14 | build flow as a CLI contract | =docs/cli.org= |
| 15 | release cadence differs from core | =nelisp-sys-version= independent of NeLisp |

The package is extraction-ready; it stays in-tree as the nursery until a
separate release cadence is actually desired.

* Split rehearsal

Stage 130.7 scripts a copy into =target/nelisp-sys-extract/= and verifies
that package tests depend only on the adapter contract, producing an
extraction report listing remaining NeLisp-private imports.

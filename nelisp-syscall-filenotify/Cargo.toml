[package]
name = "nelisp-syscall-filenotify"
version = "0.1.0"
edition = "2021"
description = "Optional file-notify syscall extension crate for NeLisp"
license = "GPL-3.0-or-later"

[lib]
name = "nelisp_syscall_filenotify"
path = "src/lib.rs"

[dependencies]
libc = "0.2"

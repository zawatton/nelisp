#+TITLE: Doc 49 — Rust-min core / self-hosted Elisp native bootstrap
#+AUTHOR: zawatton + Codex
#+DATE: 2026-04-30
#+STATE: LOCKED-2026-04-30
#+STATUS: Rust は OS ABI + executable-memory launcher に限定し、reader /
#+STATUS: evaluator / macro / compiler / stdlib loader は native-compiled
#+STATUS: Elisp seed が担当する。Phase 49.1 core-min gate と Phase 49.2
#+STATUS: =exec-bytes= dev bridge extraction は実行済み。

* §0 TL;DR

NeLisp の最終形は「Rust VM」ではない。Rust は Elisp を解釈しない。
Rust runtime は OS が Lisp へ直接渡せない境界だけを持つ。

#+begin_src
[build/install time]
  lisp/*.el + src/nelisp-cc*.el
      -> AOT native bytes / seed image

[runtime]
  Rust min core
      -> mmap seed
      -> mprotect RX / clear icache
      -> jump into native-compiled Elisp

[after bootstrap]
  native-compiled Elisp
      -> read / macroexpand / eval / compile / load more Elisp
#+end_src

この doc の方針:

| layer | owner | allowed contents |
|-------+-------+------------------|
| Rust min core | Rust | syscall shim, mmap/mprotect/munmap, JIT icache, process entry, seed loader |
| Build tool | Rust, temporary | seed generation only; reader/evaluator may exist but runtime dependencyにしない |
| Elisp seed | native Elisp | reader, evaluator, macro, compiler, loader, stdlib |
| Packages | Elisp/native extension | sqlite, filenotify, high-level process/file APIs, MCP/anvil |

* §1 Locked Decisions

** D1. Rust runtime は Elisp interpreter にならない

Rust runtime が Elisp AST / special form / macro semantics を理解し始めると、
Rust core は増殖する。よって runtime crate に evaluator / reader / compiler
を戻さない。これらは =build-tool= か native-compiled Elisp seed 側に置く。

** D2. runtime に残す Rust は「Elisp 不可能部分」のみ

残すもの:

- process entry (=main=)
- raw file descriptor syscall minimum (=read/write/open/close/stat/getenv/setenv=)
- executable memory (=mmap/mprotect/munmap/MAP_JIT=)
- instruction cache flush
- seed image loader
- panic/errno boundary

外すもの:

- SQLite
- filenotify
- high-level process helpers
- directory/file convenience API
- MCP/anvil registries
- Rust evaluator / reader / builtin stdlib
- dev-only =exec-bytes= bridge

** D3. =--no-default-features= が Rust min core gate

=nelisp-runtime --no-default-features= を「core-min」と定義する。default build
は互換性のため拡張 syscall を含んでよいが、core-min が壊れたら Rust core
最小化の regression と扱う。

** D4. =exec-bytes= は runtime core ではなく dev bridge

Doc 48 Phase 7.5.4 の =exec-bytes [ARG1..ARG6]= は native compiler の実機
smoke として必要。ただし final runtime core の責務ではない。次段階で
=nelisp-runtime-cli= か =build-tool= へ移す。

* §2 Current LOC Snapshot

2026-04-30 時点の =nelisp-runtime/src= source LOC:

| component | LOC | core-min? |
|-----------+-----+-----------|
| =syscall/unix.rs= | 251 | yes |
| =syscall/error.rs= | 51 | yes |
| =syscall/mod.rs= | 234 | yes, but re-export surface can shrink |
| =lib.rs= | 99 | yes |
| =main.rs= | 285 | dev bridge mixed in |
| =sqlite.rs= | 623 | no |
| =syscall/process.rs= | 883 | no |
| =syscall/fileio.rs= | 598 | no |
| =syscall/filenotify.rs= | 281 | no |
| *total* | *3305* | |

Core-min target:

- short term: ~900 LOC (=--no-default-features=)
- after moving =exec-bytes= out: ~600-700 LOC
- after seed loader lands: expected healthy floor ~800-1200 LOC

* §3 Execution Plan

** Phase 49.1 — core-min gate (this doc execution)

Add explicit make targets:

- =make runtime-core-min=
- =make runtime-core-min-test=

Acceptance:

- =cargo build --release --no-default-features -p nelisp-runtime= passes
- =cargo test --no-default-features -p nelisp-runtime= passes
- optional-feature integration tests compile only when their feature exists

** Phase 49.2 — dev bridge extraction

Move =exec-bytes= and its tests from =nelisp-runtime/src/main.rs= to either:

- =nelisp-runtime-cli= crate, or
- =build-tool= binary subcommand.

Runtime library keeps only reusable JIT page helpers needed by seed loader.

*SHIPPED-2026-04-30*: =nelisp-runtime-cli= crate を追加し、binary
=nelisp-exec-bytes= へ raw native-code subprocess bridge を移動した。
=nelisp-runtime= binary は =--syscall-smoke= / =--version= のみに戻した。
=nelisp-cc-runtime--exec-real= は
=nelisp-cc-runtime--locate-exec-bytes-bin= 経由で新 bridge を呼ぶ。

** Phase 49.3 — extension syscall extraction

Move optional surfaces out of core crate:

- =nelisp-syscall-fileio=
- =nelisp-syscall-process=
- =nelisp-syscall-filenotify=
- =nelisp-sqlite-rs=

The current feature gates remain as compatibility shims until packages switch.

*IN-PROGRESS-2026-04-30*:
- SQLite FFI moved to sibling crate =nelisp-sqlite-rs=.
- =nelisp-runtime= keeps the =sqlite-ffi= feature as a default-ON
  compatibility shim and re-exports =nl_sqlite_*= from the extension
  crate.
- =runtime-core-min= remains independent of SQLite because
  =nelisp-sqlite-rs= is only pulled by the optional =sqlite-ffi=
  feature.
- Shared syscall ABI type =NelispStat= moved to =nelisp-syscall-types=.
- File I/O FFI moved to sibling crate =nelisp-syscall-fileio=.
  =nelisp-runtime= keeps the =fileio-syscalls= feature as a default-ON
  compatibility shim and re-exports =nl_syscall_*= from the extension
  crate.
- File-notify FFI moved to sibling crate =nelisp-syscall-filenotify=.
  =nelisp-runtime= keeps the =filenotify-syscalls= feature as a
  default-ON compatibility shim and re-exports =nl_filenotify_*= from
  the extension crate.
- Process/subprocess FFI moved to sibling crate
  =nelisp-syscall-process=.  =nelisp-runtime= keeps the
  =process-syscalls= feature as a default-ON compatibility shim and
  re-exports fork/exec/pipe/wait/rlimit/select wrappers from the
  extension crate.

** Phase 49.4 — seed loader

Add the small image/native seed launcher to Rust min core:

- validate seed header/hash
- mmap native code/data pages
- apply minimal relocations if still needed
- clear icache
- call seed entry with syscall table pointer

The seed entry is native-compiled Elisp. It owns reader/eval/compiler/load.

*SHIPPED-2026-04-30*: =nelisp-runtime/src/seed.rs= を追加し、
=NLSEED\0\x01= native seed ABI v1 を固定した。loader は header /
syscall ABI / payload hash を検証し、heap/code segment を mmap、code を
RX 化、relocation table を適用、icache clear 後に seed entry へ jump
する。entry ABI は
=extern "C" fn(argc, argv, syscall_table) -> int=。Rust 側は Elisp を
parse/eval しない。

=nelisp-runtime --seed-image IMAGE [ARGS...]= も追加した。unit test は
header/hash validation に加え、x86_64 で実際に mmap した native code
(=return 42=) に jump する smoke を含む。

* §4 Non-goals

- Rewriting Elisp evaluator in Rust
- Adding more Rust builtins
- Moving package logic into Rust for speed
- Making Rust parse =.el= files at runtime

* §5 Risk Controls

| risk | control |
|------|---------|
| optional features silently become core dependencies | =runtime-core-min-test= gate |
| dev bridge mistaken for runtime ABI | Phase 49.2 extraction |
| seed loader grows into VM | loader can validate/map/jump only |
| packages need process/sqlite/notify | extension crates, not core |
| bootstrap cannot self-host yet | build-tool remains build-time-only escape hatch |

* §6 Done Criteria

Doc 49 is considered started, not complete, when:

- this design doc exists
- core-min make targets exist
- =cargo test --no-default-features -p nelisp-runtime= passes

Doc 49 is complete when:

- runtime Rust core has no reader/evaluator/compiler
- runtime Rust core has no sqlite/process/filenotify/high-level file API
- runtime starts from native-compiled Elisp seed

* §7 Wave 7 — NeLisp as build host (partial, BLOCKER deferred)
:PROPERTIES:
:STATUS: SHIPPED-partial / BLOCKER deferred
:DATE: 2026-05-22
:END:

** Goal

Replace =build.rs= の =emacs --batch -l scripts/compile-elisp-objects.el
-f compile-elisp-objects-emit-all= 起動を =target/release/nelisp --batch=
で代替する。これにより Doc 49 の最終ゴールである「build-time にも host
Emacs を要求しない」状態に一歩近付く。

** SHIPPED part (commits c7302830 / 449d2b96 / 9c97fd47)

- =lisp/nelisp-build-host.el= (187 LOC) — =noninteractive= /
  =load-path= / =process-environment= の defvar、=getenv/setenv= /
  =add-to-list= / =make-directory= / =system-name= の Emacs compat
  stub。
- =lisp/nelisp-cli.el= 拡張 (+224/-9 LOC) — =nelisp--cli-batch-dispatch=
  が =--batch / -Q / -L / -l / -f / --eval / --setenv= を左右順に処理、
  =nelisp--cli-batch-ensure-host= が =-L= 後に build-host を遅延 load
  する。
- W7.6a/b 最適化 (=emit-call= / =emit-extern-call= trivial-suffix
  optimization)。
- cargo build / cargo test 10/10 / make compile すべて green、Rust LOC
  変更なし。

** BLOCKER

=target/release/nelisp --batch -L lisp -l scripts/compile-elisp-objects.el
-f compile-elisp-objects-emit-all= が 600s timeout で 0 個 .o ファイル
出力。

*** 真因 (2026-05-22 実測)

| Test | 内容                                  | Time   | per-form |
|------+---------------------------------------+--------+----------|
| T0   | 起動のみ                              | 0.376s | —        |
| T1   | 10 trivial defun (toplevel)           | 2.52s  | 215ms    |
| T2   | 100 trivial defun                     | 21.72s | 213ms    |
| T4   | 100 trivial defconst                  | 21.85s | 215ms    |
| T5   | 10 defun × 50 body form               | 82.09s | 159ms    |
| T6   | 1000 toplevel =(+ 1 1)=               | 142s   | 141ms    |
| T7   | progn で 1000 =(+ 1 1)= 包む           | 142s   | 142ms    |
| T8   | while ループ内 1000 反復 (3000 sub-eval)| 2.29s  | 0.6ms    |
| T9   | =(if nil ...)= で 1000 form を skip    | 142s   | 141ms    |

決定的観察:

- T9 が T7 と同コスト → eval 実行中であろうとなかろうと =1 form parse
  あたり ~140ms= 払う。**ボトルネックは reader/parser ではなく、=load=
  の =(while cur (eval (car cur)) ...)= が呼ぶ =eval= builtin の per-call
  コスト全体**。
- T8 と T7 の 200x 差 (0.6ms vs 142ms) は =while= body が既に深い call
  stack 内で動くため。表面的 quick win ではない。
- per-form コストは eval 経由ディスパッチ 10 段の累積:
  : Phase 47 progn body
  :   → extern-call nelisp_eval_call (FFI)
  :     → Rust eval()
  :       → eval_inner_call (Phase 47 native, tag dispatch)
  :         → nl_eval_inner_cons (Rust extern)
  :           → apply_special / funcall + delegate gate
  :             → elisp nelisp--apply-fn (cond + predicates)
  :               → nelisp--apply-lambda (elisp)
  :                 → lambda body eval (再帰)
- =nelisp-phase47-compiler.el= は 5463 行 / 103 defun + 多数 defconst +
  各 defun に 50-200 body form。total form 数 = 数千 〜 数万。140ms ×
  10000 = ~30 分 (実測タイムアウト推定と一致)。

** 短期 quick win 候補の検証結果 (すべて却下)

*** Option A: nelisp-phase47-compiler.el 自身を manifest 経由 .o 化

却下。Phase 47 grammar は =seq= / =let= (compile-time const のみ) /
=defun= / =if= / =while= / =cond= / 比較 / =+/-/*= / =and= / =or= の
最小集合。compiler 本体は =cl-lib= / =pcase= / =seq.el= /
=make-hash-table= / =cl-defstruct= / =cl-loop= / =signal= を計 163 箇所
使用。self-compile には grammar の大幅拡張が必要 (Option F へ)。

*** Option B: delegate gate を skip

却下。=NELISP_USE_RUST_APPLY=1= を boot 後に =(nelisp--set-use-elisp-apply
nil)= で turn off して T7 再実行 → 142s で変化なし。delegate は
ボトルネックの一段に過ぎず、skip しても残り 9 段の累積コストが残る。

*** Option D: compiler を Emacs で byte-compile → .elc 化 → bake → bytecode interp

却下 (前提不成立)。=src/nelisp-bytecode.el= は Phase 3b "scaffolding"
段階で =nelisp-bc-compile= / =nelisp-bc-run= が =unimplemented= を投げる。
動作する bytecode interpreter が未だ存在しない。

** 長期解 (Option F)

*** Phase 47 grammar 拡張

compiler の subset (= seq + defun + cond + while + integer + symbol +
比較 + arith) で書ける ~hot fn~ だけ self-host し、richer な部分
(=pcase= / =cl-defstruct= 等) は引き続き Emacs build-host に残す段階的
移行。具体的設計は別 doc (Doc 100+ 系) で記述予定。

*** または bytecode VM 完成 (Phase 3b)

=src/nelisp-bytecode.el= の opcodes 実装を進め、.elc を nelisp で
読める状態にする。これが揃えば Option D が unlock される。

** Re-investigation (2026-05-22, R6a) — 「真因」は reader、eval ではなかった

上記 §7 の「真因」分析は誤診。再計測で真の bottleneck を特定:

| Test            | 内容                                  | Time    | per-form |
|-----------------+---------------------------------------+---------+----------|
| p-eval-list     | 1000 form list を while で =(eval x)= | 3.6s    | 3.6ms    |
| p-tokenize-only | 1000-form file を tokenize のみ        | 132s    | 26ms/tok |
| p-read-only     | 1000-form file を tokenize+parse      | 143s    | 142ms    |

決定的発見:

- =(eval form)= 自体は **3.6ms/call** で速い (= 既存 §7 の「eval 10 段
  dispatch 累積 140ms」は誤り)
- 真因は =nelisp--read-tokenize= (=lisp/nelisp-stdlib-reader.el=) の
  per-token cost **~26ms** — standalone NeLisp elisp interpreter の
  per-op floor (= setq / aref / eq が ~0.2-1ms ずつ) に律速され、
  1 token あたり 30+ fcalls 連鎖
- T6/T7/T9 が全部 ~142s だった理由は「3 ケース全部 reader を通過する」
  から (T9 は eval 不要だが parse は同じ); これが本来「真因 = reader」
  の決定的証拠だったが見落とした

** R6a 配線 (SHIPPED 2026-05-22)

未配線だった Phase 47 native reader (= Doc 116 §116.A =nelisp_reader_lex_one=
+ §116.B =nelisp_reader_parse_one=) を runtime =load= 経路に wire:

- =build-tool/src/eval/mod.rs= の =builtin_dispatch!= に 1 entry 追加
  (=+1 line Rust=、内容は既存 =crate::reader::read_all= の elisp 露出 only):
  : "nelisp--read-all-from-string-native" =>
  :   { require_arity(...,$args,1,Some(1))?;
  :     let s = match &$args[0] { Sexp::Str(s) => s.as_str(), o => ... };
  :     crate::reader::read_all(s)
  :       .map(|forms| Sexp::list_from(&forms))
  :       .map_err(|e| EvalError::internal(e.to_string())) }
- =install_builtins= の name list に同名追加 (=+1 token=)
- =lisp/nelisp-stdlib-reader.el= の =nelisp--read-all-from-string-impl=
  body を =(if (fboundp 'nelisp--read-all-from-string-native) (...native...)
  (...legacy elisp...))= に変更

実測 (= 同じ t6-toplevel-1000.el を再 load):

| Path             | Before  | After   | Factor  |
|------------------+---------+---------+---------|
| Reader (T6 load) | 142s    | 0.36s   | **394x** |
| make test (load) | green   | green   | -       |
| cargo test       | 10/10   | 10/10   | -       |
| Reader ERT       | 67/75   | 67/75   | (8 pre-existing failures unchanged) |

Phase 47 native reader は元々 binary に baked (= =crate::reader::read_all=
が boot 時に既使用)、runtime に dispatch entry 1 行が抜けていただけ。

** 残存 BLOCKER (Wave 7 完全 unblock のために必要な次の作業)

reader 経路は 390x 高速化したが、=nelisp --batch -L lisp -l
scripts/compile-elisp-objects.el -f compile-elisp-objects-emit-all= は
別の理由で fail:

- =(require 'cl-lib)= が standalone NeLisp に file 不在 (=
  =lisp/cl-lib.el= なし) で =file-error=
- =nelisp-phase47-compiler.el:191= =(cl-incf ...)= +
  =:1739= =(cl-every ...)= の 2 箇所のみ実使用
- 解消パス候補:
  1. =lisp/cl-lib.el= stub 新設 — =defmacro cl-incf= + =defun cl-every= 程度
  2. compiler 側の 2 箇所を inline 書き換え (=(setq x (1+ x))= / while loop)
- 上記 fix 後 compile gate を再計測し、Wave 7 full unblock を判定

** Deferral 判断 (更新)

- Wave 7 reader BLOCKER: **RESOLVED** (R6a SHIPPED 2026-05-22, 1 line Rust
  + 1 line elisp、Phase 47 swap = elisp reader path elided in production
  load, retained as legacy fallback for non-native Emacs test runs).
- Wave 7 cl-lib BLOCKER: **partial deferred** — 小規模 (~30 LOC stub or
  2 inline rewrites) で次 session 解消可能。
- =build.rs= が =emacs --batch= を spawn する現状は維持 (cl-lib gate
  + Wave 7 自身の cargo build.rs 統合は別 wave)。
- end-user が pre-built tarball で nelisp を動かす経路は無影響継続。

** 関連 commit / artifact

- =c7302830= — Wave 7 partial SHIPPED (BLOCKER 注記つき commit)
- =449d2b96= / =9c97fd47= — W7.6a/b emit 最適化
- =1fa1f579= — §7 BLOCKER deferred (誤診の「真因」分析含む)
- (this commit) — R6a: Phase 47 native reader wired, reader BLOCKER RESOLVED
- =scripts/compile-elisp-objects.el= — 既存 build orchestrator
- =lisp/nelisp-cc-reader-lexer.el= / =-parser.el= — Doc 116 §116.A/B
  Phase 47 native reader (= 既 built、R6a で runtime に配線)
- =build-tool/build.rs= の =link_elisp_cc_spike()= — =emacs --batch=
  起動箇所

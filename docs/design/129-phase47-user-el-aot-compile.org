#+TITLE: Doc 129 — Phase 47 grammar expansion for user/application .el AOT compile (native-comp endgame)
#+DATE: 2026-05-25
#+STATUS: ACTIVE

* Status

- *State*: ACTIVE 2026-05-25 — Stage 129.1 frontend macro expansion,
  Stage 129.2 boxed-boundary Int MVP, Stage 129.3A boxed Int
  =setq= delegation, Stage 129.3B symbol-literal =setq= helper,
  Stage 129.3C symbol-literal =defvar= helper, Stage 129.3D
  symbol-literal =defconst= helper, Stage 129.3E top-level
  =defvar= / =defconst= AOT init-helper lowering, Stage 129.3F
  top-level =defcustom= init-helper lowering, Stage 129.3G
  =defcustom= metadata descriptors, Stage 129.3H init-helper
  scheduling descriptors and runtime module-init plan, Stage 129.3I
  runtime module-init plan execution hook, Stage 129.3J runtime
  custom metadata table, Stage 129.3K AOT init helper resolution /
  call callback, Stage 129.3L default init boundary context,
  Stage 129.3M init helper ABI argv materialization,
  Stage 129.4 multi-binding runtime =let= MVP,
  Stage 129.5A static GC root-map MVP, Stage 129.5B AOT root-set
  registration substrate, Stage 129.5C call-boundary root-vector
  bridge, Stage 129.5D runtime =let= Sexp root annotations, Stage
  129.5E explicit native root push/pop bridges, Stage 129.5F
  automatic root-scope insertion for Doc 99 boundary defuns, Stage
  129.5G root-vector materialisation bridge, Stage
  129.6A top-level =require= / =provide= frontend handling, Stage
  129.6B one-argument Sexp builtin delegation helper, Stage
  129.6C runtime builtin-call1 bridge, Stage 129.6D constrained
  direct one-argument builtin call lowering, Stage 129.6E unary
  builtin table expansion, Stage 129.6F builtin calln/varargs
  bridge, Stage 129.6G fixed-arity calln builtin table expansion,
  Stage 129.6H string/format calln builtin table expansion,
  Stage 129.6I dynamic higher-order calln builtin table expansion,
  Stage 129.6J map builtin function-designator materialization,
  Stage 129.6K sort predicate function-designator materialization,
  Stage 129.6L mapcan function-designator materialization,
  Stage 129.6M maphash function-designator materialization,
  Stage 129.6N seq.el higher-order function-designator
  materialization, Stage 129.6O cl-lib higher-order
  function-designator materialization, Stage 129.6P cl-lib predicate
  family function-designator materialization, Stage 129.6Q runtime
  AOT C ABI descriptor table, Stage 129.6R extended seq.el/cl-lib
  higher-order coverage,
  Stage 129.7A one-argument =funcall= dispatcher bridge,
  Stage 129.7B two-argument =funcall=
  dispatcher bridge, Stage 129.7C =apply= dispatcher bridge, Stage
  129.7D quoted function-designator materialization, Stage 129.7E
  SysV first stack-GP argument support plus three-argument =funcall=
  dispatcher bridge, Stage 129.7F first non-trivial outgoing stack-GP
  evaluation, Stage 129.7G general outgoing stack-GP call-spill
  assembly, Stage 129.7H direct =funcall= calln/rest-list bridge,
  Stage 129.7I =apply= splicing bridge, Stage 129.7J source =&rest=
  parameter binding, Stage 129.7K non-capturing literal lambda
  =funcall= lifting, Stage 129.7L direct literal lambda lifting,
  Stage 129.7M map-family literal lambda lifting,
  Stage 129.7N indexed higher-order builtin lambda lifting,
  Stage 129.7O maphash literal lambda lifting,
  Stage 129.7P seq.el higher-order literal lambda lifting,
  Stage 129.7Q cl-lib higher-order literal lambda lifting,
  Stage 129.7R direct literal lambda capture threading,
  Stage 129.7S cl-lib predicate literal lambda lifting,
  Stage 129.7T AOT dispatcher heap-closure bridge,
  Stage 129.7U captured higher-order callback closure bridge,
  Stage 129.7V module-init closure descriptor registration,
  Stage 129.7W compiler module-init plan handoff,
  Stage 129.7X escaping literal closure value materialization,
  Stage 129.7Y direct captured-mutation closure dispatch, Stage
  129.7Z extended higher-order literal lambda lifting,
  Stage 129.8A runtime handler-stack substrate, Stage
  129.8B =throw= / =signal=
  bridge lowering, Stage 129.8C handler-push bridge lowering,
  Stage 129.8D handler-pop bridge lowering, and Stage 129.8E
  source-level =catch= normal-exit lowering, and Stage 129.8F
  source-level =condition-case= normal-exit lowering, and Stage
  129.8G source-level =unwind-protect= normal-exit lowering, and
  Stage 129.8H single-argument =error= lowering, and Stage 129.8I
  formatted =error= varargs bridge, and Stage 129.8J
  =condition-case= list condition specs, and Stage 129.8K
  =condition-case= multi-clause normal exits, and Stage 129.8L direct
  =catch= / =throw= landing-value extraction, and Stage 129.8M direct
  =condition-case= / =signal= landing-error dispatch, and Stage
  129.8N conditional =catch= / =throw= branch dispatch, Stage
  129.8O direct =unwind-protect= / =throw= cleanup dispatch, and
  Stage 129.8P conditional =unwind-protect= / =throw= cleanup dispatch,
  Stage 129.8Q conditional =condition-case= / =signal= dispatch, and
  Stage 129.8R nested conditional non-local branch trees have
  implementations in =nelisp-phase47-compiler.el= / =nelisp-gc.el= /
  =nelisp-cc-runtime.el=.
  Stage 129.4B additionally tracks top-level =defvar= / =defconst= /
  =defcustom= names as special declarations so =let= lowering can
  distinguish dynamic bindings from lexical/runtime frame slots.
  Stage 129.4C adds explicit AOT special binding push/pop runtime
  bridges and compiler-visible relocs for the later source =let=
  lowering.  Stage 129.4D lowers single-binding source special =let=
  normal exits through that bridge substrate.  Stage 129.4E extends
  that normal-exit path to all-special multi-binding =let= forms while
  preserving parallel initializer evaluation.  Stage 129.4F extends
  the same normal-exit discipline to mixed lexical/special
  multi-binding =let= forms.  Stage 129.4G handles direct =throw=
  cleanup through those source special =let= forms.  Stage 129.4H handles
  one-level conditional =throw= cleanup through the same special =let=
  shapes.  Stage 129.4I handles nested conditional quoted-tag =throw=
  trees by running special cleanup at every leaf.
  Supported in this first cut: host macro expansion in value/call
  positions without rewriting Phase 47 structural forms, top-level
  =defmacro= inside =seq= as compile-time-only definitions,
  =let*= → nested =let=, normalization for macro output
  (=progn= singletons, 3-arg =if=, =nil= → 0), and value-context
  multi-form =progn= via a new =value-seq= IR.  =value-seq= supports
  value-producing children; statement-only =write= remains top-level
  / statement-context because value emission has no rodata context.
- *129.2 MVP*: =defun-sexp-int= lowers a monomorphic
  =(defun-sexp-int NAME (OUT ARG...) BODY)= surface into ordinary
  Phase 47 DSL: =ARG= values are assumed =Sexp::Int= pointers and
  are read via =sexp-int-unwrap=; the raw integer result is written to
  caller-owned =OUT= via =sexp-int-make=.  This is intentionally a
  boxed-boundary step, not the full compiler-wide "every IR value is
  Sexp" rewrite.
- *129.3A MVP*: =defun-sexp-int-setq= extends the boxed Int boundary
  with explicit environment pointers:
  =(defun-sexp-int-setq NAME (OUT MIRROR FRAMES SCRATCH SYM ARG...) BODY)=
  boxes BODY into =OUT=, calls =nelisp_env_set_value= with
  =MIRROR/FRAMES/SYM/OUT/SCRATCH=, then returns =OUT=.  This proves
  the AOT path can delegate a symbol-cell write without adding Rust
  LOC.  It is not yet general =setq= syntax, =defvar= conditional set
  semantics, or tagged-value locals; 129.3B adds the literal-symbol
  variant below.
- *129.3B MVP*: =defun-sexp-int-setq-symbol= covers the next
  symbol-cell step: the caller supplies a writable =SYM-SLOT=, while
  the compiled function materializes a compile-time symbol literal via
  =sexp-write-symbol-lit= before calling =nelisp_env_set_value=.  The
  literal bytes are pushed as a temporary stack buffer rather than
  stored in =.rodata=, so the helper remains valid in ET_REL object
  mode.  This is still not full user =setq= syntax or top-level
  =defvar= / =defconst= semantics.
- *129.3C MVP*: =defun-sexp-int-defvar-symbol= adds a boxed-boundary
  conditional symbol-cell write for a compile-time variable name.  It
  materializes the symbol, asks =nelisp_mirror_is_bound= whether the
  value cell is already bound, and only on the unbound branch boxes
  BODY into =OUT= and delegates to =nelisp_env_set_value=.  To stay
  inside the current six-GP-argument Phase 47 ABI, =OUT= must initially
  contain the runtime unbound marker and is reused as the
  =nelisp_mirror_is_bound= marker argument before the unbound branch
  overwrites it with the boxed initial value.  This proves
  =defvar=-style conditional set, but it is still an explicit helper,
  not direct top-level execution.
- *129.3D MVP*: =defun-sexp-int-defconst-symbol= adds the matching
  boxed-boundary constant initializer.  It materializes the symbol,
  clears any existing constant flag via =nelisp_mirror_set_constant=
  with =Nil= in =OUT=, boxes BODY and delegates the value write through
  =nelisp_env_set_value=, then writes =T= into =OUT= and marks the
  symbol constant.  This proves the constant-flag substrate for
  compile-time-known names, but it is still an explicit helper rather
  than direct top-level execution.
- *129.3E MVP*: top-level =defvar= and =defconst= forms with
  integer-compatible initializers now lower to generated
  =nelisp_aot_var_N_NAME= / =nelisp_aot_const_N_NAME= init helper
  defuns.  Object output therefore accepts ordinary top-level
  declarations and exposes C-callable initializer symbols; the Doc 99
  runtime/call-boundary layer still decides when to call them and
  supplies =OUT/MIRROR/FRAMES/SCRATCH/NAME-SLOT=.
- *129.3F MVP*: top-level =defcustom= with an integer-compatible
  standard value now lowers to a generated
  =nelisp_aot_custom_N_NAME= init helper using the same conditional
  =defvar= write semantics.
- *129.3G MVP*: top-level =defcustom= now also exposes compiler-side
  metadata descriptors keyed to the same generated init helper name.
  The docstring is validated, customization options must be
  keyword/value pairs, and the descriptor carries
  =:name/:helper/:standard/:docstring/:options= for the Doc 99
  runtime scheduler.
- *129.3H MVP*: top-level =defvar= / =defconst= / =defcustom= now
  expose ordered init-helper descriptors, and =nelisp-cc-runtime=
  normalizes those descriptors with custom metadata and root
  descriptors into a pure module-init plan.  This fixes the Doc 99
  scheduling contract before the native loader grows actual helper
  invocation.
- *129.3I MVP*: =nelisp-cc-runtime-run-aot-module-init-plan= now
  executes the module-init plan in helper order through Doc99-supplied
  callbacks.  The runtime validates that the boundary context carries
  =:out/:mirror/:frames/:scratch/:name-slot= and invokes optional
  custom registration callbacks after the matching =defcustom= helper
  succeeds.  Native symbol resolution and FFI calls remain Doc 99
  loader responsibilities.
- *129.3J MVP*: =nelisp-cc-runtime= now owns a runtime AOT custom
  metadata table.  =nelisp-cc-runtime-run-aot-module-init-plan=
  registers =defcustom= metadata there by default after the matching
  helper succeeds, while still allowing Doc 99 callers to override the
  registration callback.  The stored entries are metadata-only and do
  not retain transient native call-boundary context.
- *129.3K MVP*: =nelisp-cc-runtime= now provides the standard AOT init
  helper resolution/call callback.  Helpers resolve through the
  existing Doc 81 =nelisp-cc-runtime-resolve-symbol= contract
  (= :resolved / :host-stub / :not-found=), and Doc 99 can inject the
  native call function that consumes the resolved address plus the
  validated boundary context.
- *129.3L MVP*: =nelisp-cc-runtime= now also owns the default
  init-helper boundary context allocation.  Callers can use
  =nelisp-cc-runtime-make-aot-init-context= to allocate the standard
  one-slot =OUT=, =SCRATCH=, and =NAME-SLOT= vectors, or
  =nelisp-cc-runtime-run-aot-module-init-plan-with-default-context=
  to run a plan with those slots allocated by the runtime.  Real
  native ABI invocation and real environment handles remain Doc 99
  loader work.
- *129.3M MVP*: =nelisp-cc-runtime-aot-init-helper-argv= now
  materializes the fixed native helper call argument order
  =(OUT MIRROR FRAMES SCRATCH NAME-SLOT)= from the validated init
  context.  =nelisp-cc-runtime-call-aot-init-helper= attaches that
  argv as =:abi-argv= before invoking a native-call callback, removing
  one more Doc 99 loader-side convention from ad hoc callback code.
- *129.7V MVP*: =nelisp-cc-runtime-aot-module-init-plan= now accepts
  AOT closure descriptors from
  =nelisp-phase47-compiler--closure-descriptors=.  Plan execution
  validates and registers those descriptors through
  =nelisp-cc-runtime-register-aot-closure-descriptor= by default, so
  the 129.7U =nelisp_aot_make_closure= bridge no longer depends on an
  ad hoc test-only registration step.  Native object embedding and
  root-vector pinning for closure records remain loader work.
- *129.7W MVP*: =nelisp-phase47-compiler--module-init-plan= now
  produces the normalized Doc99 handoff in one call, combining
  top-level init helpers, =defcustom= metadata, 129.5 root descriptors,
  and 129.7 closure descriptors through the runtime validator.  Native
  object embedding of that plan remains loader work.
- *129.4 MVP*: multi-binding =let= now supports runtime initializer
  values inside =defun= bodies via =let-rt-n=.  Initializers parse in
  the original environment (=parallel =let= semantics), then the body
  sees all runtime frame slots.  =let*= remains a frontend desugaring
  to nested =let=.  129.4B tracks top-level special declarations and
  rejects =let= binding those names instead of silently compiling them
  as lexical slots.  129.4C adds explicit
  =nelisp_aot_push_special= / =nelisp_aot_pop_special= bridge
  substrates for value-cell save/restore.  129.4D uses those bridges
  for single-binding source special =let= on the normal exit path.
  129.4E adds the all-special multi-binding normal-exit path.  129.4F
  adds the mixed lexical/special multi-binding normal-exit path.  129.4G
  covers direct quoted-tag =throw= bodies by saving the throw value while
  the special binding is active, popping all source special bindings,
  then emitting the throw bridge.  129.4H extends the same cleanup
  discipline to one-level =if= bodies whose throwing branch is a direct
  quoted-tag =throw=: each branch saves its value, pops the active source
  special bindings, and either returns normally or emits the throw bridge.
  129.4I generalizes that shape to nested =if= trees with direct
  quoted-tag =throw= leaves.  Heap lexframes and general tagged local
  values remain pending.
- *129.5A MVP*: =defun= params may be annotated as =(NAME :type sexp)=.
  These still use GP ABI registers, but the parser marks their =ref=
  nodes as GC-root candidates.  Any defun body containing a known
  allocator now carries =:gc-root-slots= with the referenced annotated
  Sexp slots.  This is only a static root map; runtime frame
  registration / root-vector pinning remains pending.
- *129.5B MVP*: =nelisp-gc-root-set= now has an =aot-frame= root
  category and =nelisp-gc--with-active-aot-frame= can dynamically
  register a frame-local root vector.  This proves the runtime root
  enumeration substrate for Phase 47 frames.  The compiled prologue /
  epilogue still does not emit the push/pop sequence automatically.
- *129.5C MVP*: Phase 47 can now expose =:gc-root-slots= as
  call-boundary descriptors, and =nelisp-cc-runtime= can materialize
  a root vector from those slots and run a thunk with that vector
  registered as an active AOT frame.  This fixes the metadata/runtime
  handoff shape for Doc 99 integration; native prologue/epilogue
  emission is still pending.
- *129.5D MVP*: runtime =let= bindings may now annotate the local as
  =((NAME :type sexp) VALUE)=.  Runtime slots created from those
  bindings propagate =:root-p= through =FENV=, so allocating defuns can
  include annotated local slots in =:gc-root-slots= and the 129.5C
  descriptor path.  This is conservative static metadata, not a full
  liveness map.
- *129.5E MVP*: =nelisp-cc-runtime= now exposes explicit native root
  stack bridges =nelisp_aot_push_roots= and =nelisp_aot_pop_roots=,
  and Phase 47 lowers internal =(aot-push-roots ROOTS)= /
  =(aot-pop-roots ROOTS)= forms to those relocs.  This makes native
  push/pop expressible in object output and updates
  =nelisp-gc-root-set= during the active extent.  Automatic defun
  prologue/epilogue insertion starts in 129.5F; root-vector
  materialisation from =:gc-root-slots= follows in 129.5G.
- *129.5F MVP*: Phase 47 now wraps allocating defuns that have
  =:gc-root-slots= and already expose the Doc 99 boundary locals
  =out=, =mirror=, =frames=, =scratch=, and =roots= in an internal
  =aot-root-scope= IR.  Emission inserts balanced
  =nelisp_aot_push_roots= / =nelisp_aot_pop_roots= calls around the
  body and preserves the GP return value across the pop bridge.
- *129.5G MVP*: =nelisp-cc-runtime= now exposes
  =nelisp_aot_materialize_roots=, and =aot-root-scope= emission calls
  it before =nelisp_aot_push_roots=.  The compiler maps
  =:gc-root-slots= back to frame symbols, passes those values through
  the bridge, stores the returned root vector into the =roots= frame
  slot, and then pushes that vector.  This keeps the root vector
  derived from compiler metadata instead of requiring the caller to
  prebuild it.
- *129.6A MVP*: top-level =require= and =provide= forms are now
  compile-time-only frontend forms.  =require= runs during compilation
  using literal arguments so needed macro/helper libraries can be
  loaded before parsing; =provide= is stripped without runtime module
  registration.
- *129.6B MVP*: =defun-sexp-builtin1-symbol= lowers an explicit
  one-argument boxed Sexp builtin delegation helper.  The helper
  materializes a compile-time builtin symbol into caller-owned storage,
  calls the Doc 129.6 dispatcher ABI =nelisp_aot_builtin_call1=, and
  returns the caller-owned =OUT= slot.  This proves the frontend/object
  relocation surface for builtin delegation, but ordinary user syntax
  such as =(symbol-name x)=, varargs, =format= / =mapcar= / =funcall=,
  and the runtime C/Rust dispatcher implementation remain follow-up
  work.  Later 129.6/129.7 stages cover those direct user-call
  surfaces without changing this helper ABI.
- *129.6C MVP*: =nelisp-cc-runtime-aot-builtin-call1= fixes the
  runtime-side bridge contract for the same six boundary values as the
  native ABI: =MIRROR/FRAMES/NAME/ARG/OUT/SCRATCH=.  On the Emacs side
  =OUT= is represented as a caller-owned one-slot vector; the bridge
  dispatches the one-argument builtin, writes the result into =OUT=,
  and returns =OUT=.  This lets Doc 99 tests and host integration use
  the same boxed-boundary convention before the exported
  =nelisp_aot_builtin_call1= C ABI body exists.
- *129.6D MVP*: direct =(symbol-name ARG)= calls now lower through the
  same dispatcher sequence when they appear inside a boxed-boundary
  defun that explicitly exposes =out/mirror/frames/scratch/name-slot=
  (or =name_slot=).  This is the first ordinary user-call shape for
  builtin delegation, still limited to one known builtin, one argument,
  and explicit boundary parameters.
- *129.6E MVP*: the direct one-argument lowering now uses a small
  builtin table instead of hard-coding only =symbol-name=.  Representative
  unary calls such as =identity=, =length=, =car=, =cdr=, type predicates,
  numeric/string converters, and simple string case functions lower
  through =nelisp_aot_builtin_call1=.  A same-named Phase 47 =defun= in
  the compile unit still wins over builtin delegation.
- *129.6F MVP*: =nelisp-cc-runtime= now exposes the vararg builtin
  bridge =nelisp_aot_builtin_calln=, and Phase 47 lowers direct calls
  to a small vararg builtin table (=list= / =vector= / =concat= /
  =append=) through that ABI.  This uses the existing outgoing stack-GP
  machinery when the fixed boundary prefix plus user arguments exceeds
  six GP arguments.  Exported C ABI wiring remains pending.
- *129.6G MVP*: the direct calln table now covers common fixed-arity
  host builtins as well: =cons=, =eq=, =eql=, =equal=, =memq=,
  =member=, =assq=, =assv=, =assoc=, =nth=, =elt=, =string=,
  =string<=, and =string-lessp=.  They lower through the same
  =nelisp_aot_builtin_calln= bridge, so the compiler no longer needs
  bespoke lowering for ordinary boxed Sexp calls in this group.
- *129.6H MVP*: the calln table also covers common string and message
  builtins that appear in application code: =format=, =message=,
  =string-match=, =string-match-p=, =substring=,
  =string-prefix-p=, =string-suffix-p=, and
  =replace-regexp-in-string=.  They still use the host/NeLisp
  dispatcher behind =nelisp_aot_builtin_calln=; exported C ABI wiring
  remains pending.
- *129.6I MVP*: the same calln table now includes dynamic
  higher-order builtins =mapcar=, =mapc=, and =mapconcat=.  This
  covers calls whose function argument is already materialized as a
  boxed value in the current frame.  Quoted/function designator
  materialization for arbitrary builtin arguments and captured
  closures remain 129.7 work.
- *129.6J MVP*: =mapcar= / =mapc= / =mapconcat= now also accept
  quoted/function symbol designators in their function argument
  position, e.g. =(mapcar #'foo xs)= and =(mapc 'foo xs)=.  The
  compiler materializes the builtin name into =name-slot= and the
  function designator into =scratch= before calling
  =nelisp_aot_builtin_calln=.  Heap closure capture remains 129.7
  work.
- *129.6K MVP*: =sort= is now part of the direct calln builtin table,
  and its predicate argument accepts quoted/function symbol
  designators, e.g. =(sort xs #'string<)=.  The same scratch-slot
  materialization path used by 129.6J is reused for the second source
  argument.  Exported C ABI wiring and broader higher-order builtin
  coverage remain pending.
- *129.6L MVP*: =mapcan= now joins the higher-order direct calln
  table, with the same first-argument quoted/function symbol
  materialization as =mapcar= / =mapc= / =mapconcat=.  Capturing
  lambdas remain 129.7 heap-closure work.
- *129.6M MVP*: =maphash= now joins the higher-order direct calln
  table, again using first-argument quoted/function symbol
  materialization.  Runtime dispatch flows through the same
  =nelisp_aot_builtin_calln= bridge and host/NeLisp function lookup.
- *129.6N MVP*: the seq.el higher-order family now shares the same
  direct calln path: =seq-map=, =seq-do=, =seq-filter=, =seq-remove=,
  =seq-find=, =seq-some=, =seq-every-p=, =seq-count=, =seq-reduce=,
  and =seq-mapcat=.  Their first argument is materialized through the
  scratch-slot function-designator path.
- *129.6O MVP*: the cl-lib higher-order family now also uses the
  direct calln path: =cl-mapcar=, =cl-mapc=, =cl-mapcan=, =cl-maplist=,
  =cl-mapl=, =cl-mapcon=, =cl-some=, =cl-every=, =cl-notany=,
  =cl-notevery=, =cl-count-if=, =cl-count-if-not=, =cl-find-if=,
  =cl-find-if-not=, =cl-remove-if=, =cl-remove-if-not=,
  =cl-position-if=, =cl-position-if-not=, and =cl-reduce=.
- *129.6P MVP*: the cl-lib predicate table now covers destructive
  sequence operations and association/member predicate variants:
  =cl-delete-if=, =cl-delete-if-not=, =cl-member-if=,
  =cl-member-if-not=, =cl-assoc-if=, =cl-assoc-if-not=,
  =cl-rassoc-if=, =cl-rassoc-if-not=, =cl-substitute-if=,
  =cl-substitute-if-not=, =cl-nsubstitute-if=, and
  =cl-nsubstitute-if-not=.  Substitute/nsubstitute use the second
  source argument as the function-designator slot; the rest use the
  first argument.
- *129.6Q MVP*: =nelisp-cc-runtime= now exposes a Doc129 AOT C ABI
  descriptor table for the emitted =nelisp_aot_*= bridge symbols,
  mapping each native symbol to the Emacs-side simulator bridge,
  fixed/rest arity metadata, and argument names.  This validates the
  compiler/runtime symbol contract before the standalone runtime wires
  the real dlsym/exported-address path.
- *129.6R MVP*: the higher-order calln table now covers additional
  positional callback builtins: =seq-keep=, =seq-mapn=,
  =seq-map-indexed=, =seq-sort=, =seq-uniq=, =cl-map=, =cl-sort=,
  =cl-stable-sort=, and =cl-merge=.  Their quoted/function symbol
  designators use the same scratch-slot materialization path, with
  non-first callback positions recorded in the designator-index table.
- *129.7A MVP*: direct =(funcall FN ARG)= now lowers through
  =nelisp_aot_funcall1= inside boxed-boundary defuns that expose
  =out/mirror/frames/scratch=.  =nelisp-cc-runtime-aot-funcall1=
  fixes the matching runtime bridge contract and writes the dispatch
  result into caller-owned =OUT=.  Closure allocation/capture,
  multi-argument =funcall=, and =apply= remain pending.
- *129.7B MVP*: direct =(funcall FN ARG0 ARG1)= now lowers through
  =nelisp_aot_funcall2=.  The fixed-arity ABI is
  =MIRROR/FRAMES/FN/ARG0/ARG1/OUT=, deliberately omitting =SCRATCH= so
  the call remains inside the current six-GP-register Phase 47 extern
  budget.  Three-or-more argument =funcall= is started by 129.7E;
  general calln/rest-list lowering is completed for direct =funcall= by
  129.7H.
- *129.7C MVP*: direct =(apply FN ARGS-LIST)= now lowers through
  =nelisp_aot_apply= inside boxed-boundary defuns that expose
  =out/mirror/frames/scratch=.  =ARGS-LIST= is already a boxed Sexp
  list supplied by the caller/compiler; the runtime bridge dispatches
  through =nelisp--apply= when available, falls back to host =apply=,
  writes the result into =OUT=, and returns =OUT=.  Constructing the
  argument list from rest syntax and compiling closure literals remain
  pending.
- *129.7D MVP*: quoted/function symbol designators now work for the
  shipped higher-order paths: =(funcall 'foo arg)=, =(funcall #'foo
  arg0 arg1)=, and =(apply 'foo args)= materialize =foo= into the
  caller-owned =name-slot= / =name_slot= via =sexp-write-symbol-lit=
  before delegating to =nelisp_aot_funcall1=, =nelisp_aot_funcall2=,
  or =nelisp_aot_apply=.  Dynamic function values still use the
  existing =FN= parameter path.  Lambda/closure literals and
  direct four-or-more-argument calln is handled by 129.7H.
- *129.7E MVP*: x86_64 SysV extern-call and boxed defun prologues now
  cover the first GP stack argument.  Direct =(funcall FN ARG0 ARG1
  ARG2)= lowers through =nelisp_aot_funcall3= with ABI
  =MIRROR/FRAMES/FN/ARG0/ARG1/ARG2/OUT=.  This is the first real
  stack-argument step for calln; 129.7H uses the same substrate for
  direct =funcall= rest-list construction.
- *129.7F MVP*: the first outgoing stack-GP argument may now be a
  non-trivial value expression when it is the final source argument.
  The emitter evaluates all args left-to-right into temporary stack
  saves, parks the computed stack arg in =r10=, restores register args,
  and then pushes the outgoing stack arg immediately before the call.
- *129.7G MVP*: outgoing stack-GP assembly now has a general call-spill
  path for non-trivial stack args.  When any stack arg needs evaluation,
  all args are evaluated left-to-right into temporary stack saves, GP /
  XMM register args are reloaded from that save area, then stack args
  are pushed right-to-left into the outgoing call frame.  This covers
  multiple computed stack args without changing source evaluation order.
- *129.7H MVP*: direct four-or-more-argument =(funcall FN ARG...)= now
  lowers through =nelisp_aot_funcalln=.  The ABI is
  =MIRROR/FRAMES/FN/ARGC/OUT/SCRATCH/ARG...=; the runtime bridge
  validates =ARGC=, constructs the rest-list from the trailing args,
  dispatches through the same apply fallback, writes =OUT=, and returns
  =OUT=.  Closure allocation/capture is covered by 129.7T/U/X/Y, with
  native frame write-through for captured =setq= cells still pending.
- *129.7I MVP*: source-level =(apply FN ARG... ARGS-LIST)= now lowers
  through =nelisp_aot_applyn=.  The ABI is
  =MIRROR/FRAMES/FN/ARGC/OUT/SCRATCH/ARG...=; the runtime validates
  the final argument as the list tail, appends preceding ordinary args
  in front of it, dispatches through the same apply fallback, writes
  =OUT=, and returns =OUT=.
- *129.7J MVP*: source-level =&rest= parameters in Phase 47 =defun=
  forms now lower as one final boxed ABI parameter.  Direct calls to
  such defuns collect the surplus source arguments through
  =nelisp_aot_listn=, write that constructed list into the caller's
  =scratch= slot, and pass =scratch= as the callee rest parameter.
  This covers boxed-boundary callers exposing =out/mirror/frames/scratch=;
  closure allocation/capture is covered by 129.7T/U/X.
- *129.7K MVP*: non-capturing literal lambda designators in
  =(funcall (lambda (...) ...) ARG...)= and
  =(funcall (function (lambda (...) ...)) ARG...)= now lambda-lift
  during frontend preprocessing into synthetic top-level Phase 47
  defuns and direct calls.  Direct captures are threaded by 129.7R;
  captured mutation uses the 129.7Y heap-closure dispatch path.
- *129.7L MVP*: direct literal lambda applications
  =((lambda (...) ...) ARG...)= use the same synthetic-defun lifting
  path as 129.7K, so non-capturing immediate lambdas execute through
  ordinary native direct calls.  Direct captures are threaded by
  129.7R.
- *129.7M MVP*: non-capturing literal lambdas in the function
  argument position of =mapcar= / =mapc= / =mapconcat= now lift to a
  synthetic top-level defun, then flow through the 129.6J map
  function-designator path.  Captured callbacks are routed through
  the 129.7U closure bridge.
- *129.7N MVP*: the same non-capturing literal lambda lift now follows
  the 129.6 function-designator argument index table.  This extends
  the path to =mapcan= first-argument callbacks and =sort= predicate
  callbacks.  Captured callbacks are routed through 129.7U.
- *129.7O MVP*: =maphash= callbacks now participate in the same
  non-capturing literal lambda lift, so =(maphash (lambda (k v) ...)
  table)= becomes a synthetic top-level defun plus quoted/function
  designator materialization.  Captured callbacks are routed through
  129.7U.
- *129.7P MVP*: the seq.el higher-order family also accepts
  non-capturing literal callbacks through the same indexed lambda-lift
  path.  Captured callbacks are routed through 129.7U.
- *129.7Q MVP*: cl-lib higher-order callbacks participate in the same
  non-capturing literal lambda lift.  As with the map/seq families,
  captured callbacks are routed through 129.7U.
- *129.7R MVP*: direct literal lambda calls now thread captured lexical
  values as leading synthetic defun parameters.  This covers direct
  =funcall= and direct =((lambda ...) ...)= application without heap
  closure allocation.  Higher-order callback captures use the 129.7U
  closure bridge; captured mutation in direct =funcall= uses 129.7Y.
- *129.7S MVP*: non-capturing literal lambdas in the 129.6P cl-lib
  predicate families now lift to synthetic defuns through the same
  indexed callback path, including the second-argument predicate slot
  used by =cl-substitute-if= and =cl-nsubstitute-if=.
- *129.7Z MVP*: non-capturing literal lambdas in the 129.6R extended
  higher-order family now lift through the same indexed callback path,
  including non-first callback positions such as =seq-uniq=, =cl-map=,
  and =cl-merge=.
- *129.7T MVP*: the AOT =funcall= / =apply= dispatcher now recognizes
  canonical =nelisp-closure= records before falling back to
  =nelisp--apply= or host functions.  This routes simulator calls
  through =nelisp-closure-apply=, preserving captured environments and
  shared-cell captured mutation for already-materialized heap closures.
  Higher-order callback construction starts in 129.7U; escaping lambda
  values are covered by 129.7X, and direct captured mutation dispatch is
  covered by 129.7Y.
- *129.7U MVP*: captured literal lambdas in higher-order builtin
  callback positions now lower to a synthetic AOT closure descriptor plus
  a =nelisp_aot_make_closure= bridge call before the existing
  =nelisp_aot_builtin_calln= dispatch.  The runtime side can register a
  descriptor and materialize a canonical =nelisp-closure= with captured
  values, while object output exposes the new bridge relocation.  Full
  native plan embedding/rooting and native frame write-through for
  captured =setq= cells remain pending.
- *129.7V MVP*: closure descriptors now participate in the runtime
  module-init plan.  =nelisp-cc-runtime-run-aot-module-init-plan=
  registers them by default, so =nelisp_aot_make_closure= can resolve
  compiler-emitted descriptor names through runtime-owned module
  metadata instead of test-local setup.
- *129.7W MVP*: the compiler now exposes
  =nelisp-phase47-compiler--module-init-plan=, a single normalized
  module-plan handoff that includes init helpers, =defcustom= metadata,
  root descriptors, and closure descriptors.  This closes the
  compiler/runtime metadata gap before native object embedding.
- *129.7X MVP*: literal lambda values in escaping value position now
  lower to the same AOT closure descriptor plus
  =nelisp_aot_make_closure= bridge used by captured callbacks.  This
  covers =(function (lambda ...))= and raw literal lambda values as
  boxed closure objects in =OUT=; native plan embedding/rooting and
  native frame write-through for captured =setq= cells remain pending.
- *129.7Y MVP*: direct =funcall= of literal lambdas that mutate
  captured variables now avoids by-value lambda lifting and instead
  materializes a canonical heap closure before dispatching through
  =nelisp_aot_funcall1=.  The closure body can use the runtime
  closure's shared env cells for captured mutation.  Write-through from
  those cells back into surrounding native frame slots remains pending.
- *129.8A MVP*: =nelisp-cc-runtime.el= now owns an AOT handler-stack
  simulator for =catch= / =condition-case= / =unwind-protect=.  The
  runtime can push/pop catch, condition, and unwind handlers; =throw=
  and =signal= walk the innermost-first stack, run crossed cleanup
  functions, pop through the matching handler, and return a landing
  descriptor carrying =landing-pad= / =saved-sp= / value or error data.
  Native code generation for handler records, real stack-pointer
  restore, and landing-pad jumps remain pending.
- *129.8B MVP*: boxed-boundary defuns can now lower =(throw TAG
  VALUE)= and =(signal TAG DATA)= through =nelisp_aot_throw= /
  =nelisp_aot_signal=.  Dynamic =TAG= values are passed through as
  supplied boxed values; quoted symbol tags such as ='done= are
  materialized into caller-owned =name-slot= / =name_slot= with
  =sexp-write-symbol-lit=.  =nelisp-cc-runtime= provides matching
  six-argument boundary bridges that write the 129.8A landing
  descriptor to =OUT=.  Native stack restoration and landing-pad jumps
  remain pending.
- *129.8C MVP*: the handler-registration side of the same machinery is
  now visible to object output.  Internal AOT forms
  =aot-push-catch=, =aot-push-condition=, and =aot-push-unwind= lower
  to =nelisp_aot_push_catch= / =nelisp_aot_push_condition= /
  =nelisp_aot_push_unwind= with the fixed six-GP boundary
  =MIRROR/FRAMES/SELECTOR/LANDING-PAD/SAVED-SP/SCRATCH=.  Runtime
  bridges install the corresponding 129.8A handler records.  Full
  source-level =(catch ...)= / =(condition-case ...)= /
  =(unwind-protect ...)= lowering remains pending.
- *129.8D MVP*: the normal-exit cleanup side is now visible as
  =aot-pop-handler=.  It lowers through =nelisp_aot_pop_handler= with
  =MIRROR/FRAMES/EXPECTED-KIND/OUT/SCRATCH=, writes the popped handler
  descriptor into =OUT= on the elisp bridge, and validates the expected
  handler kind when supplied.  129.8E starts using this for
  source-level =catch= normal exits; non-local exits and other handler
  forms still need native landing-pad control flow.
- *129.8E MVP*: source-level =(catch TAG BODY...)= now synthesizes
  the first balanced handler path for bodies that return normally.  It
  lowers to =aot-push-catch=, saves BODY's result in a runtime Sexp
  frame slot, emits =aot-pop-handler 'catch=, then returns the saved
  result.  Bodies containing explicit =throw=, =signal=,
  =condition-case=, or =unwind-protect= are rejected until native
  landing-pad jumps are available.
- *129.8F MVP*: source-level =(condition-case VAR BODY (CONDITION
  HANDLER...))= now has the same normal-exit path for a single symbol
  condition selector.  BODY is saved through a runtime Sexp frame slot,
  =aot-pop-handler 'condition= removes the handler on normal exit, and
  the saved BODY result is returned.  Signalled handler dispatch,
  binding =VAR= to error data, and native landing-pad jumps remain
  pending; list condition specs are covered by 129.8J.
- *129.8G MVP*: source-level =(unwind-protect BODY CLEANUP...)= now
  supports the normal-exit path.  BODY's result is saved in a runtime
  Sexp frame slot, cleanup forms run in order, and the saved BODY value
  is returned.  Non-local exits crossing the protected body still need
  cleanup landing-pad emission and stack restoration.
- *129.8H MVP*: source-level =(error DATA)= now lowers as the
  specialized condition signal =(signal 'error DATA)= through
  =nelisp_aot_signal=.  DATA is already a boxed value supplied by the
  caller/compiler.
- *129.8I MVP*: formatted =(error ARG...)= now lowers through
  =nelisp_aot_errorn= for two-or-more source arguments.  The ABI is
  =MIRROR/FRAMES/ARGC/OUT/SCRATCH/ARG...=; the runtime validates
  =ARGC=, builds the standard =error= signal data list (including
  =(format FMT ARG...)= when the first argument is a string), delegates
  to the signal boundary, writes =OUT=, and returns =OUT=.  Literal
  format strings such as =(error "%s" value)= materialize through
  =sexp-write-str-lit= into the caller-owned =scratch= slot.
- *129.8J MVP*: source-level =condition-case= normal-exit lowering now
  accepts list condition specs such as =((error quit) HANDLER...)=.
  The compiler emits one =aot-push-condition= per selector with the same
  placeholder landing metadata, then emits a matching number of
  =aot-pop-handler 'condition= forms on the normal path.  Handler-body
  dispatch for signalled conditions remains pending on native landing
  pads.
- *129.8K MVP*: the same normal-exit lowering now walks every handler
  clause in a source =condition-case= form instead of only the first
  clause.  Each symbol selector, including symbols inside list
  selectors, gets a balanced push/pop pair.  Handler-body selection for
  actual signalled exits remains pending.
- *129.8L MVP*: direct =(catch TAG (throw TAG VALUE))= now lowers
  through the 129.8A handler stack without pretending native landing-pad
  jumps are complete.  The compiler pushes the catch handler, emits the
  =nelisp_aot_throw= bridge, then extracts the returned landing
  descriptor's =:value= through =nelisp_aot_landing_value=.  Nested
  non-local exits still require real landing labels and branch-to-pad
  emission; the first conditional throw branch shape is covered by
  129.8N.
- *129.8M MVP*: direct =(condition-case VAR (signal 'TAG DATA)
  (HANDLER BODY...))= and direct =error= bodies now statically select
  the first matching handler clause, push that condition handler, emit
  the existing signal/error bridge, extract the descriptor's =:error=
  through =nelisp_aot_landing_error=, bind =VAR= when present, and run
  the handler body.  129.8Q extends the same static dispatch to one-level
  conditional bodies whose signalled branch has a direct quoted condition
  tag and whose normal branch has no unresolved non-local exit.  129.8R
  extends this to nested conditional signal trees; dynamic condition paths
  still require real landing labels and branch-to-pad emission.
- *129.8N MVP*: direct conditional =(catch TAG (if TEST (throw TAG
  VALUE) FALLBACK))= now pushes one catch handler before the branch,
  extracts =:value= through =nelisp_aot_landing_value= on the throwing
  branch, and emits the normal =aot-pop-handler 'catch= only on the
  non-throwing branch.  129.8R extends this to nested conditional catch
  trees; mismatched-tag catch bodies still require real landing labels
  and branch-to-pad emission.
- *129.8O MVP*: direct =(unwind-protect (throw 'TAG VALUE) CLEANUP...)=
  now evaluates =VALUE= into a runtime Sexp slot, runs cleanup forms in
  order, then emits the existing =throw= bridge with the saved value.
  Nested or conditional protected bodies still require real cleanup
  landing pads and branch-back emission.
- *Previous state*: DRAFT 2026-05-25 — design only.
  Scoping doc for the long-horizon "compile arbitrary user elisp to
  native code" track.  Authored after a 2026-05-25 session that
  (a) root-caused + fixed the standalone reader heap-corruption that
  blocked loading large .elc (Doc 129 §6 prerequisite, now SHIPPED),
  and (b) audited the grammar gap between the Phase 47 compiler's
  current surface and what =anvil-server.el=-class application code
  needs.
- *Parent / siblings*:
  - Doc 97 (= Sexp → asm compiler, §97.a/b/c SHIPPED) — the frontend
    this doc extends.
  - Doc 99 (= eval-loop wiring / Arch C, DRAFT) — the ABI machinery
    for the interpreter to *call* compiled code.  Doc 129 is the
    *language-coverage* axis; Doc 99 is the *call-boundary* axis.
    Both must land for user-.el AOT to pay off.
  - Doc 122 (= grammar extensions for substrate elisp化) — adds
    *low-level primitive* grammar (str/sym alloc, mut-str builder,
    extended extern-call, atomic/raw-mem).  Doc 129 *consumes* those
    primitives but targets *application-level* language constructs.
  - Doc 126 (= image format retirement, SHIPPED) — made eval-from-
    source the production STDLIB boot path.  Doc 129's payoff is the
    same shape one layer up: replace *interpret user .el* with
    *run AOT-compiled user .el*.
- *Motivation*: the standalone-NeLisp anvil cold-load is ~286s,
  dominated (≈100%) by the pure-elisp evaluator walking ~13K LOC of
  user/substrate forms at ~1 MB/s.  The Reader is already Phase 47
  native (Doc 116, 394x); the *evaluator* of user code is the
  remaining interpreted hot spot.  Doc 129 is the structural endgame:
  compile user .el to native so cold-load drops to near-link-time.

* §0 TL;DR

** §0.1 The gap, in one sentence

The Phase 47 compiler (=lisp/nelisp-phase47-compiler.el=, ~5,900 LOC)
is a frontend for a *restricted Sexp DSL* — values are raw =i64= in
registers + rbp spill slots, with no heap, no GC integration, no
tagged-value calculus, no closures, no dynamic dispatch.  It was built
for *Phase 47 self-application* (= rewriting nelisp's own Rust
primitive bodies as elisp that emits identical asm), not for compiling
arbitrary elisp.  Compiling =anvil-server.el=-class code requires
turning that "i64 calculator" into a full "Lisp value machine".

** §0.2 What the current grammar covers (= SHIPPED, Doc 97.a/b/c)

Statement forms: =write=, =exit=, =seq=, =let= (single binding,
constant-or-foldable value), =defun=, intra-program =call=,
=static-imm32-table-define=.

Value forms: integer literal, param/let symbol ref, =+= / =-= / =*=
(compile-time fold + runtime emit), =/= shifts + bitwise, comparisons
(=<= =>= =<== =>== ===), =and= / =or= / =if= / =while= / =cond=,
=extern-call= (6 register i64 args plus SysV stack-GP call-spill MVP + i64 ret), direct =syscall=, f64 arithmetic
(Doc 118), Sexp-ABI direct field access (Doc 100), hash-table desugar
(Doc 49 Wave 11).

Architecture: parse (Sexp → IR) → collect strings/tables/defuns →
two-pass emit (fixed-width instructions → address resolution →
relocations) → =nelisp-elf-write-binary=.  SysV AMD64 (+ arm64).

** §0.3 What application .el needs that is missing (= the gap audit)

Frequency from a 2026-05-25 grep of the 6 GREEN anvil modules
(=anvil-server*.el= + =anvil-discovery/sqlite/bench.el=, ~4,400 LOC):

| Construct                         | Uses | Phase 47 today |
|-----------------------------------+------+----------------|
| =defun= / =let= / =if= / =cond= / =and= / =or= / =while= | core | ✓ partial      |
| =let= / =let*= (N bindings, runtime values, lexical scope) | 193  | partial (=let-rt-n= runtime MVP) |
| =setq= (incl. global)             | 90   | partial (boxed helper + symbol literal helper) |
| =format= / =message= / =string-match= | 114  | partial (calln builtin delegation table) |
| =lambda= + captured-env closure   | 28   | ✗ (no heap env + fn ptr) |
| =require= / =provide=             | 30   | partial (top-level compile-time forms) |
| =error= / =condition-case= / =catch= / =throw= / =unwind-protect= | 55+ | ✗ (no non-local exit) |
| =dolist= / =dotimes= / =mapcar= / =apply= / =funcall= | 50+  | partial (funcall/apply + dynamic mapcar delegation) |
| =make-hash-table= + access        | 11   | partial (desugar only) |
| =defvar= / =defconst= / =defcustom= | 21   | partial (AOT init helpers + runtime custom metadata/context table; native helper call pending) |
| =defmacro= + macro use            | few  | partial (frontend macro expansion MVP) |

* §1 Mechanism groups (= the actual work, grouped by what they share)

Each group is an independent implementation effort; they are ordered
so earlier groups unblock later ones.

** §1.A Tagged-value calculus (= the foundational shift)

Today an IR value is a raw =i64= in a register.  Real elisp values are
tagged =Sexp= (Int / Float / Cons / Str / Symbol / Vector / closure).
Every value-producing op must box/unbox and type-dispatch.

- New IR value representation: a slot holds a =Sexp= (tagged pointer),
  not a bare integer.  Arithmetic unwraps Int/Float, re-boxes result.
- Reuses Doc 100 Sexp-ABI access ops (=sexp-tag=, =sexp-int-unwrap=,
  =sexp-int-make=, =sexp-float-unwrap=) already in the grammar.
- This is the *largest* single change: it rewrites =--emit-value= /
  =--emit-arith= to thread tagged values, and adds a runtime
  type-error path (signal wrong-type-argument).

*Why first*: every other group manipulates tagged values.

*** §1.A.1 Shipped MVP: boxed-boundary Int functions

The first 129.2 implementation deliberately avoids rewriting the
entire Phase 47 raw-i64 IR.  Instead, =defun-sexp-int= defines a
boxed-boundary function:

#+begin_src elisp
(defun-sexp-int add (out a b) (+ a b))
#+end_src

This lowers to:

#+begin_src elisp
(defun add (out a b)
  (sexp-int-make out
    (+ (sexp-int-unwrap a)
       (sexp-int-unwrap b))))
#+end_src

The result is ABI-useful for runtime/object integration: callers pass
a mutable =Sexp= output slot plus =Sexp::Int= input pointers, and the
compiled code writes the boxed result.  It does not yet provide
general tagged locals, GC roots, type errors, boxed truth values, or
arbitrary user-elisp value semantics.

** §1.B Heap + GC root integration

=cons=, =make-hash-table=, strings, vectors, and closures all
allocate.  Phase 47 has no heap today.

- Allocation: call the existing allocator via =extern-call= (Doc 122.A
  =nl_alloc_str= / =nl_alloc_symbol=, Doc 125 alloc/dealloc grammar).
  No new allocator — delegate.
- *GC roots*: the hard part.  The bytecode VM (=nelisp-bc-run=)
  already registers in-flight stack via =nelisp-gc--active-vms= (Doc
  3c).  AOT-compiled frames must do the equivalent: spill-slot values
  that are live across an allocation must be reachable by the
  collector.  Options: (a) a shadow stack map emitted per call site,
  (b) conservative scan of the spill region, (c) pin live values into
  a frame-local GC-rooted vector.  Recommend (c) for correctness-first
  (matches the VM's active-vm vector), optimise later.

*Why second*: closures / hash / strings in later groups all allocate.

*** §1.B.1 Shipped MVP: static root map for annotated Sexp params

Stage 129.5A adds the compiler-side metadata needed before runtime
root registration can be correct:

#+begin_src elisp
(defun make-str ((slot :type sexp) bytes len)
  (sexp-write-str slot bytes len))
#+end_src

The parameter still uses the GP ABI path, but the =ref= node carries
=:root-p t=.  If the defun body contains a known allocator
(=sexp-write-str=, =sexp-write-symbol=, =cons-make=,
=cons-make-with-clone=, =vector-make=, =record-make=, =cell-make=,
=mut-str-make-empty=, or =alloc-bytes=), the defun IR receives
=:gc-root-slots= with the referenced annotated Sexp slots.

This deliberately stops short of claiming full GC integration: the
compiled prologue does not yet register a root vector, no allocation
site consults a stack map, and runtime =let= locals cannot yet be
typed as Sexp roots.  It is the static map that later 129.5 work will
consume for frame-local GC-rooted vectors.

*** §1.B.2 Shipped MVP: AOT frame roots in the runtime root set

Stage 129.5B adds the runtime-side root category:

#+begin_src elisp
(nelisp-gc--with-active-aot-frame root-vector
  ...)
#+end_src

While the dynamic extent is active, =nelisp-gc-root-set= includes a
=(:kind aot-frame :value ROOT-VECTOR)= entry.  The existing
=nelisp-gc-reachable-set= walker already traverses vectors, so each
slot in =ROOT-VECTOR= becomes reachable through the same mark path as
bytecode VM stack roots.

This is still not full native-frame GC integration.  Stage 129.5E
adds explicit native push/pop hooks; the remaining bridge is to have
Phase 47 / Doc 99 call-boundary code materialize a root vector from
=:gc-root-slots= and insert those hooks around compiled function
execution automatically.

*** §1.B.3 Shipped MVP: call-boundary root-vector bridge

Stage 129.5C connects the 129.5A static map to the 129.5B runtime
substrate without yet pretending the backend emits native push/pop
code.

Compiler side:

#+begin_src elisp
(nelisp-phase47-compiler--gc-root-descriptor-for-defun defun-ir)
;; => (:name make-str :slots (0) :param-count 3 :rt-slot-count 0)
#+end_src

Runtime side:

#+begin_src elisp
(let ((roots (nelisp-cc-runtime-aot-root-vector-from-slots frame '(0))))
  (nelisp-cc-runtime-call-with-aot-roots roots
    (lambda () ...)))
#+end_src

Inside the dynamic extent, =nelisp-gc-root-set= exposes =roots= as an
=aot-frame= root and the normal reachable-set walker traverses it.
This is the Doc 99 handoff contract for the interpreter/compiled-code
boundary.  Stage 129.5E gives the backend explicit extern-call hooks
to use for the prologue/epilogue side.

*** §1.B.4 Shipped MVP: runtime let locals as Sexp roots

Stage 129.5D extends the static root map beyond params:

#+begin_src elisp
(defun make-str (raw)
  (let (((slot :type sexp) (id raw)))
    (sexp-write-str slot raw raw)))
#+end_src

The annotated runtime =let= local still lowers through the existing
=let-rt= / =let-rt-n= spill-slot path, but its =FENV= entry carries
=:root-p t=.  When the body contains an allocation and references that
local, the defun's =:gc-root-slots= includes the runtime slot (for the
example above, slot 1: param slot 0 plus one runtime local).

This keeps the 129.5C descriptor format unchanged.  It makes the
descriptor more useful by allowing frame-local Sexp temporaries, not
just annotated incoming params, to be copied into the AOT root vector.

*** §1.B.5 Shipped MVP: explicit native root push/pop bridges

Stage 129.5E exposes the root-frame operation as ordinary native
relocs:

#+begin_src elisp
(aot-push-roots roots)
...
(aot-pop-roots roots)
#+end_src

The compiler lowers these internal forms to:

#+begin_src elisp
(extern-call nelisp_aot_push_roots mirror frames roots out scratch)
(extern-call nelisp_aot_pop_roots mirror frames roots out scratch)
#+end_src

Runtime side, =nelisp-cc-runtime-aot-push-roots-boundary= validates
that =ROOTS= is a vector, pushes it onto
=nelisp-gc--active-aot-frames=, writes the pushed vector to =OUT[0]=,
and returns =OUT=.  =nelisp-cc-runtime-aot-pop-roots-boundary= pops
the innermost AOT root vector, verifies the expected vector when
supplied, writes the popped vector to =OUT[0]=, and returns =OUT=.

This is a native ABI surface, not yet automatic root management for
every allocating defun.  Stages 129.5F/G connect it to boundary defun
emission and materialize root vectors from =:gc-root-slots= for that
surface.

*** §1.B.6 Shipped MVP: automatic root scope for boundary defuns

Stage 129.5F connects the explicit bridge to the ordinary defun emit
path when the function already has the Doc 99 boundary locals:

#+begin_src elisp
(defun make-str
    ((out :type sexp) (mirror :type sexp) (frames :type sexp)
     (scratch :type sexp) (roots :type sexp) bytes len)
  (sexp-write-str out bytes len))
#+end_src

The parser keeps the static descriptor =(:gc-root-slots (0))=, then
wraps the body in an internal =aot-root-scope=.  The x86_64 SysV
emitter calls =nelisp_aot_push_roots= before the allocating body and
=nelisp_aot_pop_roots= afterwards; it saves the body's GP result
across the pop call so the compiled defun's return value is unchanged.

This is the first automatic prologue/epilogue insertion step.  Stage
129.5G below removes the need for the caller to prebuild the =roots=
vector.

*** §1.B.7 Shipped MVP: root-vector materialisation bridge

Stage 129.5G adds the missing vector construction step:

#+begin_src elisp
(extern-call nelisp_aot_materialize_roots
             mirror frames 1 out scratch out)
#+end_src

For an =aot-root-scope= whose =:gc-root-slots= are =(0)=, the compiler
maps slot 0 back to its frame symbol, passes that value to the bridge,
and stores the returned root vector in the =roots= frame slot before
calling =nelisp_aot_push_roots=.  Runtime-side,
=nelisp-cc-runtime-aot-materialize-roots-boundary= validates the count,
builds a fresh vector from the root arguments, writes it to =OUT[0]=,
and returns it.

This is still a boundary-defun MVP: the defun must expose the Doc 99
locals =out=, =mirror=, =frames=, =scratch=, and =roots=.  Within that
surface, root vectors now come from =:gc-root-slots= instead of a
caller-provided prebuilt vector.

** §1.C General =let= / =let*= + lexical environment

Replace the single-constant-binding =let= with an N-binding lexical
scope: a chain of frame slots (or a heap lexframe — Doc 115
=env_lexframe= shims already exist for the substrate).  Distinguish
lexical from dynamic (=defvar='d special) variables.

*** §1.C.1 Shipped MVP: multi-binding runtime let

Stage 129.4 adds a narrow but useful =let-rt-n= IR for multi-binding
=let= in =defun= bodies.  Runtime initializers are parsed against the
original =ENV/FENV=, so later bindings do not see earlier bindings
(parallel =let=, not =let*=).  Each non-foldable initializer gets a
frame slot and the body parses with all slots visible through the
existing =ref= load path.

Compile-time-foldable bindings still fold into =ENV=.  Mixed
compile-time/runtime multi-binding forms are allowed, but this is
still an MVP: duplicate variables are rejected, special-variable
dynamic binding is not modeled, and no heap lexframe / GC-root map
exists yet.

*** §1.C.2 Shipped MVP: special declaration guard

Stage 129.4B records top-level =defvar= / =defconst= / =defcustom=
names while extracting compile-time-only declarations.  During the
same compile unit, binding one of those names with =let= now signals
=:let-special-binding-pending= instead of silently lowering it to a
lexical compile-time binding or runtime frame slot.

This is intentionally a guard, not full dynamic binding.  Real special
=let= still needs value-cell save/restore, unwind cleanup, and
integration with the 129.8 non-local-exit path.

*** §1.C.3 Shipped MVP: special binding bridge substrate

Stage 129.4C adds the runtime-side bridge shape for dynamic special
binding:

#+begin_src elisp
(nelisp_aot_push_special mirror frames name value out scratch)
(nelisp_aot_pop_special  mirror frames handle out scratch)
#+end_src

The Emacs-side runtime simulator saves the previous value-cell state,
binds the symbol to the new value, returns a restore handle in =OUT=,
and restores the prior bound/unbound state on pop.  Phase 47 also
accepts explicit internal forms:

#+begin_src elisp
(aot-push-special 'dyn value)
(aot-pop-special handle)
#+end_src

Those forms lower to relocations against =nelisp_aot_push_special= and
=nelisp_aot_pop_special=, materializing quoted symbol names through
=name-slot=.  This stage only fixes the ABI and restore substrate;
source-level normal-exit lowering starts in 129.4D below.

*** §1.C.4 Shipped MVP: source special =let= normal exit

Stage 129.4D connects single-binding source =let= forms whose variable
is a tracked top-level special declaration:

#+begin_src elisp
(seq
 (defvar dyn)
 (defun f (out mirror frames scratch name-slot value)
   (let ((dyn value))
     value)))
#+end_src

The frontend lowers the body to the existing normal-exit bridge shape:

#+begin_src elisp
(seq
 (aot-push-special 'dyn value)
 (let (((aot-special-value-N :type sexp) BODY))
   (seq
    (aot-pop-special 0)
    aot-special-value-N)))
#+end_src

The =0= pop handle means "pop the innermost special binding" and is
used because the source-lowered normal path only needs stack discipline,
not an externally held handle.  The runtime still supports explicit
handle validation for lower-level =aot-pop-special= forms.

This normal-exit shape is extended by 129.4G for direct quoted-tag
=throw= bodies.  Other nested non-local bodies containing =signal=,
=error=, =condition-case=, or =unwind-protect= are rejected until the
129.8 landing-pad path can guarantee cleanup.  Multi-binding special
=let= starts in 129.4E below.

*** §1.C.5 Shipped MVP: all-special multi-binding =let= normal exit

Stage 129.4E extends the source special =let= lowering to forms whose
bindings are all tracked special declarations:

#+begin_src elisp
(seq
 (defvar dyn-a)
 (defvar dyn-b)
 (defun f (out mirror frames scratch name-slot value-a value-b)
   (let ((dyn-a value-a)
         (dyn-b value-b))
     value-b)))
#+end_src

To preserve ordinary parallel =let= initializer semantics, the
frontend first evaluates every initializer against the original
=ENV/FENV= into fresh Sexp temp slots, then pushes the special
bindings in source order, saves the BODY result, and pops the
innermost bindings:

#+begin_src elisp
(let (((aot-special-init-0 :type sexp) value-a)
      ((aot-special-init-1 :type sexp) value-b))
  (seq
   (aot-push-special 'dyn-a aot-special-init-0)
   (aot-push-special 'dyn-b aot-special-init-1)
   (let (((aot-special-value-N :type sexp) BODY))
     (seq
      (aot-pop-special 0)
      (aot-pop-special 0)
      aot-special-value-N))))
#+end_src

*** §1.C.6 Shipped MVP: mixed lexical/special multi-binding =let= normal exit

Stage 129.4F extends the same temp-first strategy to mixed forms:

#+begin_src elisp
(seq
 (defvar dyn)
 (defun f (out mirror frames scratch name-slot value-a value-b)
   (let (((a :type sexp) value-a)
         (dyn value-b))
     a)))
#+end_src

The lowering evaluates every initializer into a fresh temp slot before
any source binding becomes visible.  It then pushes the special
bindings, creates lexical aliases from their temp values, evaluates
BODY into a saved Sexp slot, pops the innermost special bindings, and
returns the saved BODY value:

#+begin_src elisp
(let (((aot-let-init-0 :type sexp) value-a)
      ((aot-let-init-1 :type sexp) value-b))
  (seq
   (aot-push-special 'dyn aot-let-init-1)
   (let (((a :type sexp) aot-let-init-0))
     (let (((aot-special-value-N :type sexp) BODY))
       (seq
        (aot-pop-special 0)
        aot-special-value-N)))))
#+end_src

This keeps ordinary parallel =let= initializer semantics: lexical
initializers and special initializers both see the original ENV/FENV,
not each other's new bindings.

*** §1.C.7 Shipped MVP: source special =let= direct =throw= cleanup

Stage 129.4G covers the first non-local cleanup path for source special
=let=:

#+begin_src elisp
(let ((dyn value))
  (throw 'tag result))
#+end_src

The compiler evaluates =result= into a runtime Sexp slot while the
special binding is active, emits one =aot-pop-special 0= for each
source special binding, then emits the existing =throw= bridge with the
saved value.  The same shape applies to all-special multi-binding and
mixed lexical/special =let= forms; mixed forms first establish lexical
aliases so the saved throw value can refer to lexical temporaries.

Stage 129.4H extends this direct cleanup discipline to one-level
conditional bodies:

#+begin_src elisp
(let ((dyn value))
  (if value
      (throw 'tag value)
    value))
#+end_src

The generated shape evaluates the =if= test while the special binding is
active, then emits one cleanup sequence per branch.  Throwing branches
save the payload, pop all active source special bindings, and dispatch to
=nelisp_aot_throw=; normal branches save the result, pop the same
bindings, and return that saved result.  Mixed lexical/special =let=
forms wrap the whole conditional in lexical aliases so the test and both
branches observe the same source bindings.

Stage 129.4I extends this from one-level =if= bodies to nested
conditional trees:

#+begin_src elisp
(let ((dyn value))
  (if value
      (if value
          (throw 'tag value)
        value)
    value))
#+end_src

Every leaf emits the same cleanup sequence: evaluate and save the leaf
value, pop all active source special bindings, then either return the
saved value or dispatch the saved payload through =nelisp_aot_throw=.
Arbitrary non-conditional non-local bodies still require the 129.8
landing-pad path to guarantee cleanup.

** §1.D =setq= / =defvar= / =defconst= / =defcustom= — symbol cells

Writing a global needs symbol value-cell access.  =nl_env_set_value=
extern already exists (used by =nelisp-cc-sf-setq.el= for the
substrate setq swap) — reuse it.  =defvar= / =defcustom= become
=defvar=-then-conditional-set sequences at top level.

*** §1.D.1 Shipped MVP: boxed Int setq delegation

Stage 129.3A adds an explicit ABI surface rather than full user
=setq= syntax:

#+begin_src elisp
(defun-sexp-int-setq set-x (out mirror frames scratch name a)
  (+ a 16))
#+end_src

This lowers to:

#+begin_src elisp
(defun set-x (out mirror frames scratch name a)
  (seq
   (sexp-int-make out (+ (sexp-int-unwrap a) 16))
   (extern-call nelisp_env_set_value
                mirror frames name out scratch 0)
   out))
#+end_src

The caller still supplies =name= as a =Sexp::Symbol= pointer and a
prebuilt env scratch vector.  That keeps the commit inside existing
grammar/extern coverage and avoids pretending full user =setq= is
available before symbol literal materialisation, =defvar= boundp
checks, and general tagged values land.

*** §1.D.2 Shipped MVP: symbol literal materialisation for boxed setq

Stage 129.3B adds a narrow variant for compile-time-known variable
names:

#+begin_src elisp
(defun-sexp-int-setq-symbol set-x x
    (out mirror frames scratch name-slot a)
  (+ a 16))
#+end_src

This lowers to:

#+begin_src elisp
(defun set-x (out mirror frames scratch name-slot a)
  (seq
   (sexp-write-symbol-lit name-slot "x")
   (sexp-int-make out (+ (sexp-int-unwrap a) 16))
   (extern-call nelisp_env_set_value
                mirror frames name-slot out scratch 0)
   out))
#+end_src

=sexp-write-symbol-lit= calls the existing =nl_alloc_symbol= helper,
but its UTF-8 bytes are materialized on the stack for the duration of
the call.  That avoids adding =.rodata= to object-mode output, which
is still forbidden by the Doc 99 ET_REL spike path.

This removes the need for the caller to prebuild the symbol object,
but it remains a boxed-boundary helper: the caller still provides
=OUT=, environment pointers, scratch storage, and a writable
=NAME-SLOT=.

*** §1.D.3 Shipped MVP: symbol literal defvar conditional set

Stage 129.3C adds a sibling helper for =defvar=-style conditional
initialization:

#+begin_src elisp
(defun-sexp-int-defvar-symbol ensure-x x
    (out mirror frames scratch name-slot a)
  (+ a 16))
#+end_src

This lowers to:

#+begin_src elisp
(defun ensure-x (out mirror frames scratch name-slot a)
  (seq
   (sexp-write-symbol-lit name-slot "x")
   (if (= (extern-call nelisp_mirror_is_bound
                       mirror name-slot out)
          1)
       name-slot
     (seq
      (sexp-int-make out (+ (sexp-int-unwrap a) 16))
      (extern-call nelisp_env_set_value
                   mirror frames name-slot out scratch 0)
      name-slot))))
#+end_src

The return value is the materialized symbol slot in both branches,
matching the useful =defvar= result shape for this boxed-boundary
helper.  The =OUT= slot is dual-use: before the bound check, the
caller must initialize it with the runtime unbound-marker Sexp; on the
unbound branch, compiled code overwrites it with the boxed initial
value and delegates the actual value-cell write to the existing env
setter.

This proves the conditional-write substrate without changing the
top-level program ABI.  General user-facing =defvar= forms still need
the Doc 99/runtime entry bridge that supplies env pointers and the
unbound marker automatically.

*** §1.D.4 Shipped MVP: symbol literal defconst value + constant flag

Stage 129.3D adds the =defconst= sibling:

#+begin_src elisp
(defun-sexp-int-defconst-symbol const-x x
    (out mirror frames scratch name-slot a)
  (+ a 16))
#+end_src

This lowers to:

#+begin_src elisp
(defun const-x (out mirror frames scratch name-slot a)
  (seq
   (sexp-write-symbol-lit name-slot "x")
   (sexp-write-nil out)
   (extern-call nelisp_mirror_set_constant
                mirror name-slot out)
   (sexp-int-make out (+ (sexp-int-unwrap a) 16))
   (extern-call nelisp_env_set_value
                mirror frames name-slot out scratch 0)
   (sexp-write-t out)
   (extern-call nelisp_mirror_set_constant
                mirror name-slot out)
   name-slot))
#+end_src

The first =nelisp_mirror_set_constant= clears an existing constant flag
so the existing =nelisp_env_set_value= constant guard does not reject a
reinitializing =defconst=.  After the value write, the final call marks
the symbol constant again.  =OUT= is reused as the Nil/T flag slot and
as the boxed BODY value slot; =NAME-SLOT= remains the function result.

As with 129.3C, this is still an explicit boxed-boundary helper.
Direct top-level execution needs the Doc 99/runtime entry ABI to
supply env pointers, scratch storage, and result/name slots.

*** §1.D.5 Shipped MVP: top-level defvar/defconst/defcustom AOT init helpers

Stage 129.3E connects ordinary =defvar= / =defconst= top-level syntax
to the helper substrate for object output, Stage 129.3F extends the
same path to =defcustom= initial values, Stage 129.3G exposes the
matching =defcustom= metadata as compiler-side descriptors, and Stage
129.3H adds the runtime scheduling plan shape:

#+begin_src elisp
(seq
  (defvar x 42 "doc")
  (defconst y 7 "doc")
  (defcustom z 9 "doc" :type 'integer))
#+end_src

The frontend lowers that to generated helper defuns:

#+begin_src elisp
(seq
  (defun-sexp-int-defvar-symbol nelisp_aot_var_0_x x
      (out mirror frames scratch name-slot)
    42)
  (defun-sexp-int-defconst-symbol nelisp_aot_const_1_y y
      (out mirror frames scratch name-slot)
    7)
  (defun-sexp-int-defvar-symbol nelisp_aot_custom_2_z z
      (out mirror frames scratch name-slot)
    9))
#+end_src

Those helpers then lower through 129.3C / 129.3D.  The generated
symbols are deterministic and linker-friendly, so =compile-to-object=
emits normal GLOBAL FUNC symbols that the Doc 99 runtime layer can
call during module initialization.  Declaration-only =(defvar x)= is
stripped when other top-level forms remain; no special-variable
metadata is emitted yet.

For =defcustom=, the compiler also exposes a side descriptor:

#+begin_src elisp
(:name z
 :helper nelisp_aot_custom_2_z
 :standard 9
 :docstring "doc"
 :options (:type (quote integer)))
#+end_src

This validates the docstring and requires customization options to be
keyword/value pairs.  The descriptor is the handoff shape for Doc 99
module scheduling; runtime customization-table registration is still
pending.

For all top-level variable initializers, the compiler exposes ordered
init-helper descriptors:

#+begin_src elisp
((:kind defvar
  :name x
  :helper nelisp_aot_var_0_x
  :index 0)
 (:kind defconst
  :name y
  :helper nelisp_aot_const_1_y
  :index 1)
 (:kind defcustom
  :name z
  :helper nelisp_aot_custom_2_z
  :index 2))
#+end_src

=nelisp-phase47-compiler--module-init-plan= is the compiler-side
single-call handoff; it gathers top-level init descriptors,
=defcustom= metadata descriptors, 129.5C root descriptors, and 129.7
closure descriptors, then normalizes them through
=nelisp-cc-runtime-aot-module-init-plan= into one pure Doc 99 module
plan:

#+begin_src elisp
(:helper-order (nelisp_aot_var_0_x
                nelisp_aot_const_1_y
                nelisp_aot_custom_2_z)
 :custom-by-helper ((nelisp_aot_custom_2_z . ...))
 :root-descriptors (...)
 :closure-descriptors (...))
#+end_src

=nelisp-cc-runtime-run-aot-module-init-plan= executes that plan in
order once Doc 99 supplies the native helper caller and boundary
context:

#+begin_src elisp
(nelisp-cc-runtime-run-aot-module-init-plan
 plan
 '(:out OUT :mirror MIRROR :frames FRAMES
   :scratch SCRATCH :name-slot NAME-SLOT)
 call-helper-fn
 register-custom-fn)
#+end_src

The runtime side validates the context keys and fires
=register-custom-fn= only after the matching =defcustom= helper has
run.  =call-helper-fn= is still the Doc 99 loader boundary: it resolves
the symbol, performs the native call, and owns any platform-specific
FFI details.

When =register-custom-fn= is nil, runtime registration defaults to
=nelisp-cc-runtime-register-aot-custom-metadata=.  Metadata is stored
by variable name in =nelisp-cc-runtime--aot-custom-table= and can be
queried through =nelisp-cc-runtime-aot-custom-metadata= or snapshotted
with =nelisp-cc-runtime-aot-custom-table-snapshot=.  The table stores
only descriptor data plus the init helper kind/index, not the
transient =OUT/MIRROR/FRAMES/SCRATCH/NAME-SLOT= pointers.

=nelisp-cc-runtime-aot-init-helper-caller= is the standard
=call-helper-fn= implementation for Doc 99.  It resolves helper
symbols through =nelisp-cc-runtime-resolve-aot-init-helper=, which
wraps the existing Doc 81 =nelisp-cc-runtime-resolve-symbol= contract:

#+begin_src elisp
(:helper nelisp_aot_var_0_x
 :status :resolved        ;; or :host-stub / :not-found
 :addr #x...
 :abi-argv (OUT MIRROR FRAMES SCRATCH NAME-SLOT)
 :descriptor ...)
#+end_src

If Doc 99 supplies a native-call callback, runtime invokes it as
=(NATIVE-CALL RESOLUTION CONTEXT INIT-DESCRIPTOR)=.  On host Emacs,
the default resolver still returns =:host-stub= so ERT can inspect the
contract without dereferencing a native address.  =:not-found= raises
before any native-call callback is invoked.

=nelisp-cc-runtime-aot-init-helper-argv= is the single runtime-side
source of truth for the generated helper ABI.  Its order matches the
Stage 129.3E/F helper parameters:

#+begin_src elisp
(OUT MIRROR FRAMES SCRATCH NAME-SLOT)
#+end_src

=nelisp-cc-runtime-make-aot-init-context= fills the standard
helper-call boundary context:

#+begin_src elisp
(:out OUT :mirror MIRROR :frames FRAMES
 :scratch SCRATCH :name-slot NAME-SLOT)
#+end_src

When the caller does not supply =OUT=, =SCRATCH=, or =NAME-SLOT=, the
runtime allocates fresh one-slot vectors for those values.  The
convenience wrapper
=nelisp-cc-runtime-run-aot-module-init-plan-with-default-context=
uses the same allocation helper before delegating to the ordinary
module-init plan runner.  This gives Doc 99 a reusable boundary
context constructor while preserving the explicit callback point for
native helper invocation.

This still is not full top-level execution inside Phase 47 itself.
The compiled object exposes the initializer entry points and the
runtime now has a validated scheduling/execution hook, metadata store,
helper resolution/call contract, default allocation for the standard
out/scratch/name slots, a canonical native helper argv, and default
AOT closure descriptor registration; actual object loading, real native
call entry, real environment handles, and native embedding of the plan
remain Doc 99 call-boundary responsibilities.
Initializer values are currently limited to the same boxed Int
expression subset accepted by =defun-sexp-int-*= helpers.

** §1.E Closures + =funcall= / =apply= dispatch

=(lambda (x) (+ x cap))= = heap env record (the captured bindings) +
a code pointer.  =funcall= / =apply= dispatch through the runtime's
existing callable-resolution (=apply_function= / =nelisp--apply=) via
=extern-call= — *delegate to the runtime dispatcher rather than
re-implementing arity/&optional/&rest handling*.  Mirrors the
bytecode VM's MAKE-CLOSURE / CALL opcodes.

*** §1.E.1 Shipped MVP: one-argument =funcall= dispatcher bridge

Stage 129.7A adds the first higher-order call surface without yet
allocating closures.  A boxed-boundary defun may write:

#+begin_src elisp
(defun call-fn
    ((out :type sexp) (mirror :type sexp) (frames :type sexp)
     (scratch :type sexp) (fn :type sexp) (arg :type sexp))
  (funcall fn arg))
#+end_src

The parser lowers the body to:

#+begin_src elisp
(seq
 (extern-call nelisp_aot_funcall1
              mirror frames fn arg out scratch)
 out)
#+end_src

The runtime-side bridge has the same ABI order:

#+begin_src elisp
(nelisp-cc-runtime-aot-funcall1 mirror frames fn arg out scratch)
#+end_src

It dispatches through =nelisp--apply= when available, falls back to
host =funcall= for host symbols/functions, writes the result to =OUT=,
and returns =OUT=.  This fixes the call-boundary contract for an
already-materialized function designator/value.  It does not yet build
heap closures from =lambda= forms, capture lexical environments, support
multi-argument =funcall=, or implement =apply=.

*** §1.E.2 Shipped MVP: two-argument =funcall= dispatcher bridge

Stage 129.7B adds the fixed two-argument sibling:

#+begin_src elisp
(defun call-fn2
    ((out :type sexp) (mirror :type sexp) (frames :type sexp)
     (fn :type sexp) (arg0 :type sexp) (arg1 :type sexp))
  (funcall fn arg0 arg1))
#+end_src

The parser lowers the body to:

#+begin_src elisp
(seq
 (extern-call nelisp_aot_funcall2
              mirror frames fn arg0 arg1 out)
 out)
#+end_src

The runtime-side bridge is:

#+begin_src elisp
(nelisp-cc-runtime-aot-funcall2 mirror frames fn arg0 arg1 out)
#+end_src

Unlike =funcall1=, the fixed two-argument ABI does not carry =SCRATCH=:
=MIRROR/FRAMES/FN/ARG0/ARG1/OUT= consumes the original six GP-register
surface.  129.7E below opens the first SysV stack-GP slot for the next
fixed arity; general calln still needs argument-list/vector
construction rather than an unbounded family of fixed bridges.

*** §1.E.2.1 Shipped MVP: three-argument =funcall= dispatcher bridge

Stage 129.7E connects the first outgoing stack-GP argument to Doc129's
higher-order call surface:

#+begin_src elisp
(defun call-fn3
    ((out :type sexp) (mirror :type sexp) (frames :type sexp)
     (fn :type sexp) (arg0 :type sexp) (arg1 :type sexp)
     (arg2 :type sexp))
  (funcall fn arg0 arg1 arg2))
#+end_src

The parser lowers the body to:

#+begin_src elisp
(seq
 (extern-call nelisp_aot_funcall3
              mirror frames fn arg0 arg1 arg2 out)
 out)
#+end_src

The runtime-side bridge is:

#+begin_src elisp
(nelisp-cc-runtime-aot-funcall3 mirror frames fn arg0 arg1 arg2 out)
#+end_src

The compiler work underneath this bridge is intentionally reusable:

- x86_64 SysV =extern-call= may now place GP args beyond the six
  register args on the outgoing call stack.  129.7G generalizes the
  non-trivial path through a temporary call-spill area so multiple
  computed stack args are evaluated left-to-right before the outgoing
  call frame is assembled.
- x86_64 SysV boxed defun prologues may now receive 7+ GP params and
  copy stack-supplied params into the same rbp-relative local slots used
  by the existing ref loader.  The current slot range remains bounded by
  the compiler's disp8 local-frame model.

*** §1.E.2.2 Shipped MVP: direct =funcall= calln/rest-list bridge

Stage 129.7H uses the 129.7G stack-argument machinery for ordinary
four-or-more-argument =funcall= calls:

#+begin_src elisp
(defun call-fn4
    ((out :type sexp) (mirror :type sexp) (frames :type sexp)
     (scratch :type sexp) (fn :type sexp)
     (arg0 :type sexp) (arg1 :type sexp)
     (arg2 :type sexp) (arg3 :type sexp))
  (funcall fn arg0 arg1 arg2 arg3))
#+end_src

The parser lowers the body to:

#+begin_src elisp
(seq
 (extern-call nelisp_aot_funcalln
              mirror frames fn 4 out scratch arg0 arg1 arg2 arg3)
 out)
#+end_src

The runtime-side bridge is:

#+begin_src elisp
(nelisp-cc-runtime-aot-funcalln mirror frames fn argc out scratch arg...)
#+end_src

This bridge is the first Doc129 rest-list construction path for direct
source call sites: it validates =ARGC= against the trailing native
arguments, uses those trailing values as the rest-list supplied to the
dispatcher, and then shares the same =nelisp--apply= / host =apply=
fallback as =nelisp_aot_apply=.  It deliberately keeps every trailing
native value as a user argument; there is no optional dispatcher slot in
this ABI.

Remaining 129.7 work: native object embedding/root-vector pinning and
native frame write-through for captured =setq= cells.

*** §1.E.3 Shipped MVP: =apply= dispatcher bridge

Stage 129.7C adds the list-backed higher-order path:

#+begin_src elisp
(defun call-apply
    ((out :type sexp) (mirror :type sexp) (frames :type sexp)
     (scratch :type sexp) (fn :type sexp) (args :type sexp))
  (apply fn args))
#+end_src

The parser lowers the body to:

#+begin_src elisp
(seq
 (extern-call nelisp_aot_apply
              mirror frames fn args out scratch)
 out)
#+end_src

The runtime-side bridge is:

#+begin_src elisp
(nelisp-cc-runtime-aot-apply mirror frames fn args-list out scratch)
#+end_src

=ARGS-LIST= is assumed to already be a proper boxed Sexp list supplied
at the boundary.  The bridge dispatches via =nelisp--apply= when
present and host =apply= otherwise, writes the result to =OUT=, and
returns =OUT=.

*** §1.E.3.1 Shipped MVP: =apply= splicing bridge

Stage 129.7I adds the ordinary source splicing form:

#+begin_src elisp
(defun call-applyn
    ((out :type sexp) (mirror :type sexp) (frames :type sexp)
     (scratch :type sexp) (fn :type sexp)
     (arg0 :type sexp) (arg1 :type sexp) (args :type sexp))
  (apply fn arg0 arg1 args))
#+end_src

The parser lowers the body to:

#+begin_src elisp
(seq
 (extern-call nelisp_aot_applyn
              mirror frames fn 3 out scratch arg0 arg1 args)
 out)
#+end_src

The runtime-side bridge is:

#+begin_src elisp
(nelisp-cc-runtime-aot-applyn mirror frames fn argc out scratch arg...)
#+end_src

The final native argument is validated as the list tail.  Any preceding
native arguments are appended in front of that tail, then dispatched
through the same apply fallback as =nelisp_aot_apply=.

*** §1.E.3.2 Shipped MVP: source =&rest= parameter binding

Stage 129.7J adds source =&rest= syntax to Phase 47 =defun= forms:

#+begin_src elisp
(defun collect
    ((out :type sexp) &rest (args :type sexp))
  args)
#+end_src

The native ABI still receives =args= as one ordinary final boxed
parameter.  Direct source calls construct that list in the caller before
entering the callee:

#+begin_src elisp
(defun call-collect
    ((out :type sexp) (mirror :type sexp) (frames :type sexp)
     (scratch :type sexp) (a :type sexp) (b :type sexp))
  (collect out a b))
#+end_src

lowers as if it had written:

#+begin_src elisp
(seq
 (extern-call nelisp_aot_listn
              mirror frames 2 scratch out a b)
 (collect out scratch))
#+end_src

The runtime-side bridge is:

#+begin_src elisp
(nelisp-cc-runtime-aot-listn mirror frames argc out scratch arg...)
#+end_src

This is deliberately a boxed-boundary MVP: the caller must expose
=out/mirror/frames/scratch= so the compiler has a destination slot for
the constructed rest list.  General closure/lambda allocation and
captured environments remain later 129.7 work.

*** §1.E.3.3 Shipped MVP: non-capturing literal lambda =funcall= lifting

Stage 129.7K starts the lambda side without pretending heap closures
exist.  A literal lambda used directly as a =funcall= designator is
rewritten during frontend preprocessing:

#+begin_src elisp
(defun caller (x)
  (funcall (lambda (y) (+ y 1)) x))
#+end_src

becomes:

#+begin_src elisp
(seq
 (defun nelisp_aot_lambda_0 (y) (+ y 1))
 (defun caller (x)
   (nelisp_aot_lambda_0 x)))
#+end_src

The same lift applies to =(function (lambda ...))= forms.  Stage
129.7R extends this direct-call path to read-only captures by threading
free variables as leading synthetic defun arguments.  This keeps
literal lambda calls on the native direct-call path while still leaving
escaping closure objects and captured mutation for later heap-closure
work.

*** §1.E.3.4 Shipped MVP: direct literal lambda lifting

Stage 129.7L applies the same lift to direct lambda application:

#+begin_src elisp
(defun caller (x)
  ((lambda (y) (* y 2)) x))
#+end_src

becomes a synthetic sibling defun plus an ordinary direct call.  Stage
129.7R also covers read-only captures for this direct application
shape, including runtime =let= slots.  It does not create heap closure
values, so escaping closure values and captured mutation still wait for
the remaining closure allocation/capture work.

*** §1.E.3.5 Shipped MVP: map-family literal lambda lifting

Stage 129.7M applies the same non-capturing lambda lift to the first
argument of the map-family builtins added in 129.6I/J:

#+begin_src elisp
(defun caller
    ((out :type sexp) (mirror :type sexp) (frames :type sexp)
     (scratch :type sexp) (name_slot :type sexp) (xs :type sexp))
  (mapcar (lambda (x) (+ x 1)) xs))
#+end_src

becomes a synthetic top-level defun plus a map call using the
function-designator materialization path:

#+begin_src elisp
(seq
 (defun nelisp_aot_lambda_0 (x) (+ x 1))
 (defun caller (...)
   (mapcar #'nelisp_aot_lambda_0 xs)))
#+end_src

The later 129.6J lowering writes =mapcar= into =name-slot= and the
synthetic function symbol into =scratch= before the
=nelisp_aot_builtin_calln= bridge.  Non-capturing callbacks keep this
lambda-lift path; captured callbacks start using the 129.7U closure
bridge described below.

*** §1.E.3.6 Shipped MVP: indexed higher-order builtin lambda lifting

Stage 129.7N generalizes 129.7M from a hard-coded map-family first
argument to the same function-designator index table used by 129.6
calln lowering:

#+begin_src elisp
(defun mapcan-caller
    ((out :type sexp) (mirror :type sexp) (frames :type sexp)
     (scratch :type sexp) (name_slot :type sexp) (xs :type sexp))
  (mapcan (lambda (x) x) xs))

(defun sort-caller
    ((out :type sexp) (mirror :type sexp) (frames :type sexp)
     (scratch :type sexp) (name_slot :type sexp) (xs :type sexp))
  (sort xs (lambda (a b) (< a b))))
#+end_src

Both forms become synthetic top-level defuns plus ordinary
quoted/function designators at the relevant callback position:

#+begin_src elisp
(seq
 (defun nelisp_aot_lambda_0 (x) x)
 (defun mapcan-caller (...)
   (mapcan #'nelisp_aot_lambda_0 xs)))

(seq
 (defun nelisp_aot_lambda_1 (a b) (< a b))
 (defun sort-caller (...)
   (sort xs #'nelisp_aot_lambda_1)))
#+end_src

The later 129.6K/L lowering then materializes the synthetic function
symbol into =scratch= and dispatches through
=nelisp_aot_builtin_calln=.  This remains the non-capturing path;
captured callback literals now flow through the 129.7U closure bridge.

*** §1.E.3.7 Shipped MVP: maphash literal lambda lifting

Stage 129.7O extends the same non-capturing literal lambda lift to the
=maphash= callback position added in 129.6M:

#+begin_src elisp
(defun maphash-caller
    ((out :type sexp) (mirror :type sexp) (frames :type sexp)
     (scratch :type sexp) (name_slot :type sexp) (table :type sexp))
  (maphash (lambda (k v) k) table))
#+end_src

The frontend rewrites it to a synthetic sibling defun plus a
quoted/function designator:

#+begin_src elisp
(seq
 (defun nelisp_aot_lambda_0 (k v) k)
 (defun maphash-caller (...)
   (maphash #'nelisp_aot_lambda_0 table)))
#+end_src

As with 129.7M/N, the lambda-lift path deliberately covers
non-capturing callbacks only.  Captured lexical variables are routed
through the 129.7U heap-closure bridge.

*** §1.E.3.8 Shipped MVP: seq.el higher-order literal lambda lifting

Stage 129.7P extends the same indexed lambda-lift path to the seq.el
higher-order family added in 129.6N:

#+begin_src elisp
(defun seq-caller
    ((out :type sexp) (mirror :type sexp) (frames :type sexp)
     (scratch :type sexp) (name_slot :type sexp) (xs :type sexp))
  (seq-filter (lambda (x) x) xs))
#+end_src

The frontend hoists the callback and rewrites the source call through
the quoted/function designator path:

#+begin_src elisp
(seq
 (defun nelisp_aot_lambda_0 (x) x)
 (defun seq-caller (...)
   (seq-filter #'nelisp_aot_lambda_0 xs)))
#+end_src

The covered family is =seq-map=, =seq-do=, =seq-filter=, =seq-remove=,
=seq-find=, =seq-some=, =seq-every-p=, =seq-count=, =seq-reduce=, and
=seq-mapcat=.  This remains the non-capturing lambda MVP; captured
callbacks use the 129.7U closure bridge.

Stage 129.7Z extends the same literal-lambda path to the additional
seq.el builtins added by 129.6R: =seq-keep=, =seq-mapn=,
=seq-map-indexed=, =seq-sort=, and =seq-uniq=.  =seq-uniq= uses the
second source argument when a test function is supplied; the others use
the first argument.

*** §1.E.3.9 Shipped MVP: cl-lib higher-order literal lambda lifting

Stage 129.7Q applies the same non-capturing callback lift to cl-lib's
common higher-order functions:

#+begin_src elisp
(defun cl-caller
    ((out :type sexp) (mirror :type sexp) (frames :type sexp)
     (scratch :type sexp) (name_slot :type sexp) (xs :type sexp))
  (cl-find-if (lambda (x) x) xs))
#+end_src

The callback becomes a synthetic top-level defun, then the call flows
through the 129.6O function-designator materialization path:

#+begin_src elisp
(seq
 (defun nelisp_aot_lambda_0 (x) x)
 (defun cl-caller (...)
   (cl-find-if #'nelisp_aot_lambda_0 xs)))
#+end_src

The covered family is =cl-mapcar=, =cl-mapc=, =cl-mapcan=,
=cl-maplist=, =cl-mapl=, =cl-mapcon=, =cl-some=, =cl-every=,
=cl-notany=, =cl-notevery=, =cl-count-if=, =cl-count-if-not=,
=cl-find-if=, =cl-find-if-not=, =cl-remove-if=, =cl-remove-if-not=,
=cl-position-if=, =cl-position-if-not=, and =cl-reduce=.  Capturing
callbacks now enter the 129.7U closure bridge instead of failing at
free-symbol analysis.

Stage 129.7Z also applies to the 129.6R cl-lib additions whose callback
position is fixed by argument order: =cl-map= uses the second source
argument, =cl-sort= and =cl-stable-sort= use the second source argument,
and =cl-merge= uses the fourth source argument.

Stage 129.7R starts the capture side for direct calls, without
claiming heap closure support.  When a literal lambda is applied
directly, the frontend computes free source variables and threads them
as leading parameters on the synthetic defun:

#+begin_src elisp
(defun caller (cap x)
  (funcall (lambda (y) (+ y cap)) x))
#+end_src

becomes:

#+begin_src elisp
(seq
 (defun nelisp_aot_lambda_0 (cap y) (+ y cap))
 (defun caller (cap x)
   (nelisp_aot_lambda_0 cap x)))
#+end_src

Runtime lexical slots follow the same rule:

#+begin_src elisp
(defun caller (x)
  (let ((cap x))
    (funcall (lambda (y) (+ y cap)) x)))
#+end_src

lowers to a =let-rt= slot for =cap= and then calls the synthetic defun
with =cap= as the first argument.

The same rewrite applies to direct =((lambda ...) ARG...)=
application.  This by-value threading is deliberately limited to direct
calls.  Higher-order callback positions such as =mapcar=,
=seq-filter=, and =cl-find-if= now use the 129.7U heap-closure path
below, because those callback ABIs receive only the callback's ordinary
arguments.  Captured =setq= remains pending because write-through
semantics require shared closure cells, not by-value parameter
threading.

Stage 129.7T connects that remaining heap-closure substrate to the AOT
runtime dispatcher without yet claiming compiler-side allocation.  When
compiled code passes an existing =nelisp-closure= record as the function
designator to =nelisp_aot_funcall1= / =funcall2= / =funcall3= /
=funcalln= / =apply= / =applyn=, =nelisp-cc-runtime= dispatches
directly through =nelisp-closure-apply=.  That preserves the canonical
closure module's captured environment and shared-cell mutation
semantics, including closures whose bodies mutate captured variables via
=setq=.

Stage 129.7U adds the first compiler-side captured-callback closure
materialization path.  When a higher-order builtin callback position
contains a literal lambda with captured lexical values, preprocessing
keeps a synthetic descriptor and rewrites the callback argument to an
internal =aot-closure-lambda= form.  Direct calln lowering then emits:

#+begin_src elisp
(seq
 (sexp-write-symbol-lit name_slot "mapcar")
 (sexp-write-symbol-lit scratch "nelisp_aot_closure_0")
 (extern-call nelisp_aot_make_closure
              mirror frames scratch 1 out scratch cap)
 (extern-call nelisp_aot_builtin_calln
              mirror frames name_slot 2 out scratch out xs)
 out)
#+end_src

The runtime bridge pairs the descriptor's capture symbols with the
captured values and constructs a canonical =nelisp-closure=, so the
129.7T dispatcher can call it through =nelisp-closure-apply=.  This
path is now backed by 129.7V module-init registration: closure
descriptors flow through =nelisp-cc-runtime-aot-module-init-plan= and
are registered by =nelisp-cc-runtime-run-aot-module-init-plan= before
callback materialization.

Stage 129.7X extends that materialization path from higher-order
callback positions to escaping value positions.  A literal lambda value
such as:

#+begin_src elisp
(defun make-closure
    ((out :type sexp) (mirror :type sexp) (frames :type sexp)
     (scratch :type sexp) (cap :type sexp))
  (function (lambda (x) (+ x cap))))
#+end_src

now records the same closure descriptor shape and lowers to:

#+begin_src elisp
(seq
 (sexp-write-symbol-lit scratch "nelisp_aot_closure_0")
 (extern-call nelisp_aot_make_closure
              mirror frames scratch 1 out scratch cap)
 out)
#+end_src

The resulting value is the canonical runtime closure object in =out=,
so callers can store or pass it through the existing 129.7T dispatcher
bridges.  Remaining closure work is native plan embedding/root-vector
pinning and native frame write-through for captured =setq= cells.

Stage 129.7Y extends direct literal-lambda =funcall= preprocessing for
captured mutation.  A form such as:

#+begin_src elisp
(defun caller
    ((out :type sexp) (mirror :type sexp) (frames :type sexp)
     (scratch :type sexp) (cap :type sexp) (x :type sexp))
  (funcall (lambda (y) (setq cap y)) x))
#+end_src

no longer attempts to lambda-lift =cap= as a by-value synthetic
argument.  Instead it records a closure descriptor whose body is
=((setq cap y))=, materializes that descriptor with
=nelisp_aot_make_closure=, and passes the resulting closure object as
the function value to =nelisp_aot_funcall1=.  This lets the canonical
runtime closure machinery own the captured mutation cell.  The remaining
native work is propagating that closure cell back into surrounding
compiled frame slots when later source code observes the mutated
captured variable.

*** §1.E.4 Shipped MVP: quoted function designators

Stage 129.7D lets the compiler handle symbol function designators
without requiring the caller to pass a pre-materialized =FN= slot:

#+begin_src elisp
(defun call-quoted
    ((out :type sexp) (mirror :type sexp) (frames :type sexp)
     (scratch :type sexp) (name_slot :type sexp) (arg :type sexp))
  (funcall 'identity arg))
#+end_src

The parser lowers this to the same dispatcher path with one extra
materialization step:

#+begin_src elisp
(seq
 (sexp-write-symbol-lit name_slot "identity")
 (extern-call nelisp_aot_funcall1
              mirror frames name_slot arg out scratch)
 out)
#+end_src

The same =name-slot= / =name_slot= convention is used for
=(funcall #'foo arg0 arg1)= and =(apply 'foo args)=.  This stage does
not allocate closure objects.  Non-capturing literal lambdas used by
=funcall= are covered by 129.7K; general heap closures and captured
environments remain part of the closure allocation/capture work.

** §1.F Exception machinery

=condition-case= / =catch= / =throw= / =unwind-protect= / =signal=
need non-local exit.  Two designs:
- *Handler stack* (recommended): a thread-local stack of (tag,
  landing-pad, saved-sp) records; =throw= / =signal= walks it, runs
  =unwind-protect= cleanups, restores sp, jumps to the pad.  Mirrors
  the bytecode VM's PUSH-CATCH / PUSH-CC / PUSH-UNWIND / POP-HANDLER
  opcodes (= the VM already implements this shape in elisp).
- setjmp/longjmp via extern — simpler but fights the spill-slot model.

*** §1.F.1 Shipped MVP: runtime handler-stack substrate

Stage 129.8A fixes the runtime-side contract before the compiler emits
handler records:

#+begin_src elisp
(nelisp-cc-runtime-aot-push-catch TAG LANDING-PAD SAVED-SP)
(nelisp-cc-runtime-aot-push-condition CONDITIONS LANDING-PAD SAVED-SP)
(nelisp-cc-runtime-aot-push-unwind CLEANUP LANDING-PAD SAVED-SP)
(nelisp-cc-runtime-aot-throw TAG VALUE)
(nelisp-cc-runtime-aot-signal ERROR-TAG DATA)
#+end_src

The elisp bridge is intentionally a simulator: =throw= / =signal= return
a landing descriptor instead of jumping:

#+begin_src elisp
(:kind catch
 :landing-pad PAD
 :saved-sp SAVED-SP
 :reason throw
 :tag TAG
 :value VALUE
 :cleanups (...))
#+end_src

For =condition= handlers the descriptor carries =:error (TAG . DATA)=.
Crossed =unwind= handlers run from inner to outer and their return values
are recorded in =:cleanups=.  Unmatched =throw= / =signal= still run
crossed cleanups, unwind the stack, and signal
=nelisp-cc-runtime-error=.

Compiler lowering for =(catch ...)= / =(condition-case ...)= /
=(unwind-protect ...)=, native stack-pointer restoration, and actual
landing-pad jumps remain later 129.8 work.

*** §1.F.2 Shipped MVP: =throw= / =signal= bridge lowering

Stage 129.8B connects the first user-visible exception forms to the
129.8A runtime stack:

#+begin_src elisp
(defun throw-tag
    ((out :type sexp) (mirror :type sexp) (frames :type sexp)
     (scratch :type sexp) (name_slot :type sexp) (value :type sexp))
  (throw 'done value))
#+end_src

The parser lowers this to:

#+begin_src elisp
(seq
 (sexp-write-symbol-lit name_slot "done")
 (extern-call nelisp_aot_throw
              mirror frames name_slot value out scratch)
 out)
#+end_src

Dynamic tags avoid the materialization step:

#+begin_src elisp
(defun signal-tag
    ((out :type sexp) (mirror :type sexp) (frames :type sexp)
     (scratch :type sexp) (tag :type sexp) (data :type sexp))
  (signal tag data))
#+end_src

lowers to:

#+begin_src elisp
(seq
 (extern-call nelisp_aot_signal
              mirror frames tag data out scratch)
 out)
#+end_src

Runtime bridge shape:

#+begin_src elisp
(nelisp-cc-runtime-aot-throw-boundary
 mirror frames tag value out scratch)
(nelisp-cc-runtime-aot-signal-boundary
 mirror frames tag data out scratch)
#+end_src

On the elisp side both bridge functions write the simulator landing
descriptor into =OUT= and return =OUT=.  The native ABI names are
=nelisp_aot_throw= / =nelisp_aot_signal=; final codegen must still use
the descriptor to restore the saved stack pointer and jump to the
landing pad rather than merely returning it.

*** §1.F.3 Shipped MVP: handler-push bridge lowering

Stage 129.8C adds the registration side of the handler-stack ABI.  The
compiler accepts internal AOT forms:

#+begin_src elisp
(aot-push-catch TAG LANDING-PAD SAVED-SP)
(aot-push-condition CONDITIONS LANDING-PAD SAVED-SP)
(aot-push-unwind CLEANUP LANDING-PAD SAVED-SP)
#+end_src

For a quoted catch tag:

#+begin_src elisp
(aot-push-catch 'done 4096 64)
#+end_src

the lowering is:

#+begin_src elisp
(seq
 (sexp-write-symbol-lit name_slot "done")
 (extern-call nelisp_aot_push_catch
              mirror frames name_slot 4096 64 scratch))
#+end_src

Dynamic =CONDITIONS= / =CLEANUP= values are forwarded directly.  The
runtime boundary bridges are:

#+begin_src elisp
(nelisp-cc-runtime-aot-push-catch-boundary
 mirror frames tag landing-pad saved-sp scratch)
(nelisp-cc-runtime-aot-push-condition-boundary
 mirror frames conditions landing-pad saved-sp scratch)
(nelisp-cc-runtime-aot-push-unwind-boundary
 mirror frames cleanup landing-pad saved-sp scratch)
#+end_src

These functions install 129.8A handler records and return the handler
descriptor.  Source-level =catch= / =condition-case= / =unwind-protect=
lowering must still synthesize real landing pads, saved stack-pointer
values, cleanup entry points, and balanced pop paths.

*** §1.F.4 Shipped MVP: handler-pop bridge lowering

Stage 129.8D adds the normal-exit counterpart:

#+begin_src elisp
(aot-pop-handler 'catch)
#+end_src

lowers to:

#+begin_src elisp
(seq
 (sexp-write-symbol-lit name_slot "catch")
 (extern-call nelisp_aot_pop_handler
              mirror frames name_slot out scratch)
 out)
#+end_src

Runtime bridge shape:

#+begin_src elisp
(nelisp-cc-runtime-aot-pop-handler-boundary
 mirror frames expected-kind out scratch)
#+end_src

The bridge pops the innermost handler, optionally validates its kind,
writes the descriptor into =OUT=, and returns =OUT=.  This makes
balanced push/pop expressible in object output; 129.8E below uses it
for the first source-level =catch= normal-exit path.

*** §1.F.5 Shipped MVP: source =catch= normal-exit lowering

Stage 129.8E starts synthesizing source-level handler control flow.
For a boxed-boundary function:

#+begin_src elisp
(defun catch-value
    ((out :type sexp) (mirror :type sexp) (frames :type sexp)
     (scratch :type sexp) (name_slot :type sexp) (value :type sexp))
  (catch 'done value))
#+end_src

the compiler lowers the normal path as if it had written:

#+begin_src elisp
(seq
 (aot-push-catch 'done 0 0)
 (let (((aot-catch-value-N :type sexp) value))
   (seq
    (aot-pop-handler 'catch)
    aot-catch-value-N)))
#+end_src

The temporary is a runtime Sexp local so the BODY result survives the
pop bridge call.  The placeholder =0= landing pad / saved stack pointer
keeps this stage honest: it proves balanced source-form lowering and
object relocations, but it is not the final non-local exit path.

Stage 129.8L covers the direct throw shape:

#+begin_src elisp
(catch 'done (throw 'done value))
#+end_src

The compiler pushes the catch handler, emits =nelisp_aot_throw=, and
then calls =nelisp_aot_landing_value= to move the landing descriptor's
=:value= payload back into =OUT=.  The throw bridge itself pops through
the matching catch handler, so this path does not emit the normal-exit
=aot-pop-handler=.

Stage 129.8N covers the first conditional throw shape:

#+begin_src elisp
(catch 'done
  (if pred
      (throw 'done value)
    fallback))
#+end_src

The compiler pushes one catch handler before the =if=.  The throwing
branch emits =nelisp_aot_throw= and =nelisp_aot_landing_value=; the
non-throwing branch saves its value, emits =aot-pop-handler 'catch=,
and returns the saved value.  Stage 129.8R generalizes the same lowering
to nested =if= trees whose throwing leaves target the same catch tag.
Mismatched-tag non-local forms in =catch= bodies still require native
landing-pad labels, stack-pointer restore, and branch-to-pad emission.
=condition-case= direct signal dispatch starts in 129.8M below; nested
condition paths and =unwind-protect= source lowering remain later
129.8 work.

*** §1.F.6 Shipped MVP: source =condition-case= normal-exit lowering

Stage 129.8F applies the same balanced source-form lowering to the
normal path of =condition-case=:

#+begin_src elisp
(defun cc-value
    ((out :type sexp) (mirror :type sexp) (frames :type sexp)
     (scratch :type sexp) (name_slot :type sexp) (value :type sexp))
  (condition-case err
      value
    (error out)))
#+end_src

lowers as if it had written:

#+begin_src elisp
(seq
 (aot-push-condition 'error 0 0)
 (let (((aot-condition-value-N :type sexp) value))
   (seq
    (aot-pop-handler 'condition)
    aot-condition-value-N)))
#+end_src

The handler body is intentionally not compiled as a landing pad yet.
Stage 129.8J extends the accepted selector shape to condition lists:

#+begin_src elisp
(condition-case err
    value
  ((error quit) out))
#+end_src

This normal path lowers as two condition pushes with the same
placeholder landing metadata followed by two condition pops after BODY
returns:

#+begin_src elisp
(seq
 (aot-push-condition 'error 0 0)
 (aot-push-condition 'quit 0 0)
 (let (((aot-condition-value-N :type sexp) value))
   (seq
    (aot-pop-handler 'condition)
    (aot-pop-handler 'condition)
    aot-condition-value-N)))
#+end_src

Signalled handler dispatch, binding =VAR= to the error data, and native
jump-to-handler emission remain later 129.8 work.

Stage 129.8K also walks every handler clause on this normal path:

#+begin_src elisp
(condition-case err
    value
  (error out)
  (quit out))
#+end_src

lowers to two condition pushes before BODY and two matching pops after
BODY.  The handler bodies remain metadata for the future landing-pad
path; they are not executed unless a non-local signal path is compiled.

Stage 129.8M covers the first direct signalled path:

#+begin_src elisp
(condition-case err
    (signal 'error data)
  (error err))
#+end_src

The compiler statically selects the first matching handler, pushes a
condition handler for the direct tag, emits =nelisp_aot_signal= or the
specialised =error= bridge, then calls =nelisp_aot_landing_error= to
bind =err= to the descriptor's =:error= payload before compiling the
handler body.  The signal bridge itself pops through the matched
condition handler, so this path does not emit the normal-exit
=aot-pop-handler=.

Stage 129.8Q covers the first conditional signalled path:

#+begin_src elisp
(condition-case err
    (if value
        (signal 'error value)
      value)
  (error err))
#+end_src

The compiler pushes the statically selected condition handler before the
=if=.  A signalled branch emits the direct signal bridge, extracts the
landing descriptor's =:error= payload, and runs the handler body; a
normal branch saves its branch value, pops the condition handler, and
returns that saved value.  Stage 129.8R extends the same lowering to
nested =if= trees whose signalled leaves resolve to the same condition
handler.  Dynamic or mismatched condition paths still require native
landing-pad labels, stack-pointer restore, and branch-to-pad emission.

*** §1.F.7 Shipped MVP: source =unwind-protect= normal-exit lowering

Stage 129.8G covers the normal path of =unwind-protect=:

#+begin_src elisp
(defun unwind-value
    ((out :type sexp) (mirror :type sexp) (frames :type sexp)
     (scratch :type sexp) (name_slot :type sexp) (value :type sexp))
  (unwind-protect
      value
    (identity value)))
#+end_src

lowers as if it had written:

#+begin_src elisp
(let (((aot-unwind-value-N :type sexp) value))
  (seq
   (identity value)
   aot-unwind-value-N))
#+end_src

Stage 129.8O covers the first direct non-local protected body:

#+begin_src elisp
(unwind-protect
    (throw 'done value)
  (identity value))
#+end_src

The compiler saves =value= into a runtime Sexp slot, runs cleanup forms
in order, then emits =nelisp_aot_throw= with the saved value.  This
preserves cleanup ordering for the direct quoted-tag throw shape without
claiming general landing-pad support.

Stage 129.8P extends this to one-level conditional protected bodies:

#+begin_src elisp
(unwind-protect
    (if value
        (throw 'done value)
      value)
  (identity value))
#+end_src

Each branch saves the protected body's value, runs cleanup forms in
order, and then either returns the saved value or emits
=nelisp_aot_throw=.  Stage 129.8R extends this leaf cleanup to nested
=if= trees with direct quoted-tag throw leaves.  Non-conditional
protected non-local bodies still require real cleanup landing pads,
native stack-pointer restore, and branch-to-cleanup / branch-back
emission.

*** §1.F.8 Shipped MVP: source =error= bridge lowering

Stage 129.8H adds the common single-argument =error= surface:

#+begin_src elisp
(defun error-value
    ((out :type sexp) (mirror :type sexp) (frames :type sexp)
     (scratch :type sexp) (name_slot :type sexp) (value :type sexp))
  (error value))
#+end_src

This lowers through the existing signal bridge:

#+begin_src elisp
(seq
 (sexp-write-symbol-lit name_slot "error")
 (extern-call nelisp_aot_signal
              mirror frames name_slot value out scratch)
 out)
#+end_src

The argument is treated as already-boxed signal data.

*** §1.F.9 Shipped MVP: formatted =error= varargs bridge

Stage 129.8I adds the formatted sibling for two-or-more argument
=error= calls:

#+begin_src elisp
(defun error-fmt
    ((out :type sexp) (mirror :type sexp) (frames :type sexp)
     (scratch :type sexp) (name_slot :type sexp) (value :type sexp))
  (error "%s" value))
#+end_src

This lowers to a stack-argument bridge:

#+begin_src elisp
(seq
 (sexp-write-str-lit scratch "%s")
 (extern-call nelisp_aot_errorn
              mirror frames 2 out scratch scratch value)
 out)
#+end_src

The runtime bridge:

#+begin_src elisp
(nelisp-cc-runtime-aot-errorn-boundary
 mirror frames argc out scratch arg...)
#+end_src

validates =ARGC=, builds the same signal data shape as the elisp
stdlib =error= implementation, and delegates to =nelisp_aot_signal=.
Literal format strings are materialized into the caller-owned
=scratch= slot; dynamic format strings can be passed as ordinary boxed
arguments.

** §1.G macroexpand frontend (= cheapest group)

=defmacro= / =when= / =unless= / =dolist= / =cl-*= / =pcase= reduce to
core forms via compile-time macro expansion.  The Wave A21 bytecode
path already does broad =macroexpand-all= before =nelisp-bc-compile=.
For Phase 47, the shipped MVP is deliberately narrower: selectively
macroexpand value/call positions while preserving DSL structural forms
such as =defun=, because host =macroexpand-all= rewrites them to
=defalias= forms outside the Phase 47 grammar.  Multi-form macro
bodies lower through =value-seq= so the final child value is preserved
in value context.  Near-zero new asm.

*** §1.G.1 Shipped MVP: top-level module forms

Stage 129.6A extends the frontend with top-level =require= /
=provide= handling:

#+begin_src elisp
(seq
  (require 'cl-lib)
  (defmacro ...)
  (provide 'my-feature)
  ...)
#+end_src

=require= is evaluated during compilation, before top-level
=defmacro= bodies are installed, and then stripped from the Phase 47
IR.  Only literal arguments are accepted: =FEATURE= must resolve to a
symbol, =FILENAME= to nil or a string, and =NOERROR= follows host
=require= semantics.  =provide= is also stripped after validating its
feature symbol, but it does not yet emit runtime module registration.

This is deliberately a frontend/module-load MVP, not general builtin
delegation.  It lets application files with ordinary top-level module
boilerplate pass through the AOT parser.  Stage 129.6B below adds the
first explicit dispatcher helper, but ordinary runtime call lowering
for =format=, =mapcar=, =funcall=, and other real calls remains later
129.6 work.

** §1.H Builtin library delegation

=format= / =concat= / =substring= / =string-match= / =mapcar= /
=nth= / =assq= / ... = hundreds of builtins.  Pragmatic route: emit a
=call= into the runtime's existing implementation via =extern-call=
(the dispatcher already resolves builtins).  Consequence: compiled
output links against the nelisp runtime (= "compiled code + runtime"),
*not* a tiny standalone ELF.  This is acceptable for the
interpreter-embedded use case (Doc 99 Arch B/C) but is in tension with
Doc 97's standalone-binary lineage — call out explicitly per output
target.

*** §1.H.1 Shipped MVP: one-argument Sexp builtin delegation helper

Stage 129.6B adds an explicit boxed-boundary helper form:

#+begin_src elisp
(defun-sexp-builtin1-symbol call-symbol-name symbol-name
    (out mirror frames scratch name-slot arg))
#+end_src

It lowers to ordinary Phase 47 DSL:

#+begin_src elisp
(defun call-symbol-name
    ((out :type sexp) (mirror :type sexp) (frames :type sexp)
     (scratch :type sexp) (name-slot :type sexp) (arg :type sexp))
  (seq
   (sexp-write-symbol-lit name-slot "symbol-name")
   (extern-call nelisp_aot_builtin_call1
                mirror frames name-slot arg out scratch)
   out))
#+end_src

The caller supplies =OUT/MIRROR/FRAMES/SCRATCH/NAME-SLOT= plus the
boxed Sexp =ARG=.  The dispatcher ABI writes the builtin result into
=OUT= and returns =OUT=, matching the boxed-boundary convention used by
the 129.2/129.3 helpers.  Object output exposes the compiled helper
symbol and unresolved relocations for =nelisp_aot_builtin_call1= and
=nl_alloc_symbol=.

This is intentionally not ordinary user syntax yet: =(symbol-name x)=
does not lower automatically, no varargs ABI exists, and heavier calls
such as =format=, =mapcar=, =apply=, and =funcall= are still pending.
The runtime C/Rust dispatcher body behind =nelisp_aot_builtin_call1=
also remains Doc 99 / later-129.6 integration work.

*** §1.H.2 Shipped MVP: runtime builtin-call1 bridge contract

Stage 129.6C adds the runtime-side analogue of the native dispatcher
ABI:

#+begin_src elisp
(nelisp-cc-runtime-aot-builtin-call1
 mirror frames 'symbol-name arg out scratch)
#+end_src

The argument order mirrors the native call emitted by 129.6B:

#+begin_src c
nelisp_aot_builtin_call1(mirror, frames, name, arg, out, scratch)
#+end_src

In host-side tests =OUT= is a caller-owned vector with at least one
slot.  The bridge resolves and dispatches the one-argument builtin
(or uses an injected dispatcher callback), writes the result to
=(aref OUT 0)=, and returns =OUT=.  This pins the Doc 99 calling
contract without pretending the final exported C ABI is already wired:
native code still needs a real =nelisp_aot_builtin_call1= symbol that
can recover/use the runtime =Env= behind =MIRROR/FRAMES=.

*** §1.H.3 Shipped MVP: constrained direct =symbol-name= call lowering

Stage 129.6D removes the custom surface form for the first user-call
case.  A normal defun with explicit boxed-boundary parameters may now
write:

#+begin_src elisp
(defun call-symbol-name
    ((out :type sexp) (mirror :type sexp) (frames :type sexp)
     (scratch :type sexp) (name-slot :type sexp) (arg :type sexp))
  (symbol-name arg))
#+end_src

The parser lowers =(symbol-name arg)= to the same sequence used by
129.6B:

#+begin_src elisp
(seq
 (sexp-write-symbol-lit name-slot "symbol-name")
 (extern-call nelisp_aot_builtin_call1
              mirror frames name-slot arg out scratch)
 out)
#+end_src

This is intentionally constrained.  The surrounding defun must expose
the boundary symbols by name, the only shipped direct builtin is
=symbol-name=, and the call remains one-argument only.  It proves the
shape needed for ordinary user-call lowering without yet claiming
general =format= / =mapcar= / =funcall= coverage.

*** §1.H.4 Shipped MVP: unary builtin delegation table

Stage 129.6E widens the 129.6D direct-call path from a single builtin
to an explicit one-argument table:

#+begin_src elisp
identity ignore not null atom consp listp symbolp keywordp
numberp integerp float stringp vectorp
car cdr car-safe cdr-safe
length reverse nreverse copy-sequence
1+ 1- abs
symbol-name prin1-to-string number-to-string string-to-number
upcase downcase
#+end_src

Each table entry lowers to the same =nelisp_aot_builtin_call1=
sequence as =symbol-name=.  The parser checks the current defun table
first: if the compile unit defines a same-named Phase 47 function,
ordinary direct-call lowering to that function wins and builtin
delegation is not used.

Stage 129.6F adds the matching vararg bridge for a small multi-arg
table; Stage 129.6G widens it to common fixed-arity builtins.  The
remaining 129.6 work is broader higher-order builtin coverage and
wiring exported native C ABI bodies.

*** §1.H.5 Shipped MVP: builtin calln / varargs bridge

Stage 129.6F adds the runtime vararg builtin bridge:

#+begin_src elisp
(nelisp-cc-runtime-aot-builtin-calln
 mirror frames 'list 2 out scratch a b)
#+end_src

The native ABI shape is:

#+begin_src c
nelisp_aot_builtin_calln(mirror, frames, name, argc, out, scratch, arg...)
#+end_src

=ARGC= must match the trailing argument count.  Host-side runtime
dispatch resolves =NAME= through the NeLisp function table or host
=fboundp=, applies the function to the boxed argument list, writes the
result to =OUT[0]=, and returns =OUT=.

The compiler lowers direct calls for this initial table:

#+begin_src elisp
list vector concat append
#+end_src

For example:

#+begin_src elisp
(list a b)
#+end_src

lowers to:

#+begin_src elisp
(seq
 (sexp-write-symbol-lit name-slot "list")
 (extern-call nelisp_aot_builtin_calln
              mirror frames name-slot 2 out scratch a b)
 out)
#+end_src

The fixed boundary prefix already consumes six GP arguments, so any
user argument to =nelisp_aot_builtin_calln= exercises the outgoing
stack-GP spill path shipped under 129.7G.  Same-unit Phase 47 defuns
still shadow builtin delegation.

*** §1.H.6 Shipped MVP: fixed-arity calln builtin table expansion

Stage 129.6G widens the calln table from constructor-like varargs to
ordinary two-argument and fixed-arity host builtins:

#+begin_src elisp
(cons a b)
(eq a b)
(nth n list)
(assq key alist)
(string= a b)
#+end_src

Each call lowers to =nelisp_aot_builtin_calln= with =ARGC= set from
the source arity.  The runtime side already dispatches by resolving the
symbol and applying the host/NeLisp function, so no new bridge shape is
needed.  Same-unit Phase 47 defuns still shadow the builtin table.

*** §1.H.7 Shipped MVP: string/format calln builtin table expansion

Stage 129.6H extends the same direct calln table to common application
string and message helpers:

#+begin_src elisp
format message
string-match string-match-p
substring string-prefix-p string-suffix-p
replace-regexp-in-string
#+end_src

This moves the high-frequency =format= / =message= / =string-match=
gap from "no lowering" to the existing boxed-boundary dispatcher path.
The host-side bridge can already call these functions by symbol and
store the result in =OUT[0]=.  Native exported C ABI bodies for the
dispatcher are still later 129.6 / Doc 99 integration work.

*** §1.H.8 Shipped MVP: dynamic higher-order calln builtin table expansion

Stage 129.6I adds the first direct =mapcar=-family builtin entries to
the calln table:

#+begin_src elisp
mapcar mapc mapconcat
#+end_src

The supported shape is deliberately dynamic:

#+begin_src elisp
(mapcar fn xs)
(mapconcat fn xs sep)
#+end_src

where =fn= is already a boxed function value supplied by the current
frame.  The compiler lowers these calls through
=nelisp_aot_builtin_calln= exactly like other calln builtins, and the
runtime dispatcher applies the host/NeLisp function.  This is not heap
closure allocation and does not yet materialize arbitrary
=#'function= arguments in ordinary builtin argument positions.

*** §1.H.9 Shipped MVP: map builtin function-designator materialization

Stage 129.6J covers the common quoted-symbol designator shape for the
=mapcar= family:

#+begin_src elisp
(mapcar #'foo xs)
(mapc 'foo xs)
(mapconcat #'foo xs sep)
#+end_src

Direct calln lowering already needs =name-slot= for the builtin symbol
itself.  For these map-family builtins, the compiler now uses the
existing =scratch= boundary slot as a second symbol materialization
slot:

#+begin_src elisp
(seq
 (sexp-write-symbol-lit name-slot "mapcar")
 (sexp-write-symbol-lit scratch "foo")
 (extern-call nelisp_aot_builtin_calln
              mirror frames name-slot 2 out scratch scratch xs)
 out)
#+end_src

This keeps the ABI shape unchanged and avoids pretending that heap
closures exist.  Capturing lambdas and general =#'function=
materialization outside this first map-family argument position remain
129.7 work.

*** §1.H.10 Shipped MVP: sort predicate function-designator materialization

Stage 129.6K applies the same quoted/function symbol materialization
to the common predicate position of =sort=:

#+begin_src elisp
(sort xs #'string<)
(sort xs 'lessp)
#+end_src

The direct calln builtin table now includes =sort=.  Since =sort='s
function designator is its second source argument, the compiler tracks
a per-builtin designator argument index and rewrites only that argument
to the caller-owned =scratch= slot:

#+begin_src elisp
(seq
 (sexp-write-symbol-lit name-slot "sort")
 (sexp-write-symbol-lit scratch "string<")
 (extern-call nelisp_aot_builtin_calln
              mirror frames name-slot 2 out scratch xs scratch)
 out)
#+end_src

This is still symbol-designator materialization, not heap closure
capture.  Capturing lambdas and additional higher-order builtin
predicate/callback positions remain later 129.6/129.7 work.

*** §1.H.11 Shipped MVP: mapcan function-designator materialization

Stage 129.6L adds the destructive-append map family member to the same
calln path:

#+begin_src elisp
(mapcan fn xs)
(mapcan #'foo xs)
#+end_src

The first form passes an already-materialized boxed function value.
The second form materializes the quoted/function symbol into
=scratch=, matching the 129.6J map-family convention:

#+begin_src elisp
(seq
 (sexp-write-symbol-lit name-slot "mapcan")
 (sexp-write-symbol-lit scratch "foo")
 (extern-call nelisp_aot_builtin_calln
              mirror frames name-slot 2 out scratch scratch xs)
 out)
#+end_src

Runtime dispatch uses the same =nelisp_aot_builtin_calln= bridge as
the rest of 129.6.  This widens higher-order builtin coverage without
claiming heap closure capture.

*** §1.H.12 Shipped MVP: maphash function-designator materialization

Stage 129.6M adds the hash-table iterator to the same higher-order
calln path:

#+begin_src elisp
(maphash fn table)
(maphash #'foo table)
#+end_src

As with the map-family builtins, the first source argument is the
function designator.  Quoted/function symbols are materialized through
the caller-owned =scratch= slot before the shared calln dispatcher:

#+begin_src elisp
(seq
 (sexp-write-symbol-lit name-slot "maphash")
 (sexp-write-symbol-lit scratch "foo")
 (extern-call nelisp_aot_builtin_calln
              mirror frames name-slot 2 out scratch scratch table)
 out)
#+end_src

The runtime bridge dispatches to host/NeLisp =maphash= through the
same =nelisp_aot_builtin_calln= ABI.  This is still callback
materialization, not heap closure capture.

*** §1.H.13 Shipped MVP: seq.el higher-order function-designator materialization

Stage 129.6N widens higher-order builtin coverage to the common seq.el
family:

#+begin_src elisp
(seq-map fn xs)
(seq-filter #'pred xs)
(seq-reduce #'combine xs seed)
#+end_src

All covered seq.el calls use their first source argument as the
function designator.  Quoted/function symbols are materialized through
the caller-owned =scratch= slot before the shared calln dispatcher:

#+begin_src elisp
(seq
 (sexp-write-symbol-lit name-slot "seq-filter")
 (sexp-write-symbol-lit scratch "pred")
 (extern-call nelisp_aot_builtin_calln
              mirror frames name-slot 2 out scratch scratch xs)
 out)
#+end_src

The covered family is =seq-map=, =seq-do=, =seq-filter=, =seq-remove=,
=seq-find=, =seq-some=, =seq-every-p=, =seq-count=, =seq-reduce=, and
=seq-mapcat=.  Runtime dispatch continues to use host/NeLisp function
lookup through =nelisp_aot_builtin_calln=.  This widens 129.6 coverage
without claiming heap closure capture.

*** §1.H.14 Shipped MVP: cl-lib higher-order function-designator materialization

Stage 129.6O extends the same first-argument function-designator
materialization to cl-lib's higher-order sequence/list family:

#+begin_src elisp
(cl-mapcar fn xs)
(cl-find-if #'pred xs)
(cl-reduce #'combine xs)
#+end_src

Quoted/function symbols are materialized through =scratch= and then
passed as ordinary calln arguments:

#+begin_src elisp
(seq
 (sexp-write-symbol-lit name-slot "cl-find-if")
 (sexp-write-symbol-lit scratch "pred")
 (extern-call nelisp_aot_builtin_calln
              mirror frames name-slot 2 out scratch scratch xs)
 out)
#+end_src

The covered family is =cl-mapcar=, =cl-mapc=, =cl-mapcan=,
=cl-maplist=, =cl-mapl=, =cl-mapcon=, =cl-some=, =cl-every=,
=cl-notany=, =cl-notevery=, =cl-count-if=, =cl-count-if-not=,
=cl-find-if=, =cl-find-if-not=, =cl-remove-if=, =cl-remove-if-not=,
=cl-position-if=, =cl-position-if-not=, and =cl-reduce=.  It reuses
the shared =nelisp_aot_builtin_calln= dispatcher and still leaves
capturing callbacks to the 129.7U closure bridge.

Stage 129.6P extends the same indexed function-designator path to the
remaining common cl-lib predicate variants:

#+begin_src elisp
(cl-member-if #'pred xs)
(cl-assoc-if fn alist)
(cl-substitute-if new #'pred xs)
#+end_src

The first two forms materialize the first source argument through
=scratch=.  The substitute/nsubstitute family materializes the second
source argument instead:

#+begin_src elisp
(seq
 (sexp-write-symbol-lit name-slot "cl-substitute-if")
 (sexp-write-symbol-lit scratch "pred")
 (extern-call nelisp_aot_builtin_calln
              mirror frames name-slot 3 out scratch new scratch xs)
 out)
#+end_src

The covered family is =cl-delete-if=, =cl-delete-if-not=,
=cl-member-if=, =cl-member-if-not=, =cl-assoc-if=,
=cl-assoc-if-not=, =cl-rassoc-if=, =cl-rassoc-if-not=,
=cl-substitute-if=, =cl-substitute-if-not=, =cl-nsubstitute-if=, and
=cl-nsubstitute-if-not=.  Stage 129.7S lets non-capturing literal
lambdas in those predicate positions lift to synthetic defuns before
the same calln handoff.  This remains callback materialization, not
heap closure capture.

Stage 129.6Q records the exported AOT bridge ABI in
=nelisp-cc-runtime--aot-c-abi-descriptors=.  Each descriptor names the
native symbol emitted by object output, the matching Emacs bridge
function, the fixed argument count, whether the bridge is vararg, and
the boundary argument names:

#+begin_src elisp
(:symbol nelisp_aot_builtin_calln
 :function nelisp-cc-runtime-aot-builtin-calln
 :fixed-argc 6
 :rest t
 :args (mirror frames name argc out scratch args...))
#+end_src

This is still an ABI contract table rather than a real dynamic symbol
export.  Real standalone address resolution remains owned by
=nelisp-cc-runtime-resolve-symbol= and the dlsym hook; the table makes
the compiler/runtime bridge surface auditable before that native export
path is complete.

Stage 129.6R extends the higher-order builtin coverage beyond 129.6N/P:

#+begin_src elisp
(seq-map-indexed #'fn xs)
(seq-uniq xs #'test)
(cl-map type #'fn xs)
(cl-merge type xs ys #'pred)
#+end_src

The covered additions are =seq-keep=, =seq-mapn=, =seq-map-indexed=,
=seq-sort=, =seq-uniq=, =cl-map=, =cl-sort=, =cl-stable-sort=, and
=cl-merge=.  Runtime dispatch still flows through
=nelisp_aot_builtin_calln=; the compiler only records the fixed callback
argument indexes needed for quoted/function designator materialization
and non-capturing lambda lifting.

* §2 Staged roadmap (Rust LOC net ≤ 0 per commit — user rule 2026-04-30/05-19)

Each grammar group ships as new elisp in =nelisp-phase47-compiler.el=
(+ siblings) compiling to asm; Rust delta is 0 (the externs they call
already exist via Doc 122 / Doc 125 / Doc 100).  Where a group needs a
genuinely new Rust extern, it must be paired with an offsetting
deletion or deferred until the extern can be elisp-emitted.

| Stage   | Group                          | Depends on | Risk | Status                  |
|---------+--------------------------------+------------+------+-------------------------|
| 129.1   | §1.G macroexpand frontend      | —          | low  | MVP SHIPPED 2026-05-25 |
| 129.2   | §1.A tagged-value calculus     | 129.1      | high | boxed Int MVP SHIPPED 2026-05-25 |
| 129.3   | §1.D setq/defvar (symbol cells)| 129.2      | low  | 129.3A boxed setq MVP + 129.3B symbol literal helper + 129.3C defvar conditional helper + 129.3D defconst helper + 129.3E/F top-level init helpers + 129.3G custom metadata descriptors + 129.3H runtime module-init plan + 129.3I execution hook + 129.3J runtime custom metadata table + 129.3K helper resolution callback + 129.3L default init boundary context + 129.3M init helper ABI argv materialization SHIPPED 2026-05-25; real native call entry / real environment handles pending |
| 129.4   | §1.C general let/let*          | 129.2      | med  | multi-binding let MVP + 129.4B special declaration guard + 129.4C special binding bridge substrate + 129.4D single-special let normal-exit lowering + 129.4E all-special multi-let normal-exit lowering + 129.4F mixed special/lexical multi-let normal-exit lowering + 129.4G direct special-let throw cleanup + 129.4H conditional special-let throw cleanup + 129.4I nested conditional special-let throw cleanup SHIPPED 2026-05-25; heap lexframes and general tagged local values pending |
| 129.5   | §1.B heap + GC roots           | 129.2      | high | 129.5A static root map + 129.5B aot-frame root-set substrate + 129.5C call-boundary root-vector bridge + 129.5D runtime let roots + 129.5E explicit native root push/pop bridges + 129.5F boundary defun auto root-scope insertion + 129.5G root-vector materialisation bridge SHIPPED 2026-05-25; fully general non-boundary defun root frames pending |
| 129.6A  | §1.G top-level module forms    | 129.1      | low  | require/provide frontend MVP SHIPPED 2026-05-25 |
| 129.6   | §1.H builtin delegation        | 129.2,.5   | med  | 129.6B builtin1 helper surface + 129.6C runtime bridge contract + 129.6D constrained direct symbol-name call lowering + 129.6E unary builtin table + 129.6F builtin calln/varargs bridge + 129.6G fixed-arity calln table expansion + 129.6H string/format calln table expansion + 129.6I dynamic higher-order calln table expansion + 129.6J map builtin function-designator materialization + 129.6K sort predicate function-designator materialization + 129.6L mapcan function-designator materialization + 129.6M maphash function-designator materialization + 129.6N seq.el higher-order function-designator materialization + 129.6O cl-lib higher-order function-designator materialization + 129.6P cl-lib predicate family function-designator materialization + 129.6Q runtime AOT C ABI descriptor table + 129.6R extended seq.el/cl-lib higher-order coverage SHIPPED 2026-05-25; real exported native addresses and remaining higher-order builtin coverage pending |
| 129.7   | §1.E closures + funcall/apply  | 129.5,.6   | high | 129.7A funcall1 dispatcher bridge + 129.7B funcall2 fixed ABI + 129.7C apply dispatcher + 129.7D quoted symbol designators + 129.7E SysV first stack-GP arg and funcall3 bridge + 129.7F final non-trivial stack-GP evaluation + 129.7G general outgoing stack-GP call-spill assembly + 129.7H direct funcall calln/rest-list bridge + 129.7I apply splicing bridge + 129.7J source rest parameter binding + 129.7K/L non-capturing literal lambda lifting + 129.7M map-family literal lambda lifting + 129.7N indexed higher-order builtin lambda lifting + 129.7O maphash literal lambda lifting + 129.7P seq.el higher-order literal lambda lifting + 129.7Q cl-lib higher-order literal lambda lifting + 129.7R direct literal lambda capture threading + 129.7S cl-lib predicate literal lambda lifting + 129.7T AOT dispatcher heap-closure bridge + 129.7U captured higher-order callback closure bridge + 129.7V module-init closure descriptor registration + 129.7W compiler module-init plan handoff + 129.7X escaping literal closure value materialization + 129.7Y direct captured-mutation closure dispatch + 129.7Z extended higher-order literal lambda lifting SHIPPED 2026-05-25; native object embedding/root-vector pinning and native frame write-through for captured setq cells pending |
| 129.8   | §1.F exception machinery       | 129.5      | high | 129.8A runtime handler-stack substrate + 129.8B throw/signal bridge lowering + 129.8C handler-push bridge lowering + 129.8D handler-pop bridge lowering + 129.8E source catch normal-exit lowering + 129.8F source condition-case normal-exit lowering + 129.8G source unwind-protect normal-exit lowering + 129.8H single-argument error lowering + 129.8I formatted error varargs bridge + 129.8J condition-case list specs + 129.8K condition-case multi-clause normal exits + 129.8L direct catch/throw landing-value extraction + 129.8M direct condition-case signal/error landing-error dispatch + 129.8N conditional catch/throw branch dispatch + 129.8O direct unwind-protect throw cleanup + 129.8P conditional unwind-protect throw cleanup + 129.8Q conditional condition-case signal dispatch + 129.8R nested conditional non-local branch trees SHIPPED 2026-05-25; native landing-pad jumps pending |

Spike target: 129.1 + 129.2 + 129.3 + a hand-picked leaf =defun= from
=anvil-server.el= that uses only those (= prove the pipeline end to
end on real application code).  Production engagement = 129.4–129.8.

* §3 Honest scope assessment

§1.A + §1.B + §1.E + §1.F together constitute a *complete native-code
language runtime*.  "Compile =anvil-server.el= to native" is, in
honest terms, "build a native-comp backend for elisp" — the same
scope as GNU Emacs's libgccjit native-comp, except hand-emitted via
the Phase 47 asm chain instead of delegating to GCC.  Realistic
horizon: multi-month to multi-year, paced one group per engagement.

This is *not* a near-term cold-load fix.  It is the structural endgame
that aligns with Doc 99 Arch C ("every byte of code derives from elisp
the Phase 47 chain compiled").

* §4 Relationship to the other axes

- *Doc 99 (call boundary)*: gives the interpreter an ABI to call
  compiled functions.  Doc 129 gives those functions a language.  A
  single compiled =anvil-server= function needs *both* (Doc 99 ET_REL
  + ABI, Doc 129 grammar coverage).
- *Doc 122 (substrate primitives)*: supplies the alloc / mut-str /
  extended-extern-call primitives that Doc 129 groups call.  Doc 129
  is "Doc 122 consumed at application level".
- *Doc 126 (eval-boot)*: precedent — replacing "interpret source" with
  a faster path.  Doc 129 is the user-code analogue of that move.

* §5 The pragmatic alternative (= why this is endgame, not now)

The Wave A21 bytecode VM (=byte-compile-file= → =.elc= → =nelisp-bc-run=)
*already* handles closures, exceptions, hash, and higher-order calls —
because it runs on the interpreter's runtime, not bare registers.  Its
only blocker for standalone use was a heap-corruption bug in the
reader (§6), now fixed.  Bytecode gives a 5–10x evaluator speedup with
*zero* new grammar.  Phase 47 AOT (this doc) gives native speed but
needs the full §1 stack.

Recommended sequencing: harvest the bytecode-VM win first (cheap,
grammar-free), pursue Doc 129 as the long-horizon native-comp endgame.

* §6 Prerequisite SHIPPED — reader heap-corruption fix (2026-05-25)

Loading a Wave A21 =.elc= with a large pre-compiled =defun= (e.g.
=anvil-server--handle-tools-call=, a 1,099-element bytecode CODE
vector) aborted standalone NeLisp with glibc =corrupted size vs.
prev_size= / =double free=.

Root cause: =build-tool/src/lib.rs= reader =PARSER_POOL_SIZE = 3 + 4 *
1024= (depth ≈1023).  The Phase 47 elisp parser
(=nelisp-cc-reader-parser.el=) recurses depth+1 per list/vector
*element*, indexing the slot-pool via *unchecked* =vector-ref-ptr=, so
any list/vector literal with ≳1024 elements overran the pool → heap
corruption (threshold confirmed: 1000-element OK, 1024 crash).

Fix (1 line, net 0 Rust LOC): size the pool to the input —
=vec![Sexp::Nil; (3 + 4 * input.len()).max(PARSER_POOL_SIZE)]=.  An
element count can never exceed the input byte count, so pool overflow
is structurally impossible.  Verified: 1,099 + 5,000-element vector
literals and the full =anvil-server.elc= (68 bcl defuns) load cleanly;
=mini.elc= regression + cargo =--lib= 10/10 green.

Proper follow-up (not blocking): the parser's depth-per-element
recursion is itself the latent flaw; an *iterative* list/vector parse
(reusing a fixed slot set, accumulating via a running tail) would
remove the input-sized-pool memory cost entirely.  Tracked as a Phase
47 reader-parser cleanup, independent of Doc 129.

#+TITLE: Doc 155 - GC collects live lexframe children (audit/lock-check SIGSEGV)
#+DATE: 2026-06-22
#+STATUS: CLOSED (2026-06-22) — fixed via Path B (sound GC, reclamation RE-ENABLED), merged to main (b31179ce; main HEAD 39646470). Root cause (§8.12): the conservative native-stack scan marks an in-flight record/vector BOX interior pointer's block but mark_slot misreads its type_tag and does NOT recurse the record's slots; the now-marked block makes the PRECISE marker skip recursion (mark_block returns 0), so a lexframe ht's children (bucket-count/buckets) are never marked and get swept -> free-link clobbers the live box -> SIGSEGV. Fix B (b31179ce, pushed): decouple "alive" from "recursed" — conserv pins with mark 4 (not 1), mark_block treats 4 like 0 (upgrade+recurse), sweep keeps+resets 4 like 1; reclaimer gate base+160 re-enabled (0). VALIDATED: full Debian no-Nix RC container gate (474-test) GREEN; audit 10/10 + (garbage-collect) + heavy churn all clean on the 838605da/256MiB repro. (History: §8.8 Fix A d5724bb3 disabled collection as a sound stopgap; Path B supersedes it and restores reclamation.)
#+SEVERITY: latent (masked in production by Doc 154 churn-reduction work)

* 1. Purpose

Track the dedicated effort to fix a garbage-collector memory-corruption bug in
the standalone reader (=target/nelisp=): under allocation churn during a deep
eval, the GC reclaims objects that are still reachable from a live lexical
=frame= (its =hash-table= / =buckets= vector), producing a NULL dereference on
the next variable lookup.

This was found while investigating the standalone =nelix audit= / =nelix
lock-check= SIGSEGV.  The *symptom* is currently masked in production (see §5);
this doc captures the *root cause* and an investigation plan so the GC fix can
be picked up as a focused, instrumented task rather than blind surgery.

* 2. Symptom

- =nelix audit MANIFEST= and =nelix lock-check MANIFEST= under the nelisp
  runtime (nelix-native backend, e.g. the 3-package =m3= manifest) crashed with
  SIGSEGV (rc=139), deterministically without gdb (5/5).
- =nelix lock= (standalone) returns rc=1 with empty output — this is a SEPARATE
  env issue (registry recipe resolution needs network/cache), NOT this GC bug.
  Do not conflate them.

* 3. Root cause (gdb core analysis)

Faulting instruction (core dump): =nl_vector_slot_ptr=, =mov (%rdi,%rsi,1),%rax=
with =rdi=0x0= (NULL base), =rsi=0x8=.

Call path:
#+begin_example
#0 nl_vector_slot_ptr            ; NULL deref
#1 nelisp_frame_stack_find_in_frame
#2..#6 nelisp_frame_stack_find_descend   ; innermost-first frame walk
#7 nelisp_frame_stack_find
#8 nelisp_env_lookup_value       ; variable lookup
#10 nl_eval_inner / nl_eval_arg_list_walk
#+end_example

A live lexframe's CHILD object is reclaimed by the GC while the frame Record
itself survives:
- =find_in_frame= walks =frame.slot0 (hash-table) -> slot1 (buckets) -> cells=.
- Guard experiments (lisp/nelisp-cc-frame-stack-find.el):
  - Guarding "frame Record is Nil" did NOT stop the crash → the frame Record is
    valid (tag != 0).
  - Guarding "frame.slot0 (hash-table) is Nil" did NOT stop the crash → the
    hash-table is valid; the *buckets* vector (=ht.slot1=) reads back NULL.
  - The reclaimed object VARIES (hash-table in one repro, buckets in another).
- ⇒ The whole =frame → hash-table → buckets → cells= chain is not kept alive
  across a collection.  Single-point NULL guards are whack-a-mole and
  insufficient.

The GC mark code *structurally* recurses this chain
(=nl_gc_mark_slot= for tag-12 Record → slots; tag-8 Vector → buf + slots; tag-7
Cons), and the root marker =nl_gc_mark_published_frame= marks
=(nl_gc_mark_slot (+ env 32))= (the frame stack).  Yet live objects are
collected → the gap is in GC *root traversal* (the =env= published to the
collection during a deep eval likely does not reach the live frame stack's full
child chain) or in *sweep*.  Pinpointing it needs GC-mark instrumentation
(see §6).

* 4. Ruled out

- Per-eval arena reset: disabled; =nelisp_eval_call= only does recursion-depth
  bounding (rec_cur/rec_max), no arena reset.
- Stack overflow: bigger stack (=ulimit -s 524288=) still crashes 5/5.
- ASLR alone: =setarch -R= (ASLR off) still crashes 5/5 (gdb avoids it via fork
  detach / timing, not ASLR — =disable-randomization off= under gdb does not
  crash either).
- =find_descend= depth off-by-one: a Nil-frame skip guard did not prevent it.
- Hash-table-only corruption: buckets are also observed NULL.

* 5. Why it is masked in production

Two Doc-154 changes reduced per-eval allocation churn enough that the GC no
longer fires at the vulnerable moment for these paths:
- nelisp =3f318889= "alloc-free builtin dispatch" (removed per-comparison
  =nl_alloc_symbol= churn).
- nelix =c435946= "parse standalone lock with native reader" (removed lock-read
  text-scan churn).

On the current feat / =origin/main= binary, =nelix audit= and =nelix lock-check=
are segfault-free (rc=0, verified m1+m3, multiple runs).  The underlying GC bug
is therefore LATENT: any future high-churn workload that triggers a GC during a
deep frame-stack eval can resurface it.

* 6. Investigation plan (for the dedicated task)

1. Reproduce reliably at nelisp =838605da= (pre-=3f318889=): =nelix audit m3.el=
   crashes 5/5.  Minimal reproducers (deep =let= recursion + churn / explicit
   =garbage-collect=) do NOT trigger it — it needs the audit/manifest-load frame
   pattern.  Consider building a closer repro from the audit eval shape.
2. Instrument the GC mark phase (=nl_gc_mark_published_frame= /
   =nl_gc_mark_roots= / =nl_gc_mark_slot= / =nl_gc_mark_vec_slots=) to log:
   - whether the frame-stack root (=env+32=) is reached at all in the crashing
     collection,
   - whether traversal descends frame → hash-table → buckets → cells,
   - the =env= identity vs the env holding the live deep frame stack.
3. Check the two mark skip conditions on the frame child chain:
   - =nl_gc_in_arena (data_ptr)= in =nl_gc_mark_vec_slots= (a false negative
     skips ALL slots of a record/vector),
   - =nl_gc_mark_block= "already marked / foreign" early-out ordering.
4. Confirm whether the published =env= during a nested eval is the one whose
   frame stack holds the live frames (root-set completeness), or whether a
   second/stale env is marked instead.
5. Fix the mark/root gap so the frame child chain survives; verify the
   838605da repro is gone AND run the full container rc-gate (474 tests) +
   re-confirm production stays green.  A GC change is high-risk: validate
   broadly.

* 7. References

- Worklog (anvil): "nelix segfault 根本: GC が live lexframe の子(hash-table/
  buckets)を回収" (2026-06-22) and the prior segfault root-cause entries.
- Memory (anvil): =reference_nelisp_gc_churn_segfault=,
  =reference_nelisp_eval_dispatch_perf=.
- Masking commits: nelisp =3f318889=, nelix =c435946=.
- Code: =lisp/nelisp-cc-frame-stack-find.el= (walk),
  =lisp/nelisp-cc-frame-push.el= / =lisp/nelisp-cc-frame-ensure-capacity.el=
  (push/grow), =scripts/nelisp-standalone-build.el= GC arms
  (=nl_gc_mark_*=, =nl_gc_sweep_*=, =nl_gc_in_arena=).

* 8. Refined findings — 2026-06-22 instrumentation session

Reproduced =nelix audit m3.el= at =838605da= (5/5 SIGSEGV) and traced GC state.
This SHARPENS §3: the crash is *not* a live object freed by a running collector
— *reclamation is disabled by design*.

** 8.1 All reclamation paths are OFF during audit

- Boundary collect =nl_gc_collect= is a NO-OP: arena-init sets the "reclaimer
  gate" =base+160 = 1= (=scripts/nelisp-standalone-build.el= ~line 989), comment:
  "Keep collection disabled until the standalone root/mark gap is fixed".  The
  root/mark gap is a KNOWN, deliberately-worked-around issue.
- Per-eval / boundary reclaim is DISABLED (Doc 140: "escape-epoch reset is
  unsound", =arena-boundary-reclaim-enabled-offset = 0=).
- The precise mark+sweep =nl_gc_collect_published= (via =nl_gc_safepoint=, called
  from =nl_sf_while='s backedge, 16 MiB trigger) is OFF by default — ARMED only
  by the =(nelisp--gc-diag 5)= test hook (=bf_gc_diag= mode 5), disarmed by mode
  6.  Normal =nelix audit= never arms it.

⇒ During audit NO collector runs; the arena only GROWS.

** 8.2 CONFIRMED: arena chunk-growth corruption (frame-object-specific)

The Linux arena is a 256 MiB FIRST CHUNK (=nelisp-standalone--linux-arena-size=,
=#x10000000=) plus =nl_chunk_alloc_new= growth chunks.  With no reclamation,
audit's deep eval (~606 frames) + packaged-registry processing keeps allocating
and crosses the 256 MiB first chunk → chunk growth.

CONFIRMED by the arena-size experiment (rebuild with =NELISP_LINUX_ARENA_SIZE=,
forcing a rebuild via =touch scripts/nelisp-standalone-build.el= since a plain
=make standalone-reader= does NOT re-pick the env):
- *512 MiB first chunk → =nelix audit m3= is 5/5 rc=0* (audit fits in one chunk,
  no growth), output correct.  vs *256 MiB → 5/5 SIGSEGV*.
  ⇒ The crash IS arena chunk-growth corruption.
- =2 GiB= first chunk produces a non-working binary (empty output) — =0x80000000=
  hits the signed-imm32 address-materialisation boundary; keep test sizes
  < 2 GiB (512 MiB is a good probe).

The corruption is FRAME-OBJECT-SPECIFIC, not generic:
- A plain cons churn that DOES force growth (700k conses ≈ 22 MB with a 16 MiB
  first chunk) SURVIVES (rc=0).  So =nl_chunk_alloc_new= growth works for plain
  cons allocation.
- Yet audit's growth nulls a lexframe child (hash-table / buckets — a
  Record/Vector reached via =record-slot-ref-ptr=).  So the bug is in how
  growth (or a pointer computed relative to the first-chunk base) handles
  RECORD / VECTOR frame objects specifically, OR an object that straddles /
  is reached across the chunk boundary.

This explains the MASKING precisely: Doc 154 churn reduction (=3f318889=
alloc-free dispatch removed ~per-comparison symbol allocs across the 606-frame
eval; =c435946= native lock-read) lowers audit's allocation enough to stay
inside the 256 MiB first chunk → no growth → no corruption.

** 8.3 Reproducer notes (what does NOT trigger it)

- Plain deep =let= recursion (300+), =while= loops, explicit =(garbage-collect)=
  (mode 0 — does a conservative scan), and *cons churn that forces growth* all
  complete cleanly.
- The trigger needs BOTH (i) allocation crossing the first-chunk boundary AND
  (ii) live lexframe RECORD/VECTOR children reachable across the growth.  The
  audit/manifest-load + packaged-registry eval shape supplies both; a minimal
  repro should pair a deep frame stack with > first-chunk allocation (tune a
  small first chunk + a frame-heavy loop allocating records/vectors).

** 8.4 Next steps (supersede §6)

1. [DONE] Arena-growth confirmed (512 MiB avoids it; cons-growth survives →
   frame-object-specific).
2. Find the growth bug: audit =nl_chunk_alloc_new= / =nl_alloc_bytes= +
   =nl_gc_in_arena= / =nl_gc_chunk_contains_any= for RECORD/VECTOR objects on a
   growth chunk — esp. any pointer/membership test computed relative to the
   FIRST-chunk base (=record-slot-ref-ptr= / =vector-ref-ptr= reaching a child
   that landed on a growth chunk).  Compare the cons path (works) vs the
   record/vector path (breaks) for what differs across the chunk boundary.
3. Proper fix: land a SOUND reclaiming GC (fix the root/mark gap so
   =nl_gc_collect= can be re-enabled, keeping the arena bounded so growth is
   rarely/never needed).  Existing machinery to make sound:
   =nl_gc_mark_published_contexts= (precise roots, capacity 64; the frame stack
   is a shared backing, so one published env marks all frames) + the mode-0
   conservative scan (=nl_gc_conserv_maybe=).
4. Interim mitigation option: a larger first chunk reduces growth frequency but
   is not a fix (any unbounded no-reclaim workload eventually grows).

** 8.5 Instruction-level localisation (2026-06-22, second session)

- Faulting op =nl_vector_slot_ptr=, =rdi=0=: the NULL is the BUCKETS VECTOR's
  DATA pointer (=vec_box+8=).  I.e. a lexframe's =hash-table.slot1= holds a
  buckets vector whose data buffer pointer is 0.  cons has no separate data
  buffer (single box) → it survives growth; a vector/record has a separate
  data buffer allocated via =nl_alloc_bytes= → that is what goes NULL.
- Synthetic repros that DO cross the boundary but do NOT crash (so "frames +
  growth" is NOT sufficient): deep =let= recursion (500 × 50 KB), frame-heavy
  churn (400k =(let ((y (cons i i))) ...)=), vector churn (300k
  =(make-vector 8 i)= pairs).  All rc=0.  The trigger needs the audit /
  manifest-load + packaged-registry allocation interleaving specifically — the
  missing ingredient is still unidentified.
- Allocation primitives: =nl_alloc_bytes= (freelist reuse → arena bump →
  =nl_chunk_alloc_new= grow) is the bottom; =nl_chunk_alloc_new= returns 0 on
  =nl_os_alloc_chunk= mmap failure (bumps counter @268436208).  =nl_alloc_vector=
  / =nelisp_cons_construct= are grammar-op primitives (not elisp defuns) that
  bottom out in =nl_alloc_bytes=.  Leading micro-suspect: a vector DATA-BUFFER
  =nl_alloc_bytes= at/across the growth boundary returns 0 (or a base-relative
  pointer that is wrong on a growth chunk), and the caller stores it without a
  NULL check.
- Core-forensics caveat: the control region literals in the source (e.g.
  =268436160=, =268436208=) are NOT absolute addresses for gdb — the arena maps
  at a runtime (ASLR) base (=arena-stats[0]= ≈ =0x7f..=), so those reads are
  base-relative.  Literal-address gdb reads on the core fail
  ("Cannot access memory at 0x100002c0"), and =info proc mappings= did not
  surface the chunks.  To do core forensics, FIRST resolve the runtime arena
  base, then read control slots as =base + (literal − 0x10000000)=.
- Next sub-task: (a) build a repro that lands a live vector's DATA BUFFER on a
  growth chunk (vs the box on the first chunk) — the synthetic repros above
  keep box+buffer co-located; or (b) instrument =nl_alloc_bytes= to log a 0
  return / a cross-chunk vector-data allocation during audit (write a marker
  byte via =nl_os_write_stderr= so it survives the crash).

** 8.6 Exit-code dissection: allocator/read/clone all EXONERATED (2026-06-22 b)

Method: a worktree at =838605da= (the last commit that crashes 5/5 at the
256 MiB default; HEAD no longer grows that far because =3f318889= alloc-free
dispatch dropped audit's allocation pressure below the first-chunk size — the
crash is now LATENT, not fixed).  Probes use distinct =exit(N)= codes
(=syscall-direct 60 N=) instead of stderr — the process crashes, so the exit
code is the only reliable channel.  ((b)'s stderr "G" marker via
=syscall-direct 4 2= never surfaced even though growth ran 29× on a cons churn
— a separate puzzle; exit codes work.)  Each probe is byte-for-byte the
original on the valid path.

Findings, in order (all on the crashing 838605da/256 MiB binary):
1. =nl_alloc_bytes= growth branch: post-growth =nl_chunk_try_alloc= NEVER
   returns 0 (no exit 77) and the returned obj is ALWAYS in
   [chunk_base, chunk_base+size) (no exit 78).  A 1M-cons churn grew the arena
   29× (=arena-stats[9]= 1->30) with rc=0.  Freelist reuse is impossible
   (=nl_dealloc_bytes= is a no-op, sweep is OFF).  => the ALLOCATOR returns
   valid, in-range, fresh pointers across growth.  EXONERATED.
2. =nl_vector_slot_ptr= split into hops: the fault is hop-1, =sexp+8= (the
   NlVector box pointer) == 0, NOT hop-2 (=box+8= data_ptr).  Encoding the slot
   tag in the exit code gave =exit 90= => the slot reads tag 0 (Nil), box 0.
   The lexframe hash-table's slot 1 (which must hold the buckets vector)
   reads as Nil.
3. =nl_record_slot_ptr= split into hops: the ht record's box (=sexp+8=) and
   data_ptr (=box+40=) are BOTH valid (no exit 85 / 100+tag).  The record READ
   path is clean — it correctly addresses a slot that genuinely contains Nil.
   EXONERATED.
4. The write path: =record-slot-set= -> =nl_record_set_slot= -> =nl_val_clone_into=
   -> (boxed) =nl_vci_box= -> =nl_sexp_clone_into=.  A read-back guard in
   =nl_vci_box= (after the clone, assert =box.tag == src.tag=) NEVER fires
   (no exit 110+tag).  => at WRITE time the cloned buckets-vector Sexp box is
   correctly populated (tag 8, valid child box).  The clone is EXONERATED.

Conclusion (precise root cause):
  A live, correctly-created 32-byte Sexp box — the cloned buckets vector stored
  in =lexframe.ht.slot1= — is ZEROED by a STRAY WRITE sometime BETWEEN its
  creation (=frame_push= step 3) and its read (=find_in_frame=), and ONLY once
  the arena has grown past the first chunk.  It is not an allocation fault, not
  a read-side addressing fault, and not a clone fault — it is a post-write
  corruption.  This supersedes §8.4 #2 (the allocator-growth-bug hunt) and §8.5
  (the "alloc returns 0 / bad ptr" micro-suspect): both are now ruled out.

Leading remaining hypothesis: the bump allocator hands out an already-live
address a second time (a growth-induced cursor / chunk-bookkeeping desync), and
the reuse zeroes the old box (e.g. a later =nl_sexp_clone_into= of a Nil value,
or =nl_alloc_zero_fill=, writes over it).  mmap(NULL) chunks cannot overlap, so
the suspect is intra-chunk cursor reuse or the first-chunk fixed-cursor
(=nl_chunk_cursor_addr= -> =268435456=) desynchronising after a growth.

Next probe: an address-reuse / high-water-mark detector in =nl_alloc_bytes=
(assert each returned obj is disjoint from the last live allocation on its
chunk), or a per-chunk cursor-monotonicity assertion across the growth event.
Diagnostic worktree was at =/tmp/nelisp-838-diag= (removed); production restored
to clean feat/256 MiB.

** 8.7 NOT freelist reuse; the stray write zeroes live 32B Sexp boxes (2026-06-22 c)

Probe A — is it freelist reuse?  A detector in =nl_alloc_bytes= that =exit(76)=
on ANY =nl_freelist_take= hit fired immediately, even on a benign 50k-cons
churn (which runs rc=0 normally).  So reuse DOES happen in normal operation
(something populates the bucketed free-lists at =268435696+= even with
=nl_dealloc_bytes= a no-op and sweep OFF — populator not yet identified), and
reuse alone is not the bug.  Decisive test: force reuse OFF (=(let ((reused
0)))=, always bump).  Audit STILL crashes 5/5 (rc=139).  => freelist reuse is
NOT the stray-write source.  EXONERATED.

Probe B — gdb on the reuse-OFF crash (deterministic, same-run reads):
- Fault is =nl_vector_slot_ptr+85= =mov (%rdi,%rsi,1)=, the THIRD-hop read
  =data_ptr + idx*8=, with =idx = 281084166= (=0x10c10106=) and =rsi=0=.
- The buckets vector is FULLY INTACT: Sexp slot tag=8, box valid, NlVector
  =cap=16, data_ptr valid, len=16, rc=2=.  The crash is the HUGE bucket index,
  not a bad vector.
- =idx = fnv1a(name) & (bucket_count - 1)=.  idx == low 32 bits of fnv1a (the
  FNV prime =0x01000193= is live in the find_in_frame stack) => the mask was
  =0xFFFFFFFF= => =bucket_count = 0= (int32: =0 - 1 = 0xFFFFFFFF=).
  i.e. =sexp-int-unwrap(ht.slot0)= read 0 instead of 16.
- The ht record's 3-slot buffer was located: slot1 correctly points to the
  intact buckets vector; the corruption is =slot0= (the bucket-count Int box
  read as Nil/0).

Synthesis with §8.6 (reuse-ON read slot1 as 0): the stray write ZEROES live
32-byte Sexp boxes; WHICH box (the bucket-count Int(16) box, or the
buckets-pointer slot) depends only on the allocation layout (reuse on/off
shifts addresses).  A 32-byte all-zero block is exactly a Nil 32B Sexp (tag 0,
payload 0) — precisely what =nl_sci_store_imm= writes on its Nil-immediate
branch (=ptr-write-u64 dst {0,8,16,24} 0=).  So the leading suspect is a
clone/store whose DST is wrongly computed (growth-sensitive) onto a live box,
writing 32 zeros over it.

Memory-model note (re-confirmed): the source control-region literals
(=0x10000000..0x10000318=) are NOT runtime addresses — the per-unit
chunk-arena-rewrite turns them into =nl_arena_base + (lit - 0x10000000)=
loads.  All literal-address gdb reads fail ("Cannot access memory at
0x10000310"); this is also why core forensics (§8.5) failed.  Read control via
the runtime arena base (=nl_arena_base= bss symbol, resolvable in gdb) instead.

Next probe (definitive): set a HARDWARE watchpoint on the bucket-count / buckets
box address within ONE run — break right after the box is created in
=frame_push=, =watch *box=, continue, and catch the instruction that writes the
32 zeros.  ASLR varies addresses per run, so this must be a single-run
break→watch→continue (not a fixed-address watchpoint).  Alternative: instrument
=nl_sci_store_imm='s Nil branch to record when DST already holds a live boxed
tag.  Either pinpoints the exact wrong-DST store.

** 8.8 ROOT CAUSE FOUND: form-boundary collection runs (unsound) (2026-06-22 d)

The "stray write" IS the GC freeing a live block.  Probe chain (838605da/256
MiB worktree, gdb defaults to ASLR-off so addresses are stable across runs):

1. Set =nl_gc_sweep= to a NO-OP → audit 5/5 rc=0.  So a SWEEP is freeing a
   live block.  (The free-link write =nl_gc_free_block_link= writes the bucket
   next-pointer at =hdr+8= = the OBJECT's first word, clobbering its tag —
   exactly the "32 zeros / wild value" seen in §8.6/§8.7.  The bucket-count
   Int(16) box read back as =[next-ptr, ...]=, so =sexp-int-unwrap= → garbage
   → huge bucket index → wild read.)
2. Distinct =exit()= at each sweep call-site → audit exits 71 = the BOUNDARY
   =nl_gc_collect= (not the safepoint / not the =garbage-collect= builtin,
   which audit never calls; =nl_sf_while= is a stub so the safepoint never
   fires).
3. =nl_gc_collect='s reclaimer gate =268435616= (= base+160) reads 0 at run
   time (exit 91), and gdb confirms =*(nl_arena_base+160) = 0=.  So collection
   is ENABLED, despite the comment claiming it is disabled.
4. ROOT CAUSE: the gate is seeded by TWO functions and they DISAGREE.
   =nelisp-standalone--arena-init-metadata-forms= (STATIC, line ~989) writes
   =base+160 = 1= (disabled).  But linux/win/macOS use the DYNAMIC variant
   =nelisp-standalone--arena-init-metadata-forms-dynamic= (line ~1031), whose
   line 1053 writes =base+160 = 0= with comment "collect ENABLED (S0 sound-GC:
   form-boundary mark-sweep)".  So on every real (dynamic-arena) target,
   form-boundary collection RUNS.

Why it only crashes after growth: =nl_gc_collect= marks via =nl_gc_mark_roots=
(precise) then sweeps the WHOLE chunk list (=nl_gc_sweep_chunks=).  The precise
marker does NOT fully cover the lexframe hash-table's child boxes (bucket-count
Int, buckets vector) once they land on a GROWTH chunk — the documented
"standalone root/mark gap".  (=nl_gc_in_arena= itself DOES walk all chunks, so
the gap is in the mark ROOT-SET/recursion over the frame stack, not the bounds
check.)  Without growth (512 MiB first chunk) every live object is on chunk 0
and survives; with growth the unmarked growth-chunk frame children are swept →
freed → corrupted.  This is precisely the bug this doc was opened for.

FIX (verified): set the DYNAMIC init's =base+160= back to =1= (collection
DISABLED), matching the STATIC variant and the standing comment ("Keep
collection disabled until the standalone root/mark gap is fixed").  One line
(1053): =(+ ,b 160) 0 0= → =1=.  With the fix, audit 5/5 rc=0.  TRADE-OFF:
no-reclaim reintroduces memory pressure (a 1M-cons micro-bench that finished
with collection on now times out at 40s; real nelix workloads are modest and
unaffected).  HEAD (=096b81de=/feat) carries the SAME line 1053 = 0 — the
crash is LATENT there only because =3f318889= alloc-free dispatch keeps audit
under the growth boundary.

Two fix paths:
- (A) DISABLE collection (line 1053 → 1).  Minimal, matches documented intent,
  verified.  Cost: memory pressure (the pre-S0 state).
- (B) SOUND GC: make =nl_gc_mark_roots= fully mark the frame-stack hash-table
  children (so growth-chunk frame children survive), keeping collection on.
  The "real" fix; larger and riskier; needs the precise root traversal over
  env→frames→backing→frame records→ht record→slots audited for growth-chunk
  coverage.

Production change is gated on the container RC gate (per policy); not yet
applied to HEAD.

** 8.9 Path B (sound GC) investigation — gap is marking-REACH, not missing-push (2026-06-22 e)

Path A (line 1053 → 1) shipped (=d5724bb3=, gate GREEN).  Path B = re-enable
collection (line 1053 = 0) + close the marking gap.  Investigated on the
838605da worktree (collection ON, crashes).  Findings:

- First obvious fix — make the BOUNDARY =nl_gc_collect= also call
  =nl_gc_mark_published_contexts= (mark every nested eval/load context, the arm
  the MIDFORM safepoint already uses): does NOT help (audit still 5/5 SIGSEGV).
- Why: a depth probe (=exit(100+depth)= on the first collect with published
  depth>0) shows depth reaches 2, so outer contexts ARE pushed — but the
  BOUNDARY collect fires in =bf_load_eval_loop= AFTER =nl_driver_eval_published=
  has POPPED its context, so at the top-level boundary published depth = 0
  (nothing to mark there) and the conservative C-stack scan range is tiny
  (=rsp..STACK_TOP= ≈ 368 B — the loop frame).  So neither published-context
  marking nor the conservative scan covers the live frame at the boundary.
  (Side note: =syscall-direct 4 2= stderr writes from the GC/arena units never
  surface — only =syscall-direct 60= exits do — so all probes use exit codes.
  nelix also redirects the runtime's stderr.)
- gdb retention walk-up from the faulting buckets slot (gdb disables ASLR →
  addresses stable): buckets-slot → ht slots buffer (slot0 bucket-count,
  slot1 buckets) → ht record box on a GROWTH chunk.  Two record boxes share
  the SAME slots data_ptr (refcount-clone / shared record).  Crucially, NO
  pointer in the growth-chunk region points at the ht box — its referrer (the
  lexframe record / backing vector) lives in a DIFFERENT region (first chunk).
  So the live object graph is cross-chunk: frame records on chunk 0, their
  ht/buckets children (allocated by =frame_push= DURING the audit) on growth
  chunks.

Conclusion: the gap is marking-REACH, not a missing push.  The precise marker
should reach the growth-chunk ht via env→frames→backing→frame→ht (and
=nl_gc_in_arena= DOES cover growth chunks), yet the crashing ht is not marked —
so the real retention path is NOT the boundary collect's =ctx=+32 frame stack
at the moment it fires.  Likely the live frame is retained via a path outside
{the single =ctx=, the popped published stack, the 368 B conservative window}
— e.g. a shared/cloned record or a closure-captured environment the traversal
does not follow.  Closing this soundly is a substantial GC project (identify
the exact retention edge, extend the marker, re-validate the full gate AND
nested-load/closure edge cases — the same gap resisted the conservative-scan
and published-context fixes already in tree).  Re-enabling collection without
that complete fix would reintroduce the latent crash.

RECOMMENDATION: keep Path A (sound, shipped, gate-green).  Path B remains a
scoped follow-up; the concrete next step is to instrument the FREEING site
(=nl_gc_free_block=) to dump, for the first frame-/ht-shaped block it frees,
the chain of referrers (memory-search up to a root) — that names the exact
unmarked retention edge to teach the marker.

** 8.10 HW-watchpoint: the ENTIRE ht is unmarked + swept; referrer elusive (2026-06-22 f)

Caught the exact free with a HARDWARE watchpoint (gdb disables ASLR, so the
crash-run addresses are stable; break at first =nl_gc_collect=, then
=watch *(hdr) if (*(hdr)&7)==2= on the bucket-count box header):

- The header transitions 40 -> 42 (BT=40, mark 0 -> 2 = FREE).  Backtrace:
  =nl_hdr_set_mark <- nl_gc_free_block_link <- nl_gc_free_block <-
  nl_gc_sweep_one <- nl_gc_sweep_step <- nl_gc_sweep_chunk <-
  nl_gc_sweep_chunks ×4 <- nl_gc_sweep <- nl_gc_collect <- bf_load_eval_loop
  <- bf_load=.  So it is the BOUNDARY collect's sweep, fired during a NESTED
  =bf_load= (loading a nelix file mid-audit).
- Mark bits of the whole ht cluster at that moment (low3: 0=unmarked,
  1=live-marked, 2=free): ht record box = 0 (UNMARKED), and its slots buffer
  + bucket-count box + buckets box all sweep to 2.  So the ENTIRE ht record —
  not just one child slot — is unmarked.  => marking never reached the ht
  record at all; this is NOT a per-slot recursion miss.
- Walked UP for the ht record box's referrer: NOT found in the first chunk
  ([arena_base, +256 MiB]) NOR in the searched growth region.  The retention
  edge to the live ht is not where the obvious roots point.

Two refined hypotheses (next experiments):
1. ROOT-COVERAGE: the ht's frame is on some env's frame stack that the
   boundary collect does not mark — but the published-context fix (§8.9) marks
   ALL published envs and did NOT help, and the referrer isn't in chunk 0, so
   the retaining env/edge is genuinely outside the marked set (closure capture,
   a detached frames-record, or an arena-scratch-only reference).
2. STALE-FRAME (use-after-pop): the ht's frame was already POPPED (dead, so
   correctly unmarked+freed), but =nelisp_frame_stack_find_descend= later walks
   it via a STALE backing-vector slot / wrong depth.  Under Path A (no reclaim)
   the popped frame's memory persists intact, so the stale read silently
   "works" — masking a frame-stack-management bug (pop not clearing
   backing[depth], or depth off-by).  Collection ON frees the popped frame ->
   the stale read crashes.  If true, the FIX is in frame pop/depth, NOT the GC.

Decisive next step for (2): at the crash, read env+32 frames-record depth and
verify whether the crashing frame's index is < live depth (in-bounds = root
gap) or >= depth / a stale slot (use-after-pop).  Tooling notes: gdb conditional
breakpoints on =nl_gc_free_block= are far too slow to reach the relevant sweep
(millions of frees) — use a HW watchpoint (debug register, no per-free cost);
stderr writes (=syscall-direct 4 2=) from the GC/arena units never surface and
nelix redirects the runtime stderr, so use exit codes or gdb only.

** 8.11 Path B deep dive: the ht is NEVER marked; recursion breaks below the frames-record (2026-06-22 g)

Continued Path B per user request.  All on the 838605da worktree, gdb (ASLR
off → stable addresses), HW watchpoints.  Chased the EXACT live object graph
of the freed ht:

- The freeing collect is the BOUNDARY =nl_gc_collect= fired during a NESTED
  =bf_load= (=nl_gc_collect <- bf_load_eval_loop <- bf_load=).  Its =ctx= (env)
  = 0x7fffa7e00480 — IDENTICAL to the env =nelisp_frame_stack_find= walks at
  the crash (frames-ptr = ctx+32 = 0x7fffa7e004a0, a boot/global env).
- The crashing frame is backing[20] of a depth-25 stack; backing len = 64 ≥ 25
  (no len/depth mismatch).  backing[20] -> frame Sexp slot (tag 12) -> frame
  box 0x7fffa982a7c0 (FIRST chunk) whose data_ptr/slots live on a GROWTH chunk
  (the ht + bucket-count + buckets).  =in_arena= = 1 for ALL of them; the frame
  box is NOT boot.  So leaf-level marking WOULD work if the recursion reached
  it.
- HW watchpoint =if mark==1= on the ht box: it NEVER fires — the ht is NEVER
  marked live.  So the precise recursion simply never reaches the ht.
- NOT the conservative scan: forcing SCAN_FLAG=0 (conserv OFF) still crashes
  5/5.  So the earlier "conserv marks a record BOX interior pointer and poisons
  precise recursion" idea is REFUTED for this case.
- NOT the rootstack: =nl_gc_mark_rootstack= walks 32-byte Sexp SLOTS (tag@+0)
  and recurses via =nl_gc_mark_slot=, so it does not block-mark box interiors.
- The frames-record box DOES get marked live (=nl_hdr_set_mark <- nl_gc_mark_block
  <- nl_gc_mark_slot <- nl_gc_mark_roots=) — but observed at the TOP-LEVEL
  collect (=nl_gc_collect <- nl_eval_source_all <- driver=).  Marks are reset
  between collects; the freeing NESTED collect re-marks from the same ctx+32.

Net: at the NESTED bf_load collect, =mark_slot(ctx+32)= marks the frames record
(its ctx == the live env), yet the ht is never reached.  So the recursion
breaks SOMEWHERE BELOW the frames record — in frames-record -> slot0 backing
vector -> backing[i] -> frame record -> slot0 ht — even though every node is
in_arena and the frames record itself is marked.  The leading mechanism is a
"marked-but-NOT-recursed" double-mark skip: =nl_gc_mark_slot= recurses a
Record/Vector ONLY when =nl_gc_mark_block(box)= returns 1 (the FIRST mark); if
some path block-marks an intermediate node (backing vector or a frame) WITHOUT
recursing it, the precise marker then sees it already-marked and SKIPS the
child recursion, orphaning everything below.  (Source of that premature
block-mark not yet pinned — conserv and rootstack are ruled out; remaining
candidates: =nl_gc_mark_root_blocks= block-marking a scratch slot that aliases
a chain node, the type_tag =mark_slot(box)= arm, or a shared/cloned node marked
via a second edge.)

CONCRETE NEXT STEP: watch the BACKING box and a mid-chain frame box for
=mark==1= but FILTER to the NESTED collect (ignore the top-level
=nl_eval_source_all= collect) — that pinpoints which link is block-marked
without recursion.  THE FIX (once pinned) is to DECOUPLE "alive" from
"recursed": give the marker a distinct "needs-recursion" state so any node
that was block-marked by a pinning path is still child-recursed by the precise
marker (e.g. mark 1 = recursed, a new value = pinned-not-recursed; mark_block
upgrades pinned->recursed and returns 1 so recursion proceeds; sweep keeps
both).  This is a real GC-marking change requiring full-gate revalidation, so
Path A (collection disabled) remains the shipped fix; Path B continues to be a
scoped effort with the recursion break now localized to the
frames-record→…→ht precise traversal.

** 8.12 Path B SHIPPED: sound GC via "alive vs recursed" decouple (2026-06-22 h)

Pinned the exact mechanism and fixed it; reclamation is re-enabled.

Mechanism (HW-watchpoint, 838605da/256MiB):
- The freeing collect is the boundary =nl_gc_collect= during a nested
  =bf_load=; its =ctx= == the env =find= walks.  The crashing frame is
  backing[20] of a depth-25 stack (backing len 64, no mismatch); the frame box
  is on chunk 0, its ht/children on a growth chunk, all =in_arena=1=.
- A =watch ... if mark==1= on the ht box NEVER fires => the ht is NEVER marked;
  the precise recursion never reaches it.  Post-mark chain marks show
  frames-record / backing / frame box all mark 1, but the ht unmarked.
- =watch frame_box if mark==1= backtrace: the FIRST mark of the frame box is
  =nl_gc_mark_block <- nl_gc_mark_slot <- nl_gc_conserv_word <- nl_gc_conserv_scan
  <- nl_gc_conserv_maybe <- nl_gc_mark_roots=.  So the CONSERVATIVE SCAN marks
  the frame's box.  When the scan's candidate pointer is a record/vector BOX
  interior (not the Sexp slot), =nl_gc_mark_slot= reads box+0 (the type_tag, a
  Symbol-tag byte) as a Sexp tag and takes the Symbol arm — marking the block
  but NOT recursing the record's slots.  The block is now mark 1, so when the
  PRECISE marker later reaches the object via its real Sexp slot,
  =nl_gc_mark_block= returns 0 (already marked) and recursion is SKIPPED — the
  ht's bucket-count/buckets are never marked -> swept -> the =nl_gc_free_block_link=
  next-link write clobbers the live box's tag -> SIGSEGV.
  (Note §8.10/8.11 had tentatively "refuted" conserv because SCAN_FLAG=0 still
  crashed — but that just trades this bug for the OTHER in-flight crash conserv
  was added to fix; conserv is both necessary AND was poisoning precise
  recursion.)

FIX (b31179ce): decouple "alive" (pinned) from "recursed".
- =nl_gc_conserv_owner= pins a kept block with mark 4 (alive, not-recursed)
  instead of calling =nl_gc_mark_block= (which set mark 1).
- =nl_gc_mark_block= treats mark 4 like mark 0: upgrade 4->1 and return 1
  ("recurse"), so the precise marker recurses a conservatively-pinned block.
- =nl_gc_sweep_one= keeps + resets mark 4 exactly like mark 1.
- Reclaimer gate base+160 re-enabled (dynamic init line: 1 -> 0).
Soundness: terminates (each block goes 0/4 -> 1 at most once, then mark_block
returns 0), never frees a pinned/live block, and only ever marks MORE
(strictly safer than the old conserv, which already could not recurse box
interiors).

VALIDATION:
- 838605da/256 MiB (reliable repro): audit 10/10 rc=0 (was 0/5 SIGSEGV);
  =(garbage-collect)= builtin + 300k make-vector/hash churn rc=0.
- HEAD (=b31179ce=): audit 5/5 rc=0; sanity + gc-builtin clean.
- Full Debian no-Nix RC container gate (474-test nelix RC gate) GREEN against
  the local commit (NELISP_REPO_URL=/work/nelisp).
Pushed to feat.  Path B supersedes Path A: form-boundary reclamation is on
again, soundly.

* 9. Closeout (2026-06-22)

CLOSED.  Resolution shipped to =main= (fast-forward merge; feat branch
deleted local+remote):
- =b31179ce= nelisp: sound form-boundary GC — decouple "alive" from "recursed".
- =39646470= main HEAD after merge (Doc 155 §8.6-8.12 forensics + Fix A/B).
Fix B re-enables form-boundary collection soundly via the mark-4 "pinned vs
recursed" decouple (=nl_gc_conserv_owner= pins with 4; =nl_gc_mark_block=
upgrades 4->1 and recurses; =nl_gc_sweep_one= keeps+resets 4 like 1).
Validation: full Debian no-Nix RC container gate (474 tests) GREEN; the
reliably-SIGSEGV'ing 256 MiB audit now runs 10/10 + (garbage-collect) +
make-vector/hash churn all clean.

Net journey: §8.1-8.5 mis-localised to arena chunk-growth corruption; §8.6-8.7
exonerated allocator/read/clone and showed a post-write stray zero; §8.8
root-caused it to the dynamic init enabling unsound boundary collection
(Fix A: disable, stopgap); §8.9-8.11 localised the marking-reach gap with HW
watchpoints; §8.12 pinned the conservative-scan-poisons-precise-recursion
mechanism and fixed it (Fix B), restoring reclamation.  No further action.

#+title: Phase 47.D — Dynamic-linked ELF emission (pure-elisp AOT)
#+startup: overview

* 1. Goal

Teach the pure-elisp AOT toolchain (~nelisp-elf-write.el~ + ~nelisp-static-linker.el~)
to emit a *dynamically linked* ELF executable so the standalone reader can call
arbitrary shared-library functions (GnuTLS, FreeType, HarfBuzz, libc beyond the
syscall set).  This is the keystone that unblocks, in the consumer
~nelisp-emacs~ bridge: D1 TLS (~emacs-tls-ffi.el~), F1 font metrics
(~emacs-font-ffi.el~), and E4 complex-script shaping — all currently
"author-only" because the emitted ELF is freestanding/static (no PT_INTERP, no
DYNAMIC, no way to resolve ~gnutls_*~ / ~FT_*~).

The runtime already has the *call* side: ~call-ptr ADDR a0..a6~ (forward FFI
indirect call, nelisp-standalone-build.el).  The only missing piece is *symbol
resolution* — making ld.so fill a GOT slot with the resolved address that
~call-ptr~ then jumps to.

* 2. Current state (static, freestanding)

- ~nelisp-elf-write.el~: ehdr/phdr/shdr/sym/rela byte writers; ET_EXEC/ET_DYN,
  PT_LOAD/PT_PHDR, R_X86_64_64/PC32/PLT32.  *No* PT_INTERP, PT_DYNAMIC, DT_*,
  .dynsym/.dynstr/.hash, GOT, GLOB_DAT.
- ~nelisp-static-linker.el~: 2-pass linker.  ~nelisp-link--compute-layout~ lays
  sections at VA base 0x400000; ~nelisp-link-units~ drives emission through
  ~nelisp-elf-write-binary~.  Entry at vaddr+~0x78~ after Ehdr+Phdr.
- Result: ~statically linked, no DYNAMIC section~ (readelf), so dlopen/dlsym and
  external symbols are unreachable (confirmed: the syscall shim emulates libc).

* 3. Design — eager GLOB_DAT imports (no lazy PLT)

Use the *simplest* dynamic-linking shape that ld.so accepts: import each needed
function as a GLOB_DAT relocation into a dedicated GOT slot, resolved eagerly at
load.  Avoid lazy PLT/.plt stubs entirely — the code reads the GOT slot directly
and uses ~call-ptr~.  This drops a whole layer of complexity (no PLT0, no
.rela.plt lazy path, no _dl_runtime_resolve).

New ELF artifacts to emit:
1. *.interp* (PROGBITS, SHF_ALLOC) = "/lib64/ld-linux-x86-64.so.2\0", covered by
   a *PT_INTERP* phdr.
2. *.dynstr* (STRTAB) — lib soname strings (libc.so.6, libgnutls.so.30,
   libfreetype.so.6) + imported symbol names.
3. *.dynsym* (DYNSYM) — index 0 = null; one STT_FUNC/STB_GLOBAL/SHN_UNDEF entry
   per import (st_name → .dynstr).
4. *.hash* (HASH) — the SysV ELF hash table over .dynsym (ld.so requires DT_HASH
   or DT_GNU_HASH; SysV .hash is the simplest to generate: nbucket/nchain +
   buckets + chain, with `elf_hash`).
5. *.got* (PROGBITS, SHF_ALLOC|SHF_WRITE) — one 8-byte slot per import,
   initially 0.
6. *.rela.dyn* (RELA) — one ~R_X86_64_GLOB_DAT~ per import: r_offset = GOT slot
   VA, r_info = (dynsym-index << 32) | GLOB_DAT, r_addend = 0.
7. *.dynamic* (DYNAMIC, SHF_ALLOC|SHF_WRITE) — DT_NEEDED (one per soname),
   DT_STRTAB/DT_STRSZ, DT_SYMTAB/DT_SYMENT, DT_HASH, DT_RELA/DT_RELASZ/DT_RELAENT,
   DT_NULL.  Covered by a *PT_DYNAMIC* phdr.
8. Program headers, in order: PT_PHDR, PT_INTERP, PT_LOAD(R+X text/rodata),
   PT_LOAD(R+W data/.got/.dynamic), PT_DYNAMIC.

Codegen wiring (nelisp-standalone-build.el):
- A build-time table maps (soname, symbol) → GOT-slot index.
- Emit ~(call-ptr (ptr-read-u64 <GOT_SLOT_VA>) a0..a6)~ for each external call.
- ~emacs-tls-ffi.el~ / ~emacs-font-ffi.el~ ultimately route ~nl-ffi-call~ to this
  path (a new ~nl-ffi-call~ over GOT instead of the syscall shim) when the symbol
  is non-syscall.

* 4. Phases (each ends at a verifiable gate)

- *P1 — loadable dynamic ELF, zero imports.* Add constants + writers; emit
  .interp + minimal .dynamic (DT_HASH/STRTAB/SYMTAB with a 1-entry null dynsym) +
  PT_INTERP/PT_DYNAMIC.  Gate: the standalone reader still runs (`output 42`) as
  a *dynamically linked* ELF (readelf -d shows a DYNAMIC section; `ldd` lists the
  interpreter).  This proves the dynamic skeleton loads.
- *P2 — one libc import, called via GOT.* Import e.g. ~getpid~; GLOB_DAT into a
  GOT slot; a smoke form does ~(call-ptr (ptr-read-u64 GOTVA))~ and prints it.
  Gate: matches the real getpid.
- *P3 — multi-lib imports.* DT_NEEDED libgnutls/libfreetype; import the symbols
  ~emacs-tls-ffi.el~/~emacs-font-ffi.el~ use; expose them to elisp via a
  GOT-backed ~nl-ffi-call~.  Gate: ~emacs-tls-ffi~ handshake + ~emacs-font-ffi~
  glyph advance verified on the binary (consumer-side D1/F1).

* 5. Risks

- ld.so is unforgiving: a malformed .dynamic/.hash → silent segfault at load,
  no diagnostics.  Mitigation: build P1 against a hand-verified reference layout
  (compare bytes to a gcc-produced minimal dynamic ELF with the same imports);
  keep a known-good backup binary and restore on any load failure.
- .hash correctness (elf_hash + bucket/chain) is the classic foot-gun — unit
  test the hash-table builder in host emacs against known ELF hashes first.
- VA/offset alignment for the second (R+W) PT_LOAD and p_align=0x1000.

* 6. Status

- 2026-06-27: design recorded.
- 2026-06-27: *P1 DONE* (commit 16ca49db).  ~nelisp-elf-build-dynamic-binary~
  emits a minimal ET_EXEC with PT_INTERP/PT_DYNAMIC + .interp/.hash/.dynsym/
  .dynstr/.dynamic; ld.so loads it and an exit(42) entry runs (verified, exit
  42); ~readelf -d~ shows the DYNAMIC section.  Pure builders + the ld.so exec
  smoke covered by ~test/nelisp-elf-dynamic-test.el~ (5/5); existing
  ~nelisp-elf-write-test.el~ stays green (71/71).  The static
  ~nelisp-elf--build-rich~ path is untouched.
- 2026-06-27: *P2 DONE*.  ~nelisp-elf-build-dynamic-binary~ now takes
  ~:imports~ (list of (SONAME . SYMBOL)) + ~:text-fn~ (GOT-alist -> fixed-length
  entry bytes).  Each import gets a .dynsym/.dynstr/.hash entry, a .got slot, an
  ~R_X86_64_GLOB_DAT~ reloc in .rela.dyn, and DT_NEEDED/DT_RELA/DT_RELASZ/
  DT_RELAENT in .dynamic.  Verified: a binary importing libc ~toupper~ and
  calling it through the ld.so-resolved GOT slot exits 65 (= toupper(?a));
  ~readelf -d~ shows NEEDED libc.so.6 + RELA.  Tests:
  ~test/nelisp-elf-dynamic-test.el~ 7/7; ~nelisp-elf-write-test.el~ 71/71.
- 2026-06-27: *P3 linker integration DONE*.  ~nelisp-link-units-dynamic~
  (nelisp-static-linker) links compile units into a dynamic ELF: it shares the
  layout via ~nelisp-elf--dynamic-layout~ / ~nelisp-elf-dynamic-got-vas~, pins
  each import's GOT slot as an absolute ~__got_<sym>~ symbol, and lets the
  existing 2-pass reloc machinery resolve unit references (an ~abs64~ reloc on
  ~movabs rax, __got_foo~).  Verified: a unit whose .text calls libc ~toupper~
  via its ~__got_toupper~ slot links + runs (exit 65).  Tests:
  ~test/nelisp-elf-dynamic-test.el~ 8/8; ~nelisp-static-linker-test.el~ 64/64.
  (The emitter was refactored to source its layout from
  ~nelisp-elf--dynamic-layout~ so emit-time and link-time GOT VAs cannot drift;
  P1/P2 stay green.)
- 2026-06-27: *P3a DONE* — .rodata/.data/.bss carry-through (data exits 99;
  rodata+import exits 65).  *Explicit entry symbol* — ~nelisp-link-units-dynamic~
  honors ENTRY-SYM (e_entry = resolved _start VA), prerequisite for the reader.
- 2026-06-27: *P3 Step B DONE* — the real reader links dynamic via
  ~NELISP_READER_DYNAMIC=1 make nelisp~ (default stays static).  The 4 MiB
  reader becomes `dynamically linked, interp /lib64/ld-linux-x86-64.so.2' and
  ld.so loads it with ALL functionality intact: the binary FFI smokes
  (PTY/IPv6+datagram/pipe-async/SIGCHLD), maphash, and syscall-direct all pass.
  (The arena is a fixed mmap at 0x10000000, independent of the ELF layout;
  section refs go through relocs resolved against the dynamic layout, so the
  flip is behaviour-preserving.)
- 2026-06-27: *P3 Step C — PLT mechanism DONE* (commit 0e27f1d4).  The linker now
  generates a ~.plt~ stub per import (~nelisp-elf--dynamic-layout~ adds a
  ~:plt-off~/~:plt-sz~/~:plt-va-map~ region in the RX segment;
  ~nelisp-elf-build-dynamic-binary~ emits each stub as ~FF 25 disp32~ =
  ~jmp qword [rip+GOT]~), and ~nelisp-link-units-dynamic~ *defines the bare import
  name at its PLT stub VA*.  Consequence: an ordinary direct ~call SYM~ — exactly
  what ~extern-call~ emits — to a shared-library symbol now resolves (pc32 =
  S-(P+4), addend 0) to the in-binary stub and jumps through the ld.so-filled GOT.
  This is the keystone: external libc/GnuTLS/FreeType calls work with *no codegen
  change*.  Verified: a unit doing ~call toupper~ (the extern-call shape) linked
  with import libc:toupper exits 65 (= toupper(?a)).  Tests:
  ~test/nelisp-elf-dynamic-test.el~ 11/11; ~nelisp-static-linker-test.el~ 64/64;
  the older ~__got_<sym>~ direct-GOT path stays green.
- 2026-06-27: *P3 Step C — runtime ~nl-ffi-call~ wired + verified on the reader.*
  The dynamic reader now imports libc symbols and dispatches them at runtime:
  - ~nelisp-standalone--reader-extern-imports~ = the (SONAME . SYMBOL) import
    list (libc ~toupper~/~tolower~ today; the GnuTLS/FreeType seam for D1/F1),
    passed to ~nelisp-link-units-dynamic~ in ~nelisp-standalone-build-reader~.
  - ~nelisp-standalone--applyfn-extern-arms~ = a ~nl-ffi-call~ dispatch arm that,
    per imported name, does ~(extern-call SYM (wf_argval args N))~ — routed
    through the Step C PLT stub → GOT.  Appended to the reader dispatch table and
    registered in ~nelisp-standalone--reader-builtins~ *only when
    NELISP_READER_DYNAMIC is set* (the static reader has no PLT; there it stays
    an unknown builtin → graceful nil, no link breakage).
  Verified on the actual 4 MiB dynamic binary (NOT host): ~(nl-ffi-call "toupper"
  97)~ => 65, ~(nl-ffi-call "tolower" 65)~ => 97, the symbol form ~'toupper~ too.
  Static-reader regression intact: statically linked, ~(+ 40 3)~ => 43, maphash
  => 30, ~nl-ffi-call~ absent (nil).  New permanent smoke: ~make
  standalone-reader-ffi-smoke~.
- 2026-06-27: *D1 (TLS) + F1 (font) — real GnuTLS/FreeType verified on the
  binary.*  The FFI surface is now a declarative table
  ~nelisp-standalone--reader-extern-table~ of (SYMBOL SONAME ARITY) rows; both
  the import list and the ~nl-ffi-call~ dispatch chain are generated from it, so
  a new call is one row.  Marshalling: each C arg is passed by ~wf_argval~ as an
  i64 (covers ints *and* pointers, since a pointer is an address); pointer
  arguments are produced caller-side with ~alloc-bytes~ (address as integer) and
  read back with ~ptr-read-*~; pointer returns come back as the raw address via
  ~wf_write_int~.  Multi-soname linking emits one DT_NEEDED per distinct SONAME.
  Verified on the actual dynamic binary (~ldd~ resolves all three libs):
  - *D1*: ~(nl-ffi-call "gnutls_check_version" 0)~ returns the GnuTLS ~const
    char*~; reading it back yields ~"3.8.9"~ (pointer-return marshalling).
  - *F1*: ~(nl-ffi-call "FT_Init_FreeType" &lib)~ => rc 0 and writes the
    FT_Library handle; ~(nl-ffi-call "FT_Library_Version" lib &maj &min &patch)~
    writes ~2 / 13 / 3~ (FreeType 2.13.3) — pointer-out-param + 4-arg
    marshalling.  ~FT_Done_FreeType~ releases it.
  DT_NEEDED on the binary: libc.so.6 + libgnutls.so.30 + libfreetype.so.6.
  ~make standalone-reader-ffi-smoke~ now asserts all three (libc + D1 + F1) with
  version-prefix checks.  Static reader unaffected (statically linked, no
  GnuTLS/FreeType NEEDED, ~nl-ffi-call~ absent → nil).
  This closes the D1/F1 *FFI substrate*: ~emacs-tls-ffi.el~ / ~emacs-font-ffi.el~
  are not present in this checkout, but their handshake / glyph-advance paths are
  now just additional rows in ~--reader-extern-table~ over the proven
  elisp → apply → ~extern-call~ → PLT → GOT → shared-lib path.
- 2026-06-27: *D2 (real TLS handshake) + F2 (real glyph advance) — end-to-end on
  the binary.*  The extern table now carries the full client-handshake +
  glyph-advance symbol sets (~gnutls_init~/~_credentials_set~/~_server_name_set~/
  ~_handshake~/~_protocol_get_*~/~_bye~/~_deinit~; ~FT_New_Face~/
  ~FT_Set_Pixel_Sizes~/~FT_Get_Char_Index~/~FT_Get_Advance~/~FT_Done_Face~).
  - *F2*: ~make standalone-reader-ffi-smoke~ loads DejaVuSans.ttf via
    ~FT_New_Face~ (C path built in an arena buffer), ~FT_Get_Char_Index ?A~ → 36,
    ~FT_Get_Advance~ → 2162688 (= 33.0 px in 16.16) — a real glyph advance from a
    real font file.
  - *D2*: ~make standalone-reader-tls-smoke~ opens a raw TCP socket
    (~syscall-direct~ socket/connect to 1.1.1.1:443) and drives a *complete
    GnuTLS client handshake* — SNI set to "one.one.one.one", ~gnutls_handshake~
    returns 0, ~gnutls_protocol_get_name~ reports *TLS1.3*, then bye/deinit.  A
    pure-elisp standalone binary completing a real TLS 1.3 handshake over a real
    socket via FFI.  (Network-gated: skips with no egress.)
  Build fix this needed: the AOT parse/emit recurse over the dispatch IR, and the
  ~nl-ffi-call~ chain at the *bottom* of the ~171~-arm reader dispatch nests deep
  enough to blow the default ~max-lisp-eval-depth~ (~excessive-lisp-nesting~) once
  the FFI table grew — ~nelisp-standalone--compile-to-unit~ now raises it to
  40000.  Known reader-runtime quirk (not an FFI defect): building the SNI C
  string deep inside the handshake's nested ~let*~ can fault; allocating it in the
  outer ~let*~ (as the smoke does) is robust — every FFI call verifies
  individually.  Host ERT still green: dynamic-ELF 11/11, static-linker 64/64,
  target-test 46/46; static reader unaffected.
- 2026-06-27: *D3 (TLS application data) + F3 (glyph bitmap) — on the binary.*
  Added ~gnutls_record_send~/~gnutls_record_recv~ and ~FT_Load_Glyph~/
  ~FT_Render_Glyph~ to the extern table.
  - *D3*: after the D2 handshake, the reader ~gnutls_record_send~s an HTTP/1.1 GET
    (60 bytes encrypted) and ~gnutls_record_recv~s the reply — the first recv
    returns ~GNUTLS_E_AGAIN~ (-28) and a second blocking call delivers 511 bytes
    of decrypted body, whose status line reads ~"HTTP/1.1 200"~.  A pure-elisp
    binary completing a full HTTPS round trip (handshake + encrypted send +
    decrypted recv) over a raw socket via FFI.
  - *F3*: ~FT_Load_Glyph~ + ~FT_Render_Glyph~ rasterise ?A at 48 px; reading the
    glyph slot (~face->glyph~ at FT_FaceRec offset 152, ~slot->bitmap.rows/width~
    at FT_GlyphSlotRec 152/156) yields a 33×35 bitmap — a real rendered glyph,
    demonstrating struct-field marshalling via fixed ABI offsets.
  ~make standalone-reader-ffi-smoke~ now covers F1/F2/F3; ~make
  standalone-reader-tls-smoke~ covers D2+D3.  Host ERT green; static reader
  unaffected.
- 2026-06-27: *F4 (affine glyph transform via FT_Vector / FT_Matrix).*  Added
  ~FT_Set_Transform~.  The reader builds an FT_Matrix (4 FT_Fixed at +0/+8/+16/
  +24) and an FT_Vector delta (2 FT_Pos at +0/+8) in arena buffers and passes both
  by pointer.  With a 2× scale matrix (xx=yy=0x20000) applied before
  Load_Glyph/Render_Glyph, the rasterised ?A grows from 33×35 to 65×70 — exactly
  the transform multistruct path Emacs' font backend uses.  ~make
  standalone-reader-ffi-smoke~ asserts the transformed glyph is strictly larger
  than the untransformed one (self-validating, font-version independent).  Host
  ERT green; static reader unaffected.
- 2026-06-28: *"while+FFI arena exhaustion" was a MISDIAGNOSIS — corrected, real
  cause localized.*  The crash that the D3 note attributed to the bump arena
  filling up under a ~while~ ~record_recv~ retry is NOT arena exhaustion:
  + a pure ~while~ of 2 M iterations only times out (slow, no crash); a ~while~
    doing 200 K ~alloc-bytes~ or 200 K ~nl-ffi-call~s completes; arena use stays
    far under the 256 MiB chunk-0 reservation.
  + ~nl_alloc_bytes~ never invokes the collector and the collector is gated to a
    NO-OP during eval (no GC-frees-live path); the arena grows by *appending*
    disjoint mmap chunks (never relocates), so held pointers stay valid; the
    native stack is 1 GiB and a 120-deep nested ~let*~+FFI form is fine.
  + the SNI-in-inner-~let*~ and tight-FFI-loop shapes earlier blamed now pass
    20/20 and 10/10 on the clean-cache binary.
  Direct fault analysis (strace + gdb) of the shape that *does* still crash — a
  ~while~ bounded retry driving ~gnutls_record_recv~ — shows the SIGSEGV is
  ~si_addr=NULL~ with ~rip~ *inside ~libgnutls.so.30~'s ~.text~* (not reader
  code), after the app-data record arrives.  The byte-identical *unrolled*
  sequential ~record_recv~ completes the same HTTPS round trip (status
  ~"HTTP/1.1 200"~) every time.  So the trigger is reader eval (the loop's
  ~=~/~1+~/~setq~ + arena bumps) executing *between* two ~gnutls_record_recv~
  calls, which leaves GnuTLS dereferencing NULL inside its own record layer —
  consistent with state/memory corruption across the interleaved calls rather
  than any arena/stack limit.  *Shipped form*: ~tls-smoke~ uses the robust
  unrolled recv.  *Open item* (not fixed here — needs a GnuTLS-debug-level repro):
  pin down what reader-side activity between record-layer calls corrupts the
  GnuTLS session; until then, avoid interleaving reader work inside a
  ~record_recv~ retry loop (batch the recvs, or hop out to elisp between whole
  records).  No arena/GC change is warranted.
- 2026-06-28: *f64 (double) FFI through ~nl-ffi-call~ — libm verified on the
  binary.*  The reader FFI surface so far passed every C argument and return as
  an i64 (ints + pointers).  Extended ~nelisp-standalone--build-ffi-dispatch~ so a
  table row can carry a signature plist ~(:args (CLASS ...) :ret CLASS)~ with
  CLASS in {i64,f64}; a row without one keeps the i64-only behaviour (backward
  compatible).
  + *f64 arg*: reads the boxed-float arg's IEEE-754 bits with ~sexp-float-unwrap~,
    binds them to a local, and passes ~(:f64 (bits-to-f64 LOCAL))~ so the SysV
    ABI places it in an XMM register.  (The extern-call f64-arg classifier only
    accepts ~bits-to-f64~ wrapping a *ref*, never a call — hence the let-binding.)
  + *f64 return*: ~(f64-bits (extern-call-f64 SYM ...))~ captures xmm0 as i64
    bits; the result is re-boxed into a Lisp float with the ~sexp-write-float~
    grammar op (tag 62).  NOTE: the bare extern name ~nl_sexp_write_float~ is a
    *generic* call that cannot emit a ~bits-to-f64~ value arg (~:unknown-value-kind~
    at emit); the ~sexp-write-float~ grammar op emits its VALUE as an f64-leaf and
    is the correct form.
  Added a libm row set (~sqrt~/~pow~/~sin~/~cos~/~hypot~, and ~ldexp~ for a mixed
  f64+i64 signature) to the declarative table, which also pulls ~libm.so.6~ into
  DT_NEEDED.  Verified on the 4 MiB dynamic binary (NOT host): ~(= (nl-ffi-call
  "sqrt" 4.0) 2.0)~ => t, and ~pow(2,10)=1024~ / ~ldexp(1.5,3)=12~ (f64+i64) /
  ~hypot(3,4)=5~ all => t; ~(floatp (nl-ffi-call "sqrt" 4.0))~ => t.  (The reader
  top-level printer renders any float — even a literal — as ~#<object>~, so the
  smoke asserts on numeric equality, not the printed form.)  ~make
  standalone-reader-ffi-smoke~ now also covers libm(f64); libc/D1/F1/F2/F3/F4 still
  PASS (no regression).  Static reader unaffected (the extern arms are appended
  only when ~NELISP_READER_DYNAMIC~ is set).
- 2026-06-28: *~defun~ in the reader's native evaluator — fixes the
  ~eval-runtime-image defun~ smoke.*  Root cause (investigated): the native
  evaluator's special-form set (~nelisp-standalone--reader-real-sf-manifest~ +
  the M6 catch/throw patch) implemented only ~if/let/let*/setq/while/progn/quote/
  lambda/function/condition-case/unwind-protect/catch/throw~.  ~defun~ (and other
  definers/macros) were absent and there is no macroexpand fallback, so a runtime
  ~(defun NAME ARGS BODY...)~ fell through to a function call and silently
  signalled ~(void-function defun)~ (rc 0, empty output) — which is why a dumped
  ~(defun image-hot () 99)~ could not define the function and ~eval-runtime-image
  IMG "(image-hot)"~ returned nothing.  Fix: ~nl_sf_defun~ + ~nl_sp_eq_defun~,
  injected into the combiner-cons unit by the same build-time patch that adds
  catch/throw (~--patch-combiner-cons~ / ~--patch-apply-special~; lisp/ stays
  pristine).  Rather than reimplement the mirror function-cell + scratch-vec
  install ABI, ~nl_sf_defun~ SYNTHESISES ~(fset (quote NAME) (lambda . (cdr
  args)))~ in the per-eval arena and evaluates it via ~nelisp_eval_call~, reusing
  the working ~fset~ builtin + ~lambda~ special form; borrowed leaves are cloned
  (~nl_sexp_clone_into~, mirroring ~nl_cons_stash_void_function~), and ~defun~
  returns the NAME symbol (Elisp semantics), not the closure.  Verified on the
  binary: ~(progn (defun add1 (a) (+ a 1)) (add1 41))~ => 42, recursive ~(fib
  12)~ => 144, ~(defun ff () 7)~ => ~ff~, and the ~eval-runtime-image defun~
  command smoke now PASSES.  No regression: if/let/setq/fset and the full
  ffi-smoke (libc + f64 + D1/F1-F4) still PASS.  *Still open* (separate,
  defun-independent): ~make standalone-reader-test~ now advances past the
  defun smoke and exposes a pre-existing ~compile-elisp-artifact~ breakage — that
  subcommand returns rc 0 but writes no output file even for non-defun input
  (~(+ 1 2)~), unrelated to this change.  Other definers/macros (defvar/defconst/
  defalias/when/unless/cond/and/or/prog1/prog2/dolist) remain unimplemented in the
  native evaluator (would each need a handler or a macroexpand fallback).
- 2026-06-28: *~defvar~ / ~defconst~ in the reader's native evaluator.*  Same
  synthesis-and-eval shape as ~defun~ (build a primitive form in the per-eval
  arena, run it through ~nelisp_eval_call~; injected via ~--patch-combiner-cons~
  / ~--patch-apply-special~, lisp/ stays pristine):
  + ~defconst~ -> ~(set (quote NAME) VALUE)~, returns NAME.
  + ~defvar~ with VALUE -> ~(if (boundp (quote NAME)) nil (set (quote NAME)
    VALUE))~ (override-protection: only sets when unbound); without VALUE a pure
    declaration; both return NAME.
  Reuses the working ~set~/~boundp~/~quote~ builtins (no mirror set-value
  scratch-vec ABI).  Verified on the binary: ~(defconst kk (+ 40 2)) kk~ => 42,
  ~(defvar vv 7) vv~ => 7, ~(setq vv 99)(defvar vv 7) vv~ => 99 (no override),
  ~(defvar dd)(boundp 'dd)~ => nil.  No regression: defun, ~eval-runtime-image
  "(image-hot)"~ => 99, and the full ffi-smoke (libc + f64 + D1/F1-F4) still PASS.
  Still missing from the native evaluator: defalias/defmacro/cl-defun and the
  macros when/unless/cond/and/or/prog1/prog2/dolist.

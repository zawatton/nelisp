#+TITLE: Doc 157 — built-in-class type lattice for the bare reader (cl-generic / oclosure substrate)
#+DATE: 2026-06-23
#+STATUS: LATTICE WORKING + OCLOSURE RE-IMPLEMENTED — cl--find-class / cl-type-of /
#+STATUS:   cl--class-allparents + the built-in type hierarchy are live standalone;
#+STATUS:   oclosure-define registers its types; OClosures work on list-closures
#+STATUS:   (cl--generic-isnot-nnm-p verified, §4).  cl-generic FULL dispatch is
#+STATUS:   gated on a reader PERFORMANCE wall: binding the eval-based dispatcher
#+STATUS:   compiler (needs compiled-function-p) makes cl-generic load build ~15-20
#+STATUS:   dispatchers that don't finish in 280s on the interpreter (§5).
#+BRANCH: spike/flat-arena-boot-install

* 0. Why

Doc 156 §4.3 traced the cl-generic =cl--generic-nnm= oclosure gap to "the
built-in-class type lattice", a C-core-coupled subsystem the reader lacked.  Two
consumers need it:
- cl-generic's =cl--generic-typeof-generalizer= computes a value's dispatch
  precedence via =(cl--class-allparents (cl--find-class (cl-type-of VALUE)))=;
- =oclosure-define= needs =(cl--find-class 'closure)= as the base parent of every
  oclosure type (and cl-generic builds =cl--generic-nnm= as an oclosure).

The real =cl-preloaded.el= cannot load standalone: its =cl--class= /
=built-in-class= / =cl-structure-class= structs + =cl--define-built-in-type= +
=cl--struct-register-child= assume the Emacs C-core type infrastructure, so it
loads but registers nothing (=cl--define-built-in-type= ends up unbound).

* 1. Approach — "Path B": a minimal lattice on the reader's own cl-defstruct

Rather than port cl-preloaded's circular bootstrap, re-implement the minimal
lattice on the reader's record-based cl-defstruct.  Two parts:

** 1a. cl-defstruct option support (prelude) — PREREQUISITE, DONE

The lattice's class structs use options the prelude cl-defstruct lacked:
- =:conc-name PREFIX= — cl-structure-class uses =(:conc-name cl--struct-class-)=.
- =:predicate NAME= / =:predicate nil= — custom / suppressed predicate.
Added both (general): the accessor prefix + predicate name are now resolved from
the options (default "NAME-" / =NAME-p=), threaded through
=nelisp-cl-macros--struct-record= so the =setf= accessor-info registry stays in
sync, and predicate emission is gated on a non-nil predicate.  Verified:
=(:conc-name foo--)= → =foo--a=; =(:predicate foo-good-p)= works + no default
=foo-p=; =(:predicate nil)= suppresses; =setf= on conc-name accessors works;
default structs + the 6-package subset unchanged.

** 1b. The lattice itself — =src/cl-preloaded-shim.el= (LOADED, not baked)

A self-contained, standalone-gated file:
- =cl--class= (=:include= target for oclosure--class) + =built-in-class=
  (=:include cl--class=, BOA =built-in-class--make=) via the reader cl-defstruct.
- =cl--class-allparents= — depth-first first-seen merge (SET exact; order ≈
  =merge-ordered-lists=).
- =cl--define-built-in-type NAME PARENTS …= → =(put NAME 'cl--class
  (built-in-class--make NAME nil (mapcar #'cl--find-class PARENTS)))=.
- =cl-type-of= — predicate-based value→type (the reader =type-of= is unreliable).
- the type hierarchy: t / atom / sequence / list / array / number(-or-marker) /
  integer(-or-marker) / fixnum / bignum / float / marker / symbol / boolean /
  null / record / vector / string / cons / function / closure /
  byte-code-function / hash-table / buffer / …

*Must be LOADED, not baked*: the registrations are top-level =(put …)= forms, and
a top-level =put= in the AOT-baked prelude does NOT persist into the boot image
(only definitions do — the same constraint that put cl--find-class's
=cl-simple-setter= registration in cl-lib.el, Doc 156).  Load after cl-lib.el.
General builtins it also needs (=ignore=, =closurep=, =byte-code-function-p=,
=interpreted-function-p=) ARE baked in the prelude (defuns persist).

* 2. Status — WORKING

Verified on =target/nelisp= (load cl-lib + cl-preloaded-shim):
- =(cl-type-of 5)= → =fixnum=, =(cl-type-of "x")= → =string=.
- =(cl--find-class 'integer)= live; =(cl--class-allparents (cl--find-class 'fixnum))=
  → =(fixnum integer number number-or-marker atom t integer-or-marker)= (set exact).
- =(cl--find-class 'closure)= live; after =(load oclosure.el)=,
  =(cl--find-class 'oclosure)= is registered — *oclosure-define now works*.
- No regression: subset / cl-defstruct predicate+setter / setf places all pass.

With the lattice, cl-generic dispatch advances past the type/oclosure wall
through =ignore= → =closurep= to the issue in §4.

* 3. What this unblocks now

cl--find-class / cl-type-of / cl--class-allparents + the type hierarchy are a
reusable substrate: cl-typep-style type checks, oclosure-define/oclosure-lambda
type resolution, and the cl-generic typeof-generalizer's precedence computation
all work.  This is the foundation; only §4 remains for full method dispatch.

* 4. OClosure on list-closures — RE-IMPLEMENTED (=src/oclosure-shim.el=)

oclosure.el implements OClosures on Emacs' *aref-able* closure objects: the type
goes in the docstring slot (=aref 4=), slot values in the constants slot
(=aref 2=), read back with =aref=.  The reader's closure is a =(closure ENV ARGS .
BODY)= LIST and =(aref CLOSURE n)= on it aborts (verified =(aref (lambda (x) x) 0)=
→ abort), so oclosure.el's constructor + accessors don't work standalone.

*Re-implemented* the surface cl-generic needs, on list-closures.  The reader's
lexical env stores captured values as opaque mutable cells (unreadable from
outside), so the TYPE is embedded as a literal marker =(quote (--oclosure-type-- .
TYPE))= = the first BODY form, read back positionally via =(nth 3 closure)=:
- =oclosure-lambda= → =(let* (SLOTBINDS) (lambda ARGS '(--oclosure-type-- . TYPE)
  (ignore SLOTS) BODY))=.
- =oclosure-type= reads the =nth 3= marker; =oclosure--fix-type=/=--copy= are
  identity/no-ops; =oclosure--get=/=--set= are best-effort (slot cells aren't
  externally readable — cl-generic never reads slots off an OClosure: its
  generalizers are cl-defstructs, and =cl--generic-nnm= has no slots).
*Verified*: a =cl--generic-nnm= OClosure funcalls correctly and
=(cl--generic-isnot-nnm-p NNM)= → nil while =(cl--generic-isnot-nnm-p (lambda …))=
→ t — exactly cl-generic's use.  Also baked =always= + =current-load-list= (an
unbound special-var read in =cl-generic-define-method= is an uncatchable abort).

* 5. The final wall — dispatch-COMPILATION is a reader PERFORMANCE limit

With the lattice (§1–3) + the OClosure re-impl (§4), cl-generic gets through method
*expansion*, =cl-generic-ensure-function=, =cl-generic-define-method='s generalizer
bookkeeping, and =cl--generic-isnot-nnm-p=.  =cl-defmethod= then reaches
=(cl--generic-make-function generic)= once a method is registered, which builds the
dispatch function via =cl--generic-get-dispatcher= → =(funcall cl--generic-compiler
BIG-LAMBDA)=.

*Traced the abort (2026-06-23)*: =cl--generic-compiler= was UNBOUND, because its
defvar init =(if (not (compiled-function-p (lambda (x) (+ x 1)))) (lambda (exp)
(eval exp t)) #'byte-compile)= errors when =compiled-function-p= is void — reading
the unbound var is an uncatchable abort.  Defining =compiled-function-p= (→ nil,
correct: the reader has no compiled functions) binds =cl--generic-compiler= to the
EVAL fallback =(lambda (exp) (eval exp t))=.

*But that is a wall, not a fix*: with the compiler bound, =cl-generic.el='s own load
eagerly builds ~15–20 dispatchers (one per internal method =cl-no-applicable-method=
/… plus every =cl--generic-prefill-dispatchers= call), each a =(eval BIG-NESTED-
LAMBDA t)= (make-hash-table + with-memoization + apply) on the slow reader
interpreter.  cl-generic load did NOT complete within *280 s* (=EXIT 124=).  cl-generic
itself notes the eval path is a bootstrap fallback and byte-compilation gives ~5×;
on the reader there is no byte-compiler, so the dispatchers stay interpreted and the
build is impractical.

*Decision*: =compiled-function-p= is intentionally WITHHELD from the prelude (Doc
157 §6 note) so cl-generic stays *loadable* — the compiler stays unbound and
=cl-defmethod= aborts gracefully on first use rather than hanging the whole load.
Full cl-generic method dispatch is therefore gated on a *reader-core performance*
item: the reader gaining compiled functions (byte-compilation), or a much cheaper
dispatcher representation than the prefill-heavy eval build.  Both the type lattice
(§1–3) and the OClosure re-impl (§4) are done and remain useful regardless.

* 6. Files

- =scripts/nelisp-stdlib-prelude.el= (vendor/nelisp): cl-defstruct
  =:conc-name=/=:predicate= (§1a) + baked =ignore=/=always=/=closurep=/
  =byte-code-function-p=/=interpreted-function-p= + =current-load-list= defvar.
- =src/cl-preloaded-shim.el= (nelisp-emacs): the type lattice (§1b).
- =src/oclosure-shim.el= (nelisp-emacs): the reader-native OClosure (§4).

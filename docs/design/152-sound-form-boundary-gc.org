#+TITLE: Doc 152 — Sound form-boundary tracing GC（稼働 GC の有効化と健全性）
#+AUTHOR: nelisp / nelisp-emacs
#+DATE: 2026-06-14
#+STATUS: DESIGN（実装前）。Doc 146（統合設計）の「② root-coverage 健全性」+「稼働化」を、run-suite 完走を gate にして carve out。
#+OPTIONS: toc:3 num:t

* §0 要旨

standalone reader (=target/nelisp=、nelisp-cc 製 static ELF、Rust core 無し) は **現状 GC を一切走らせていない**
（実測 §1）。reuse / form-boundary collect が実際には発火せず、純 bump + chunk-growth で **never-free**。
結果:
1. *メモリ無限増*（実測: 2.4GB 確保で =COLL=0=、drop 参照も未回収、=used=2.44GB=）。
2. *anvil-pkg ERT suite (~13 ファイル + 182 テスト) を nemacs に load すると非決定的に abort*（Doc 142 gate5 系。
   heavy structured load 下で reader が落ちる。GC が無く arena が無秩序成長することが背景）。

本書は **sound な form-boundary tracing mark-sweep を「実際に稼働」させる**設計。既存資産の上に建てる:
- Doc 146: =rc は vestigial（=nl_dealloc_bytes= は no-op、実 freer は GC sweep のみ）→ *tracing が唯一の権威*=。
  pure-tracing は実質既得。残る健全性課題は **② root-coverage gap**（mark が live を取りこぼすと sweep が
  use-after-free）。
- =nelisp-gc-inner.el=: tri-color mark phase (=run-mark-phase=) + sweep phase (§7 per-family free-list rebuild) が
  *実装済*。だが reader の form-boundary には *配線/有効化されていない*（実測 COLL=0）。
- 旧 *epoch boundary-reclaim* は unsound として OFF（§2）。これは「sound tracing reclaim ができたら置換」と
  コメントに明記されている（=scripts/nelisp-standalone-build.el:929-936=）。

*評価 gate*: (a) =COLL>0= かつ heavy-alloc 後も =used= が bounded、(b) 既存 4438 PASS 維持、
(c) **anvil-pkg full suite が nemacs で完走（非決定 abort 消失）**。

* §1 現状（2026-06-14 実測）

** 1.1 GC は走っていない
=(nelisp--arena-stats)= tuple = =(base size bump used live next free COLL REUSE CHUNK-COUNT reserved used)=。
- boot 直後: =chunk=33 COLL=0 REUSE=0 used≈95MB=。
- =(make-string 100)= を ~18万 cons（途中 drop あり）後: =chunk=68 COLL=0 REUSE=0 used=2.44GB reserved=2.75GB=。
- = *COLL=0 のまま*。drop した参照も回収されず =used= が 2.44GB へ。**form-boundary collect / reuse は事実上 OFF**。

** 1.2 allocator 構成（=nelisp-standalone--arena-source=、nelisp-cc が native emit）
- chunked bump: =nl_alloc_bytes= → exact-fit free-list reuse（=nl_freelist_take=）→ 満杯なら =nl_chunk_alloc_new=。
- block header 8B: =BLOCK_TOTAL=（8-align）high bits + mark(0 live/1 marked/2 free) low 3 bits。object = hdr+8。
- =nl_dealloc_bytes= = *no-op*（rc-drop は何も解放しない）。実解放は GC sweep のみ。
- reuse-on は zero-fill 済（=nl_alloc_zero_fill=、Doc 146 §2.1 の reuse-correctness root-cause-fix）= dirty-memory
  問題は解決済。

** 1.3 既存 GC 機構（未配線）
- =nelisp-gc.el=: root-set 列挙のみ（globals 4 HT / vm-stack / aot-frame / aot-pinned-root）。Phase 3c.1。
- =nelisp-gc-inner.el=: tri-color mark（grey queue、=children-of-addr=、color）+ sweep（§7 free-list rebuild）+
  finalizer queue。Doc 30 (Phase 7.3)。
- =nelisp-gc-verify.el= / =-verify-hooks.el=: heap / mark-state-clean / free-list / card-table / poison-from-space
  検証。=NELISP_GC_VERIFY_DEBUG= で有効化。**だが現 standalone build には未収録**（nemacs で env を立てても無反応）。

* §2 なぜ旧 reclaim は unsound だったか（置換対象）

=scripts/nelisp-standalone-build.el:929-936=:
#+begin_quote
Doc 140 boundary reclaim DISABLED (was 1): the escape-epoch reset is unsound … it recycles per-form regions
holding values that escaped to globals via function-call returns / refcount-aliasing clones, a use-after-free.
Re-enable only with a sound tracing-GC reclaim.
#+end_quote

- 旧方式 = *epoch / region reset*: 「この form の epoch で確保した領域は form 終了で garbage」。
- *不健全な理由*: 値は form を *escape* する — 関数戻り値が global へ格納される、clone が aliasing する、等。
  epoch reset はこれらを reachability で追わず領域単位で捨てるため use-after-free。
- = *健全な基盤は唯一 reachability tracing*: 「root から到達可能なものは生存」。allocation epoch は無関係。

* §3 設計目標と不変条件

| # | 不変条件 | 検証手段 |
|---+----------+----------|
| S1 | *Soundness*: reachable な box を絶対に free しない | GC verify poison-from-space + suite/gates |
| S2 | *Completeness*: form 境界で unreachable garbage を全て回収（free-list へ） | arena-stats =used= bounded |
| S3 | *Precision*: root 集合と子 walk が型ごとに完全（取りこぼし=S1 違反） | §5 root-coverage 監査 + verify-heap |
| S4 | *Bounded*: 回収により arena が無秩序成長しない | heavy-alloc 後 =used/reserved= 飽和 |
| S5 | *No-regression*: 4438 PASS 維持、boot 健全 | gates |
| G  | *Gate*: anvil-pkg full suite が nemacs で完走（非決定 abort 消失） | smoke-nelisp-suite on nemacs |

非目標（別 Doc）: peak RSS 削減（Doc 146 ③ moving GC）/ tagged-immediate 値表現（Doc 146 ④）。本書は
*reclaim を健全に稼働させる*ことに集中（容量無限増の停止 + suite 完走）。④/③ は本 GC の上に後続。

* §4 アルゴリズム — form-boundary stop-the-world tri-color mark-sweep

reader driver が *top-level form を 1 つ eval し終えた境界*で、watermark 閾値（前回 GC 以降の bump 量 > T）を
超えていれば collection を走らせる。

** 4.1 Root set（精密・完全）— §5 が要
1. *globals*: NeLisp の 4 global hash table（=nelisp-gc--globals-root=）。
2. *current VM stack(s)*: 実行中 VM state vector（=nelisp-gc--vm-stacks-root=）。
3. *live AOT frame roots*: 実行中 native frame の frame-local root vector スタック（=nelisp-gc--aot-frames-root=）。
4. *AOT pinned roots*: module-lifetime pinned root vectors（=nelisp-gc--aot-pinned-roots-root=）。
5. ***just-evaluated form の結果値（escape root）***: driver が *境界 GC の間、form の戻り値を登録 root スロットに
   保持*する。旧 epoch reclaim はこれを root 化せず捨てて use-after-free を起こした（§2）。**本設計の核心**:
   form result は global 格納前でも *driver が一時 root として pin* → reachable → 生存。
6. *imm-aware*（Doc 146 ④ が入った場合）: root slot が tagged-immediate word を持つなら =nl_val_is_imm= で skip。

** 4.2 Mark（=nelisp-gc-inner-run-mark-phase= を使用）
- root 値を grey queue へ。grey を pop → black、子を grey へ（tri-color）。
- *子 walk を型ごとに完全列挙*（§5）: Cons(car/cdr Sexp slot)、Vector(elems)、Record(fields)、HashTable(k/v)、
  CharTable / BoolVector（Doc 146 ② で明示された取りこぼし候補）、Closure(captured env)、Marker(buffer)、
  Str/MutStr(子無し)。各 box の storage form = 32B Sexp slot を辿る。

** 4.3 Sweep（=nelisp-gc-inner run-sweep-phase= 系 + emitted =nl_gc_sweep_one=）
- 全 chunk を走査（chunk-aware: head→next の chunk descriptor 連鎖、各 chunk の [hdr][obj]… 列）。
- mark1（live）→ mark0 に戻す。mark0（unmarked）→ free-list へ（family-keyed bucket、=nl_gc_free_block=）。
  mark2（既 free）→ skip。
- reuse は zero-fill 済（§1.2）= dirty-memory 健全。

** 4.4 Trigger
- 配線点: reader driver の top-level form 境界（現 epoch reclaim 呼出し位置を置換）。
- 閾値 T（bump-since-last-GC）で amortize。初期 T=64MB（Doc 146 §3.0.5 で 64MB が non-moving floor と実測、
  trigger 頻度の妥当値）。

* §5 Root-coverage 完全性（Doc 146 ②）— 最重要・S1/S3 の核心

mark の root/子 walk に *1 つでも取りこぼし*があれば、live box が unmarked → sweep が free → 次の alloc が
reuse → use-after-free = §1.2 の非決定 abort そのもの。健全化はここが全て。

** 5.1 監査項目
1. *全 root 種別の列挙完全性*: §4.1 の 1-5。特に **(5) escape root**（form 戻り値）と、
   **per-binding env scaffolding vector / scratch slot**（Doc 146 が「scratch-slot 監査」と名指し）。
2. *全 box 型の子 walk*: §4.2。Doc 146 ② が名指しする **CharTable 子 / BoolVector 子**の walk 欠落を最優先で塞ぐ。
3. *AOT frame の safe-point / frame-layout 整合*: =nelisp-gc-inner-collect-roots= が safe-point から root を正しく
   復元するか（vid→addr、=roots-from-safe-point=）。

** 5.2 差分検証（diagnostic-first）
- *GC verify を standalone build に収録*し（§6）、=poison-from-space= を ON にして collection を走らせる:
  free した領域を poison し、以後の touch を検出。anvil-pkg suite load を走らせ、**最初に poison-touch する
  アクセス = 取りこぼした live への参照**を捕捉 → その親型の子 walk / root 種別を §5.1 に追加 → 反復。
- これにより「非決定 abort」を「決定的な verify violation（どの型のどのスロットが未 mark か）」へ変換する。

* §6 検証計画

1. *GC verify を standalone build に入れる*（現状未収録、§1.3）: =nelisp-gc-verify= / =-verify-hooks= を
   reader のビルドモジュール集合に追加し、=NELISP_GC_VERIFY_DEBUG=1= で =verify-heap= /
   =verify-mark-state-clean= / =verify-free-list= / =poison-from-space= を collection 各相に挿す。
2. *単体*: =make standalone-chunk-growth-test=（既 PASS）+ 新 =standalone-gc-soundness-test=
   （alloc→drop→GC→arena-stats で =COLL>0= かつ =used= 減 を assert、escape 値が GC を跨いで正しい を assert）。
3. *回帰*: 既存 4438 ERT。
4. *Gate (G)*: nemacs で anvil-pkg full suite を load+run（=loadrun.el= 系）。
   - GC OFF（現状）: 非決定 abort / =used= 無限増。
   - GC ON（本設計）: 完走 + =COLL>0= + =used= bounded を期待。これが本書の成功判定。
5. *差分*: 同一 suite load で OFF/ON の arena-stats（COLL / used / reserved）を比較。

* §7 段階（各段で 4438 緑 + boot 健全を維持）

- *P1 配線（flag-off 既定）*: gc-inner mark+sweep を form 境界に配線、=collect-enabled= slot で gate。GC verify を
  standalone build に収録。default OFF（現状維持）。検証: build 緑 + verify モジュール load 確認。
- *P2 root-coverage 監査（S1/S3 の核心、§5）*: escape root の driver 配線 + CharTable/BoolVector 子 walk +
  scratch-slot 監査。=poison-from-space= で suite load を回し violation を潰すまで反復。
- *P3 有効化*: watermark T で collection ON。検証: gc-soundness-test + 4438 + **anvil-pkg suite 完走（gate G）** +
  =used= bounded。
- *P4 後続*: 増分/世代別 GC（perf）、Doc 146 ③ moving GC（peak/munmap）、④ tagged-immediate（imm-aware mark は
  §4.1.6 で既に考慮）。

* §8 リスク

| リスク | 影響 | 緩和 |
|--------+------+------|
| root/子 取りこぼし | live free → use-after-free（非決定破壊） | §5.2 poison-from-space で決定化、verify-clean まで P3 を ON にしない |
| escape root 未配線 | form 戻り値 free（旧 epoch と同じ罠） | §4.1.5 を P2 最初に実装 + 専用 test |
| chunk-aware sweep の連鎖誤り | 一部 chunk 未走査 → garbage 残 or 二重 free | verify-heap で全 chunk の [hdr][obj] 整合 assert |
| perf（全 mark / 境界毎） | load 低速化 | watermark T で amortize、P4 で増分化 |
| Doc 146 ④ 値表現との干渉 | imm word を box と誤認 | §4.1.6 imm-aware（=nl_val_is_imm= skip）、④ 導入時に同時更新 |

* §9 関連

- Doc 09 (phase3c-gc) / Doc 29 (phase7.2-allocator) / Doc 30 (phase7.3-gc-inner): 本 GC 機構の出自。
- Doc 140 (chunked-arena) / Doc 148 (xplatform-chunk-reclaim-parity): chunk 構造 + epoch reclaim（置換対象）。
- Doc 145 (transient-arena) / Doc 146 (値表現+GC正当性+moving、本丸): 上位統合。本書 = Doc 146 ②（root-coverage
  健全性）+ 稼働化を run-suite gate で先行実施するサブセット。④（tagged-immediate）/③（moving）は本 GC の後続。
- nelisp-emacs Doc 142 gate5: nemacs での anvil-pkg suite を駆動する側（本 GC の最終 gate G の現場）。

* §10 最初の一手

P1 の前半 = **GC verify を standalone build に収録**（=scripts/nelisp-standalone-build.el= のモジュール集合に
=nelisp-gc-verify= / =-verify-hooks= を追加）し、=NELISP_GC_VERIFY_DEBUG=1= で verify がロードされることを
=make standalone-reader= 後の smoke で確認する。これが P2 の差分検証（poison-from-space で live 取りこぼしを
捕捉）の足場になる。

* §11 P1 実測知見（2026-06-14）と設計訂正

P1 着手で source を精査・実測した結果、§0–§10 のうち *GC 機構の所在*に重要な訂正がある。

** 11.1 GC は native（elisp gc-inner/gc-verify は reader 非使用）
- reader の稼働 GC は =nelisp-standalone--arena-source=（elisp defconst → nelisp-cc が native emit）内の
  =nl_gc_collect= / =nl_gc_mark_*= / =nl_gc_sweep= / =nl_gc_free_block=。driver が top-level form 境界で
  =nl_gc_collect= を呼ぶ（=scripts/...:3228/3252/5634=）。
- =nelisp-gc-inner.el= / =nelisp-gc-verify.el=（§1.3/§6 で「build に収録」と書いた elisp 群）は *reader が使わない別実装*。
  → **§6.1「elisp gc-verify を build に収録」は誤り**。verify は *native 側*（swept block の poison: reuse OFF 時に
  全 payload poison、=scripts/...:732=）で行う。=NELISP_GC_VERIFY_DEBUG= は無関係（実測で nemacs 上無反応）。

** 11.2 collect が走らない真因 = gate でなく trigger/配線（実測）
- =nl_gc_collect= の RECLAIMER GATE = 固定アドレス =268435616=（= 静的 base 0x10000000 + 160）を読み、=1= で no-op。
- arena init は2系統: 静的 =--arena-init-metadata-forms=（base+160 = *1 disabled*）と
  動的 =--arena-init-metadata-forms-dynamic=（Doc 140 Stage 8、runtime mmap base、base+160 = *0 enabled*）。
- *実測*: build-time env で静的 init の base+160 を 0 に flip し rebuild → 3.4GB 確保しても =COLL=0=（collect 不発）。
  = **静的 init は active でない**。active は動的 init（base+160 既に 0=enabled）。*なのに collect が発火しない*。
- = 真因は documented gate ではなく、(a) =nl_gc_collect= の gate 読みが *固定アドレス 268435616* なのに動的 base は
  *runtime mmap アドレス*で不一致の疑い、(b) form 境界の collect 呼出しが trigger 閾値（=268435560= = 16MiB）で
  gate される配線、のいずれか/両方。**enable レバーは documented gate でなく trigger/アドレス整合側**。
- *実用上の reclaim*: 稼働しているのは =nl_gc_collect= でなく **per-eval LIFO scratch reset**
  （nested eval 戻り時、結果が inline immediate (tag<=3) かつ mutation-epoch 不変 = no-escape なら bump を entry
  mark へ reset。=scripts/...:1990+=）。純数値再帰は回収するが、*heap 結果を産む form（cons/string/defun = suite の
  大半）は escape gate で reset されず never-free* → §1 の無限増。

** 11.3 訂正後の P1（正しいレバー）
- *誤*: elisp gc-verify を build に収録。
- *正*: 動的 init 経路で =nl_gc_collect= が form 境界で実発火する配線を有効化する（trigger 閾値 + gate アドレス整合）。
  その上で reuse OFF（native poison）で suite を走らせ、root/mark gap が live を free → poison fault で §5 を決定化。
- ただし *この trigger/アドレス配線は native GC 著者の mental model が要*。本調査で固定アドレス vs runtime base、
  2系統 init、trigger gate の関係を静的読解で繰返し誤読した。確実な enable は rebuild 反復を伴う *専用の native-GC
  集中作業*で、root/mark gap 修正（§5、本丸）と一体。

** 11.4 結論
§5（root-coverage 健全性）が本丸である点は不変で、source コメント
（=「Keep collection disabled until the standalone root/mark gap is fixed」=）が直接裏付ける。だが「まず collect を
診断有効化する」P1 は、*documented gate を 1→0 するだけでは不足*（active な動的 init では既に 0、なお不発）で、
*trigger/アドレス配線*の native 修正が要ると実測で判明。P1 は native-GC 内部に踏み込む集中作業として再スコープし、
§5 の root/mark gap 監査と同一セッションで進めるのが妥当。

** 11.5 *決定的発見（2026-06-14 続行）= native GC は static 固定-base 模型で、現 dynamic-base build に未 wire*
§11.2 の「trigger/アドレス整合」を実機で詰めた結果、根因が確定:
- 稼働 reader で固定アドレスの gate slot を user elisp から読むと **SIGSEGV**:
  =./target/nelisp --load '(ptr-read-u64 268435616 0)'= → EXIT=139。一方 =(nelisp--arena-stats)=（runtime-base
  相対の読み）は正常に tuple を返す。
- = **現 Doc 140 Stage-8 dynamic-base build では固定アドレス領域 0x10000000+offset が unmapped**。arena は
  runtime mmap base に在り、=nl_gc_collect= が読む固定 gate（=268435616= = 静的 base+160）/ trigger（=268435560=）/
  reuse（=268435624=）/ mutation-epoch（=268435544=）は *この build に存在しない*。
- = =nl_gc_collect= と固定アドレス gate 群は *static 固定-base arena 向けに書かれており、Stage-8 dynamic-base へ
  port されていない*。よって collect は dead/未呼出（COLL=0・純 bump）。**§11.2-11.3 で「gate を flip すれば」と
  考えたのは誤りで、そもそも GC 機構全体が現 arena 模型に wire されていない**（私が静的読解で固定アドレスと
  runtime base を混同し続けた根因もこれ）。
- *per-eval LIFO scratch reset* が pure 数値再帰を回収できるのは、それが *runtime-base 相対のカーソル/mark* で
  動くため（固定アドレス mutation-epoch に依存する gate 部分は同様に未 wire の可能性、要確認）。

** 11.6 訂正後のスコープ（sound-GC 実装の本体）
sound-GC を現 reader で稼働させるには、以下を *native-GC 著者の mental model で一体実装*する必要がある（gate flip
ではない）:
1. **GC のアドレス模型を Stage-8 dynamic-base へ port**: gate / trigger / mutation-epoch / collect / mark / sweep の
   全固定アドレス参照を *runtime arena base 相対*へ。chunk descriptor 連鎖（head/next）を sweep が辿る。
2. **driver の form 境界で =nl_gc_collect= を実呼出し**（trigger 閾値を runtime base 相対 cursor で判定）。
3. **§5 root-coverage 完全性**: globals / vm-stack / aot-frame / pinned + escape root + CharTable/BoolVector 子walk +
   scratch-slot。
4. **reuse OFF poison で差分検証**（§5.2）→ root/mark gap を決定化 → 反復 → clean で本有効化（gate G）。
- これは「P1 で verify を入れる」より大きい *GC の dynamic-base 移植 + 健全化* で、Doc 140 Stage-8 の GC 未対応分の
  完了に相当。

** 11.7 *次セッションの一意 resume entry（2026-06-14 確定）*
§5 には 2 面ある: *(静的)* mark 関数の子walk 完全性のコード精査（collect 実発火 不要・独立に可能）と、
*(動的)* poison 差分検証（§5.2、collect 実発火 = §11.6-1 が前提）。よって作業順は:

1. **§5（静的）子walk 監査 ← 次セッションの開始点**。=nelisp-standalone--arena-source= の native mark
   （=nl_gc_mark_roots= / =-mark_slot= / =-mark_block= / =-mark_cons= / =-mark_vec_slots= / =-mark_pool=）の
   *type-tag dispatch を全列挙*し、=nelisp-sexp-layout.el= / =nelisp-heap-types.el= の全 box 型 × 子スロットと
   突き合わせ、*子を walk していない型を gap 表に列挙*（Doc 146 ② = CharTable / BoolVector が筆頭候補）。
   collect 不発・固定アドレス unmapped でも *コード精査だけで進む*。成果 = §5.1 監査表（gap 候補確定）。
2. **§11.6-1 アドレス模型 port**（gate/trigger/epoch/collect/mark/sweep を runtime-base 相対へ）+ §11.6-2
   form 境界 collect 実呼出し → =COLL>0=。
3. **§5（動的）poison 差分検証**（reuse OFF）で 1 の gap 候補を確定 + 取りこぼし追加 → 反復 → clean。
4. 本有効化 → gate G（anvil-pkg suite 完走 / =used= bounded / 4438 PASS）。

= *次セッションは「§5 静的子walk 監査（上記 1）」から*。これは §11.6-1 と独立に着手でき、gap 表が 2-3 の
poison 検証の的を絞る。本 doc を §5.1 監査表で更新しながら進める。

* §11.8 §5.1 静的子walk 監査結果（2026-06-14 実施）

=nl_gc_mark_slot=（=scripts/nelisp-standalone-build.el:1379=）の type-tag dispatch を全列挙し、各 Sexp tag
（=lisp/nelisp-sexp-layout.el= の 0–12）の子walk 完全性を精査した。

| tag | 型 | 子/buffer | mark_slot の処理 | 判定 |
|-----+----+-----------+------------------+------|
| 0–3 | Nil/T/Int/Float | なし(immediate/inline payload) | else→0 | OK |
| 4 | Symbol(String) | name buf のみ | =nl_gc_mark_buf (ptr-read-u64 sp 16)= | OK（value/function/plist は global HT=root 側。box は name のみ） |
| 5 | Str | buf | =nl_gc_mark_buf (sp+16)= | OK |
| 6 | MutStr | buf | box + =nl_gc_mark_buf (box+8)= | OK |
| 7 | Cons | car/cdr | =nl_gc_mark_cons=（box+2子再帰） | OK |
| 8 | Vector | data buf + elem slots | box + buf + =mark_vec_slots(data,0,len)= | OK |
| *9* | *CharTable* | default/parent/entries値 + entries buf | *box block のみ mark* | **GAP-1** |
| *10* | *BoolVector* | bits buffer(別 heap block) | *box block のみ mark* | **GAP-2** |
| 11 | Cell | value word | box + value word(imm-aware 再帰) | OK |
| 12 | Record | type_tag + data buf + field slots | box + (box+0)slot + buf + slots再帰 | OK |

** GAP-1（確定・高確度）: tag 9 CharTable の子未 walk
=nl_gc_mark_slot= の tag-9 arm は =(nl_gc_mark_block (ptr-read-u64 sp 8))= で *box block を mark するだけ*
（コメント「tag 9/10 boxed-no-walk (do not occur)」= char-table 不出現の前提）。char-table get/set source
（=lisp/nelisp-cc-evalport-nonenv-char-table.el=）より box（=sp+8=）の子は:
- =box+32=: *default 値*（32B Sexp slot）
- =box+88=: *parent* char-table box ptr
- =box+64=: entries 配列 ptr、=box+80=: entries_len、stride 40 = =[key:u64@+0][value:32B Sexp slot@+8]=
  → entries buffer block + 各 entry の value slot
*fix*: tag-9 arm を Vector/Record 同様に拡張 — box mark + =nl_gc_mark_slot (box+32)=（default）+ parent box を
mark/再帰 + entries buffer を =nl_gc_mark_buf= + 各 i で =nl_gc_mark_slot (eptr + i*40 + 8)=。
*顕在化条件*: Emacs-compat（syntax/case/category table 等）が char-table を生成すると、default/parent/entry値が
char-table 経由でのみ到達可能な場合に sweep される use-after-free。前提「do not occur」が vendor/Emacs-compat load で破れる。

** GAP-2（確定）: tag 10 BoolVector の bits buffer 未 mark
NlBoolVector box layout（=lisp/nelisp-cc-jit-bool-vector-len.el=）: =box+0= = *value.ptr（Vec<bool> bits buffer、
別 heap block）*、=box+8= cap、=box+16= length、=box+24= rc。tag-10 arm は box block のみ mark し、=box+0= の
*bits buffer を mark しない* → bits buffer が sweep/reuse される。Sexp 子は無い（bits は raw）ので fix は
=nl_gc_mark_buf (ptr-read-u64 box 0)= の 1 edge 追加のみ。

** 結論 + 次手
- 子walk 漏れは *tag 9 CharTable と tag 10 BoolVector の 2 件*（Doc 146 ② の名指しと一致、offset 付きで確定）。
  他 tag（0–8, 11, 12）は子/buffer を walk 済。
- これらは「mark の取りこぼし」= sweep が free する候補。**§11.6-1 で collect 実発火後、reuse OFF poison で
  「char-table/bool-vector を多用する load が poison-touch するか」を動的確認** → 確認できたら上記 fix を適用。
- ただし *root 側*（§4.1 の globals/vm-stack/aot-frame/pinned + escape root）の完全性は本静的監査の範囲外
  （mark は root から辿るので、root 取りこぼしは別途 §11.6-2 の root 列挙監査で。escape root が筆頭）。
- = 静的監査の deliverable（GAP-1/2 の fix-spec）は確定。残りは collect 実発火（§11.6-1）+ poison 動的検証 +
  root 列挙完全性。

* §11.9 root 側 静的監査（§4.1）+ §11.5 訂正（2026-06-14）

** root 列挙は概ね完全（escape root は既に handled）
=nl_gc_mark_roots=（=scripts/...:1518=、引数 ctx/result/out/pool/src/cursor/bsym）が mark する root:
- =ctx+0=（mirror / globals）/ =ctx+32=（frame stack）/ =ctx+64=（unbound marker）
- =result=（current parsed form）/ ***out（in-flight / last result）***/ =pool=（parse-pool RAW buffer, cap slots）
- =src=（source string）/ =cursor=（reader cursor）/ =bsym= + shared symentry（268436328、block + Symbol slot で
  name buf も mark。過去 name-buf が growth chunk で未mark → munmap → SIGSEGV を起こした gap の修正がコメントに
  記録 = root-gap を crash 駆動で塞いだ前例）。
- = **§4.1.5 で「escape root が旧 epoch reclaim の罠」と強調したのは tracing GC には当たらない**: tracing GC は
  =out=（form 結果）を *既に root として mark* している。旧 epoch reclaim（region-reset、reachability を辿らない別
  機構）が escape を取りこぼしたのであって、tracing の root 側は escape を root 化済。**root 列挙に明白な欠落は
  見えない**（frame-stack/pinned の transitive 到達の完全性は動的確認が要るが、root カテゴリは網羅）。

** §11.5「固定アドレス unmapped」の訂正
collect call site（=scripts/...:3224=）は =(< bump@268436184 trigger@268435560)= を *native で読んで* collect を
gate しており、reader は crash せず動く = **native は 0x10000000 領域の固定アドレスを問題なく read している**。
§11.5 の SIGSEGV は =./target/nelisp --load '(ptr-read-u64 268435616 0)'= = *user-elisp の ptr-read-u64* 由来で、
これは arena オブジェクト境界の bounds-check 等で固定 gate アドレスを弾いた可能性が高い（native のアクセスとは別経路）。
= **§11.5/§11.6 の「GC は dynamic-base に未 wire / アドレス模型 port が要る」は早計**。native は固定 gate/trigger を
読めており、COLL=0 の原因は *gate 値（268435616 が 1 か）/ trigger 条件（bump<trigger）/ COLL カウンタの意味* の
いずれかに絞られる。

** COLL=0 の正確な原因は runtime instrumentation で（静的読解は誤診を反復）
静的 base+160 を 0 に build しても COLL=0 だった（§11.2）理由は、(a) 動的 init が別アドレスに gate を書く /
(b) trigger 条件 / (c) COLL カウンタ位置、のどれかだが *静的読解では確定できず繰返し誤診*。
= **§11.6-1 の正しい最初の一手は「アドレス模型 port」ではなく、=nl_gc_collect= と call site に
=nl_write_stdout_bytes= 等の debug print を 1 行差し込んで rebuild し、「collect が呼ばれるか / gate 読みは何か /
trigger 比較の両辺は何か」を実機で観測する instrumentation**。それで COLL=0 の真因（gate 値か trigger か）を確定
してから enable する。GAP-1/2（§11.8、child 側、offset 確定）は enable と独立に fix を用意できる。

** 更新後の作業順
1. *§11.8 GAP-1/2 fix を用意*（CharTable 子walk + BoolVector bits buf、コード確定済・独立）。
2. *collect instrumentation*（debug print）で COLL=0 の真因観測 → gate/trigger を正す → =COLL>0=。
3. *reuse OFF poison* + GAP-1/2 fix 適用済 reader で suite load → 残 gap を動的に潰す。
4. 本有効化 → gate G。

* §11.10 instrument collect 実測 — COLL=0 は測定アーティファクト（2026-06-14 確定）
§11.6-1 の instrumentation を実施し、=COLL=0= の真因を *確定* した。結論: **GC は無効ではない。
有効（GATE=0）で正常に発火・sweep しており、=COLL=0= は「死んだ telemetry slot を読んでいた」だけの
測定アーティファクトだった。** §11.2〜§11.6 の「GC が走らない／gate=1 で無効／アドレス模型 port が要る」
という一連の仮説はすべて誤り。

** 実測手順と数値
- =nelisp--arena-stats= が露出する field を解読: field2=bump / field4=*live-after-last-gc(268435576)* /
  field5=trigger(268435560) / field7=*GATE(268435616)*。
- 起動直後の実測: =GATE=0=（= collect は no-op gate OFF = mark_roots+sweep を実行）、=trigger=352321536=
  (352 MiB)、=bump=9.8 MiB=、=field4(liveGC)=0=。
- string churn N=100..400（=(make-string 1000000 120)= を捨てる top-level form）を sweep:
  *全 N で EXIT=0、bump は 9.8 MiB から増えない*（捨てられた heap 結果は per-eval LIFO reset が完全回収）、
  ただし =trigger= が 352→419 MiB に上昇。trigger の唯一の writer は collect 後再計算（3232/3256/5638）
  なので **collect は発火している**。
- 死んだ slot 268435576 を *collect-call counter* に転用する 1 行を =nl_gc_collect= 入口（gate より前）に挿入し
  rebuild（=make standalone-reader=、6 秒、106 units）。再実測:
  - bootstrap-only: =COLLECT-CALLS=1=（reader 自身の定義 load で trigger を 1 回跨ぐ）。
  - retained growth（=(setq G (cons (make-string 100000 120) G))= ×400, len=400 保持）:
    =COLLECT-CALLS=26=, *EXIT=0（crash なし）*, trigger 395 MiB。
- instrument は revert 済（slot 転用は debug 専用、=git diff= 空に戻し clean rebuild 確認）。

** COLL=0 の真因（確定）
1. *driver も =nl_gc_collect= も slot 268435576（live-after-last-gc / arena-stats field4）を一度も書かない*。
   driver は collect 後に =268435560=(trigger) しか更新しない。よってこのスロットは *永久に 0*。
   過去の「COLL=0」観測はこの advisory スロットを読んでいたため、collect の発火回数と無関係に 0 を返していた。
2. *GATE=268435616 は runtime で 0*。静的 arena-init（901-910）が base+160=1 を書くが、*動的 init（969-974）が
   base+160=0 を上書き*し、Stage-8 runtime base ではこちらが勝つ。よって collect は no-op されず実行される。
3. trigger 比較は =268436184=(chunk-bytes-reserved, 単調増加) vs trigger。collect 後 trigger =
   =max(3*live+1MiB, reserved+16MiB)=。捨て churn では reserved が頭打ち（chunk 再利用）になり trigger に
   到達しなくなる = *steady-state で collect が止まるのは正常*（メモリ圧が無い）。retained 成長では reserved が
   climb し続け collect が 26 回発火した。

** 設計含意の訂正（Doc 146 / Doc 152 への反映）
- **「GC は root/mark gap が直るまで意図的に無効（gate=1）」は誤り**。runtime では GATE=0 で *常時有効・発火中*。
  「never-free で unbounded 増加」も部分的に誤り — LIFO reset + tracing sweep が実際に回収している
  （churn で bump 9.8 MiB 一定、retained でも crash せず 26 collections）。
- *残る本当の課題は「GC が走らない」ではなく root-coverage の **不健全性***: aggressive reuse 下で
  非 root の中間値を collection 跨ぎで保持すると freed block が再配布され corruption（source 698-704 の
  caveat）。これは **GAP-1（CharTable 子walk）/ GAP-2（BoolVector bits buf）/ arena-stats 診断 probe の
  reuse 罠** = §5/§11.8 の対象そのもの。pure churn が全 N で clean な一方、=arena-stats= を form 間に挟むと
  SIGSEGV する（gcprobe2）のはこの診断 probe の reuse caveat であって、collection 自体の crash ではない。
- *run-suite の GC abort（gap b）の再解釈*: 「collect が一度も走らない」ではなく、collect が走った際の
  root/子walk 不足で *まだ到達可能な block を sweep して corruption→SIGSEGV*。よって gate G への道は
  「collect を enable する」ではなく（既に enable）、**GAP-1/2 + root 完全性を埋めて sweep を健全化する**。

** 更新後の作業順（§11.9 を置換）
1. *GAP-1/2 fix を実装*（CharTable 子walk + BoolVector bits buf、§11.8、offset 確定済）。これが gap b の核心。
2. *reuse ON + arena-stats 診断 probe を form 間に挟む再現*（gcprobe2 相当）を回帰ハーネス化し、fix 前=SIGSEGV /
   fix 後=clean を示す（sweep 健全性の動的ゲート）。
3. *swept-block poison（reuse OFF）* で残る root/子walk gap を炙り出し、見つかった分を child/root に追加。
4. anvil-pkg suite を instrumented reader で load 完走（COLLECT-CALLS>0 を確認しつつ）→ gate G。

* §11.11 実 crash (gcprobe2) の局所化 — 診断 probe でなく実アロケータ/GC soundness バグ（2026-06-14）
§11.10 を受け「実 crash の root gap」を追跡。gcprobe2 SIGSEGV を最小再現し、原因の*範囲を確定*した。
GAP-1/2（CharTable/BoolVector 子walk）は **現 reader で tag-9/10 box が一切構築されない**ため到達不能
dead gap と判明（make-char-table/make-bool-vector primitive 無し、tag-9/10 alloc 無し、char-table リテラルは
tag-8 Vector として読まれ既に walk 済、clone .o は eval dispatch 未 wire）。よって子walk fix は実装せず、
*実際に crash する経路*を追った。

** 最小再現と切り分け（全て実機、target/nelisp）
- crash する: =(progn (make-string 1000000 120) nil)= と *list 構築 form* を交互 ×60。
- backtrace: =nl_eval_arg_list_walk= で =mov (%rdi,%rsi,1)= rdi=0x28 = **パース済み arg-list cons の壊れた cdr が ~null を deref**。
- *否定された原因*（重要）:
  - **collect ではない**: gate=1（collect no-op）を各 form で立てても crash（V3）。
  - **reuse ではない**: reuse 閾値を巨大化（reuse OFF）しても crash（V2）。source 698-704 の「reuse が freed block 再配布」帰属は *誤り*。
  - **LIFO reset でもない**: active =nelisp_eval_call= shim（shim-source）は *再帰カウンタのみ*、per-eval reset は
    2026-06-01 に unsound として除外済（コメント 2017-2031）。
  - **arena-stats の telemetry write でもない**: =268436192=/=268436240= 書込みを ablation rebuild しても crash 継続
    （両者は chunk-0 記述子領域内 base+736/+784 で、head chunk の実 cursor は global bump 268435456 = データ未破壊）。
- *条件の絞り込み*:
  - =(list 1 2 3)=（3 cons）交互 → clean。**=(list 1..13)=（13 cons）交互 → crash**。arena-stats が ~13 要素 list を
    返すのと一致。= 「**中規模 list 構築 + 捨て大 alloc churn**」が条件で arena-stats 固有ではない（= 実コードでも起き得る）。
  - 捨て *100KB* + arena-stats → clean。捨て *1MB* → crash。*保持* 1MB（=(setq G (cons …))=）→ clean。
    *裸* 捨て 1MB（progn 無し、結果が escape）→ clean。pure progn-nil-1MB churn（list 無し）→ clean。
  - = crash は「結果が immediate(nil) になる progn 内で ~1MB を churn」＋「別 form で中規模 list を構築」の *組合せ*。

** 1MB make-string の実体（鍵）
=make-string= は単発 1MB alloc ではなく、=mut-str-make-empty=（cap 16）→ =m5_push_repeat=（grow doubling）の
**成長バッファ**で構築（dispatch 2148-2152）。1M 文字 = 16→…→1M の ~16 段成長 = 64KB/128KB/256KB/512KB/1MB の
*中間大バッファを毎回 churn*（旧バッファは即 garbage）。よって 1 form あたり ~3MB の大 alloc churn が起きる。

** 2 つの regime の示唆（未確定、要追加検証）
- collect ON（既定、W4/V5）でも、collect OFF（V3）でも crash する → 単一原因でない可能性:
  (1) collect ON: GC の **root gap**（パース済みフォーム or in-flight =ms= MutStr が非 root に保持された状態で form 境界
      collection が走り、まだ到達可能な cons を sweep → poison/reuse → 後続 eval が壊れた cons を読む）。
  (2) collect OFF: make-string leak で bump 急増 → chunk-0(256MB) 接近 → **chunk-growth 経路**の不具合 or unmapped 接触。
- どちらが run-suite を実際に阻むかは *anvil-pkg suite を reader で直接 load* して確認すべき（synthetic probe の
  micro-bisect でなく）。§11.10 では実 churn/retained で collect は 26 回 clean 発火しており、real workload が
  この pattern を踏むかは未確認。

** 更新後の作業順（§11.10 を更新）
1. *gate-G の直接測定にピボット*: =(progn (make-string ~1MB) nil)= 系の synthetic probe でなく、anvil-pkg suite
   ロード（or 中規模 list 構築 + 大文字列を含む実コード）を reader で走らせ、SIGSEGV/abort が *実際に出るか* 観測。
2. 出る場合のみ regime (1)/(2) を分離: collect ON/OFF 各々で最小再現 → root gap なら mark_roots に in-flight
   pin を追加、chunk-growth なら境界処理を修正。
3. 出ない場合: この crash は synthetic probe（中規模 list + 1MB churn の人工密度）の限界として記録し、
   gate-G は通常ワークロードで成立とみなす。

* §11.12 gate-G 直接測定 — 実 suite は crash、原因は allocator/chunk-growth（2026-06-14）
§11.11 の pivot に従い anvil-pkg full suite を canonical reader（=../nelisp/target/nelisp=、本セッションで
clean rebuild 済）で直接 load（=make smoke-nelisp-suite NELISP=../nelisp/target/nelisp=、内部 =run-suite=）。

** 結果: gate-G は BLOCKED（実ワークロードが crash する）
- =make= の EXIT=0 は recipe が =$$out= で stdout 捕捉し nelisp の exit を握り潰す *誤導*。実際は stderr に
  =Segmentation fault=、stdout の =:suite-run= 結果は空 = **nelisp process が SIGSEGV**。
- gdb backtrace（深さ 84+ frame、実 let/progn/cons ネスト）: =#0 nl_cons_cdr_ptr+182= =mov (%rdi,%rsi,1),%rax=
  rdi=0x7fff96812830 rsi=8 → **dangling/garbage な cons ポインタ（0x7fff… = mmap grown-chunk 領域）の +8 read が
  fault**。§11.11 synthetic repro と *同クラス*（壊れた cons → cons-field read で SIGSEGV）。stack overflow ではない
  （thread stack ~2 MB 使用、深さ ~86 frame）。

** 原因 regime の確定: GC root gap ではなく allocator/chunk-growth
- collect を **disable**（=(ptr-write-u64 268435616 0 1)= を suite eval 先頭に前置、driver は本 slot を reset しない）
  しても suite は **依然 SIGSEGV**（EXIT=139）。= GC は無関係（synthetic V3 と一致、regime 2）。
- dangling ポインタが 0x7fff mmap 領域 = **suite は chunk-0 の 256 MB を超えて chunk-growth している**。
  =nl_os_free_chunk= は no-op（munmap しない）ので chunk は解放されない → 0x7fff… が unmapped に見えるのは
  「grown-chunk の mapped 範囲外を指す garbage ポインタ」か「live cons box が後続 alloc に上書きされ cdr が garbage 化」
  のいずれか = **post-growth の bump/chunk allocation が live cons に重なる**疑い。
- fix locus は GC でも reuse でもなく **bump/chunk-growth allocator**（=nl_alloc_bytes= / =nl_chunk_try_alloc= /
  =nl_chunk_alloc_new= / =nl_chunk_cursor_addr=）。concurrency（actor/thread が並行 alloc → growth race）も要確認
  （=nl_chunk_alloc_new= は非 atomic、CAS は cursor 前進のみ保護）。

** 含意（Doc 152 全体の総括）
- §11.10: 「GC 無効」は誤り、collect は有効・発火・健全（real churn で 26 回 clean）。
- §11.11+§11.12: run-suite の真の blocker は **GC でも GAP-1/2（dead）でもなく、256 MB 超の heavy alloc 下の
  chunk-growth allocator soundness**。Doc 146 の root-coverage/moving-GC 路線とは別問題。
- gate-G への道 = chunk-growth allocator の overlap/race を特定・修正（次フェーズ）。

** 次フェーズ（§11.11 作業順を更新）
1. chunk-growth の最小再現: chunk-0(256MB) を意図的に小さく rebuild → 早期 growth で crash を高速再現。
2. growth 前後の bump/cursor/chunk-base を instrument し、重なる alloc（非単調 obj アドレス）を捕捉。
3. single-thread か concurrent か切り分け（actor 無効で suite が通るか）。
4. 修正 → gate-G（suite 完走 / =:suite-run t=）。

* §11.13 §11.12 訂正 — crash は chunk-growth でない。固定 ~650k-iteration 限界（root cause 未特定）（2026-06-14）
§11.12 の「chunk-growth allocator が原因」は **誤り**。最小化を進めた結果、原因は growth でも memory でもない
ことが判明したので訂正する。

** 最小再現（1 行、heap alloc ゼロ）
=(let ((i 0)(s 0)) (while (< i N) (setq s (+ s 1)) (setq i (1+ i))))= の **純カウントループ**が
N≈650k（=600k OK / 700k crash=、決定論的）で SIGSEGV。cons も make-string も arena-stats も growth も不要。

** 切り分けで否定された原因（全て実機）
- **chunk-growth でない**: =NELISP_LINUX_ARENA_SIZE=1073741824= で chunk-0 を 1 GiB に拡大 rebuild しても
  1M cons / count-loop は crash。256 MiB 超の growth は不要。
- **memory leak / 枯渇でない**: while 直後・*同一 form 内*（境界 collect 前）で読んだ in-form bump は
  N=200k/500k とも =9830312=（9.8 MiB、完全に flat）。ループは heap を一切 leak しない（=+= / =1+= の Int 結果は
  immediate tag 2、heap box 非生成）。
- **GC root gap でない**: 単一 top-level form なので collect は境界で発火せずループ中は走らない。かつ collect
  disable（gate=1）でも crash（§11.12）。
- **reuse でない**: collect off = sweep 無し = free-list 空 = reuse 経路に入らない。なお crash。
- **rec_max でない**: =rec_max= を 300000 → 50000 に下げ rebuild しても閾値は ~650k のまま（250k clean）。
  recursion-counter 束縛なら閾値が ~6 分の 1 に下がるはずだが下がらない = rec_max 非依存。

** crash signature（=nl_cons_cdr_ptr= と =nelisp_mirror_walk_bucket= の両方で出る）
=#0 nelisp_mirror_walk_bucket+238= =mov 0x18(%rdi),%r10= rdi=0 = **関数テーブル（env mirror）の bucket cell の
car（=(KEY.ENTRY) pair）が null** → str-eq 引数 null → null deref。bump flat（mirror は boot 領域・下位アドレス、
ループは上位 bump に何も置かない）なのに mirror が壊れる = **固定回数の eval 後に mirror の bucket セルが null 化**。
（suite では =nl_cons_cdr_ptr= で dangling cons として出る — 同じ「壊れたポインタを read」クラス。）

** 現状: root cause 未特定（次は watchpoint）
heap alloc ゼロ・固定 ~650k iteration で mirror セルが null 化する機構は、上記の主要容疑（GC/growth/reuse/
memory/rec_max）を *全て排除* した上でまだ不明。有力な残り仮説 = **固定サイズの per-eval scratch 領域
（=nl_eit_with_scratch= 等が使う、bump でない固定リング）の cursor が ~650k 使用後に overflow し、隣接する
mirror bucket 配列を上書きして car を null 化**。確定には mirror bucket セル（または scratch 領域）への
**hardware watchpoint** で「誰が null を書くか」を捕捉する必要がある。

** 次フェーズ（§11.12 を置換）
1. =nl_eit_with_scratch= と materialising accessor（=nl_cons_car_ptr= の immediate-slot scratch）が使う
   *固定 scratch 領域*の所在・サイズ・cursor 管理を source で特定。
2. その領域 or 壊れる mirror bucket セルに gdb hardware watchpoint を張り、~650k 回目の eval で null を書く
   命令を捕捉。
3. 修正（scratch cursor の wrap/bound、または該当領域の分離）→ count-loop / suite で回帰確認 → gate-G。

** Doc 152 総括（更新）
- §11.10: GC は有効・健全（real churn 26 回 clean）。「GC 無効」は誤り。
- §11.11/§11.12: GAP-1/2 は dead、chunk-growth 説は §11.13 で撤回。
- §11.13: run-suite の真の blocker = **固定 ~650k-iteration の eval で env-mirror が破壊される core-eval バグ**
  （GC/growth/reuse/memory/rec_max 全て無関係）。root cause 未特定、watchpoint で要追跡。

* §11.14 ROOT CAUSE 確定 — `while` 特殊形式が TCO 無しの CPS 再帰で native stack を溢れさせる（2026-06-14）
§11.13 で未特定だった ~650k-iteration crash の原因を **watchpoint/backtrace で確定**。GC 投資の全謎が決着。

** 確定した root cause
crash 時の deep backtrace に明確な再帰サイクル:
=nl_sf_while_iter → nl_sf_while_loop → nl_sf_while_body → nl_sf_while_body_done →
 nl_sf_while_body_eval → nl_sf_while_body_cdr → nl_sf_while_body → … → nl_sf_while_iter=
= **`while` 特殊形式（=lisp/nelisp-cc-sf-while.el=）が CPS（継続渡し）で実装され、各 guest iteration が
末尾呼び出しの連鎖（~11 defun）で次 iteration に回るが、nelisp-cc が末尾呼び出し除去（TCO）をしないため、
1 iteration ごとに ~11 native frame（~2 KiB）を積む**。
- 650k iteration × ~11 frame ≈ 7M frame → driver の 1 GiB mmap native stack（=[0x7fffb7e00000,0x7ffff7e00000)=,
  line 5131）を溢れさせる。crash 時 =rsp=0x7fffa8728a00= は stack 底（0x7fffb7e00000）より下 = ~1.3 GiB 消費。
- =bump= flat（9.8 MiB）の理由: residue は arena でなく native stack 上。
- =rec_max= 非依存の理由: この CPS 連鎖は =nelisp_eval_call= の =rec_cur= 増分を経由しない（while_iter/loop/body は
  内部 defun で、rec ガードの外）ので rec_max=300000 ガードが発火する前に stack が溢れる。
- crash signature が =nl_cons_cdr_ptr= / =nelisp_mirror_walk_bucket= で出るのは、stack 溢れ後の最初の deref が
  そこに当たるため（症状であって原因でない）。

** これが run-suite blocker
suite 内の長い loop / 再帰を含むテストが同じ機構で 1 GiB stack を溢れさせて SIGSEGV（§11.12 の dangling cons は
同じ stack 溢れの別 manifestation）。GC・chunk-growth・reuse・memory は全て無関係（§11.13 で排除済）。

** 修正方針（2 択）
1. **host-while 書き換え（局所・推奨）**: =nl_sf_while_iter/loop/body= の CPS 再帰を、AOT メタ言語が持つ native
   =(while …)= ループに書き換え、iteration を *反復* で回す（body 各 form を =nelisp_eval_call= で eval、結果 discard）。
   GC sweep/mark の host =(while (and (> hdr 0) …) (… nl_gc_sweep_one …))= が host-while 内 function-call の前例。
   注意点 = 既存 CPS が厳守する「extern-call は arg 位置 0（rsp ≡ 0 mod 16）」を host-while 内 =nelisp_eval_call= /
   =nl_eval_is_truthy= 呼び出しでも満たすこと（nelisp-cc の while+extern-call codegen の alignment 検証が要る）。
2. **nelisp-cc TCO（広範）**: AOT compiler に自己/相互末尾呼び出し除去を入れる。while 以外の CPS 再帰も一括で
   救うが compiler 改修で影響範囲が広い。

** 次フェーズ
- 方針1で =nl_sf_while= を host-iterate に書き換え → count-loop（700k/1M）と anvil-pkg suite で回帰（=:suite-run t=）
  → gate-G 達成。alignment は nelisp-cc の while-body extern-call emit を要確認。

** Doc 152 総括（最終）
- §11.10: GC は有効・健全。「GC 無効」は誤り。
- §11.11/12/13: GAP-1/2 dead、chunk-growth/memory/reuse/rec_max を順に排除。
- **§11.14: run-suite の真の blocker = `while` 特殊形式の TCO 無し CPS 再帰による native stack 溢れ**。
  GC とは完全に無関係。修正は host-while 書き換え（局所）で gate-G に到達可能。

* §11.15 `while` host-iterate fix SHIPPED + suite の残 crash は別バグ（浅い dangling-cons）（2026-06-15）
§11.14 の方針1（host-iterate）を実装し commit。count-loop は完全修正、ただし anvil-pkg suite は *別の*
crash が残ることが判明。§11.12 の「suite = chunk-growth/stack-overflow」枠組みを訂正する。

** `while` fix SHIPPED（commit a0d28544）
=lisp/nelisp-cc-sf-while.el= を host-iterate に書換（public =nl_sf_while= が host =(while (= status 0)
(setq status (nl_sf_while_step args env out 0)))= で回す、step は 1 iteration→status を返し再帰しない）。
- 検証完了: =(while (< i N) (setq s (+ s 1)) (setq i (1+ i)))= で N=700k/1M/1.5M/2M/3M すべて s=N i=N EXIT=0
  （旧は ~650k で SIGSEGV）。correctness（sum0to4=10 / ゼロ反復 body 不実行 / tick 順）も OK。net -18 LOC。
- = while 由来の native stack 溢れ（深い ~1.3 GiB 消費の crash）は解消。

** suite の残 crash は STACK OVERFLOW ではない（§11.12 訂正）
while fix 後も suite は SIGSEGV するが、crash 状態が count-loop と *全く異なる*:
- =rsp=0x7ffff7dfb948= = 1 GiB native stack の TOP から僅か **~18 KiB**、**FRAME_DEPTH=87**（浅い）。
  = stack overflow ではない。count-loop は rsp が stack 底（~1.3 GiB 消費）= 深い溢れ、だった。両者は別物。
- backtrace: =#0 nl_cons_cdr_ptr= ← =#1 nl_sf_let_setup_done= = **`let` の binding cons の cdr を読む際に
  dangling/garbage ポインタを deref**。
- **collect 無効（gate=1）でも crash** = GC root gap でもない。**reuse**（§11.12）でも、**stack overflow** でも、
  **GC** でもない、浅い深さの genuine な memory corruption / dangling-cons bug。
- = §11.12 の「suite crash = chunk-growth allocator」も、その後の「stack overflow」想定も *誤り*。suite の crash は
  独立した浅い dangling-cons バグ。while の stack 溢れ（= count-loop / arena-stats-interleave 再現で見ていたもの）
  とは別系統で、while fix では解消されない。

** 次フェーズ（独立タスク）
suite の浅い dangling-cons crash を別途診断: =nl_sf_let_setup_done= が読む binding cons がどこで dangling 化するか
（collect off でも起きる = allocator が live cons に重なる alloc を返すか、特定 primitive が cons を壊すか）。
gdb watchpoint で当該 cons の car/cdr 破壊点を捕捉する（while の時と同手法）。

** Doc 152 総括（更新）
- §11.10: GC は有効・健全。
- §11.11/12/13: GAP-1/2 dead、chunk-growth/memory/reuse/rec_max 排除。
- §11.14: count-loop blocker = `while` の TCO 無し CPS 再帰 → §11.15 で host-iterate 修正 SHIPPED（検証済）。
- §11.15: suite には *別の* 浅い dangling-cons バグ（=nl_sf_let= 経由、GC/stack/reuse 非該当）が残る。gate-G には
  これの独立修正が要る。

* §11.16 suite crash の精密特定 — `nl_let_setup` 跨ぎで `args` パラメータが clobber される（2026-06-15, focused session A）
§11.15 の「浅い dangling-cons」を watchpoint 手法で更に絞り込み、機構を *特定* した（完全 fix は未了）。

** 確定した crash 機構
=nl_sf_let_setup_done (setup-rc args env out)= が =setup-rc=0= 後に =(nl_cons_cdr_ptr args)= を呼ぶが、その
=args= が **0x7fff96812830（garbage）**。一方:
- =nl_sf_let (args …)= 入口では =args= は *有効*（=(sexp-tag args)= も =(nl_cons_car_ptr args)= も成功している
  = bindings を取得し nl_let_setup を呼べた）。
- 入口と setup_done の間にある呼び出しは **=nl_let_setup= ただ一つ**（=nl_sf_let_got_bindings= が
  =(nl_let_setup bindings env 0)= を呼び、その後 =args= を =setup_done= に渡す）。
= **`nl_let_setup` の extern-call を跨いで、=nl_sf_let_got_bindings= が保持する =args= パラメータが clobber される**。
これは while 書換で踏んだ「nelisp-cc が local/param を call 跨ぎで確実に保存しない」と *同クラス* の codegen 問題
（callee-saved register 非保存 or spill-slot 上書き）。CPS 設計が state を param で thread する前提が、重い
=nl_let_setup=（binding 値 pre-eval + frame push、多数レジスタ使用/深い再帰）で破れている。

** 排除済み（§11.13-15 + 本セッション）
- **stack overflow でない**: rsp は 1GiB stack TOP から ~18KB、FRAME_DEPTH=87（浅い）。
- **chunk-growth でない**: chunk-0 を 1.5GiB にして growth を消しても同一 crash（=args=0x7fff96812830= 不変）。
- **GC root gap でない**: collect 無効でも crash。**reuse でない**: collect off で free-list 空。
- **stack-scratch escape でもない**: 0x7fff96812830 は growth 非依存の固定値で、現 stack（0x7fffb7e..–0x7ffff7e..）
  *より下*＝この run の stack が到達しないアドレス。= 固定 garbage（=nl_let_setup= 後に =args= レジスタ/slot に
  残った値）。

** 次フェーズ（fix）
=nelisp-cc が =nl_sf_let_got_bindings= で =args= をどのレジスタ/spill-slot に置き、=nl_let_setup= の呼び出しで
それが保存されるか= を aot-compiler のレジスタ割当て emit で確認する。fix 候補:
1. nelisp-cc が extern-call 跨ぎで保持する param/local を確実に callee-saved に退避（or 全 live を spill）する
   codegen 修正（while で踏んだのと同根 → これを直すと両方救う）。
2. もし特定の重い extern-call（nl_let_setup 等）だけの問題なら、その呼び出し規約/clobber リストを見直す。
gdb で =nl_sf_let_got_bindings= の =args= 格納場所を特定し、=nl_let_setup= 前後で値が変わる瞬間を watchpoint。

** Doc 152 総括（更新）
- §11.10: GC 健全。§11.11-13: GAP-1/2 dead、growth/memory/reuse/rec_max 排除。
- §11.14: count-loop blocker = `while` TCO 無し CPS → §11.15 host-iterate 修正 SHIPPED（検証済）。
- §11.16: suite blocker = `nl_let_setup` 跨ぎの `args` param clobber（while と同根の nelisp-cc 保存問題）。
  GC/growth/reuse/stack-overflow/scratch-escape は全て排除済。fix は nelisp-cc の call 跨ぎ保存 codegen。

* §11.17 最小化 probe の所見 — corruption は eval/mirror 層に広く、probe 法は信頼性低（2026-06-15）
§11.16 の機構を最小再現で実証しようと run-suite を per-file/per-test marker probe で replicate したが、
*probe 自身が別 crash を誘発* し、minimization は信頼できないと判明。記録する。

** probe の所見
- run-suite を marker 付きで replicate（suite-readiness + 各 file load + 自前 runner）した probe2.el は
  =SMOKE-LOADED= 直後で SIGSEGV。gdb backtrace は run-suite の =nl_sf_let= 経路と *別*: frame21
  =nl_eval_source_all= 直下の **top-level =(setq anvil-pkg-nelisp-ert--tests nil)=**（probe が足した form）が
  =nl_sf_setq → nelisp_mirror_bucket_prepend → nl_val_clone_into → nl_vci_box → nl_sexp_clone_into= で crash。
  = probe の setq 自体が別 corruption を踏んでいる。
- =nelisp--write-stdout-bytes= の marker は SIGSEGV 近傍で ~2 行欠落するため crash 点の特定が ±2 form ぶれる。
- = marker+probe 法は (a) probe-introduced crash (b) marker buffer loss の二重に信頼性が低い。authoritative な
  対象は run-suite EVAL を gdb 下で直接見た §11.16（=nl_sf_let= の args clobber）に限定すべき。

** 示唆: corruption は eval/mirror 層に広い
これまでに観測した crash site: =nl_cons_cdr_ptr=（let bindings, §11.12/16）/ =nelisp_mirror_walk_bucket=
（function lookup, §11.13）/ =nl_sexp_clone_into=（setq mirror insert, §11.17）。いずれも **eval state（args/
binding cons）または env-mirror（function/value table）が壊れた pointer を読む/clone する**点で共通。= 単一の
corruption source（arena/mirror を上書きする何か、or call 跨ぎで pointer を保てない codegen）が複数箇所で表面化
している可能性が高い。§11.16 の「call 跨ぎ param 非保存」codegen 仮説と整合的（mirror clone も内部で多数の
extern-call を跨いで box pointer を保持する）。

** 次フェーズ（fix の本筋）
nelisp-cc の **call（特に extern-call）跨ぎの register/spill 保存 codegen** を =lisp/nelisp-aot-compiler.el= で監査:
- defun の param/local が callee-saved register に置かれた場合、callee（extern 含む）がそれを保存する前提が
  成り立っているか。spill-slot 方式なら deep call で上書きされないか。
- while で踏んだ「local が call 跨ぎで消える」と同根なら、ここを直すと while-class + let-args-clobber +
  mirror-clone を一括で救える可能性。
小さな再現（top-level の重い setq / let を 1 個）を gdb watchpoint で「box pointer がどの命令で garbage 化するか」
捕捉するのが確実。

** Doc 152 総括（更新）
- §11.10-16: GC 健全、while fix SHIPPED、suite blocker = call 跨ぎ pointer 非保存（§11.16）。
- §11.17: minimization probe は信頼性低（probe 由来 crash + marker loss）。corruption は eval/mirror 層に広く
  単一 codegen 根因の疑い。fix の本筋は nelisp-cc の across-call 保存 codegen 監査。

* §11.18 ROOT CAUSE 確定 — suite crash は moving-GC compaction の forwarding バグ（§11.16 訂正、2026-06-15）
hardware watchpoint で corruptor を現行犯逮捕。§11.16 の「across-call で args param が clobber」は *誤り*
（misleading な stale-return-address backtrace に基づいていた）。真因を訂正する。

** watchpoint で捕捉した真の corruptor
crash した cons_cdr_ptr は、入力 cons（=let form =args= = 0x7fffa1facfd8、tag 7 で *有効*）の **cdr word
[cons+8] = 0x7fff96812830（garbage）を読んで box pointer として deref → SIGSEGV**（cons_cdr_ptr +166 で cdr 読み、
+182 で deref）。= cons は有効、その cdr が garbage に上書きされている。
cdr slot（0x7fffa1facfe0）に条件付き hw watchpoint（==0x7fff96812830）を張ると、書込み元は:
=nl_compact_rw_edge ← nl_compact_rw_cons ← … ← nl_compact_rw_vec_slots=（深い再帰 vector walk）
= **GC の moving-compaction（Doc 146 §5、flag 268435608=1 boot init ON）の edge-rewrite**。
=nl_compact_rw_edge= は =new = nl_compact_fwd(old)= を計算し =(ptr-write-u64 addr 0 new)= で cdr を更新するが、
**=nl_compact_fwd= が cdr の relocated アドレスとして garbage 0x7fff96812830 を返している** = forwarding map
（=nl_compact_hash= mask 0xFFFFF + gen ベース、line 1551/1575-1586）のバグ（hash 衝突 / generation staleness /
誤 relocation のいずれか）。old(valid 0x7fffac…) → new(garbage) の誤対応。

** §11.16 がなぜ誤りだったか
=nl_sf_let_setup_done +55= の =(nl_cons_cdr_ptr args)= は args=0x7fffa1facfd8（有効 cons）を渡しており、
breakpoint(rdi==garbage) は発火しない。garbage 0x7fff96812830 は cons_cdr_ptr が *内部で* cdr を読んだ値。
gdb frame dump で setup_done の args slot は有効値を示したのに rdi が garbage に見えたのは、cons_cdr_ptr 内部で
rdi が cdr 値に変わるため。= 「args が clobber」ではなく「有効 cons の cdr が compaction で破壊」が正。

** compaction 無効化で第二の crash が露出
=(ptr-write-u64 268435608 0 0)=（compaction OFF → mark+sweep）を suite eval 先頭に前置すると
=nl_compact_rw_edge= crash は *消える* が、別の crash =nelisp_apply_function ← nl_apply_builtin= が現れる。
= suite blocker は最低 2 層: (1) moving-GC compaction の forwarding 破壊（第一）、(2) compaction OFF 時に出る
apply_function 系の別 crash（sweep/reuse か別バグ、未診断）。

** 排除済み（全セッション）: GC-disabled 想定(§11.10 で否定)/GAP-1/2 dead/chunk-growth(1.5GB でも crash)/
reuse/stack-overflow(浅い)/across-call param clobber(§11.16、本節で訂正)/scratch-escape。

** 次フェーズ（fix）
1. **moving-GC forwarding map を監査**: =nl_compact_fwd / nl_compact_insert / nl_compact_lookup / nl_compact_hash=
   （line 1575-1638）。old→new の誤対応（hash 衝突時の probe、gen 判定、relocation 計算）を特定。
   §11.10 で「peak RSS には moving GC が要る」とされたので、compaction を正すのが本筋。
2. or 暫定: compaction を OFF（268435608=0、boot init line 6979 を 0 に）して mark+sweep に固定し、
   第二の apply_function crash を別途診断 → gate-G。peak RSS は犠牲。
3. small repro（深い vector/list を持つ form を 1 個、collection を強制発火）+ watchpoint で
   nl_compact_fwd の誤返り値を捕捉するのが確実。

** Doc 152 総括（更新）
- §11.10: GC 有効・(simple churn では)健全。§11.14: while fix SHIPPED。
- §11.16→§11.18 訂正: suite blocker = **moving-GC compaction の forwarding 破壊**（across-call ではない）。
  watchpoint で nl_compact_rw_edge を現行犯確認。fix の本筋は nl_compact_fwd forwarding map の監査。

* §11.19 forwarding map 監査結論 — map は健全、真因は moving-GC の root-coverage/consistency（2026-06-15）
§11.18 の compaction バグを forwarding map 監査で深掘り。**forwarding map 自体は構造的に健全**で、真因は
moving-GC が pre-existing な root-coverage gap（Doc 146 ②）を露見させる consistency 問題と判明。

** forwarding map の構造（line 1554-1642、監査結果 = 健全）
- table @ 268436360（2^20 slot × 24B、old@+0/new@+8/gen@+16、24MiB mmap once）。
- =nl_compact_hash= = (old>>4)&0xFFFFF。=nl_compact_insert/lookup_probe= = generation 付き linear probe
  （gen@268436376、compaction ごと ++、stale entry は gen 不一致で empty 扱い）。
- =nl_compact_fwd(old)= = boot は identity / live は to_space_base(268436384)+stored-offset / 0=未 forward。
- =nl_gc_compact=（line 1929）は **完全な moving GC**: phase2(fwd 計算) → to-space chunk 確保 →
  phase3(=nl_compact_rw_roots= で edge 書換) → phase4(=nl_compact_move_chunks= で memmove) →
  phase5/6(old chunk munmap + chunk ptr 更新)。= 「rewrite without move」ではない（§11.18 の一時仮説は否定）。

** 真因（watchpoint + mappings で確定）
corruption write 時の =rax/rdx = 0x7fff96812830=（書く forwarded addr）、その時の mappings は
**0x7fff96600000-0x7fffb7e00000（= to-space chunk）が mapped で 0x7fff96812830 を含む** = *write 時点では valid*。
だが **crash 時（後で setq_done が読む時）には 0x7fff96812830 は unmapped**（deref fault）。
= compaction N が block を to-space-N(0x7fff966…)へ move + cdr を to-space-N アドレスに rewrite したが、
後続の compaction N+1 が to-space-N を **munmap**（phase6）した際、その block を指す **pointer が再 rewrite されず
stale** に残った → dangling → crash。
= moving-GC は move+munmap するため、**in-flight な非 root pointer**（eval 機構が C local / parse pool /
arg list 等に保持する、build-glue が列挙できない box ptr — nelisp_eval_call コメント 2017-2031 の
「escape boxes」）が collection 跨ぎで stale 化する。**mark+sweep は move/munmap しないのでこの gap を mask**
していた（§11.10 で simple churn が clean だった理由）。compaction(moving)が gap を露見させた。

** = Doc 146 ②(root coverage) + ③(moving GC) の問題
forwarding map の bug ではなく、**moving GC の前提（全 live pointer が root から到達 = 再 rewrite される）が
eval 機構の non-root in-flight pointer で破れている**。Doc 146 ② の root-coverage 完備が moving GC の健全性条件。

** gate-G への path
1. **暫定（推奨）: compaction OFF 固定**（268435608 boot init line 6979 を 0 → mark+sweep）。move/munmap しない
   ので root-gap を mask、§11.18 の compaction crash は消える。peak RSS は犠牲（reserved 駆動）。
   → ただし §11.18 の通り **第二の crash（nelisp_apply_function←nl_apply_builtin）が露出**するので、それを
   別途診断・修正する必要がある（次フェーズ）。
2. **本筋: root coverage 完備**（Doc 146 ②）。eval 機構の in-flight box ptr を全て GC root に登録
   （frame/pinned だけでなく parse pool / arg 構築途中 / materialising accessor scratch 等）。完備すれば
   compaction(moving)が健全化し peak RSS も解決。重い（eval 機構全体の root 監査）。

** Doc 152 総括（更新）
- §11.10: GC は mark+sweep では健全（root-gap を mask）。§11.14: while fix SHIPPED。
- §11.16→18→19: suite blocker = **moving-GC compaction が root-coverage gap を露見**（forwarding map は健全、
  across-call でもない）。in-flight 非 root pointer が move+munmap 跨ぎで dangling。
- gate-G path: (1) compaction OFF + 第二 crash 修正（暫定） or (2) root coverage 完備（本筋、Doc 146 ②）。

* §11.20 第二の crash 診断 — compaction OFF も同じ root-coverage gap、escape hatch は gate-G に効かない（2026-06-15）
ユーザ指示で compaction を OFF 固定（boot init flag 268435608=0 → mark+sweep）して rebuild し、§11.18 で予告した
「第二の crash」を診断。結論: **compaction OFF は gate-G への道ではない**（root cause が同一のため）。

** 第二の crash（mark+sweep, collect ON）
=nl_kw_is_keyword+122= =movzbq (%rdi),%rax= rdi=0（**NULL deref**）。
backtrace: =nl_eval_inner ← nelisp_eval_call ← nl_sf_progn_body_step ←=（progn body 評価、深さ ~14）。
= **progn body の form が NULL**（cons の car が NULL 化）。eval が NULL form を keyword 判定しようとして crash。

** collection 起因 = §11.18 と同じ root-coverage gap（manifestation 違い）
collect を完全 OFF（268435616=1）すると crash が *別箇所*（=nelisp_apply_function=、leak/exhaustion 系）に
変わる → =nl_kw_is_keyword(NULL)= crash は **collection 起因**と確定。
= mark+sweep の collection が form 境界で発火した際、eval 機構が in-flight（非 root）に保持する still-referenced
な cons を **free + reuse/blank** し、progn body の form を NULL 化している。compaction の move+munmap（§11.18）と
**同一の root-coverage gap（Doc 146 §2）の別 manifestation**:
- compaction: 参照中 block を move + 旧 to-space を munmap → dangling forwarded edge。
- mark+sweep: 参照中 block を free + reuse/blank → NULL/garbage な form。

** = escape hatch（compaction OFF）は無効、かつ net-negative
- suite は mark+sweep でも crash する（gap を踏む経路が変わるだけ）。
- compaction は vendor load で健全（comment: -53% peak RSS, 20/20 soak deterministic）。OFF にすると suite を
  直さず vendor load の RSS を退行させる。→ **flag は 1（compaction ON）に戻した**（コメントで知見を記録）。
- collect 完全 OFF も不可（reclamation 無し → leak → exhaustion/別 crash）。
= **どの GC mode でも suite は通らない。gate-G は root coverage 完備（Doc 146 §2）が必須**で、GC-mode の
  escape-hatch による shortcut は無い。

** gate-G への道（確定）
**Doc 146 §2 root coverage 完備**のみ: eval 機構が collection 跨ぎで保持する in-flight box ptr
（parse pool / arg 構築途中 / materialising-accessor scratch / nelisp_eval_call の escape boxes、コメント
2017-2031）を *全て GC root に登録*する。完備すれば compaction(move+munmap)も mark+sweep(free+reuse)も健全化し、
suite が通る。重い（eval 機構全体の root 監査 + root 登録機構）。

** Doc 152 総括（更新）
- §11.10/14: GC は simple churn では健全（root-gap を踏まない）、while fix SHIPPED。
- §11.16→20: suite blocker = **root-coverage gap（Doc 146 §2）**。compaction(§11.18)も mark+sweep(§11.20)も
  同 gap の別 manifestation。forwarding map(§11.19)も across-call(§11.16 訂正)も無罪。
- gate-G = root coverage 完備（唯一の道）。GC-mode 切替の escape hatch は効かない。

* §11.21 Doc 146 §2 root coverage 実装 — conservative native-stack scan で suite GC crash 解消（2026-06-15）
Doc 146 §2 の root-coverage gap を **conservative native-stack scanning** で閉じ、anvil-pkg suite の GC crash
（§11.18-20）を解消した。suite-readiness が SIGSEGV せず clean に完走するようになった。

** 実装（scripts/nelisp-standalone-build.el、+45/-3 行、Rust 無改変）
- *rsp 取得*: 既存 IR op =(aot-current-sp)=（line 4841/15420、=mov rax,rsp= を emit）。
- *stack-top 捕捉*: =driver= の let* で arena-init 直後に =(ptr-write-u64 268436456 0 (aot-current-sp))=
  （driver-entry rsp = mmap 1GiB stack の top）。arena-init 前だと slot 未 map で fault するので *直後*。
- *scan*: =nl_gc_conserv_word=（8-aligned + =nl_gc_in_arena=（deref 無し range 判定）+ tag<13 → =nl_gc_mark_slot=）/
  =nl_gc_conserv_scan=（host while で [rsp, top) を 8byte 毎走査）/ =nl_gc_conserv_maybe=（SCAN_FLAG@268436464 gate）。
  =nl_gc_mark_roots= の先頭で =nl_gc_conserv_maybe= を呼ぶ。
- *ADDITIVE で sound*: 走査は live native stack の word を「arena obj ptr らしき」なら mark+recurse するだけ
  = 解放を *減らす* 方向のみ。偽陽性は bounded over-retention（mark_slot は in-arena guard + idempotent）。
  eval 機構が C-stack に持つ in-flight 非 root box ptr（build-glue 列挙不可の escape boxes）を一括 root 化。

** 結果（検証済）
- suite（=make smoke-nelisp-suite=）: **EXIT=0、決定論的 ×3**、=(:suite-run nil :suite-ready nil
  :suite-blocked-by (native-async-lower-primitive native-text-http-lower-primitive) …)= を clean に返す。
  = §11.18-20 の SIGSEGV（compaction の nl_compact_rw_edge / mark+sweep の nl_kw_is_keyword(NULL)）は *消えた*。
- =(+ 40 2)=→42、小〜中ループ（≤100k）正常。

** config: scan ON + compaction OFF（boot init）
- SCAN_FLAG@268436464=1、compact@268435608=0。conservative scan は mark+sweep で sound（move/munmap しない）。
- moving GC（compaction）は root-coverage gap で *unsound*（§11.18-19、conservatively-found block を move して
  C-stack ptr を書換えられない）。よって scan ON なら compaction は OFF。peak RSS は犠牲（compaction の -53% を失う）。
- *FUTURE*: conservatively-found block を *pin*（move 対象外）にすれば compaction を再有効化でき RSS も回復。

** 残る gate-G blocker（root coverage とは別）
1. **count-loop s=0（pre-existing, orthogonal）**: =(while (< i N) (setq s (+ s 1)) (setq i (1+ i)))= が
   N≥~200k で s=0 i=0 を返す（N≤100k は正常）。**canonical（efdc2a29、本変更前）でも再現** = 本 root-coverage
   実装とは無関係の別バグ。conservative scan でも直らず（非 collection 経路）、非 crash（結果のみ誤り）。閾値が
   16MB GC trigger 近辺なのが示唆的。要 separate 調査（while host-iterate or frame/let の高 N 挙動）。
2. **missing primitives**: =native-async-lower-primitive= / =native-text-http-lower-primitive=（Doc 44 の別
   workstream）。suite-ready=nil の直接原因で、これが無いと ERT 本体は走らない。

** Doc 152 総括（更新）
- §11.10/14: GC は mark+sweep で健全（root-gap mask）、while stack-overflow fix SHIPPED。
- §11.16→20: suite blocker = root-coverage gap（compaction/sweep 両 manifestation）。
- **§11.21: root coverage を conservative stack scan で実装、suite GC crash 解消（SHIPPED）**。
  gate-G の残りは (1) 高 N count-loop 正当性バグ（別）+ (2) async/http primitive（別）。

* §11.22 count-loop s=0 診断 — allocation が let-frame の binding cell を破壊（ASLR 依存、2026-06-15）
§11.21 で flag した「高 N count-loop が s=0」を診断。結論: **eval の transient allocation が、同じ form 内で生きている
let-frame の binding 値（i/s）を破壊する** layout 依存バグ。root coverage（§11.21）とは独立。

** 症状と決定論的 repro
- =(let ((i 0)(s 0)) (while (< i N) (setq s (+ s 1)) (setq i (1+ i))) <read i s>)= が **s=0 i=0** を返す。
- *ASLR ON*（既定）: 非決定論的（N≈100k 超で時々 s=0、再実行で s=N のことも）。
- *ASLR OFF*（=setarch $(uname -m) -R=）: **決定論的に s=0**（N=10k でも）。= 決定論的 repro 確立。
- loop 自体は走って*正常終了*する（i は内部的に N まで増えて =(< i N)= が false になり抜ける）。

** 機構（周期 print が暴いた決定的証拠）
loop body に =(if (= (mod i 50000) 0) (write (concat "@" (format "%d" i) " s=" (format "%d" s))))= を挟むと、
periodic 点で **=@0 s=0=** を出力する:
- =(mod i 50000)= は *real i* を読む（50000 の倍数で if が発火＝real i が見えている）。
- *その直後*の =(format "%d" i)= は **i=0** を読む（同一 iteration 内）。
= mod と format の *間*にある =concat=/=format= の **allocation が i（と s）の binding を 0 に破壊**している。
plain loop では =(+ s 1)=/=(1+ i)= の評価が作る transient arg-list cons の allocation が同様に i/s を破壊し、
末尾の read で 0 になる。

** = allocation が「生きている let-frame binding cell」を上書き/再利用
単一 top-level form なので mid-form collect は走らない（form 境界のみ）→ free 由来ではない。よって
*allocator が、まだ生きている let-frame の binding 格納域に重なる/再利用するアドレスを返している*（bump 非単調 or
per-eval scratch の binding cell 再利用）。ASLR が arena/mmap layout を変えるため、重なるか否かが run 依存
（setarch -R の固定 layout では必ず重なる）。Doc 146 の「長い単一 form は transient を回収しない（per-eval reset
無効）」territory と関連。conservative scan（§11.21）は free を防ぐだけで *overwrite* は防げない → 直らない（実測）。

** root coverage（§11.21）との独立性
- §11.21 の scan ON + compaction OFF でも count-loop s=0 は残る（実測）。
- canonical（efdc2a29、§11.21 前）でも再現。= 本バグは GC collection とは別経路（allocation 時の binding overwrite）。

** 次フェーズ（fix / 更なる診断）
=setarch -R= の決定論的 repro 上で、gdb で let-frame の i/s binding cell アドレスを特定（=nl_sf_let=/=nl_let_setup=
が frame を push し binding を置く所で break）→ その cell に hardware watchpoint → loop 中に *どの allocation が
0/garbage を書くか*捕捉。修正は allocator が live binding cell に重ならないこと（bump 単調性 / per-eval scratch の
binding 保護）を保証する。

** Doc 152 総括（更新）
- §11.21: root coverage 実装（conservative scan）で suite GC crash 解消（SHIPPED）。
- §11.22: 残 blocker の count-loop s=0 を診断 = allocation が live let-frame binding を破壊（ASLR 依存、setarch -R
  で決定論的 repro）。root coverage とは独立、非 crash の結果破壊。次は gdb watchpoint で overwrite 元を特定。

* §11.23 count-loop s=0 再診断（§11.22 訂正）— format が integer arg を 0 と読む（value Sexp 破壊）
gdb watchpoint 調査の過程で §11.22 の「allocation が let-frame binding cell を破壊」が**誤り**と判明。真相は
**`(format "%d" X)` が長い loop 後に X 問わず "0" を返す = format が読む integer arg の値 Sexp が破壊されている**。

** 決定的な切り分け（全て setarch -R 決定論 repro）
- =(let ((i 0)(s 0)) (while (< i 20000) (setq s (+ s 1)) (setq i (1+ i))) <X>)= で:
  - =(if (= s 20000) "S-CORRECT" …)=（**文字列 alloc 無し**）→ **S-CORRECT** = s の binding は *正しい*（cell 無傷）。
  - =(format "%d" s)= → "0"。 =(format "%d" 20000)=（**literal**, 同 loop 後）→ **"0"**。 =(format "%d" (+ s 0))= → "0"。
  - = s 固有でも binding でもない。**format が読む integer arg の値が 0**。
- format の literal 大値（loop *無し*）は正常: =(format "%d" 20000/50000/1000000)= → 正しい。=number-to-string= も正しい。
  = format/value-encoding 自体は健全。**長い loop 後に**だけ壊れる。
- 機構: format %d = =(m5_push_dec ms (ptr-read-u64 (m5_fmt_argptr argp) 8))=。出力 "0"（空でなく）= m5_push_dec が
  **v=0** を受領 = =(ptr-read-u64 arg 8)= が 0 = **format arg の値 Sexp の +8 が 0 に破壊**されている。
- = §11.22 の「binding cell」は誤り（=(= s N)= が正しいことが反証）。実体は *format が読む live value Sexp の破壊*。

** 位置づけ（§11.18-22 の theme と一致）
単一の長い top-level form は mid-form collect も per-eval reset も無く transient garbage を回収しない（Doc 146 の
disabled per-eval reset）。~16k iter（≈16MB GC trigger 近辺）で蓄積した transient が **live な value Sexp（format の
integer arg）を上書き**する。threshold が 16MB trigger 近辺なのは依然示唆的（trigger 跨ぎ時の allocator 挙動 or
chunk/freelist 相互作用の疑い）。ASLR で layout が変わり overlap するか否かが run 依存（setarch -R で決定論的）。
conservative scan（§11.21）は free を防ぐだけで *overwrite* は防げないため直らない（実測一致）。

** 次フェーズ
setarch -R 決定論 repro 上で、format の integer arg の値 Sexp（=m5_fmt_argptr= が返す arg の +8）に hardware
watchpoint を張り、loop 中/format 直前に *どの allocation/命令が +8 を 0 に書くか* 捕捉する。m5_push_dec/udec は
inline 化されており break 不可なので、=m5_fmt_loop= の %d 分岐 or arg-list 構築点で arg Sexp アドレスを取得して
watchpoint する。fix = transient garbage が live Sexp に重ならない保証（bump 単調性 / per-eval transient 回収）。

** Doc 152 総括（更新）
- §11.21: root coverage（conservative scan）で suite GC crash 解消（SHIPPED）。
- §11.22→§11.23 訂正: count-loop s=0 = binding cell でなく **format が読む integer value Sexp の transient-garbage
  破壊**（=(= s N)= は正しい）。setarch -R 決定論 repro、~16MB threshold。root coverage とは独立、非 crash。
  次は format arg の値 Sexp に watchpoint で overwrite 元特定。

* §11.24 count-loop watchpoint hunt の所見 — slippery heisenbug、root は in-form transient 非回収（2026-06-15）
§11.23 の watchpoint 計画を gdb で実行。結論: **正確な overwrite 命令の固定は困難**（probe ごとに manifestation が
変わる slippery heisenbug）。ただし root は §11.18-23 と一貫: *単一の長い form が transient garbage を回収しない*。

** gdb 調査（setarch -R + gdb disable-randomization off、決定論的 layout）
- =m5_fmt_loop= entry で =nl_cons_car_ptr(argp)= の値（format の integer arg）= **20000（正しい！）**。
  = §11.23 の「value Sexp が entry 時点で 0 に破壊」も不正確 — entry では値は無事。corruption は format の
  *processing 中*（entry 後）に起きる。
- =m5_push_dec= の call site（m5_fmt_loop +496/+1584）に break しても発火せず = 失敗ケースの format は
  その %d→m5_push_dec path を通っていない（fmt string か dispatch の corruption の疑い）。
- =m5_push_dec= を symbol break + boot hit を ignore した run では、今度は **=nl_kw_is_keyword= で別 SIGSEGV**
  （§11.20 と同型の NULL deref）。= 同じ workload が probe の layout 変化で *s=0 / format="0" / nl_kw_is_keyword
  crash* と異なる症状を出す = 典型的な memory corruption の heisenbug。

** = 正確な byte を gdb で固定するのは非生産的
manifestation が probe（breakpoint / inferior call / ignore count）ごとの微小な layout 変化で動くため、
watchpoint で「この命令が overwrite」と一点に固定できない。corruption する live data も form ごとに変わる
（format の場合は fmt path、=(= s N)= では無害）。

** root（確定、§11.18-24 で一貫）
*単一の長い top-level form は mid-form collect も per-eval reset も無く transient garbage を一切回収しない*
（Doc 146 の disabled per-eval reset）。~16k iter（≈16MB GC trigger 近辺）で蓄積した transient が live arena data
（parsed-form 定数 / value Sexp / eval state / format path）を上書きし、layout 依存で様々に表面化する。
ASLR で overlap 有無が run 依存（setarch -R で決定論的）。conservative scan（§11.21）は free を防ぐだけで
*overwrite* は防げず直らない（実測一致）。

** 推奨: byte-chasing から root fix へピボット
個別の overwrite 命令を追うより、root（in-form transient 非回収）を直すべき:
1. **in-form 回収**: 長い form 内で *allocation-triggered collection*（form 境界だけでなく bump が trigger を
   跨いだ時点で collect）を可能にする。ただし mid-eval collect は root coverage 完備が前提（§11.21 の
   conservative scan + frame/eval state の root 化）→ §11.21 を mid-eval でも有効化する方向。
2. or **sound per-eval reset 再有効化**（Doc 146 の disabled reset を、escape 解析 or conservative pin で safe 化）。
3. 検証: 長い loop を複数 top-level form に分割（form 境界で collect → transient 回収）すると corruption が
   消えるはず → root の確認 + 暫定回避策（プログラム側の構造）。

** Doc 152 総括（更新）
- §11.21: suite GC crash 解消（conservative scan、SHIPPED）。
- §11.22→23→24: count-loop s=0 = 単一長 form の transient 非回収による live data overwrite（slippery heisenbug、
  probe で manifestation が動く）。binding cell（§11.22）も value-Sexp-at-entry（§11.23）も不正確。
  root は in-form transient 非回収。fix は byte-chasing でなく in-form 回収（mid-eval collect + root 完備）or
  sound per-eval reset。

** 検証結果（root 確定、2026-06-15）
§11.24 の推奨検証を実施し root を *確定*:
- *A* 単一 form 1M iters（setarch -R）→ **s=0**（corrupted）。
- *B* 10 top-level form × 100k に分割し global G に累積（form 境界で collect 介在）→ **G=1000000（正しい）**。
= form 分割で transient が form 境界 collect により回収され corruption が *消える* → **root = 単一長 form の
in-form transient 非回収**が確定。fix = in-form 回収（mid-eval collect + root 完備）or sound per-eval reset。
暫定回避 = 長い単一 form を避ける（複数 form に分割 / 実コードでは通常そうなっている）。

* §11.25 in-form 回収の実装試行 — mid-eval collect は unsound（crash）、revert（2026-06-15）
§11.24 の推奨に従い in-form 回収を実装したが、**mid-eval collect が crash する**ことが判明し revert した。
正直な結果として記録する。

** 実装内容（試行、revert 済）
- =nl_gc_stash_roots(ctx result out pool src cursor bsym)=: 7 root を block に格納し @268436472 に stash。
  3 つの eval loop（=nl_eval_source_all= / =bf_load_eval_loop= / =bf_eval_source_string_loop=）の冒頭で呼ぶ。
- =nl_gc_collect_self=: stash した roots で =nl_gc_collect= を呼ぶ。
- =nl_gc_maybe_collect_self=: bump（268435456）が閾値（blk+56、初期 12 MiB）を超えたら collect_self + 再 arm。
  注: gate は当初 268436184（growth-chunk-reserved）だったが、sub-256 MiB の loop は growth せず 0 のままで
  発火しないと判明 → bump-based に変更。
- =nl_sf_while= の host while で毎 iteration =(extern-call nl_gc_maybe_collect_self)= を呼ぶ
  （iteration 境界 = body eval 完了で clean point の想定）。

** 結果: UNSOUND（crash）
build は通るが、発火すると crash:
- count-loop（setarch -R, 1M）: =nl_gc_mark_slot ← nl_gc_mark_roots ← nl_gc_collect ← nl_gc_collect_self
  ← nl_gc_maybe_collect_self ← nl_sf_while= で **SIGSEGV**。= mid-while collect が mark 中に bad data を辿った。
- =(+ 40 2)= boot smoke すら **SIGSEGV**（stash/boundary 経路 or build 全体の破壊）。
= **mid-eval collect は §11.21 の conservative scan + stashed roots だけでは root coverage が不完全**で unsound。
§11.21/Doc 146 §2 の「mid-eval/moving collect は *完全な* root coverage が前提」という caveat が成立。
mid-while の in-flight state（materialising-accessor scratch / 評価途中の box ptr / register 上の root 等）に
collect が見落とす live pointer があり、それを sweep して mark_slot が dangling を辿る。

** revert + 現状
変更を全 revert（=git checkout=）し §11.21 の健全状態に復帰: boot =(+ 40 2)=→42、suite EXIT=0
（=:suite-run nil :suite-ready nil=）。count-loop s=0 は残る（非 crash の既知 issue、§11.22-24）。

** 次フェーズ（fix の本筋）
in-form collect を sound にするには、まず **mid-eval root coverage を完備**する必要がある（§11.21 の conservative
stack scan を register まで拡張 / materialising-accessor scratch を root 化 / 評価途中の全 box ptr を shadow stack
に積む）。これは Doc 146 §2 の hard prerequisite そのもので、相当の作業。あるいは **sound per-eval reset**
（Doc 146 で disabled、escape 解析で safe 化）。どちらも大きい。count-loop s=0 は実コードでは長い単一 form を
避ければ顕在化しない（§11.24 で分割は正常と確認済）ので、優先度は gate-G の残り（async/http primitive）より低い。

** Doc 152 総括（更新）
- §11.21: suite GC crash 解消（conservative scan、SHIPPED、現状の健全 baseline）。
- §11.22-24: count-loop s=0 = 単一長 form の transient 非回収（heisenbug、root 確認済）。
- §11.25: in-form collect 試行 → mid-eval collect は unsound（crash）→ revert。fix は mid-eval root coverage 完備
  （hard）or sound per-eval reset。count-loop は長 form 回避で実害なし、優先度低。

* §11.26 async/http primitive 実装 — anvil-pkg suite readiness gate を GREEN 化（gate-G readiness blocker 解消）

** 背景: gate-G の readiness blocker
gate-G = anvil-pkg ERT suite が standalone NeLisp で完走すること。suite は実行前に
=anvil-pkg-nelisp-smoke-suite-readiness= で readiness を gate する。標準 reader では:
=:suite-ready nil :suite-blocked-by (native-async-lower-primitive native-text-http-lower-primitive)=。
- =native-async-lower-primitive-p= (NeLisp 枝) = =(and (fboundp 'nelisp-make-process) (fboundp 'make-process))=。
  =nelisp-make-process= は有るが =make-process= が無い。
- =native-text-http-lower-primitive-p= = =(and (or nelisp-http-get …) (or url-retrieve-synchronously
  (curl-process-lower-primitive-p)))=。=nelisp-http-get= は有るが curl 経路が =executable-find= 不在で nil。
- honesty probe =:nelisp-call-process-executes= も nil（下層が実行されない）。

** 根本: reader は同期 native process builtin を持つが Emacs-compat 面が無い
標準 reader（nelisp-cc AOT、Rust 無し）は以下を **runtime builtin として expose 済**（検証）:
- =syscall-direct=（raw 6-arg syscall）, =ptr-read/write-u64=, =alloc-bytes=, =atomic-fetch-add=,
  =fork-spawn=（fork(2)）。なお =ptr-read/write-u8= は AOT-only（runtime で呼ぶと abort）。
- =nelisp-process-call-process= / =nelisp-process-start=（=nl_bi_process_*= + =nl_os_process_*=、
  fork(2)+execve(2)+wait4(2) を syscall-direct で行う **同期** spawn）。直接呼ぶと実プロセスを spawn・
  stdout を *ファイル* に redirect・正しい exit code を返す（marker test / curl path test で実証）。
問題は純粋に **routing/surface**: elisp の =nelisp-make-process= は host =make-process= に委譲（line 372
=(apply #'make-process …)=）、=nelisp-call-process= は不在の =nelisp-sys= syscall substrate を仮定。
両者とも reader 上では走らない。

** 実装: =packages/nelisp-process/src/nelisp-process.el= に guarded reader adapter（§11.26）
guard =(and (fboundp 'nelisp-process-call-process) (not (fboundp 'make-process)))= で reader だけに適用
（Emacs / Rust-runtime 経路は不変、dual-target「NeLisp 利用時は native 低層へ delegate」）。
- =executable-find= / =nelisp-sys-executable-find=: 標準 PATH dir（/usr/bin 等）を =test -x= で走査し
  **絶対 path** を返す（exit-status のみ。reader に =getenv= も elisp-level file reader も無いため PATH split
  と =command -v= 出力の読み戻しは不可）。native execve は PATH 検索しないので絶対 path 必須。
- =nelisp-call-process=: 動く native 同期 builtin へ直 delegate（program を絶対 path に解決、string/(:file)
  destination は stdout をファイル捕捉）。
- =make-process=: native 同期 spawn で実プロセスを実行 → sentinel を同期発火 → already-exited な軽量
  process object を返す。reader に async eventloop が無いため真の async ではなく、stdout を FILTER で配送
  できない（file-read 制約）が、実プロセスを spawn する点で honest。
- 最小 process accessor（=process-status/-live-p/-exit-status/-send-string/-send-eof/kill-process=）を
  =unless fboundp= で定義（Emacs/Rust-runtime を shadow しない）。

** 結果: readiness gate 全 GREEN（default smoke、override 無し）
=(:suite-ready t :suite-blocked-by nil … :make-process t :nelisp-sys-executable-find t
  :curl-process-lower-primitive t :native-async-lower-primitive t :native-text-http-lower-primitive t
  :native-gap-accounted t)=、=:nelisp-call-process-executes t=。=executable-find= は実際に
=/usr/bin/curl= / =/bin/sh= を解決し nonexistent は nil。=native-gap-accounted t=（honest、stub でない）。
suite loadability も完走（12 file / 221 tests register）。

** 残: suite *実行* crash = 既存 GC heisenbug（§11.24）で、本 adapter とは無関係（決定的検証済）
readiness 通過後、=ert-run-tests-batch-and-exit= で 182 test body を eval すると SIGSEGV（EXIT=139、×3 一貫）。
module + test の load 単体は crash しない → crash は test *実行*（大量 allocation）中。
**決定的切り分け**: reader process 関数を全て *non-forking canned* に差し替えても（fork ゼロ）、readiness は
通り gate-G は **同じく EXIT=139（×3）**。→ crash は本 adapter の fork/process 経路と無関係で、§11.24 の
moving-GC root-coverage gap（単一長 computation の transient 非回収）が 182-test の大量 allocation で発火した
もの。fix は §11.25 と同じ本筋（mid-eval root coverage 完備 or sound per-eval reset、いずれも大）。

** commit / 配置
- adapter: branch =feature/anvil-pkg-process-http-primitives=（worktree =dev/nelisp.wt-mod-expt=、smoke が
  default で load する source）に commit =4662378b=（process file のみ。同 worktree の
  =scripts/nelisp-standalone-build.el= 未コミット変更は前作業分で本作業と無関係、温存）。
- native process builtin（=nl_bi_process_*= 等）は dev/nelisp 側に commit 済（=cf01918c= 系）、reader はそこから build 済。

** Doc 152 総括（更新2）
- §11.26: async/http primitive 実装で **gate-G の readiness gate を GREEN 化**（=:suite-ready t=、
  =:nelisp-call-process-executes t=）。残る suite 実行 crash は §11.24 の既存 GC blocker（adapter 無関係、
  non-fork でも同一 crash と実証）。gate-G 完走には §11.25/§11.26 共通の GC fix（mid-eval root coverage 完備
  or sound per-eval reset）が前提。

* §11.27 root coverage 完備 (1) — conservative scan が scratch-slot の所有 block を取りこぼす gap を特定・修正（SIGSEGV 解消、2026-06-15）

** suite 実行 crash の現行犯 backtrace（§11.26 で readiness GREEN 化後に露出）
readiness=t で suite が ERT 本体実行に入ると SIGSEGV（EXIT=139 ×3 一貫）。gdb（=disable-randomization off=）で捕捉:
=#0 nl_freelist_take  #1 nl_alloc_bytes  #2 nl_bf_bind_sym  …  #19 nl_apply_lambda_inner=（rax=0）。
= crash は GC の mark/sweep/compact ではなく **freelist allocator が破損 freelist node を deref**。
freelist コメント（line 699-704/731-746）が真因を明記:「intermediate value held in a NON-root slot while a GC
fires gets its freed block re-handed-out → corruption … pre-existing GC root-coverage gap that the single
list's near-zero reuse merely masked」= **live block が sweep→ freelist→ reuse され、still-live 内容が
freelist next ptr を破壊**。

** gap の精密特定（§11.21 の記録を更新）
- nelisp-cc は **stack machine**（逆アセンブリ: =push %rax=/=pop %rdi=、locals は =-N(%rbp)= に spill、
  callee-saved register に値を跨がせない）→ §11.21 conservative scan が全 stack を走査すれば register root は無い。
- layout: **object = block+8、header(8B)@block+0**（=nl_gc_mark_block= が =(- obj 8)=）。
- §11.21 =nl_gc_conserv_word= は stack word =w=（tag<13）を **32B Sexp slot** とみなし =nl_gc_mark_slot(w)= で
  **子（box@w+8）**を mark するが、**=w= を含む alloc-bytes scratch block 自身（header@w-8）は mark しない**。
- eval 機構は in-flight Sexp 値を =(alloc-bytes 32 8)= scratch slot（=func_slot=/=out_slot=/arg 構築途中/
  materialising-accessor scratch = §11.19/20 の「escape boxes」）に保持。collect 時にこの scratch block が
  conserv で子だけ mark され block 本体は未 mark → sweep → freelist → reuse → 破壊。これが §11.19/20 の gap の
  正体。plist 格納値は GC-trace される（repro で pre=post 確認、別仮説を棄却）。

** fix（=scripts/nelisp-standalone-build.el=、=nl_gc_conserv_owner= 追加 + =nl_gc_conserv_word= 改修）
conserv が見つけた slot =w= につき、=w-8= が **妥当な block header**（8-aligned BT ∈ [16, 16MiB]、block 末尾が
in-arena）の時のみ =nl_gc_mark_block(w)= で **所有 block も keep-alive**。header 妥当性検証で =(+ env 32)= 等の
interior/inline ptr 誤 mark（header word の low 3bit 上書き → 値破壊）を回避。ADDITIVE = mark+sweep で sound。

** 結果
- **SIGSEGV 解消**: suite EXIT=139 → 非 crash（×3）。
- **回帰なし**（rebuild + 検証）: =(+ 40 2)=→42、200k loop→200000、GC repro plist pre=post 生存、
  =make smoke-nelisp-suite-readiness= = =:suite-ready t :suite-blocked-by nil=（全 GREEN 維持）。
- = §11.27 は verified な root-coverage gap（scratch-slot 所有 block）を 1 クラス閉じた。

** 残: full suite は **完走せず非決定的 clean-exit（EXIT=0、結果未出力）**
crash は消えたが 182-test 実行が完走しない。exit は =nl_os_syscall_path= 経由の exit(0)、地点は run ごとに変動
（princ 診断で load 完走 M3 N=182 を確認できる run もあれば、loop 第一反復前に exit する run もある = §11.24 系の
slippery heisenbug）。exhaustion ではない（chunk alloc 失敗は crash 経路）。回帰検証は全 clean なので本 fix の広範な
false-positive 破壊ではなく、**より深い残存 gap（他クラスの in-flight ptr）or 非 GC 要因**と推定。要追加調査
（次フェーズ: 残 clean-exit の現行犯特定 — file 副作用 progress + 二段 header 検証強化 or 別 in-flight ptr クラスの列挙）。

** 注意（診断 tooling）
- reader の =princ= 出力は **block-buffered**で early-exit 時に flush されず消失 → 進捗追跡は file 副作用
  （=nelisp-process-call-process= で touch/echo）を使う。
- =nelisp--write-stdout-bytes= は bare では unbuffered だが、全 source load 後の文脈では出力が出ない（loaded
  source による再定義疑い）。診断 marker は =princ=（正常終了時）か file 副作用（early-exit 時）で。

** Doc 152 総括（更新3）
- §11.27: conservative scan に **scratch-slot 所有 block の keep-alive** を追加し、freelist crash（SIGSEGV）を
  解消（回帰なし、verified gap 1 クラス完備）。gate-G 完走には残る非決定的 clean-exit（深い残存 gap）の特定が必要。

* §11.28 残 clean-exit の現行犯特定 — corrupted syscall NR → exit(0)、root は pre-existing int 破壊（§11.22 と同種、2026-06-15）

** 現行犯（proximate cause）= path-syscall の NR が exit(60) に破壊
gdb（=catch syscall exit exit_group=）で early clean-exit を捕捉: =Catchpoint (call to syscall exit),
nl_os_syscall_path= で発火、rdi=0。=nl_os_syscall_path (nr cpath)= = =(syscall-direct nr cpath 0 0 0 0 0)=
（generic path-syscall wrapper、line 6189）。= **=(nl_os_syscall_path 60 0)= = =exit(0)= が呼ばれた** =
load 中の file I/O 用 path-syscall（open=2 / newfstatat=262 / access=21 等）の **NR 引数（小整数）が 60 に破壊**
され、意図した syscall でなく exit(0) を実行。=kill-emacs= は不在（fboundp nil）、suite source にも exit 呼出無し
→ legitimate な exit ではなく **値破壊**と確定。

** root cause = pre-existing GC int 破壊（§11.22/24 と同一クラス、§11.27 とは別）
- §11.27 の owner-marking を **boot 除外 + 二段 header 検証**で tighten（false-positive で小 tagged int を
  誤 mark する懸念を潰す版）して rebuild → suite は **同じく clean-exit（×2、crash 再発なし）**。
  = clean-exit は §11.27 の false-positive 起因ではない（tighter でも不変）。
- これは §11.22「count-loop s=0」(=(while ...) (setq s (+ s 1))= が N≥~200k で s=0)= と同じ
  「~16MB GC trigger 近辺の transient 非回収による live int 破壊」。§11.21 で **orthogonal / pre-existing /
  非 collection 経路 / 16MB 閾値**と明記済。全 suite source の load が 16MB を越え、この破壊が syscall NR を直撃。
- = scratch-slot gap（§11.27、freelist crash）と int-破壊 gap（§11.22/28、clean-exit）は **別クラス**。
  §11.27 は前者を閉じたが後者は未着手。

** 非決定性の正体
exit 地点は run ごとに変動（princ diag で load 完走する run もあれば smoke load 中に exit する run も）。
= 破壊される int が layout（ASLR / 割当順）依存。setarch -R でも tooling 側の非決定が残る。

** gate-G への道（更新）
**§11.22/28 の int-破壊 gap を閉じる**こと: 「~16MB trigger 近辺で transient 非回収が live int を破壊」の
機序を §11.24 の watchpoint 路線で特定（count-loop s=0 が最小再現 = suite より遥かに速い反復で同一 root を踏む）。
count-loop を直せば suite clean-exit も解ける見込み（同一クラスのため）。§11.27 の scratch-slot fix は維持
（freelist crash 解消は独立の前進）。

** 診断 tooling 追補
- 進捗追跡は =nl-write-file=（fork 無し・unbuffered・全 source load 後も動作確認済）が最も信頼できる。
  ただし heavy 文脈では非決定的に load 途中で exit するため、決定的観測は gdb（=catch syscall exit=）が確実。

** Doc 152 総括（更新4）
- §11.28: 残 clean-exit の現行犯 = **corrupted syscall NR → exit(0)（=nl_os_syscall_path= 経由）**。root は
  §11.22/24 の pre-existing int-破壊クラス（§11.27 の false-positive ではない＝tighter 版でも不変と実証）。
  gate-G 完走には int-破壊 gap の修正が必要で、その最小再現は **count-loop s=0**（suite より速い）。

* §11.29 bump 単調性の検証 — bump は構造的に単調、衝突源は freelist の live-block reuse（§11.22 仮説 refute、2026-06-15）

** count-loop s=0 は現 reader で再現しない（§11.27 の layout shift で masked）
setarch -R 決定論 repro で escalate: N=50k〜3M で =(= s N)= → 全て **正しい（111）**、=(format "%d" s)= も
N=1M/3M で **正しい**。§11.22-24 当時（N=10k でも s=0）から変化 = §11.27 の reader 編集が binary 静的 layout を
変え、決定論 layout での transient と live data の overlap が（≤3M で）当たらなくなった。**incidental masking、
root は残存**（suite は依然 clean-exit）。= count-loop はもはや suite バグの最小再現でない。

** bump 単調性: コード解析で構造的に単調と確定（empirical instrument は heisenbug に阻まれる）
- =nl_chunk_try_alloc=（bump path、line 787）: =old = [cursor]; new = old + want; if new > size → fail（new
  chunk）; else CAS(cursor, old, new); obj = base + old + 8=。= CAS が disjoint な =[old,new)= を予約 →
  **chunk 内で構造的に単調**、2 つの allocation が重なることは不可能。
- 非単調になりうるのは (a) freelist reuse（=nl_freelist_take=、collect が freelist を populate した後のみ）、
  (b) growth chunk が低位 VA（mmap 依存、§11.21 の nl_gc_is_boot が扱う既知事項）の 2 つだけ。chunk は別 mmap で
  重ならないので (b) でも live data の overlap は起きない。
- = **bump 非単調は衝突源でない**（§11.22 の「allocation が live binding を上書き」仮説を refute）。
- *empirical instrument は全て失敗*: loop 内に ptr-read/write-u64 / number-to-string / concat / nl-write-file の
  どれを足しても、un-instrumented では通る workload（plain 3M count-loop は正しく完走）が **clean-exit / 出力消失**
  する（§11.24 の「probe で manifestation が動く slippery heisenbug」と一致）。number-to-string/concat は §11.23 の
  壊れる format 系そのもので、instrument が測定対象の corruption を踏む。低位 buffer に ptr-write-u64 で記録する
  number-to-string-free 版でも N=3M で無出力 → byte-level の empirical 追跡は本質的に不可能。

** = 衝突源の確定（コード解析）= freelist の live-block reuse（§11.27 class）
単一 form の count-loop（collect 無し → freelist 空 → reuse 無し → 純 bump = 単調）は live data を上書きしえない
→ 再現しないのは当然。真の衝突は **collect が freelist に載せた still-live block を =nl_alloc_bytes= が reuse**
（root-coverage gap）で、これは **collect が走る workload = suite**（load 中の form 境界 collect）でのみ起きる。
§11.27 が scratch-slot class を閉じ freelist crash を解消したが、**他の in-flight ptr class（§11.28 の syscall NR を
持つ値を含む）が残存** → suite clean-exit。= gate-G の blocker は bump でなく **freelist-reuse-of-live-block の
root-coverage gap**（§11.27 と同じ方向の残課題）。

** gate-G への道（再確認・更新）
- byte-level の heisenbug 追跡は §11.22-29 で 4 セッション unproductive（probe が manifestation を動かす）。これ以上は非効率。
- 本筋は **root coverage の残 class 完備**（§11.27 の conservative scan を、scratch-slot 以外の in-flight ptr class
  ＝ accessor scratch / 評価途中 box / parse pool 等にも拡張）で、collect 後の freelist が live block を渡さないように
  する。完備すれば suite reuse-corruption が消える。
- or **sound per-eval reset**（Doc 146 disabled、reachability ベースで escape 安全化）。
- 暫定: 現状でも readiness gate は GREEN（§11.26）、freelist crash は解消（§11.27）。suite 完走（gate-G）は未達。

** Doc 152 総括（更新5）
- §11.29: bump は **構造的に単調**（=nl_chunk_try_alloc= の CAS disjoint 予約）で衝突源でない（§11.22 仮説 refute）。
  empirical bump instrument は heisenbug に阻まれる（probe が測定対象を踏む）。count-loop s=0 は §11.27 の layout
  shift で現 reader では再現せず（≤3M 正しい、incidental masking）。真の衝突 = **freelist の live-block reuse**
  （root gap、suite でのみ collect 後に発生、§11.27 class の残 in-flight ptr）。gate-G は root coverage 残 class
  完備 or sound per-eval reset が必要で、byte-level 追跡は打ち切り推奨。

* §11.30 count-loop clean-exit は CHUNK GROWTH（chunk-0 crossing）が trigger — §11.29 の「freelist reuse」を count-loop については訂正（2026-06-15）

** 閾値が chunk-0（256MiB）crossing と完全一致
§11.29 を「count-loop は ≤3M で再現せず」と書いたが **不十分**だった。setarch -R で高 N まで escalate:
- =(= s N)=（非 format、value 正否）: N=3.5M → **111（正しい）**、N=4M / 4.5M / 5M / 6M → **空（clean-exit、222 ではない）**。
- = 閾値は 3.5M〜4M。count-loop の per-iteration 割当 ≈ 64 bytes（=(< i N)= / =(+ s 1)= / =(1+ i)= の arg-list cons
  数本 × 24-32B）。→ **3M≈192MB✓ / 3.5M≈224MB✓ / 4M≈256MB✗** = clean-exit 閾値が **chunk-0 サイズ（256MiB,
  =nelisp-standalone--linux-arena-size= = #x10000000）crossing と完全一致**。
- = **count-loop clean-exit の trigger は CHUNK GROWTH**（chunk-0 を使い切り =nl_chunk_alloc_new= で growth chunk へ
  遷移する所）。単一 form は collect 無し → freelist 空 → reuse 無し なので、§11.29 の「freelist reuse」は
  count-loop には当たらない（**訂正**）。bump も chunk 内は単調（§11.29）。= 残るのは **chunk-0→growth-chunk 遷移の
  バグ**（growth chunk の VA / descriptor / cursor 配置 / 遷移時の in-arena・boot 判定）。

** 確認の限界（tiny-chunk-0 build 失敗 + tooling 不安定）
- *決定的確認の意図*: =NELISP_LINUX_ARENA_SIZE= で chunk-0 を 16/64MiB に縮め growth を早期化（~250k-1M iters）して
  clean-exit が前倒しになるか見る計画。だが **非 default arena size で standalone-reader build が失敗**
  （16MB: =normal-top-level()= Error 255 / 64MB: =nelisp-elf--build-rich= 系の link/write error）。= arena size が
  build-time の ELF reloc / layout 計算に影響（=nl_arena_base= reloc 周辺）。build 側の別 issue で、本確認は blocked。
- *direct instrument も不可*: loop 後に =(ptr-read-u64 268436184/268435456/268436168 ...)= を =/=/=+= して返す probe は
  **N=100 baseline ですら無出力**（computed-value 返却が value-printer/tooling の §11.23 系不安定を踏む）。=(if (= s N)
  111 222)= の literal 返却だけが信頼でき、それで閾値=chunk-0 crossing を確定した。

** 次フェーズ（chunk growth path のデバッグ — concrete で findable）
heisenbug byte-chase（§11.22-29、4 セッション unproductive）と違い、**chunk growth は離散イベント**で path も小さい:
- =nl_chunk_alloc_new=（line 772）/ =nl_chunk_init_descriptor=（761、desc@base+768, cursor init 1024, data@base+1024）/
  =nl_os_alloc_chunk=（mmap）/ =nl_chunk_try_alloc= の遷移、=268436160=（chunk-0 desc head）/ =268436168=（cur-chunk）/
  =268436184=（reserved 累計）の更新。
- §11.21 の =nl_gc_is_boot= コメントが既に「OS が growth chunk を chunk-0 base より **低位 VA** に置きうる」と flag。
  低位 VA の growth chunk が in-arena/boot 判定や（collect 時の）forwarding を誤らせる既知リスク。
- *確認手段*: (a) build を非 default arena size で通るよう直して tiny-chunk-0 で高速 repro、or (b) gdb で
  =nl_chunk_alloc_new= に break し growth chunk の base VA を観測（chunk-0 base との大小）+ 遷移直後の最初の数
  allocation を追う。count-loop 4M は collect 無しなので、(b) は heisenbug でなく決定的に追える見込み。

** Doc 152 総括（更新6）
- §11.30: count-loop clean-exit の trigger = **CHUNK GROWTH（chunk-0 256MiB crossing）**（閾値完全一致で確定）。
  §11.29 の「freelist reuse」は count-loop には不適用（単一 form は collect 無し）と訂正。bump は chunk 内単調。
  = 衝突先 = **chunk-0→growth-chunk 遷移**（growth chunk の VA/descriptor/cursor or 遷移ロジック）。tiny-chunk-0 での
  決定的確認は非 default-arena build 失敗で blocked、direct instrument は tooling 不安定で不可。次は build 修正 or
  gdb で =nl_chunk_alloc_new= 観測（collect 無しゆえ heisenbug でなく追える）。suite clean-exit も同 trigger の疑い
  （suite も load+test で 256MiB 超 → growth）。

* §11.31 gdb で nl_chunk_alloc_new 観測 — chunk growth は確認、ただし arena が runtime base + layout 依存で精密追跡は難航（2026-06-15）

** 観測結果（=catch syscall= + =break nl_chunk_alloc_new / nl_os_alloc_chunk=、count-loop 4.2M）
- *chunk-0 は runtime high VA に mmap*: boot の =nl_os_alloc_chunk ← nl_arena_init ← driver ← _start= で
  rax=**=0x7f004dc00000=**（size rdi=0x10000000=256MiB）。= Doc 140 通り arena は固定 0x10000000 でなく **runtime
  mmap base**。fixed slot literal（=268436160= 等）は compile 時に =(+ nl_arena_base (n - #x10000000))= へ rewrite
  される（§3.35）。→ gdb で literal 0x100002c0 等を直読すると =Cannot access memory=（実 slot は nl_arena_base+offset）。
- *chunk growth は count-loop 中に発生*: =nl_chunk_alloc_new ← nl_alloc_bytes ← nl_vector_slot_ptr ←
  nelisp_mirror_lookup_entry=（symbol lookup の vector 確保）/ =← nl_cons_sym_eq ← nl_sp_eq_lambda=（apply）。
  chunk-0 満杯（~256MiB = 4M iters、§11.30）で複数回 growth。= §11.30 の chunk-growth trigger を gdb で確認。
- *nl_chunk_alloc_new のロジック*: =base = nl_os_alloc_chunk(size)=（size = nl_chunk_size_for、既定 64MiB）→
  =desc = nl_chunk_init_descriptor(base size)=（desc@base+768, cursor init 1024, data@base+1024）→ chain link
  （=cur=0= なら head@268436160、else =cur+48 → desc=）→ cur-chunk@268436168 = desc → count@268436176++ →
  reserved@268436184 += size → return desc。

** 精密追跡が難航する理由（正直な記録）
- *arena が runtime base*: slot が nl_arena_base+offset にあり、gdb で fixed literal を直読できない。実 slot を読むには
  nl_arena_base（runtime 値）を先に取得して加算する必要があり、batch gdb で煩雑。
- *layout 依存（heisenbug）*: clean-exit は setarch -R 固定 layout で起きるが、gdb（disable-randomization off）は
  layout が異なり、growth は起きても **clean-exit が（その run の VA 配置では）再現しない**ことがある。= 「growth で
  必ず壊れる」のでなく「growth で確保した chunk の VA が特定の live 領域と数値的に干渉する layout で壊れる」。
- *gdb batch の commands ブロック*（=finish=+=continue= で全 growth VA を log する計画）が batch mode で期待通り
  動かず、全 growth chunk の base VA 列挙と exit 点 bt の同時取得に失敗。
- = §11.22-29 の heisenbug と同じ層: 「growth」まで絞れたが、growth chunk の VA が *どの live data と干渉するか* は
  layout 依存で、gdb/instrument が layout を動かすため一点固定が難しい。

** 次フェーズ（最も確実な経路）
1. **非 default-arena build を修正**（§11.30 で 16/64MiB が =nelisp-elf--build-rich= で fail）。arena size が
   build-time の reloc/layout 計算（nl_arena_base 周辺）に効くので、そこを直せば tiny chunk-0 で **growth を ~250k
   iters に前倒し**でき、setarch -R の決定論 layout で高速かつ非 gdb で repro 観測できる（gdb perturbation 回避）。
2. or **growth chunk の VA を chunk-0 の上に固定**（mmap addr hint = chunk-0_base + 256MiB 等の MAP_FIXED_NOREPLACE）。
   growth chunk が常に chunk-0 の上位連続域に来れば、低位/干渉 VA の layout 依存性が消え、corruption が解消する可能性
   （§11.21 の「OS が低位 VA に置く」リスクを構造的に除去）。これは fix 候補そのもの。
3. suite も同 trigger（256MiB 超で growth）の疑いが濃く、(2) が効けば suite clean-exit も解ける見込み。

** Doc 152 総括（更新7）
- §11.31: gdb で chunk growth trigger を確認（chunk-0 は runtime high VA、growth は count-loop の symbol-lookup
  vector 確保等から発生）。ただし arena-at-runtime-base + layout 依存（gdb が layout を動かす）で *growth chunk の
  どの VA が何と干渉するか* の一点固定は未達。最確経路 = 非 default-arena build 修正での決定論 repro、or **growth
  chunk VA を chunk-0 上位に固定**（fix 候補）。byte-level 追跡は依然 heisenbug で、build 修正 or VA 固定が本筋。

* §11.32 growth chunk VA を chunk-0 上位に固定する fix 試行 — fallback で no-op、clean-exit 解消せず、revert（2026-06-15）

** 実装した fix（linux =nl_os_alloc_chunk=）
=nl_arena_base==0=（chunk-0、arena 未初期化）は従来通り =mmap(NULL)=。GROWTH（=nl_arena_base= 設定済）のみ
=hint = nl_arena_base + total-reserved=（全 chunk の直上連続点）を =MAP_FIXED_NOREPLACE=（0x100000|0x22=0x100022）で
要求し、EEXIST 時のみ =mmap(NULL)= fallback。狙い = growth chunk を chunk-0 直上に連続配置し低位/干渉 VA を排除。

** 結果: 解消せず（count-loop 4M/5M/6M とも依然 clean-exit）
gdb で fix 後の配置を観測:
- chunk-0 base = **=0x7f17fbc00000=**（size 256MiB）。
- growth chunk #1 base = **=0x7f17f3c00000=** = chunk-0 より **128MiB 下位**。
- = 私の hint（chunk-0 直上 =0x7f180bc00000=）は **MAP_FIXED_NOREPLACE が EEXIST**（chunk-0 直上は libc/stack 等で
  占有）→ **fallback の =mmap(NULL)=** が動き、kernel は mmap を **降順**配置するため growth を chunk-0 の *下* に置いた。
- = fix は本 layout では **常に fallback = no-op**。clean-exit 継続。

** 判明したこと（重要）+ revert
- **growth chunk は chunk-0 の下位 VA に置かれる**（kernel の降順 mmap）— §11.21 が flag した「OS が growth chunk を
  chunk-0 base より下位 VA に置く」が *実際に毎回起きている* と確認。corruption はこの「growth-below-chunk-0」状態で
  発生（閾値 = chunk-0 crossing、§11.30）。
- ただし「chunk-0 上位に置く」は **上位が空いていない**ため hint では不可（fallback 必至）。fix は no-op + 複雑性のみ
  なので **revert**（=git checkout=、reader を default に再ビルド、tree clean）。
- *no-collect の count-loop で below-chunk-0 growth chunk が具体的に何を壊すか* の機序は依然 unpinned。

** 次フェーズ（VA hint でなく構造的アプローチ）
1. **大きい chunk-0 を =MAP_NORESERVE= で予約**（例 4GiB、demand-paged）→ bump が単一連続域内で伸び **growth chunk が
   発生しない**（count-loop 4M=256MiB も chunk-0 内）。Doc 140 の small-chunk-0 を実質 revert するが、=MAP_NORESERVE=
   なら RSS は実使用分だけ（旧 8GiB-committed 問題は回避）。= growth path を踏まない＝最も確実。build 失敗（§11.30 の
   非 default-arena build error）が large size でも出るかは要確認。
2. or **chunk0-base 相対の数値仮定を全 audit**: growth chunk が chunk-0 の *下* にある前提で壊れる箇所
   （=data-addr nl_arena_base= 相対の slot 計算、in-arena/boot 判定の VA 比較、obj↔slot 変換など）を grep。§11.21 は
   =nl_gc_is_boot= を直したが、**allocation 経路（collect 無しでも通る）に残る VA 仮定**が count-loop clean-exit の真因の疑い。
3. 暫定: readiness GREEN（§11.26）+ freelist crash 解消（§11.27）は維持。suite/count-loop clean-exit（gate-G）は未達。

** Doc 152 総括（更新8）
- §11.32: 「growth chunk VA を chunk-0 上位に固定」fix を実装したが、**chunk-0 直上は占有されており MAP_FIXED_NOREPLACE
  が必ず fallback** → no-op、clean-exit 解消せず、revert。gdb で **growth chunk は毎回 chunk-0 の下位 VA**（kernel 降順
  mmap）と確認（§11.21 の low-VA リスクが実在）。VA hint では上位固定不可。最確経路 = **大 chunk-0 を MAP_NORESERVE で
  予約し growth 自体を無くす**、or **allocation 経路の chunk0-base 相対 VA 仮定を audit**。byte-chase は打ち切り。

* §11.33 大 chunk-0（MAP_NORESERVE）fix 試行 — 解消せず。真因は「chunk growth」でなく ~4GiB TOTAL 割当（leak）と判明、§11.30-32 を訂正（2026-06-15）

** 実装した fix
=nelisp-standalone--linux-arena-size= を 256MiB→4GiB、linux mmap flags に MAP_NORESERVE（0x4000、flags 34→16418）
追加。狙い = bump を単一連続 demand-paged chunk-0 内に収め growth path を踏まない。

** 結果: 解消せず。そして DECISIVE な切り分け（8GiB chunk-0）
- 4GiB chunk-0 で count-loop 4M/5M/6M とも **依然 clean-exit**。gdb: BOOT mmap size=0x100000000（4GiB、fix 適用確認）、
  chunk-0 desc.size=4GiB（正しく書込）。だが **5M loop 中に growth chunk が出る**。
- first growth 時の **chunk-0 cursor offset = 0xfffffff0（≈4GiB-16）** = chunk-0（4GiB）が **genuinely full**。
  = count-loop は **~4GiB を割り当てている**（per-iter ≈ 1KB。§11.29 で per-eval reclamation は DISABLED と確認済 →
  arg-list cons / mirror-lookup の vector 確保 / boxed int 等が **回収されず leak**）。
- *8GiB chunk-0 で再テスト*: 4M（≈4GiB offset）は 8GiB chunk-0 の **中＝満杯でない**のに **STILL clean-exit**。
  = **clean-exit は chunk-full / growth が trigger でない**（§11.30-32 は *症状* を追っていた、**訂正**）。真の trigger
  は **~4GiB TOTAL 割当**（bump offset が 0x100000000 を越える付近）。
- OLD 256MiB chunk-0 の「4M 閾値」も整合: per-iter ≈ 1KB なら 4M = 4GiB total。§11.30 の「4M=256MiB=chunk-0 crossing /
  per-iter 64B」は **前提誤り**（実 per-iter は ~1KB、4M = 4GiB total）。3.5M=3.5GiB 正常 / 4M=4GiB 失敗 = 4GiB-total 境界。

** 32-bit truncation ではない
=nl_chunk_try_alloc= の offset/obj 算術を逆アセンブル → 全て 64-bit（REX.W、=cmp %r10,%rax=、=lock cmpxchg %rdx=）。
= 4GiB での単純な 32-bit truncation バグ **ではない**。機序（offset/total が ~4GiB に達した時に何が壊れるか）は未特定だが、
chunk-full でも growth でも 32-bit truncation でもない。

** = 真の root は LEAK（per-eval reclamation 無効）+ ~4GiB 限界
count-loop が ~4GiB も割り当てる **leak** が本体。per-eval reclamation が DISABLED（§11.25 で unsound と判明し OFF）の
ため transient（arg-list / mirror-lookup vector / boxed int）が回収されず ~1KB/iter 蓄積、4M で 4GiB に達して corruption。
**reclamation が効けば count-loop は bounded で 4GiB に永久に達しない** → clean-exit しない。= chunk growth/VA は無関係、
本筋は §11.25 と同じ **sound な in-form/per-eval reclamation**（= 深い root-coverage 問題）。size 変更は revert
（4GiB 境界は size に依らず踏むため無効）、reader は default に再ビルド、tree clean。

** gate-G への道（更新）
- suite clean-exit も同 root の疑い濃厚（load+182test の heavy 割当が reclamation 無しで限界に達する）。
- 本筋 = **sound reclamation の再有効化**（§11.25 の mid-eval collect は unsound だった → reachability ベースの
  root-coverage 完備 or escape 解析での per-eval reset safe 化）。これは Doc 152 全体で繰り返し本筋とされてきた最難関。
- 暫定 = 現状（readiness GREEN §11.26 + freelist crash 解消 §11.27）維持。実コードは長い単一 form を避ければ実害小
  （§11.24 split 確認済）。

** Doc 152 総括（更新9）
- §11.33: 大 chunk-0（4/8GiB MAP_NORESERVE）fix は **解消せず、revert**。DECISIVE: 8GiB chunk-0 で 4M（4GiB、未満杯）が
  clean-exit → trigger は **chunk-full/growth でなく ~4GiB TOTAL 割当**（§11.30-32 は症状を追っていた、訂正）。bump 算術は
  64-bit（32-bit truncation でない）。真 root = **per-eval reclamation 無効の leak（~1KB/iter）が ~4GiB 限界に達する**。
  fix の本筋は §11.25 と同じ **sound reclamation 再有効化**（最難関）。byte-chase / chunk-VA 系は全て打ち切り。

* §11.34 sound reclamation 実装試行（in-loop collect, 2回目）— while-step 境界 collect は依然 unsound、revert（§11.25 を再確認、2026-06-15）

** 実装（§11.25 の教訓を踏まえた改良版）
- =nl_gc_collect_loop(env args out)=（GC source）: §11.27 の sound form-boundary collect を土台に、while-step
  境界用の reachability collect。STASH を使わず **CURRENT live roots を直接**マーク: conservative scan
  （§11.21/§11.27、C-stack の driver roots/while args-out/scratch）+ 明示 env（mirror/frames/unbound）+ args + out
  + shared symentry、その後 sweep。**GROWTH gate**（cur-chunk != chunk-0 head）+ **trigger throttle**
  （reserved >= 268435560、collect 後 reserved+16MiB に re-arm）で boot/短 loop を no-op 化。
- =nl_sf_while=: host loop の各 step 後に =(extern-call nl_gc_collect_loop env args out)= を毎回呼ぶ。

** 段階的に判明した 2 つの問題
1. *初版（throttle 用に 2nd across-call local =cnt= を追加）*: **§11.14 invariant 違反**（across-call local は
   =status= のみ可、2nd local は frame を壊す）→ **boot crash**（=(+ 40 2)= が =nelisp_env_set_value ←
   nelisp_mirror_is_constant ← mirror_lookup ← nl_record_slot_ptr= で SIGSEGV、collect 経路ですらない）。
   → cnt を排し throttle を collect_loop 内（trigger slot）へ移し、collect_loop を毎 step 無条件 call に変更。
2. *改良版（cnt 無し、collect_loop が自己 gate）*: **boot OK（=(+ 40 2)=→42）、小 loop OK（100/1000/10000 正常）**
   だが **長い loop（100k+、chunk-0 を越えて growth する規模）が clean-exit**（空出力）。= collect が **発火する時
   （growth 後）に unsound**＝live data を sweep。10k（growth 無し→ collect no-op）は正常、100k+（growth → collect
   発火）は壊れる、と差が出た。= **regression**（baseline は全 loop 正常）。

** = §11.25 を再確認: while-step 境界 collect は incomplete roots で unsound
§11.27 の conservative scan + 明示 env/args/out/symentry を足しても、while-step 境界（step は return 済）には
**collect が見落とす live data**が残る（§11.25 の「materialising-accessor scratch / 評価途中の box ptr」=
C-stack に無く conservative scan が拾えない arena 内 in-flight 状態）。form-boundary collect が mark する
=nl_gc_mark_pool=（pool slot 走査）等の精密 root 処理も while-boundary 版には無い。→ 発火時に live を sweep → 破壊。

** revert + 現状
全 revert（=git checkout=）、reader を baseline に再ビルド: boot =(+ 40 2)=→42、100k loop→100000（全 loop 正常）、
tree clean。readiness GREEN（§11.26）+ freelist crash 解消（§11.27）維持。

** 結論（sound reclamation の現実的評価）
- in-loop / mid-eval collect は §11.25・§11.34 の **2 回とも同じ wall**（incomplete mid-eval root coverage）で unsound。
- sound 化には **complete mid-eval root coverage** が hard prerequisite: conservative scan を (a) C-stack だけでなく
  **arena 内 in-flight scratch（materialising-accessor / 評価途中 box）**まで、(b) form-boundary collect 相当の精密
  root（mark_pool 等）まで拡張する必要。= eval 機構全体の root 監査（大規模・多セッション）。
- or **escape-safe per-eval reset**（clone/macro-cache/mirror-share escape を reachability or pin で安全化）も同等に大。
- = count-loop / suite の clean-exit を直すには上記の大規模作業が必須。実コードは長い単一 form を避ければ実害小
  （§11.24 form-split で正常）。現実的選択: (1) complete root coverage に多セッション投資、or (2) 現 milestone
  （readiness GREEN + freelist crash 解消）を一区切りとし reclamation を別 epic に切り出す。

** Doc 152 総括（更新10）
- §11.34: in-loop reclamation collect を §11.27 土台 + CURRENT roots（stash 無し）+ growth/throttle gate で再実装。
  cnt-local は §11.14 違反で boot crash（→ 除去）。除去後も **発火時（growth 後）に unsound**＝ long loop を regress
  （§11.25 再確認）。while-step 境界には conservative scan + 明示 roots が拾えない in-flight arena 状態が残る。revert、
  baseline 健全。sound reclamation は **complete mid-eval root coverage**（大規模）が hard prerequisite。

* §11.35 host-freeze インシデント + 安全網（bin/nemacs メモリ上限）— leak は現バイナリで再確認、deep fix は §11.34 のまま未解決（2026-06-16）
** インシデント
=bin/nemacs --driver=nelisp= の REPL（=nelisp --repl --no-prompt --no-print=、=nemacs-main--event-loop= の単一
=while= form を実行）が ~23 分で RSS 48.4GiB（62GiB の 77%）+ swap 6.3GiB まで leak、host 空きメモリ 522MiB で
マシン全体がフリーズ。=kill -9= で即解放（used 56→5.8GiB）。= §11.33/§11.34 の「per-eval reclamation 無効 leak」が
実コード（event-loop の長寿命単一 form）で顕在化した実例。
** 現バイナリでの leak 再測定（HEAD 60973fb6、default 256MiB chunk-0、ulimit -v cap で安全に）
- baseline =--eval 42= = peak RSS 60MiB。
- 単一 form =(while (< i N) (setq s (+ s i) i (1+ i)))=（自明な整数ループ、意味的にはほぼ無確保）:
  N=50k → 428MiB（≈ **7.5KiB/iter** leak）、N=500k/2M → 1GiB cap を即超過。= form 境界 collect が
  単一 form 内では一度も発火しない（§11.24/§11.33 を再確認）。per-iter の確保元 = arg-list cons /
  mirror-lookup vector / boxed int（§11.33）。
** 安全網（実装済、Rust 無改変・shell のみ）
- =bin/nemacs= に =NEMACS_NELISP_MEMORY=（default 8388608 KiB = 8GiB、=unlimited= で無効化）を追加、
  既存 =ulimit -s= 隣で =ulimit -v= を nelisp driver 起動2経路（doctor + main launch）に適用。
- 検証: 8GiB cap 下で正常 eval（42 / (+ 40 2) / 50k loop）は rc=0 正常、runaway（5M iter）は 7GiB で
  **rc=88（mmap 失敗 → clean exit）** = 48GiB フリーズ点の遥か手前で死ぬ。1GiB 等の低 cap は起動初期 mmap が
  通らず segfault（rc=139）になるため、default は起動を壊さない 8GiB を採用。
- = leak の **根治ではない**（mitigation）。長寿命単一 form の leak 自体は §11.34 の sound reclamation
  （complete mid-eval root coverage）が解けるまで残る。だが host 全体フリーズは構造的に防止。
** deep fix の状態（変化なし）
- 根治 = §11.34 の通り while-step/mid-eval 境界での sound collect。2 回失敗（§11.25/§11.34）、hard prerequisite =
  complete mid-eval root coverage（arena in-flight scratch + nl_gc_mark_pool 相当の精密 root）。multi-session epic。
- 代替候補（未着手）: per-primitive-call の transient（arg-list/mirror-lookup vector/boxed int）を
  escape しない場合に scratch 再利用 or fixnum immediate 化して **per-iter 確保自体を削減**すれば、
  sound GC なしでも実用上 bounded にできる可能性（7.5KiB/iter は自明ループには過大、削減余地が大きい）。

* §11.36 確保削減 (1): special-form dispatch を非確保 byte 比較化 — per-call -66% / loop baseline -49%、正しさ保持（2026-06-16）
** 発見（attribution）
=(while (< i N) (setq s (+ s i) i (1+ i)))= の per-iter 確保を slope 法（N=100k/200k の peak RSS 差 / 100k、
ulimit -v cap で安全に）で attribution: **bare var-ref は 0B、関数呼び出し1回 = +1696B**。主因は special-form
dispatch でなく per-call と判明（特定後に dispatch も寄与と確認）。
** root: nl_apply_special が呼び出しごとに 11 個の special-form 名を確保比較
=nl_eval_inner_cons= は head が symbol の cons を eval するたび =nl_apply_special= を呼び、progn/if/let/let*/
setq/while/lambda/function/quote/condition-case/unwind-protect の 11 個を順次 =nl_sp_eq_*= で比較。各
=nl_sp_eq_*= → =nl_cons_sym_eq (sym buf len)= が **=alloc-bytes 32= ×2 (tmp_slot/result_slot) + =nl_alloc_symbol=
で比較用シンボルを intern 確保**。= 通常関数呼び出し（+/car 等、全 11 probe を通過）で ~11 × 確保。
** fix（=lisp/nelisp-cc-evalport-combiner-cons.el=、Rust 無改変）
=nl_cons_sym_eq= を **非確保の名前 byte 比較**に書換: symbol Sexp の name は [16]=bytes-ptr / [24]=len
（=nl_alloc_symbol_write= レイアウト）なので、=(if (= (sexp-tag sym) 4) (if (= (ptr-read-u64 sym 24) len)
(nl_sym_name_eq_bytes buf (ptr-read-u64 sym 16) 0 len) 0) 0)=。tail-recursive byte comparator
=nl_sym_name_eq_bytes= を同 manifest に追加（cross-unit 依存回避）。intern/slot 確保を完全に排除。
** 効果（実測、cache clean 後再ビルド）
- per-call: **1696 → 576 B/iter（-66%）**、自明ループ baseline: **5376 → 2736 B/iter（-49%）**。
- 正しさ: special form 全種 + macro(when/unless) + lambda + catch/throw + 関数呼び出し 15 hand-test 全正解、
  =make standalone-reader-test / -load-smoke / -fmt-smoke / -prelude-test / -repl-smoke= 全 PASS。
- = GC soundness と無関係の安全クラス（解放でなく「確保しない」）。§11.25/34 の unsound 壁を踏まない。
** ⚠ build gotcha（重要・要記録）
=target/standalone-units/<target>/NAME.o.unit= の per-unit cache は **source .el 編集を freshness 検出しない**
ことがある（=nelisp-standalone--cached-unit= の source-file dep が patched unit で効かない疑い）。最初の
rebuild は stale unit をリンクし変更が反映されず（=strings target/nelisp= に新 symbol 不在で判明、RSS が
KB 単位で完全同一）。**combiner-cons 等 patched unit を編集したら該当 .unit を rm（or =rm -rf
target/standalone-units=）してから rebuild すること**。
** 次の削減候補（未着手、リスク漸増）
- nl_sp_eq_* の =buf=（×11、8-16B、条件付き free）を length-gate（head の name-len を1回読み、長さ一致の
  probe だけ実行）で大半 skip。または nl_apply_special を name-len dispatch に再構成。
- arg-list eval（eval_slot/rest_slot ×args + cons）/ func_slot / args_slot / boxed-int の削減（hot path、要慎重）。

** §11.36 追記: recycle 方式は no-op（2026-06-16、試行 + revert）
=nl_eval_arg_list_walk= の dead scratch（eval_slot/rest_slot、construct が値コピーするので construct 後
dead）を buf と同じ =nl_gc_free_block= で recycle 試行 → **peak RSS に変化ゼロ**（per-arg 192B 不変）。
原因: =nl_alloc_bytes= の freelist reuse は =want >= @268435656= かつ size-segregated bucket 一致が要で、
40B（=alloc-bytes 32=）の小ブロックは bump され freed ブロックが =nl_freelist_take= で取られない
（@268435624=0 で reuse 自体は有効だが小ブロックが対象外）。= **free しても arena bump は減らない**。
既存の nl_sp_eq_* buf recycle も同様に現状 no-op。**fix1 が効いたのは recycle でなく elimination（確保しない）**
だったため。→ 安全な更なる削減は (a) arg-list scratch を **構造的に eliminate**（construct を値直接受けに、要 hot-path
改修）か (b) freelist を小ブロックでも reuse させる（§11.22/27/29 の freelist-reuse corruption territory =
GC soundness 問題、危険）。recycle 単純追加は無効と確定、再試行不要。

#+TITLE: Doc 81 — nelisp-cc primitive trampoline architectural extension (= Phase 7.1.6 prerequisite)
#+AUTHOR: NeLisp / Claude
#+DATE: 2026-05-09
#+STATUS: SHIPPED (all stages 81.1-81.4) 2026-05-10
#+PARENT: Doc 28 (= Phase 7.1 native compiler)、Doc 77c (= 100% 純 elisp commitment)
#+UNLOCKS: Phase 7.1.6.a〜.f cluster takeovers (= D-B Cranelift 削除 -4,782 Rust LOC)
#+DEPENDS_ON: Phase 7.1.5 LOCKED (= 既 SHIPPED 2026-05-09 per Doc 28 §11.6)、Phase A.5.1 SHIPPED (= Sexp::tag() / cons_box_ptr())

* §0 要旨 (TL;DR)

Track O 2026-05-09 audit で identify した *Blocker 2* (= "nelisp-cc has
no primitive trampoline emit mode") の concrete design + sub-task
breakdown.  Phase 7.1.5 perf gate は Track S audit (= Doc 28 §11.6、
2026-05-09) で *fib 3964x / fact-iter 24.1x / alloc-heavy 23.0x* =
*PASS-PASS-PASS* CLOSED 確認済、本 Doc 81 が *唯一の architectural
blocker*.  scope = nelisp-cc backend (=src/nelisp-cc.el= +
=src/nelisp-cc-x86_64.el= + =src/nelisp-cc-arm64.el= +
=src/nelisp-cc-runtime.el=) に *4 新 SSA opcode* + *1 新 entry-point ABI
mode* + *1 新 FFI extern-C 解決 path* を追加.  total ~*+650-800 elisp
LOC* / *Rust ±0* / *~1 週間* (= Stage 81.1〜81.4 で 8 営業日).
完了後、Phase 7.1.6.a (=cons= takeover、cons.rs ~-415 Rust LOC) 〜
7.1.6.f (=dsl= + =bridge= cleanup、~-2,015 Rust LOC) の 6 cluster +
7.1.7 deletion (= 計 ~-4,782 Rust LOC) が *並行 dispatchable*.

*Status (2026-05-10)*:
- Stage 81.1 *SHIPPED* (8ab6da1) — entry-ABI + 3 SSA opcode +
  x86_64 emit + car PoC, +800 elisp / 0 Rust, 8 ERT (PASS).
- Stage 81.2 *SHIPPED* (ba664c1) — 4th opcode
  =ssa-call-primitive= + arm64 mirror of 4 lower-* fn + 5-entry
  primitive-table-stage2 (cons.rs API parity) + 2 new ABI modes,
  +447 elisp / 0 Rust, 17 ERT (PASS, +9 vs Stage 81.1).
- Stage 81.3 *SHIPPED* (feat/doc-81-stage3) — vector primitive
  trampoline ABI: 2 new ABI modes (=:trampoline-binary-aref= /
  =:trampoline-ternary-aset=) + 4 vector entries (=length / aref
  / aset / elt=) in =primitive-table-stage3= (= 5 cons + 4 vector
  = 9 total) + 2-backend dispatcher updates (x86_64 + arm64 accept
  the new modes) + extern-C symbol parity (=nl_jit_access_*= from
  build-tool/src/jit/access.rs).  +127 elisp impl / +295 ERT / 0
  Rust, 26 ERT (PASS, +9 vs Stage 81.2).  Phase 7.1.6.b
  (=access.rs= takeover) elisp-emit prerequisite *unblocked*.
  Recognition pass / dlsym bridge production deferred to Stage
  81.4 per design doc §5.3 reframing.
- Stage 81.4 *SHIPPED* (feat/doc-81-stage4) — recognition pass
  wiring (=nelisp-cc-pipeline--recognize-primitives=) + dlsym bridge
  contract surface (=nelisp-cc-runtime-resolve-symbol= +
  =-resolve-symbol-function= override hook) + pipeline-stats
  recognized-primitives count.  Recognition pass is opt-in via
  =nelisp-cc-pipeline-recognize-primitives-enable= (default nil) so
  the Phase 7.1.5 LOCKED 1086/1091 ERT corpus runs unchanged through
  the legacy =:call= codepath until Phase 7.1.6.a flips the flag and
  routes the cons cluster.  +~250 elisp / 0 Rust, 33 ERT (PASS, +7
  vs Stage 81.3).  Doc 81 *COMPLETE (all stages 81.1-81.4)*; cons-
  heavy bench gate (= original §5.4 plan) reframed as a *Phase
  7.1.6.a follow-up* (= bench harness + perf gate are part of the
  cluster takeover PR's responsibility, not the architectural
  extension itself).  dlsym bridge production for standalone NeLisp
  = Phase 7.1.6.a's Rust hook responsibility (= contract documented
  on =nelisp-cc-runtime-resolve-symbol-function= docstring).

* §1 動機

** §1.1 Phase 7.1.6 prerequisite

Doc 28 §3.6 (= Phase 7.1.6 eval-site primitive lowering takeover) は
*Cranelift JIT が emit している =nl_jit_*= trampoline 群を nelisp-cc
backend で直 emit する経路*.  しかし 2026-05-09 時点の nelisp-cc は:

1. *SSA IR 上の primitive lowering opcode が無い* — =cons= / =car= /
   =cdr= / =setcar= / =setcdr= は =:call= opcode の callee meta で
   identify されるが、host machine code emit pattern は generic CALL
   rel32 (= System V AMD64 / AAPCS64 ABI 経由) しか持たない.
   =build-tool/src/jit/cons.rs= が emit する *inline NIL fast path* (=
   tag-byte load + immediate compare + conditional branch + helper
   tail-call) は表現不能.

2. *Sexp memory layout を読む opcode が無い* — Phase A.5.1 で =Sexp=
   は =#[repr(C, u8)]= に pin されており、tag-byte = offset 0 / payload
   ptr = offset 8 (=SEXP_PAYLOAD_OFFSET=) として inline で読める.  しかし
   nelisp-cc には =mov al, byte ptr [rdi]= / =mov rax, qword ptr
   [rdi+8]= 相当の SSA-level abstraction が無く、=:load-var= /
   =:store-var= は heap-cell / spill-slot 専用 (= elisp i64 wrapper).

3. *外部 C symbol への CALL emit path が無い* — 現 =--lower-call= は
   =nelisp-defs-index= 経由で elisp callee fixup を解決 (= 同一 mmap
   page 内 letrec / inner closure).  =nl_jit_cons_car= のような *外部
   linker symbol* を =Linkage::Import= 相当で resolve する path が無い.

4. *Trampoline ABI mode が無い* — 現
   =nelisp-cc-runtime-compile-and-allocate= の出力 page は host ABI
   (=System V AMD64= 6-arg int register / =AAPCS64= x0..x7) の
   =extern "C" fn(i64, i64, ..., i64) -> i64= 形状.  Cranelift trampoline
   は =extern "C" fn(*const Sexp, *mut Sexp) -> i64= (= unary) /
   =extern "C" fn(*const Sexp, *const Sexp, *mut Sexp) -> i64= (= binary
   constructor) で *out-pointer をもらって `i64` で OK/ERR を返す* 別 ABI.

** §1.2 Track O Blocker 2 articulation (= 何が現状 nelisp-cc に不在か)

Track O 2026-05-09 audit (= Phase 7.1.6.a 着手判定) で以下 inventory:

| 項目                                | 現状 nelisp-cc                         | 7.1.6 takeover で必要              |
|-------------------------------------+----------------------------------------+------------------------------------|
| SSA opcode set                       | 10 opcode (= const/load-var/store-var/  | *+4 opcode* (= ssa-call-primitive   |
|                                      | call/call-indirect/closure/copy/branch/ | / ssa-load-tag / ssa-load-payload-  |
|                                      | jump/return; phi は phi-out 後消去)     | ptr / ssa-cmp-tag-imm)              |
| Sexp 内部 layout 読取り              | 無し (= elisp wrapper i64 のみ)         | *tag byte at [rdi+0]* + *payload   |
|                                      |                                          | ptr at [rdi+8]* の direct emit       |
| FFI external C symbol 解決           | =nelisp-defs-index= (= elisp letrec のみ) | *=declare-extern-c=* (= linker      |
|                                      |                                          | symbol を Phase 7.5 fixup pass で   |
|                                      |                                          | mmap exec page の RIP-relative にpatch) |
| entry-point ABI                      | =extern "C" fn(i64, i64, ...) -> i64=     | *trampoline mode* (= 上記別 ABI)    |
|                                      | (System V AMD64 / AAPCS64 6-arg)         | +既存 ABI mode 維持 (= dual mode)   |
| 条件分岐 i8 immediate compare        | 無し (= 既存 =:branch= は i64 reg 比較) | =cmp byte ptr [rdi], imm8= +       |
|                                      |                                          | =je/jne= (x86_64) / =cbz/cbnz= (arm64) |

これら 5 項目が揃わないと Phase 7.1.6.a (=cons=) で =declare_unary_with_nil_inline=
等価の inline NIL fast path が elisp-emit で書けない.  Phase 7.1.6.b〜.f
は =cons= takeover の pattern に乗るので *81 完了 = 6 cluster 全部
unblocking*.

* §2 Current nelisp-cc state

** §2.1 既存 SSA IR opcode set

=src/nelisp-cc.el= (= 1,985 LOC) + 13 module total *~12,263 LOC elisp*
(Doc 28 §0 LATEST に articulate 済).  SSA opcode set は backend pcase
dispatch (=src/nelisp-cc-x86_64.el::nelisp-cc-x86_64--lower-instr=
line 1984 / =src/nelisp-cc-arm64.el::nelisp-cc-arm64--lower-instr=
line 1111) で 10 種:

#+begin_example
  pcase opcode
    'const          → load literal i64 into def reg
    'load-var       → load named var (cell / spill / register)
    'store-var      → store def value to named var
    'call           → CALL rel32 + ABI marshalling (System V AMD64 / AAPCS64)
    'call-indirect  → CALL r/m64 (function-pointer call、closure 展開後)
    'closure        → LEA rax, [rip + cell:inner:N] (or alloc-closure rel32)
    'copy           → MOV reg, reg (phi-out 後の resolution)
    'branch         → TEST + JE/JNE (current = i64 reg vs 0 比較)
    'jump           → JMP rel32 (unconditional)
    'return         → MOV rax, value-reg + epilogue + RET
    'phi            → ERROR (= phi-out pass 必須)
#+end_example

これに =:tail-call=, =:tail-call-self=, =:self-direct-call=,
=:inner-function-index= 等の meta が plist で乗る (= Phase 7.7 inliner
の各 pass が annotate).

** §2.2 既存 backend emit dispatch (x86_64 / arm64)

各 backend は:
- *prologue / epilogue* = System V AMD64 (rbp save + rsp align) /
  AAPCS64 (fp/lr save + sp align)
- *register file* = 16 x86_64 GPR (rax..r15) / 32 arm64 GPR (x0..x30 + sp)
- *spill slot* = stack frame の RBP-relative / FP-relative slot
- *fixup table* = call-fixups (= elisp letrec 名 → 同 mmap page 内
  CALL rel32 disp32 patch) / cell-fixups (= =cell:NAME= label → RIP-rel
  load patch)

外部 C symbol fixup table は *存在しない*.

** §2.3 既存 entry-point ABI (= nelisp-cc-runtime-compile-and-allocate)

=src/nelisp-cc-runtime.el::nelisp-cc-runtime-compile-and-allocate=
(line 1336、1408 LOC total) の 8-step pipeline:

#+begin_example
  AST → SSA (T6 frontend)
      → Phase 7.7 passes (T158: escape / inline / rec-inline / lift)
      → safe-points (T11)
      → linear-scan (T4)
      → backend compile (T9 x86_64 / T10 arm64)
      → tail-call rewrite (`--lower-tail-call`)
      → safe-point stub injection
      → exec-page allocation (W^X mmap PROT_EXEC)
      → real exec via subprocess `nelisp-exec-bytes`
#+end_example

返り値 plist に =:exec-page=, =:final-bytes=, =:exec-result= 等.
ABI shape は固定 = =extern "C" fn(i64, i64, ..., i64) -> i64=.

** §2.4 既存 FFI call resolution (= --lower-call の制約)

=src/nelisp-cc-x86_64.el::nelisp-cc-x86_64--lower-call= (line 1415):
- =meta :fn CALLEE-SYM= = elisp symbol (= letrec 名 / inner closure 名)
- 出力 = =CALL rel32= with =disp32 = 0= placeholder + =call-fixups=
  entry =(fixup-offset . CALLEE-SYM)=
- Phase 7.5 link pass (= =nelisp-cc-x86_64-compile-with-link=) が
  =nelisp-defs-index= 経由で CALLEE-SYM の同 page 内 entry offset を
  resolve、disp32 に patch

*制約*: CALLEE-SYM は *同 mmap page 内の elisp letrec / inner* に限る.
外部 linker symbol (= =nl_jit_cons_car= 相当) は resolve 不能.

* §3 Extension scope

** §3.1 New entry-point ABI mode (= primitive trampoline shape)

=nelisp-cc-runtime-compile-and-allocate= に *第 4 引数 ENTRY-ABI* を
追加.  既定 =:host-int= (= 既存 =extern "C" fn(i64, ..., i64) -> i64=).
新値 =:trampoline-unary=, =:trampoline-binary-ctor=,
=:trampoline-binary-mut= で primitive trampoline shape を選択.

#+begin_src elisp
  ;; 既存 (= shape 1):
  (nelisp-cc-runtime-compile-and-allocate LAMBDA-FORM 'x86_64 EXEC-ARGS)
    ;; → page :: extern "C" fn(i64, i64, ..., i64) -> i64

  ;; 新 (= shape 2 = unary out-param trampoline):
  (nelisp-cc-runtime-compile-and-allocate LAMBDA-FORM 'x86_64 nil
                                          :entry-abi :trampoline-unary)
    ;; → page :: extern "C" fn(*const Sexp, *mut Sexp) -> i64
    ;; arg0 = arg_ptr (rdi / x0), arg1 = out_ptr (rsi / x1)
    ;; 返り値 = TRAMPOLINE_OK (0) / TRAMPOLINE_ERR (1)
#+end_src

実装 = backend prologue emit pass で =--frame-layout-mode=
(=:host-int= / =:trampoline-unary= / =:trampoline-binary-ctor= /
=:trampoline-binary-mut=) を選択.  =nelisp-cc-x86_64--emit-prologue= /
=nelisp-cc-arm64--emit-prologue= に mode 引数追加.

** §3.2 New SSA opcodes (= ssa-call-primitive, ssa-load-tag, ssa-load-payload-ptr, ssa-cmp-tag-imm)

| opcode               | operands                | meta            | semantics                                   |
|----------------------+-------------------------+-----------------+---------------------------------------------|
| =ssa-call-primitive= | (PRIM-SYM ARG-VALS...) | =:abi=          | 外部 C symbol への CALL.  =:abi= で          |
|                      |                          | (= shape の指定) | trampoline-unary / -binary-ctor / -binary-mut |
| =ssa-load-tag=       | (PTR-VAL)              | nil             | =mov al, byte ptr [PTR-VAL]= = tag byte     |
|                      |                          |                  | (8-bit zero-extend to i64)                   |
| =ssa-load-payload-ptr= | (PTR-VAL)            | nil             | =mov rax, qword ptr [PTR-VAL+8]= = payload   |
|                      |                          |                  | ptr (=NlConsBox*= / =NlVectorBox*= 等)       |
| =ssa-cmp-tag-imm=    | (TAG-VAL IMM-VAL)      | =:cond=         | i8 immediate compare + branch (= TAG_NIL    |
|                      |                          | (=:eq= / =:ne=) | / TAG_CONS / TAG_INT 等の constant 比較)     |

*注*: =ssa-call-primitive= は既存 =call= opcode と独立 (= meta merge で
 統合する option もあるが、call-fixups table の扱いを分離した方が
 link pass の責務が clean).  =ssa-load-tag= / =ssa-load-payload-ptr= は
 *unsafe memory read* (= GC safe-point の在 / 不在で扱いが変わる).

** §3.3 Backend emit additions (x86_64)

=src/nelisp-cc-x86_64.el= に 4 新 lower 関数:

#+begin_src elisp
  ;; +200 LOC ≈
  (defun nelisp-cc-x86_64--lower-call-primitive (cg instr) ...)
    ;; → CALL rel32 with extern-c-fixups entry (= linker symbol patch)
    ;; ABI marshalling (= shape ごとに rdi/rsi/rdx 配置)
  (defun nelisp-cc-x86_64--lower-load-tag (cg instr) ...)
    ;; → MOVZX r64, byte ptr [src-reg]
    ;; opcode bytes: 0F B6 /r (8-byte zero-extend)
  (defun nelisp-cc-x86_64--lower-load-payload-ptr (cg instr) ...)
    ;; → MOV r64, qword ptr [src-reg + 0x08]
    ;; opcode bytes: REX.W 8B /r ModR/M+disp8
  (defun nelisp-cc-x86_64--lower-cmp-tag-imm (cg instr) ...)
    ;; → CMP r8, imm8 + JE/JNE rel8
    ;; opcode bytes: 80 /7 ib + 74/75 cb
#+end_src

=nelisp-cc-x86_64--lower-instr= の =pcase= に 4 新 arm 追加.

** §3.4 Backend emit additions (arm64)

=src/nelisp-cc-arm64.el= に 4 新 lower 関数 (= 同 4 opcode の AAPCS64
版):

#+begin_src elisp
  ;; +160 LOC ≈
  (defun nelisp-cc-arm64--lower-call-primitive (cg instr) ...)
    ;; → BL #imm26 with extern-c-fixups (= ADRP + LDR の 2-instr lazy
    ;; pattern も option、初手は BL rel26 + linker stub で十分)
  (defun nelisp-cc-arm64--lower-load-tag (cg instr) ...)
    ;; → LDRB Wt, [Xn]  (32-bit zero-extend; UXTB で 64-bit 化)
    ;; encoding: 0011 1001 01 imm12 Rn Rt
  (defun nelisp-cc-arm64--lower-load-payload-ptr (cg instr) ...)
    ;; → LDR Xt, [Xn, #8]
    ;; encoding: 1111 1001 01 imm12 Rn Rt (imm12 = 1 = 8-byte offset)
  (defun nelisp-cc-arm64--lower-cmp-tag-imm (cg instr) ...)
    ;; → CMP Wn, #imm12 + B.EQ/B.NE rel19
    ;; encoding: 0111 0001 00 imm12 Rn 11111  +  0101 0100 imm19 cond
#+end_src

=nelisp-cc-arm64--lower-instr= の =pcase= に 4 新 arm 追加.

** §3.5 FFI call resolution path (= external C symbol via =declare-extern-c=)

新 fixup table: =extern-c-fixups= (= alist =((PATCH-OFFSET . SYMBOL-NAME) ...)=).
Phase 7.5 link pass (= =nelisp-cc-x86_64-compile-with-link= /
=nelisp-cc-arm64-compile-with-link=) で resolve.

resolve 戦略:
1. =mmap PROT_EXEC= page allocation 後、=dlsym= 相当で host runtime の
   =nl_jit_cons_*= symbol address を query (= Linux =dlsym(RTLD_DEFAULT,
   "nl_jit_cons_car")=、macOS =dlsym(RTLD_DEFAULT, ...)=)
2. *initial implementation* = =nelisp-cc-runtime= が elisp 側で symbol
   address を =anvil-runtime= module call で query (= 既存
   =nelisp-cc-runtime--exec-real= と同 subprocess bridge 経由)
3. *future optimization* = nelisp-cc-runtime に *known-extern-c
   table* (= alist =((SYM . ABS-ADDR) ...)= ) を keep して dlsym roundtrip
   を回避

*caveat*: System V AMD64 で CALL rel32 = ±2GB range、=dlsym= が返す
absolute address が mmap page から ±2GB を超える場合は *trampoline
indirection* (= =mov rax, abs64 / call rax=) に fallback.  arm64 BL
rel26 = ±128MB なので同様の indirection が頻出.

** §3.6 Pipeline integration (= where takeover hooks in)

=src/nelisp-cc-pipeline.el::nelisp-cc-pipeline-run-7.7-passes= (= line 240)
の前段に *primitive recognition pass* を新設 (= 80 LOC ≈):

#+begin_src elisp
  (defun nelisp-cc-pipeline--recognize-primitives (function)
    "Replace `:call' instructions whose callee = primitive (= cons / car
  / cdr / setcar / setcdr / aref / aset / + / - / * / etc.) with
  `:ssa-call-primitive' + (optional) inline `:ssa-load-tag' / `:ssa-cmp-
  tag-imm' fast-path blocks."
    ...)
#+end_src

primitive recognition table は =nelisp-cc-runtime-primitive-table= (=
defconst alist =((SYM ARITY ABI-SHAPE) ...)=) で集中管理.

* §4 LOC budget

** §4.1 Per-component LOC estimate

| component                                     | 既存 LOC | 追加 LOC | 注 |
|-----------------------------------------------+----------+----------+----|
| =src/nelisp-cc.el= (SSA IR core)              | 1,985    | +60      | 4 新 opcode の =cl-defstruct= 拡張 + verifier |
| =src/nelisp-cc-x86_64.el= (x86_64 backend)    | 2,592    | +200     | 4 lower-* fn + extern-c-fixups |
| =src/nelisp-cc-arm64.el= (arm64 backend)      | 1,273    | +160     | 4 lower-* fn + extern-c-fixups |
| =src/nelisp-cc-runtime.el= (entry-point)      | 1,408    | +120     | =:entry-abi= 引数 + 4 ABI mode dispatch + dlsym bridge |
| =src/nelisp-cc-pipeline.el= (pipeline)        | 286      | +80      | primitive recognition pass + table |
| =src/nelisp-cc-callees.el= (callee resolver)  | TBD      | +40      | extern-c link table |
| ERT (=test/nelisp-cc-test.el=)                | TBD      | +120     | round-trip + per-opcode + integration |
| *aggregate elisp*                              |          | *+780*   |    |
| Rust delta                                     | -        | *±0*     | Stage 81.x は elisp のみ (= cluster 削除は 7.1.6 で発動) |

*concrete count target* = *+780 elisp LOC* (= 中央値、range 650-900).

** §4.2 Aggregate (= +nelisp-cc elisp、Rust ±0)

- elisp: *+780 LOC* (Stage 81.1〜81.4 合計)
- Rust: *±0 LOC* (= Stage 81.x 内では削除発動しない)

monotonic shrink rule (= user 2026-05-09 articulate):
- Stage 81.x 単独では Rust LOC は減らない (= elisp 拡張のみ)
- *Phase 7.1.6.a 着手時に -415 Rust* (= cons.rs 削除) で *cumulative
  net = +780 elisp + -415 Rust* で全体の *Rust shrink rule 維持*
- Phase 7.1.6.a〜.f 完了で *cumulative = +780 elisp + -4,022 Rust*
- Phase 7.1.7.a〜.d 完了で *cumulative = +780 elisp + -4,782 Rust*

** §4.3 Rule satisfaction (= Phase 7.1.6 cluster takeover での Rust shrink で offset)

「Rust LOC は monotonic に減らす」ルールは *Doc 81 単独では
elisp +780 で Rust ±0 = 純拡張*.  ルールは *本 Doc 81 が unlock する
Phase 7.1.6.a (= 単独 -415 Rust) と必ず同 PR or 同 batch で merge*
することで担保 (= 81.4 bench gate PASS 時点で 7.1.6.a のドラフト
PR を repository 上に上げ、Doc 81 と同時 main merge).

* §5 Sub-task breakdown

** §5.1 Stage 81.1 — entry-point ABI mode + minimum opcode (= car only PoC) (~3 日)

scope:
- =nelisp-cc-runtime-compile-and-allocate= に =:entry-abi= 引数追加
  (=:host-int= 既定 + =:trampoline-unary= 1 mode のみ)
- =ssa-call-primitive=, =ssa-load-tag=, =ssa-cmp-tag-imm= 3 opcode
  (= =ssa-load-payload-ptr= は Stage 81.2、cons constructor まで保留)
- x86_64 backend のみ (= arm64 は Stage 81.2)
- =--lower-call-primitive=, =--lower-load-tag=, =--lower-cmp-tag-imm=
  3 lower fn
- extern-c-fixups table + dlsym bridge (= initial = symbol table
  hardcode、=nl_jit_cons_car= 1 件のみ)
- ERT: =car nil → nil= の trampoline shape で end-to-end PASS

LOC delta:
- elisp: +280 (= core 30 + x86_64 100 + runtime 80 + pipeline 30 + ERT 40)
- Rust: ±0

deliverable: =draft/doc-81-stage1-entry-abi= branch で =car= 1 件の
trampoline-shape exec page + ERT 5 件 PASS.  Phase 7.1.5 既存 1086/1091
ERT は全部 PASS 維持.

*** §5.1.detail Stage 81.1 — articulation (Track U/V/Y 同 detail level)

**** §5.1.1 entry-ABI mode design (= =:trampoline-unary= signature)

新 ABI mode 1 件 = =:trampoline-unary=.  既存 =:host-int= mode (=
=extern \"C\" fn(i64, ..., i64) -> i64=) に対し、out-pointer 経由で値を
返し i64 で OK/ERR を返す shape:

#+begin_src elisp
  ;; pseudo-code: nelisp-cc-runtime.el line ~1336 entry-point 拡張
  (cl-defun nelisp-cc-runtime-compile-and-allocate
      (lambda-form arch exec-args &key (entry-abi :host-int))
    (cl-ecase entry-abi
      (:host-int
       ;; 既存 path = extern "C" fn(i64, ..., i64) -> i64
       (nelisp-cc-runtime--compile-host-int lambda-form arch exec-args))
      (:trampoline-unary
       ;; 新 path = extern "C" fn(*const Sexp, *mut Sexp) -> i64
       ;; arg0 = arg_ptr (rdi / x0), arg1 = out_ptr (rsi / x1)
       ;; 返り値 = TRAMPOLINE_OK (0) / TRAMPOLINE_ERR (1)
       (nelisp-cc-runtime--compile-trampoline-unary lambda-form arch exec-args))))

  (defun nelisp-cc-runtime--compile-trampoline-unary (lambda-form arch exec-args)
    "Frame layout mode = :trampoline-unary.
     Prologue: rdi → arg-slot, rsi → out-slot, no rax marshalling.
     Epilogue: mov [rsi], result-reg / xor eax, eax / ret."
    (let ((frame-mode :trampoline-unary))
      (nelisp-cc--with-frame-mode frame-mode
        (nelisp-cc-runtime--run-pipeline lambda-form arch exec-args))))
#+end_src

prologue / epilogue emit pass (= =nelisp-cc-x86_64--emit-prologue=) は
=frame-mode= で分岐:

#+begin_example
  :host-int             :trampoline-unary
  push rbp              push rbp
  mov rbp, rsp          mov rbp, rsp
  ;; 6-arg int regs     mov [rbp-8], rdi  ; arg_ptr
  ;; 直接 reg 使用       mov [rbp-16], rsi ; out_ptr
                         ;; 以降は arg-slot / out-slot 経由で access
#+end_example

**** §5.1.2 3 SSA opcode design

| opcode               | operands             | meta type      | return type | 備考 |
|----------------------+----------------------+----------------+-------------+------|
| =ssa-load-tag=       | (PTR-VAL)            | nil            | i64 (zero-extended u8) | =mov al, byte ptr [PTR-VAL]= 相当.  return type は *unsigned i64* (= zero-extend、sign-extend 不可: tag byte 0xFF は -1 扱い禁止) |
| =ssa-cmp-tag-imm=    | (TAG-VAL IMM-VAL)    | =:cond= (=:eq=/=:ne=) | i1 (branch flag) | =cmp r8, imm8= + =je/jne rel8= 相当.  IMM-VAL は const-folding 段で immediate 化済前提 (= reg→imm の literal flow) |
| =ssa-call-primitive= | (PRIM-SYM ARG-VALS...) | =:abi= (=:trampoline-unary=) | i64 (TRAMPOLINE_OK/ERR) | extern-c-fixups 経由で =nl_jit_cons_*= symbol を resolve.  *out-ptr は caller が alloca 済前提* |

*critical IR design decision*: =ssa-load-tag= return type = *zero-extended
u64* (= signedness 注意).  Sexp tag byte が 0x80 を超えた case (=
=TAG_FORWARDED= 等の future enum value) で sign-extend すると negative
i64 になり =cmp= の符号フラグで誤 branch する罠を回避.  cl-defstruct
の =:returns= slot に =(unsigned i64)= 明記.

**** §5.1.3 x86_64 backend emit (= 3 lower-* fn pseudo-code)

#+begin_src elisp
  ;; src/nelisp-cc-x86_64.el:1984 dispatcher 拡張 + 3 lower-* fn 新設
  (defun nelisp-cc-x86_64--lower-load-tag (cg instr)
    "Emit MOVZX r64, byte ptr [src-reg].
     opcode bytes: REX.W 0F B6 ModR/M (= 4-byte instruction)."
    (let* ((src-reg (nelisp-cc-x86_64--alloc-reg-for cg (instr-arg0 instr)))
           (dst-reg (nelisp-cc-x86_64--alloc-reg-for cg (instr-def instr))))
      (nelisp-cc-x86_64--emit-bytes cg
        (logior #x48 (nelisp-cc-x86_64--rex-rb dst-reg src-reg)) ; REX.W
        #x0F #xB6                                                ; MOVZX r64, r/m8
        (nelisp-cc-x86_64--modrm-disp0 dst-reg src-reg))))

  (defun nelisp-cc-x86_64--lower-cmp-tag-imm (cg instr)
    "Emit CMP r8, imm8 + JE/JNE rel8 (3 + 2 = 5 bytes).
     IMM-VAL は ssa-cmp-tag-imm の operand として const-folding 済"
    (let* ((tag-reg (nelisp-cc-x86_64--alloc-reg-for cg (instr-arg0 instr)))
           (imm     (instr-arg1 instr))
           (cond    (instr-meta-cond instr)))      ; :eq / :ne
      (nelisp-cc-x86_64--emit-bytes cg
        #x80                                       ; CMP r/m8, imm8
        (nelisp-cc-x86_64--modrm-reg-imm 7 tag-reg) ; /7 = CMP
        (logand imm #xFF)
        (cl-ecase cond ((:eq) #x74) ((:ne) #x75))   ; JE rel8 / JNE rel8
        0))) ; rel8 placeholder, branch-fixups で patch

  (defun nelisp-cc-x86_64--lower-call-primitive (cg instr)
    "Emit CALL rel32 with extern-c-fixups entry.
     ABI marshalling: :trampoline-unary では rdi = arg, rsi = out-slot."
    (let* ((sym  (instr-arg0 instr))
           (args (instr-args-tail instr))
           (abi  (instr-meta-abi instr)))
      (nelisp-cc-x86_64--marshal-trampoline-args cg abi args)
      (nelisp-cc-x86_64--emit-bytes cg #xE8 0 0 0 0)        ; CALL rel32
      (nelisp-cc-x86_64--push-extern-c-fixup cg sym (- (cg-pos cg) 4))))
#+end_src

dispatcher 拡張 (= line 1984 =pcase=) に 3 arm 追加:

#+begin_src elisp
  (defun nelisp-cc-x86_64--lower-instr (cg instr)
    (pcase (instr-opcode instr)
      ;; ... 既存 10 arm ...
      ('ssa-load-tag       (nelisp-cc-x86_64--lower-load-tag cg instr))
      ('ssa-cmp-tag-imm    (nelisp-cc-x86_64--lower-cmp-tag-imm cg instr))
      ('ssa-call-primitive (nelisp-cc-x86_64--lower-call-primitive cg instr))
      (_ (error "unknown opcode %S" (instr-opcode instr)))))
#+end_src

**** §5.1.4 car PoC implementation pseudo-code (= car opcode → tag check + payload load + return)

=car= primitive の SSA expansion (= primitive recognition pass output):

#+begin_src elisp
  ;; (car X) → 以下 5 instr の SSA block に展開
  ;;   t1 = ssa-load-tag X
  ;;   if (ssa-cmp-tag-imm t1 TAG_NIL :eq) goto B-nil
  ;;   t2 = ssa-call-primitive nl_jit_cons_car X  ; slow path
  ;;   goto B-end
  ;; B-nil:
  ;;   t2 = ssa-const NIL
  ;;   goto B-end
  ;; B-end:
  ;;   return t2

  ;; 注: Stage 81.1 の car PoC では fast-path inline は SKIP (= 全部
  ;; ssa-call-primitive 直行で simplicity を優先).  fast-path は Stage
  ;; 81.2 で cons constructor と一緒に articulate.
  (defconst nelisp-cc-runtime--primitive-table-stage1
    '((car  1 :trampoline-unary  nl_jit_cons_car)) ; 1 件のみ
    "Stage 81.1 の primitive table = car のみ.")
#+end_src

**** §5.1.5 sub-task 81.1.a〜81.1.d breakdown

| sub-stage | scope                                                            | days | elisp Δ | Rust Δ |
|-----------+------------------------------------------------------------------+------+---------+--------|
| 81.1.a    | =:trampoline-unary= ABI mode + frame-layout-mode + prologue/epilogue 分岐 | 1    | +80     | 0      |
| 81.1.b    | 3 SSA opcode struct 拡張 + verifier (= cl-defstruct の =:returns= slot) | 0.5  | +30     | 0      |
| 81.1.c    | x86_64 lower-load-tag / -cmp-tag-imm / -call-primitive 3 fn + dispatcher arm | 1    | +100    | 0      |
| 81.1.d    | extern-c-fixups table + dlsym bridge (=nl_jit_cons_car= 1 件) + ERT 5 件 | 0.5  | +70     | 0      |

aggregate: ~3 営業日 / +280 elisp / 0 Rust.

**** §5.1.6 +280 LOC source articulation

| 配置先                                       | LOC delta | 内訳 |
|---------------------------------------------+-----------+------|
| =src/nelisp-cc.el= (core SSA struct)         | +30       | 3 opcode の cl-defstruct 拡張 + verifier |
| =src/nelisp-cc-x86_64.el= (backend emit)     | +100      | 3 lower-* fn + dispatcher arm + REX/ModR/M helper |
| =src/nelisp-cc-runtime.el= (entry-point)     | +80       | =:entry-abi= 引数 + =:trampoline-unary= dispatcher + dlsym bridge skeleton |
| =src/nelisp-cc-pipeline.el= (recognition)    | +30       | 1-entry primitive table (=car= のみ) + recognition stub |
| =test/nelisp-cc-test.el= (ERT)               | +40       | 5 件 (=car nil= / =car (cons 1 2)= / trampoline-shape verify / abi-dispatch / dlsym-bridge) |
| *aggregate*                                  | *+280*    | |



scope:
- =ssa-load-payload-ptr= 4 番目の opcode 追加 (= cons constructor で
  必要)
- =:trampoline-binary-ctor= / =:trampoline-binary-mut= ABI mode 追加
- arm64 backend に同 4 lower-* fn 追加
- =nl_jit_cons_cdr= / =nl_jit_cons_make= / =nl_jit_cons_setcar= /
  =nl_jit_cons_setcdr= の extern-c-fixups 4 件追加
- ERT: =car / cdr / cons / setcar / setcdr= 5 op の round-trip PASS

LOC delta:
- elisp: +260 (= core 20 + x86_64 50 + arm64 100 + runtime 30 +
  pipeline 30 + ERT 30)
- Rust: ±0

deliverable: 5 cons primitive を nelisp-cc emit で完全 cover、Phase
7.1.6.a で削除する =build-tool/src/jit/cons.rs= の機能を *parity 100%*
で elisp emit 経路で出せる.

*** §5.2.detail Stage 81.2 — articulation

**SHIPPED 2026-05-10 — feat/doc-81-stage2 branch**: 4th opcode
=ssa-call-primitive= + arm64 mirror of 4 lower-* fn + 5-entry
=primitive-table-stage2= + 2 new ABI modes (=:trampoline-binary-ctor=
/ =:trampoline-binary-mut=) all landed.  ERT diff: +9 (8→17 in
=test/nelisp-cc-stage81-poc-test.el=); =make test= 2613/2613
expected, 0 unexpected.  Rust delta = 0 LOC (verified
=git diff --stat -- '*.rs' 'Cargo.*'= empty).  Stage 81.3 (=
recognition pass + takeover routing) is next.

**** §5.2.1 full opcode set extension (= =ssa-call-primitive= dispatch table for 5 cons primitives)

Stage 81.1 で 1 entry (=car=) だった primitive table を 5 件に拡張、4
番目の opcode =ssa-load-payload-ptr= を追加:

#+begin_src elisp
  ;; src/nelisp-cc.el: 4 番目 opcode 追加
  ;; ssa-load-payload-ptr (PTR-VAL) → i64
  ;;   semantics: mov rax, qword ptr [PTR-VAL + SEXP_PAYLOAD_OFFSET]
  ;;   SEXP_PAYLOAD_OFFSET = 8 (= Phase A.5.1 で pin、Sexp #[repr(C, u8)] 由来)
  ;;   return type = unsigned i64 (= NlConsBox* / NlVectorBox* 等の raw ptr)

  ;; src/nelisp-cc-pipeline.el: primitive table 5 件版
  (defconst nelisp-cc-runtime--primitive-table-stage2
    '((car    1 :trampoline-unary       nl_jit_cons_car)
      (cdr    1 :trampoline-unary       nl_jit_cons_cdr)
      (cons   2 :trampoline-binary-ctor nl_jit_cons_make)   ; alloc
      (setcar 2 :trampoline-binary-mut  nl_jit_cons_setcar) ; write
      (setcdr 2 :trampoline-binary-mut  nl_jit_cons_setcdr)) ; write
    "Stage 81.2 = cons.rs parity 5 件.")
#+end_src

ABI mode 拡張:
- =:trampoline-binary-ctor= = =extern \"C\" fn(*const Sexp, *const Sexp, *mut Sexp) -> i64=
  (= car-arg, cdr-arg, out-ptr; cons constructor 用)
- =:trampoline-binary-mut= = =extern \"C\" fn(*mut Sexp, *const Sexp) -> i64=
  (= cons-cell-arg, value-arg; setcar/setcdr 用、out-ptr 不要 = mutate-in-place)

**** §5.2.2 arm64 backend mirror (= AAPCS64 calling convention)

x86_64 backend の 4 lower-* fn を arm64 に *symmetric port* (= 同 4 opcode
の AAPCS64 版).  *x86_64 vs arm64 design symmetry*:

| opcode             | x86_64                              | arm64                                     |
|--------------------+-------------------------------------+-------------------------------------------|
| ssa-load-tag       | =MOVZX r64, byte ptr [src]= (4-byte) | =LDRB Wt, [Xn]= + =UXTB Xt, Wt= (8-byte 2-instr) |
| ssa-load-payload-ptr | =MOV r64, qword ptr [src+8]= (4-byte) | =LDR Xt, [Xn, #8]= (4-byte single-instr) |
| ssa-cmp-tag-imm    | =CMP r8, imm8= + =JE/JNE rel8= (5-byte) | =CMP Wn, #imm12= + =B.EQ/B.NE rel19= (8-byte) |
| ssa-call-primitive | =CALL rel32= (5-byte)                | =BL #imm26= (4-byte) + linker stub if >±128MB |

#+begin_src elisp
  ;; src/nelisp-cc-arm64.el:1111 dispatcher 拡張
  (defun nelisp-cc-arm64--lower-load-tag (cg instr)
    "Emit LDRB Wt, [Xn] + UXTB Xt, Wt.
     LDRB encoding: 0011 1001 01 imm12=0 Rn Rt
     UXTB Xt, Wt = AND Xt, Xt, #0xFF (= UBFM Xt, Xt, #0, #7) は LDRB
     後の zero-extend で実は不要、64-bit Wt zero-extend 機構で OK."
    (let* ((src (nelisp-cc-arm64--alloc-reg-for cg (instr-arg0 instr)))
           (dst (nelisp-cc-arm64--alloc-reg-for cg (instr-def instr))))
      (nelisp-cc-arm64--emit-word cg
        (logior #x39400000           ; LDRB Wt, [Xn, #0]
                (ash src 5)
                dst))))

  (defun nelisp-cc-arm64--lower-load-payload-ptr (cg instr)
    "Emit LDR Xt, [Xn, #8].
     Encoding: 1111 1001 01 imm12=1 Rn Rt (imm12 = 1 → byte offset 8)."
    (let* ((src (nelisp-cc-arm64--alloc-reg-for cg (instr-arg0 instr)))
           (dst (nelisp-cc-arm64--alloc-reg-for cg (instr-def instr))))
      (nelisp-cc-arm64--emit-word cg
        (logior #xF9400400           ; LDR Xt, [Xn, #8]
                (ash src 5)
                dst))))
#+end_src

frame-layout-mode (= prologue/epilogue) は AAPCS64:
- =:trampoline-unary= = x0 = arg-ptr, x1 = out-ptr → fp/lr save + slot 配置
- =:trampoline-binary-ctor= = x0/x1 = args, x2 = out-ptr
- =:trampoline-binary-mut= = x0/x1 = args, no out-ptr (= mutate-in-place)

**** §5.2.3 5 cons primitives (= read 2 / write 2 / alloc 1)

| primitive | ABI shape                  | C symbol           | semantics |
|-----------+----------------------------+--------------------+-----------|
| =car=     | =:trampoline-unary=        | =nl_jit_cons_car=  | (cons-cell) → car-value (read) |
| =cdr=     | =:trampoline-unary=        | =nl_jit_cons_cdr=  | (cons-cell) → cdr-value (read) |
| =cons=    | =:trampoline-binary-ctor=  | =nl_jit_cons_make= | (car-val, cdr-val) → new cons-cell (alloc) |
| =setcar=  | =:trampoline-binary-mut=   | =nl_jit_cons_setcar= | (cons-cell, val) → val (write, mutate) |
| =setcdr=  | =:trampoline-binary-mut=   | =nl_jit_cons_setcdr= | (cons-cell, val) → val (write, mutate) |

各 primitive の expansion (= recognition pass output):

#+begin_src elisp
  ;; (cons X Y) → 以下 SSA に展開
  ;;   alloca out-slot (= 16-byte Sexp slot on stack)
  ;;   t1 = ssa-call-primitive nl_jit_cons_make X Y :abi :trampoline-binary-ctor
  ;;   t2 = ssa-load-from out-slot
  ;;   return t2

  ;; (setcar C V) → 以下 SSA
  ;;   t1 = ssa-call-primitive nl_jit_cons_setcar C V :abi :trampoline-binary-mut
  ;;   ;; 返り値 = V そのもの (mutate-in-place、out-slot なし)
  ;;   return V

  ;; Stage 81.2 では fast-path inline (= NIL check + payload-ptr load + return) は
  ;; *まだ inline せず*、全部 ssa-call-primitive 直行.  fast-path は Stage 81.3
  ;; pipeline integration で recognition pass に追加 (= per-primitive 別 emit).
#+end_src

**** §5.2.4 sub-task 81.2.a〜81.2.c breakdown

| sub-stage | scope                                                            | days | elisp Δ | Rust Δ |
|-----------+------------------------------------------------------------------+------+---------+--------|
| 81.2.a    | =ssa-load-payload-ptr= 4 番目 opcode + verifier + x86_64 lower fn | 0.5  | +50     | 0      |
| 81.2.b    | arm64 backend port (= 4 lower-* fn + dispatcher + frame mode 3 件) | 1    | +120    | 0      |
| 81.2.c    | =:trampoline-binary-ctor= / =-mut= ABI mode + 5-entry primitive table + ERT | 0.5  | +90     | 0      |

aggregate: ~2 営業日 / +260 elisp / 0 Rust.

**** §5.2.5 +260 LOC source articulation

| 配置先                                       | LOC delta | 内訳 |
|---------------------------------------------+-----------+------|
| =src/nelisp-cc.el= (core)                    | +20       | =ssa-load-payload-ptr= cl-defstruct 拡張 + verifier |
| =src/nelisp-cc-x86_64.el= (x86_64 backend)   | +50       | 1 lower-* fn (=load-payload-ptr=) + 2 ABI mode の prologue/epilogue 拡張 |
| =src/nelisp-cc-arm64.el= (arm64 backend)     | +120      | 4 lower-* fn (Stage 81.1 で skip した分) + dispatcher arm + AAPCS64 frame mode 3 件 |
| =src/nelisp-cc-runtime.el= (entry-point)     | +30       | 2 ABI mode dispatcher (=:trampoline-binary-ctor= / =-mut=) |
| =src/nelisp-cc-pipeline.el= (recognition)    | +30       | 5-entry primitive table + symbol → ABI shape lookup |
| =test/nelisp-cc-test.el= (ERT)               | +30       | 10 件 (= 5 primitive × {x86_64, arm64} round-trip) |
| Reference: =callees= linker table の +10 LOC は 81.3 で集約 | (10)      | (= shared with 81.3) |
| *aggregate*                                  | *+260*    | |



scope:
- =nelisp-cc-pipeline--recognize-primitives= pass 実装
- =nelisp-cc-runtime-primitive-table= defconst 設置 (= cons 5 件 +
  arith 12 件 + access 4 件 + predicate 1 件 + syscall 2 件 = 24 件分の
  metadata、ただし emit は cons 5 件のみ、他は Phase 7.1.6.b〜.e で発動)
- nelisp-cc-test.el の round-trip ERT 拡充 (= primitive recognition →
  emit → exec の e2e)
- =nelisp-cc-test.el= に *trampoline-shape exec ERT* 追加 (= Sexp tag
  byte の actual layout を verify)

LOC delta:
- elisp: +160 (= pipeline 20 + runtime 30 + callees 40 + ERT 70)
- Rust: ±0

deliverable: existing 1086/1091 ERT 全 PASS 維持 + 新規 trampoline
ERT 30+ PASS.

*** §5.3.detail Stage 81.3 — articulation

**SHIPPED 2026-05-10 — feat/doc-81-stage3 branch (vector primitive
trampoline ABI reframing)**: Stage 81.3 was reframed away from the
recognition-pass-first plan toward extending the trampoline ABI to
the vector cluster (= Phase 7.1.6.b prerequisite, Doc 28 §3.6.b).
Two new ABI modes shipped (=:trampoline-binary-aref= for `(vec, i64)
→ Sexp` shape, =:trampoline-ternary-aset= for `(vec, i64, val) → ()`
shape), four vector entries (=length / aref / aset / elt=) added on
top of the Stage 81.2 cons table to form a 9-entry
=primitive-table-stage3=, and both backends' =-lower-call-primitive=
ABI validators extended to accept the new modes.  Recognition pass
wiring + dlsym bridge production deferred to Stage 81.4.  ERT diff:
+9 (17 → 26 in =test/nelisp-cc-stage81-poc-test.el=).  Rust delta
= 0 LOC.  elisp delta = ~+127 impl + ~+295 ERT.

**** §5.3.1 pipeline integration (= =nelisp-cc-pipeline-run-*= が primitive lowering を 認識する hook)

=src/nelisp-cc-pipeline.el::nelisp-cc-pipeline-run-7.7-passes= (= 既存 line 240) の
*前段* に primitive recognition pass を 1 件挿入:

#+begin_src elisp
  (defun nelisp-cc-pipeline-run-7.7-passes (function)
    "Phase 7.7 inliner pipeline.  Stage 81.3 で primitive recognition pass を冒頭に挿入."
    (nelisp-cc-pipeline--recognize-primitives function) ; <-- NEW (Stage 81.3)
    (nelisp-cc-pipeline--escape-analysis    function)   ; 既存
    (nelisp-cc-pipeline--inline-pass        function)   ; 既存
    (nelisp-cc-pipeline--rec-inline-pass    function)   ; 既存
    (nelisp-cc-pipeline--lift-pass          function)   ; 既存
    function)

  (defun nelisp-cc-pipeline--recognize-primitives (function)
    "Replace `:call' instructions whose callee = primitive (= cons / car
  / cdr / setcar / setcdr) with `:ssa-call-primitive' + (optional) inline
  `:ssa-load-tag' / `:ssa-cmp-tag-imm' fast-path blocks."
    (cl-loop for block in (function-blocks function)
             do (cl-loop for instr in (block-instrs block)
                         when (and (eq (instr-opcode instr) 'call)
                                   (assq (instr-meta-fn instr)
                                         nelisp-cc-runtime-primitive-table))
                         do (nelisp-cc-pipeline--rewrite-as-primitive
                             function block instr))))
#+end_src

**** §5.3.2 takeover routing (= eval-site cons/car/cdr が Cranelift から nelisp-cc に切替わる切替点)

routing rule (= recognition table 24 entries):

#+begin_src elisp
  ;; src/nelisp-cc-pipeline.el (or nelisp-cc-runtime.el)
  (defconst nelisp-cc-runtime-primitive-table
    '(;; cons cluster (= Phase 7.1.6.a 発動)
      (car    1 :trampoline-unary       nl_jit_cons_car)
      (cdr    1 :trampoline-unary       nl_jit_cons_cdr)
      (cons   2 :trampoline-binary-ctor nl_jit_cons_make)
      (setcar 2 :trampoline-binary-mut  nl_jit_cons_setcar)
      (setcdr 2 :trampoline-binary-mut  nl_jit_cons_setcdr)
      ;; arith cluster (= Phase 7.1.6.c 発動、metadata only / emit は別経路 = ssa-iadd 等の既存 op で reuse)
      (+ 2 :host-int ssa-iadd)
      (- 2 :host-int ssa-isub)
      (* 2 :host-int ssa-imul)
      (=  2 :host-int ssa-icmp-eq)
      (<  2 :host-int ssa-icmp-lt)
      (>  2 :host-int ssa-icmp-gt)
      (<= 2 :host-int ssa-icmp-le)
      (>= 2 :host-int ssa-icmp-ge)
      (logior 2 :host-int ssa-bor)
      (logand 2 :host-int ssa-band)
      (logxor 2 :host-int ssa-bxor)
      (ash    2 :host-int ssa-ash-sequence)
      ;; access cluster (= Phase 7.1.6.b、後続 stage で発動)
      (aref   2 :trampoline-binary-mut  nl_jit_access_aref)
      (aset   3 :trampoline-ternary-mut nl_jit_access_aset)
      (elt    2 :trampoline-binary-mut  nl_jit_access_elt)
      (length 1 :trampoline-unary       nl_jit_access_length)
      ;; predicate cluster (= Phase 7.1.6.d)
      (eq 2 :host-int ssa-icmp-eq-i64)
      ;; syscall cluster (= Phase 7.1.6.e)
      (file-exists-p     1 :trampoline-unary nl_jit_syscall_file_exists)
      (file-readable-p   1 :trampoline-unary nl_jit_syscall_file_readable))
    "24 primitive metadata.  Stage 81.3 の cons cluster 5 件のみ emit 発動、
     他 cluster は metadata だけ keep (= Phase 7.1.6.b〜.e で activate).")
#+end_src

takeover 切替点 = =nelisp-cc-pipeline--recognize-primitives= が *table
hit したら即 emit 切替*.  table miss = Cranelift baseline 経路維持 (=
fall-through、Phase 7.1.6 が完了するまで dual-path で並走).

**** §5.3.3 ERT design (= per-opcode round-trip, integration smoke, takeover correctness)

3 層の ERT:

#+begin_src elisp
  ;; Layer 1: per-opcode round-trip (Stage 81.1 で 5 件、81.2 で 10 件 SHIPPED)
  (ert-deftest nelisp-cc-test-ssa-load-tag-roundtrip ()
    "ssa-load-tag が NIL Sexp から TAG_NIL (= 0) を返す."
    (let* ((sexp (nelisp-cc-runtime--alloc-nil))
           (page (nelisp-cc-runtime-compile-and-allocate
                  '(lambda (s) (ssa-load-tag s)) 'x86_64 (list sexp))))
      (should (= (nelisp-cc-runtime--exec page) 0))))

  ;; Layer 2: integration smoke (Stage 81.3 で 15 件)
  (ert-deftest nelisp-cc-test-cons-pipeline-integration ()
    "(cons 1 2) を pipeline に通し、recognition pass で ssa-call-primitive に書換、
     emit して exec、結果が cons cell (car=1, cdr=2) であること."
    (let* ((page (nelisp-cc-runtime-compile-and-allocate
                  '(lambda () (cons 1 2)) 'x86_64 nil)))
      (let ((result (nelisp-cc-runtime--exec page)))
        (should (cons-p result))
        (should (= (car result) 1))
        (should (= (cdr result) 2)))))

  ;; Layer 3: takeover correctness (Stage 81.3 で 15 件)
  (ert-deftest nelisp-cc-test-takeover-vs-cranelift-equivalence ()
    "同 lambda を Cranelift baseline と nelisp-cc trampoline-emit 両方で走らせ
     結果が semantic equivalent (= equal で比較) であること."
    (let ((form '(lambda (n) (cons n (cons (1+ n) nil)))))
      (let ((cranelift-result (nelisp-cc-test--exec-cranelift form 5))
            (trampoline-result (nelisp-cc-test--exec-trampoline form 5)))
        (should (equal cranelift-result trampoline-result)))))
#+end_src

**** §5.3.4 sub-task 81.3.a〜81.3.c breakdown

| sub-stage | scope                                                            | days | elisp Δ | Rust Δ |
|-----------+------------------------------------------------------------------+------+---------+--------|
| 81.3.a    | =nelisp-cc-pipeline--recognize-primitives= pass + 24-entry table 設置 | 0.5  | +50     | 0      |
| 81.3.b    | extern-c link table (=callees.el= 拡張) + dlsym integration polish | 0.5  | +30     | 0      |
| 81.3.c    | 3-layer ERT 拡充 (= round-trip 既存 + integration 15 + takeover 15) | 1    | +80     | 0      |

aggregate: ~2 営業日 / +160 elisp / 0 Rust.

**** §5.3.5 +160 LOC source articulation

| 配置先                                       | LOC delta | 内訳 |
|---------------------------------------------+-----------+------|
| =src/nelisp-cc-pipeline.el= (recognition)    | +50       | =recognize-primitives= pass impl + 24-entry table + per-primitive expander |
| =src/nelisp-cc-callees.el= (link table)      | +30       | extern-c link entry generator + dlsym bridge polish |
| =test/nelisp-cc-test.el= (ERT)               | +80       | Layer 2 integration 15 件 + Layer 3 takeover 15 件 (Layer 1 は 81.1/81.2 で SHIPPED) |
| *aggregate*                                  | *+160*    | |

注: =recognize-primitives= の +50 LOC のうち 20 LOC は =ssa-call-primitive=
への書換 helper、30 LOC は 5 cons primitive 専用 fast-path inline (=
NIL check + payload-ptr load) を recognition pass で先 emit する path.
他 19 cluster (= arith / access / predicate / syscall) は Phase 7.1.6.b〜.e
で *専用 expander* を追加する形で incremental 拡張.



scope:
- =bench-actual= harness に =cons-heavy= 軸を新設 (= =(let loop ((n
  N)) (if (zerop n) nil (cons n (loop (1- n)))))= 相当 elisp、Cranelift
  baseline と nelisp-cc trampoline-emit で同 workload 走らせて perf 比較)
- gate: nelisp-cc trampoline-emit perf ≧ Cranelift baseline (= 等価か
  +α、regression 0)
- 既存 3-軸 (= fib / fact-iter / alloc-heavy) の perf も regression 0
- bench result を =memory project_doc81_stage4_bench_pass= に articulate

LOC delta:
- elisp: +80 (= bench harness 拡張 + ERT 30)
- Rust: ±0

deliverable: cons-heavy bench で Cranelift baseline と semantic
equivalent + perf ≧ baseline、Doc 28 §3.6.a (= Phase 7.1.6.a =cons=
takeover) の *gate condition 全部達成*、=draft/doc-81-archi-extension=
を main merge → Phase 7.1.6.a dispatch ready.

*** §5.4.detail Stage 81.4 — articulation

**SHIPPED 2026-05-10 — feat/doc-81-stage4 branch (recognition pass +
dlsym bridge)**: Stage 81.4 was reframed away from the cons-heavy
bench gate (= original v2 plan) toward shipping the *recognition
pass + dlsym bridge production* that §5.3 reframing deferred.
Concretely:

- =nelisp-cc-pipeline--recognize-primitives= (= ~30 LOC) walks every
  block of an SSA function, examines each `:call' instruction's
  `:fn' meta, looks it up in
  =nelisp-cc-pipeline-primitive-table-stage3=, and rewrites matching
  instructions in place to opcode `ssa-call-primitive' with `:abi'
  / `:symbol' meta sourced from the table.  Operand count is
  validated against the table's declared arity — mismatch raises
  =nelisp-cc-runtime-error :recognition-arity-mismatch=.  Idempotent
  (= second sweep produces 0 rewrites because rewritten sites are
  no longer `:call' opcodes).  Preserves original `:fn' meta for
  debug pretty-printing.

- =nelisp-cc-pipeline-recognize-primitives-enable= defcustom (=
  default nil) gates the pass.  Phase 7.1.5 LOCKED 1086/1091 ERT
  corpus runs unchanged through the legacy =:call= codepath until
  Phase 7.1.6.a flips the flag.  Wired into
  =nelisp-cc-pipeline-run-7.7-passes= as a *post-inliner* step (= so
  any `:call' sites the simple-inline / lambda-lift passes
  introduce are not accidentally recognized as primitives —
  synthetic letrec callees never share names with elisp surface
  primitives by construction).  Independent of
  =nelisp-cc-enable-7.7-passes= so callers can A/B-test the
  trampoline-emit path with the inliner pipeline disabled.

- =nelisp-cc-pipeline-stats= struct gained a
  =recognized-primitives= field (= count of rewrites the pass
  performed during this compile, 0 when the flag is off).  The
  bench harness consumes this for attribution.

- =nelisp-cc-runtime-resolve-symbol= (= ~30 LOC) is the dlsym
  bridge contract surface.  Returns =(STATUS . ADDR-OR-NIL)= where
  STATUS ∈ ={:resolved, :host-stub, :not-found}=.  Host Emacs
  default = =:host-stub= with sentinel address
  =#x0DEADBEEF= (Doc 81 §0 hard-constrains 0 Rust LOC; Emacs has
  no general dlsym without a Rust dynamic module which Phase
  7.1.6.a will introduce as part of the cluster takeover PR's
  Rust delta budget).  Standalone NeLisp overrides via
  =nelisp-cc-runtime-resolve-symbol-function= (defvar) which Phase
  7.1.6.a will register at runtime startup.  Override contract is
  documented in the comment block above the
  =--resolve-symbol-stub-addr= defconst — idempotent on the same
  symbol within one page allocation, never errors on unknown
  symbols (= returns =:not-found= so the link pass can choose
  fallback behavior).

- =nelisp-cc-runtime-resolve-symbol-host-stub-p= predicate for
  ergonomic ERT / link-pass discrimination of the host stub
  response without remembering the sentinel constant.

LOC delta: +~250 elisp / 0 Rust.  ERT: 26 → 33 (+7 in
=test/nelisp-cc-stage81-poc-test.el= covering recognition default
off / rewrite / non-primitive skip / arity mismatch / idempotency
/ resolve-symbol stub / pipeline stats integration).

*Note* — the cons-heavy bench gate that was the original §5.4
v1 / v2 plan is *reframed as a Phase 7.1.6.a follow-up*.  Stage
81.4 ships the architectural infrastructure end-to-end; perf
measurement against Cranelift baseline is the cluster takeover
PR's responsibility (= it owns both the elisp-emit activation =
flag flip and the corresponding Rust deletion = -415 LOC, so the
bench gate naturally lives there).  The original §5.4.1〜§5.4.5
articulation below remains as the bench design template Phase
7.1.6.a will execute.

§5.4.detail (cons-heavy bench gate, original v2 plan, Phase 7.1.6.a follow-up)

**** §5.4.1 cons-heavy bench design (= 1M cons alloc + recycle / 100k cons-list traversal)

新 bench 軸 =cons-heavy= = 2 sub-workload:

#+begin_src elisp
  ;; bench/cons-heavy/alloc.el — 1M cons alloc + recycle
  (defun bench-cons-heavy-alloc (n)
    "N 個の cons を順次 alloc、結果を捨てて GC pressure を測定.
     N=1,000,000 で baseline ~2.0s (Cranelift) を想定.
     trampoline-emit は cons.rs 経路と semantic equivalent なので
     ±5% 範囲内に収まることを期待."
    (let ((acc nil))
      (dotimes (i n)
        (setq acc (cons i acc)))
      acc))

  ;; bench/cons-heavy/traverse.el — 100k cons-list traversal
  (defun bench-cons-heavy-traverse (lst)
    "LST を car/cdr で traverse して accumulate.
     N=100,000 で baseline ~50ms.  car/cdr の inline NIL fast-path が
     正常に効いていれば trampoline-emit でも同 perf."
    (let ((sum 0))
      (while lst
        (setq sum (+ sum (car lst)))
        (setq lst (cdr lst)))
      sum))
#+end_src

選定理由:
- =alloc= = =cons= constructor の trampoline-binary-ctor path stress
  (= mmap exec page → =nl_jit_cons_make= roundtrip × 1M、ABI marshalling
  overhead が出る)
- =traverse= = =car= / =cdr= の trampoline-unary path stress (= NIL
  fast-path inline が無いと毎回 =nl_jit_cons_car= roundtrip で激遅、
  inline 効果を直接測れる)

**** §5.4.2 Cranelift baseline measurement (= existing bench harness fork)

既存 =bench-actual= harness (= Doc 28 §11.6 で fib / fact-iter /
alloc-heavy 3 軸を実測した本体) を fork して =cons-heavy= 軸を追加:

#+begin_src elisp
  ;; bench-actual.el 既存 entry-point に 4 軸目追加
  (defconst bench-actual-axes
    '((fib          bench-fib          30)
      (fact-iter    bench-fact-iter    20)
      (alloc-heavy  bench-alloc-heavy  100000)
      (cons-heavy   bench-cons-heavy   1000000))) ; <-- NEW Stage 81.4

  (defun bench-actual-run-axis (axis-name backend)
    "BACKEND = 'cranelift / 'trampoline-emit (= nelisp-cc).
     各 axis を 5 回走らせて median を取り baseline と比較."
    (let* ((spec (assq axis-name bench-actual-axes))
           (fn   (nth 1 spec))
           (n    (nth 2 spec))
           (samples (cl-loop repeat 5 collect
                             (bench-actual--measure fn n backend))))
      (cl-sort samples #'<)
      (nth 2 samples)))                ; median of 5
#+end_src

baseline = =bench-actual-run-axis 'cons-heavy 'cranelift= の median.
trampoline-emit の値が baseline 比 ≧ 0.9x (= -10% 以内) なら gate PASS.

**** §5.4.3 gate threshold (= -10% 以下 throughput regression、cons takeover が production-ready 基準)

| gate                          | threshold              | failure mode | fallback |
|-------------------------------+------------------------+--------------+----------|
| =cons-heavy/alloc=            | ≧ 0.9x baseline        | trampoline overhead が ABI marshalling で 10% 超 | recognition pass で *fast-path inline* (= NIL check skip + direct alloc) を強化、再測 |
| =cons-heavy/traverse=         | ≧ 0.9x baseline        | inline NIL fast-path が効いてない | =ssa-cmp-tag-imm= の branch hint (= je/jne の short form) 確認、x86_64 codepath review |
| 既存 3-軸 (fib/fact-iter/alloc-heavy) | ≧ 1.0x baseline (= regression 0) | Stage 81.x 拡張で既存 path に副作用 | recognition pass を =enabled-p= flag 経由で opt-out 可能化、Phase 7.1.6.a 着手前に flag off で safety net |
| ERT 既存 1086/1091           | 全 PASS 維持            | trampoline-shape 変更で隣接 layout corrupt | Stage 81.x 各 commit で =make test-all= 必須 |

gate PASS 条件 = 上記 4 項目 *全部 green*.  失敗時は Doc 28 §3.6.a (=
=cons= takeover) を *未着手* とし、Doc 81 を v2 改訂 (= 追加 opcode /
ABI mode の articulate) に戻す.

**** §5.4.4 sub-task 81.4.a〜81.4.b breakdown

| sub-stage | scope                                                            | days | elisp Δ | Rust Δ |
|-----------+------------------------------------------------------------------+------+---------+--------|
| 81.4.a    | =bench-actual= harness に =cons-heavy= 軸 2 sub-workload 追加 + median 5-sample 機構 | 0.5  | +50     | 0      |
| 81.4.b    | 4-gate threshold check + fallback fail-fast assertion + memory snapshot | 0.5  | +30     | 0      |

aggregate: ~1 営業日 / +80 elisp / 0 Rust.

**** §5.4.5 +80 LOC source articulation

| 配置先                                       | LOC delta | 内訳 |
|---------------------------------------------+-----------+------|
| =bench/cons-heavy/alloc.el= (新規)           | +20       | =bench-cons-heavy-alloc= 1M-iter workload |
| =bench/cons-heavy/traverse.el= (新規)        | +20       | =bench-cons-heavy-traverse= 100k-iter workload |
| =bench/bench-actual.el= (既存拡張)           | +20       | 4 軸目登録 + median 5-sample 機構 + dual-backend dispatch (cranelift / trampoline-emit) |
| =bench/bench-actual-gate.el= (新規 or 既存拡張) | +20       | 4-gate threshold check + memory snapshot writer (= =memory project_doc81_stage4_bench_pass=) |
| *aggregate*                                  | *+80*     | |

memory snapshot = Stage 81.4 PASS 時に =project_doc81_stage4_bench_pass=
を =memory-add= で書込:

#+begin_example
  Stage 81.4 cons-heavy bench gate PASS (2026-MM-DD):
  - cons-heavy/alloc      : Cranelift Xms / nelisp-cc Yms / ratio Z (≧ 0.9 PASS)
  - cons-heavy/traverse   : Cranelift Xms / nelisp-cc Yms / ratio Z (≧ 0.9 PASS)
  - fib(30)               : ratio Z (≧ 1.0 PASS, no regression)
  - fact-iter(20)         : ratio Z (≧ 1.0 PASS, no regression)
  - alloc-heavy(100k)     : ratio Z (≧ 1.0 PASS, no regression)
  - ERT 1086/1091 全 PASS
  → Phase 7.1.6.a (=cons= takeover, -415 Rust) dispatch ready
#+end_example

* §6 Risk + mitigation

** §6.1 性能回帰 (= elisp-side IR overhead vs Cranelift native)

risk: nelisp-cc が emit する trampoline shape の machine code 品質が
Cranelift baseline 未達 (= linear-scan vs Cranelift regalloc2 + LLVM
ISA encoder の差)

mitigation:
- Stage 81.4 の =cons-heavy bench= を *gate*.  baseline 未達なら 81.4
  blocked、emit pattern tuning を Stage 81.5 (= 仮設) で実施
- Phase 7.7 inliner との合算で Cranelift より速くなる可能性が高い (=
  Phase 7.1.5 fib 3964x の前例)
- worst-case = 5-10% perf gap → *user 2026-05-09 strategic directive
  「100% 純 elisp」を優先*、5-10% gap は acceptable

** §6.2 ABI 不一致 (= System V AMD64 vs AAPCS64)

risk: trampoline ABI shape (=*const Sexp, *mut Sexp -> i64=) は
=System V AMD64= = =rdi, rsi -> rax=、=AAPCS64= = =x0, x1 -> x0=、
両者で *out-pointer 経由の return value writeback* が必要.

mitigation:
- Stage 81.1 で x86_64 のみ実装、out-pointer writeback は明示的な
  =MOV qword ptr [rsi], src= emit
- Stage 81.2 で arm64 移植、=STR Xn, [X1]= で同等
- ERT に *cross-platform parity test* (= same elisp source、x86_64 /
  arm64 両方 compile + same Sexp output) を入れる

** §6.3 FFI call resolution の external C symbol 解決

risk: =dlsym= roundtrip が exec page 起動毎に走ると overhead 大、また
mmap page から symbol address ±2GB / ±128MB を超える場合の indirection
fallback.

mitigation:
- *known-extern-c table* (= compile-time hardcode、=anvil-runtime=
  module から symbol absolute address を取得して elisp side に baked
  in、=nelisp-cc-runtime-primitive-table= の field として保持)
- range overflow 時は *trampoline indirection* (= =mov rax, abs64 +
  call rax=) に自動 fallback、emit pattern を =--lower-call-primitive=
  内で range check で分岐
- ERT に *long-range fixup test* (= mmap page を high address に
  allocate して indirection path 強制) を入れる

** §6.4 Phase 7.1.5 LOCKED 維持 (= 既 close した perf gate を破らない)

risk: Doc 81 の SSA IR 拡張 (= 4 新 opcode + recognition pass) が
既存 =fib= / =fact-iter= / =alloc-heavy= 3-軸 bench に regression 入れる.

mitigation:
- Stage 81.1〜81.4 各 stage で =make bench-actual= を *gate*.
  regression 検知 (= baseline -5% 以下) なら stage 戻し
- primitive recognition pass は *opt-in* (= existing IR は =:call=
  opcode のまま、新 =:ssa-call-primitive= opcode は明示的に migrate
  された primitive のみ)
- existing 1086/1091 ERT 全 PASS を *hard gate* (= ERT 5 件
  failing は既存、Phase 7.7 fix で着手中、新規 fail は許容しない)

* §7 Test plan

** §7.1 nelisp-cc-test.el での opcode round-trip ERT

新規 ERT 30+ 件:
- per-opcode round-trip: 4 新 opcode × (build → verify → emit → bytes
  match) = 16 件
- per-shape ABI: =:trampoline-unary= / =:trampoline-binary-ctor= /
  =:trampoline-binary-mut= × x86_64/arm64 = 6 件
- e2e exec: =car nil= / =car '(1 . 2)= / =cdr nil= / =cdr '(1 . 2)= /
  =cons 1 2= / =setcar= / =setcdr= = 7 件
- extern-c fixup: short-range / long-range / unknown-symbol-error = 3 件

** §7.2 既存 fib / fact-iter / alloc-heavy bench で性能 regression 0

=make bench-actual= で:
- fib(30): ≧ 30x bytecode = 30x gate
- fact-iter: ≧ 20x gate
- alloc-heavy: ≧ 5x gate
- 全 PASS-PASS-PASS 維持 (= Phase 7.1.5 LOCKED §11.6 actual)

** §7.3 cons-heavy bench (= 新規) で takeover 検証

=bench-actual cons-heavy= 軸新設:
- workload: =(let loop ((n 100000)) (if (zerop n) nil (cons n (loop
  (1- n)))))= 相当 elisp
- baseline: Cranelift =nl_jit_cons_*= path (= 現 build-tool/src/jit/cons.rs)
- target: nelisp-cc =:ssa-call-primitive= path (= 本 Doc 81 で実装)
- gate: nelisp-cc perf ≧ Cranelift baseline (= regression 0、+α
  acceptable)

* §8 Out of scope

** §8.1 cluster takeover 本体 (= Phase 7.1.6.a〜.f は別 Doc)

Doc 81 は *architectural extension のみ*.  実際の cluster takeover (=
Cranelift =cons.rs= / =arith.rs= / =access.rs= / =predicate.rs= /
=syscall.rs= / =dsl.rs= / =bridge.rs= 削除) は Phase 7.1.6.a〜.f で
別 PR / 別 commit.

** §8.2 jit/* 削除 (= Phase 7.1.7 別 Doc)

=build-tool/src/jit/= directory 削除 + Cranelift Cargo deps 削除は
Phase 7.1.7 (= Doc 28 §3.7、4 sub-stage).

** §8.3 Cranelift Cargo deps removal (= Phase 7.1.7.c)

=cranelift= / =cranelift-jit= / =cranelift-module= の 3 crate 削除は
Phase 7.1.7.c.  Doc 81 単独では *build-tool は build 通る* (= 81.x は
elisp のみ).

* §9 References

- Doc 28 §3.6.a (= Phase 7.1.6.a =cons= takeover articulation)
- Doc 28 §11.6 (= Phase 7.1.5 LOCKED、Track S audit 2026-05-09)
- Doc 77c §2.4.2 (= Path D-B、100% 純 elisp commitment)
- =src/nelisp-cc.el= line 121-280 (= SSA IR core、=cl-defstruct
  nelisp-cc--ssa-instr=)
- =src/nelisp-cc-x86_64.el= line 1984 (= =--lower-instr= dispatch)
- =src/nelisp-cc-arm64.el= line 1111 (= =--lower-instr= dispatch)
- =src/nelisp-cc-runtime.el= line 1336 (= =compile-and-allocate=
  entry point)
- =build-tool/src/jit/cons.rs= line 150-249 (=
  =declare_unary_with_nil_inline= = takeover 対象 trampoline shape)
- =build-tool/src/jit/bridge.rs= line 60-130 (= =unified_fn_ptr=
  symbol resolver)
- =build-tool/src/eval/sexp.rs= line 217-229 (= =SEXP_TAG_*= constants)
- =build-tool/src/eval/sexp.rs= line 270-339 (= =SEXP_PAYLOAD_OFFSET=
  + =cons_box_ptr()= helpers)
- Track O 2026-05-09 audit (= Blocker 2 articulation、本 Doc 81 起草の
  trigger)
- Track N 2026-05-09 audit (= 4 cluster 並行 dispatch articulation、
  =nelisp-cc-runtime-compile-and-allocate= entry-point identification)

* §10 Refactor history

** v1 (2026-05-09 DRAFT)

初稿、Track O Blocker 2 + Track N §3.6 articulation を統合.
Doc 28 §11.6 (Phase 7.1.5 LOCKED) を依存 SHIPPED として明示.
Stage 81.1〜81.4 4 段階 = 8 営業日 plan.  elisp +780 LOC / Rust ±0、
Phase 7.1.6.a 同時 merge で monotonic shrink rule 担保.

** v2 (2026-05-09 — Stage 81.x sub-stage detail expansion)

§5.1〜§5.4 を Track U/V/W/Y と同 detail level (= per-stage ABI / opcode /
backend emit / pipeline / bench gate を pseudo-code + sub-task breakdown
付き) に articulate.  各 stage に §5.x.detail サブセクションを追加:

- §5.1.detail Stage 81.1 (entry-ABI + 3 opcode + x86_64 + car PoC, 6 sub-section, 4 sub-task)
- §5.2.detail Stage 81.2 (full opcode + arm64 + 5 cons primitives, 5 sub-section, 3 sub-task)
- §5.3.detail Stage 81.3 (pipeline integration + ERT, 5 sub-section, 3 sub-task)
- §5.4.detail Stage 81.4 (cons-heavy bench gate, 5 sub-section, 2 sub-task)

aggregate Doc 81 expansion: +780 elisp / 0 Rust 維持.  critical IR design
decision = =ssa-load-tag= return type を *unsigned i64* に固定 (§5.1.2).
x86_64 vs arm64 design symmetry を §5.2.2 で 4 opcode × 2 ISA 表で
articulate.  Phase 7.1.6.a (= cons takeover、-415 Rust) と batched merge
で per-commit shrink rule satisfy.

#+TITLE: Doc 86 — Phase 2: eval/ refactor master design
#+AUTHOR: NeLisp design
#+DATE: 2026-05-10

* Status

- *State*: PARTIAL SHIPPED v2 (= §86.2 + §86.1.a SHIPPED 2026-05-10、
  Doc 87 / 88 / 89 detail design DRAFT SHIPPED、v2 reconcile 2026-05-10
  with Doc 87/88/89 actual findings — see §13)
- *Parent*: Doc 83 §6.2 (= Phase 2 着地 ~6,000-8,000 LOC 試算)、Doc 84
  SHIPPED (= Phase 1 末端 closure、strategy.rs 全消滅)、Doc 80 SHIPPED
  (= pre-stdlib substrate)
- *Children*: Doc 87 (= §86.1 detail)、Doc 88 (= §86.2 detail)、Doc 89
  (= §86.3 detail)、これら 3 件で Doc 86 の estimate を実測 reconcile
- *Unlocks*: Phase 2 全 stage SHIPPED 後、build-tool/src/ Rust LOC が
  21,796 → ~6,000-8,000 (= -13,000〜-15,000 net 削減)、Phase 3 (= 究極
  D-B 採用、Doc 77c 系列) へ進める
- *OOS for THIS doc*: 実装作業 (= 各 sub-stage doc で個別 dispatch)、
  Phase 3 究極 D-B (= Doc 77c master plan)、Phase 47 (= Doc 85 spike +
  将来 Doc 91-95)

* §0 TL;DR

| 領域 | 現 LOC | v1 試算 | v2 reconciled (Doc 87/88/89) | sub-stage doc |
|---------------------------+--------+----------+----------+---------------|
| =eval/builtins.rs=        | 3,804  | -2,000〜-3,000 | **-1,571** (Doc 87) | §86.1 / Doc 87 |
| =eval/error.rs=           | 308    | -308 (= 全消滅) | -113 net SHIPPED + 残 deferred to §86.3 | §86.2 / Doc 88 |
| =eval/env.rs= + 連動      | 564 + ~700 | -1,500 (= 連動含む) | **-1,214** (Doc 89, ffi.rs 0 連動 corrected) | §86.3 / Doc 89 |
| *合計 (Phase 2 着地)*     | 21,796 → ~17,000-19,000 | *-3,000〜-5,000 net* | ***~-3,043 net*** *(= 21,796 → ~18,753)* | — |

Doc 83 §6.2 が示した「Phase 2 着地 ~6,000-8,000 LOC」は **dispatch arm
移管の最大効果 (= -3,000) + 連動削減 (= -1,500) + Phase 3 D-B 連動 (=
-9,500)** を合算した値。本 Doc 86 は **Phase 2 単独で実現する範囲 (=
-3,000〜-5,000)** に focus、残 -9,500 は Phase 3 (D-B) で別途 attack。

*v2 reconcile (2026-05-10)*: Doc 87/88/89 detail design 起こしで判明した
実測値は v1 estimate を **下振れ** (= -3,043 net、v1 lower bound -3,000
にギリギリ届く水準)。理由は §13 で articulate。Doc 86 master を読む
reader は §0 / §1.2 / §10.1 表の reconciled 列を canonical とすべき。

実装順序: §86.1 (= 最大効果) → §86.2 (= 単純) → §86.3 (= 最 risky =
self-reference 注意)。並列性は限定的 (= §86.1 と §86.3 が同 builtins/env
の interaction を持つので半並列)。

* §1 Phase 2 全体着地点

** §1.1 Doc 83 §6.2 試算の再検証

Doc 83 §6.2 (2026-05-09 執筆) が提示した値:

- =builtins.rs= dispatch arm 全 elisp 移管 = -2,000〜-3,000 LOC
- =error.rs= を =nelisp-stdlib-error.el= に移管 = -500 LOC
- =env.rs= scope chain elisp 化 = -1,500 LOC

実態 (2026-05-10 grep):

| 領域           | Doc 83 試算 | 実測  | 差分                         |
|----------------+-------------+-------+------------------------------|
| =builtins.rs=  | (= 削減 -2,000〜-3,000) | 3,804 LOC 全体 | OK、削減上限は 3,804 |
| =error.rs=     | -500        | 308   | -192 (= Doc 83 試算過大、実態は -308 が上限) |
| =env.rs=       | (= 削減 -1,500、連動含む) | 564 LOC 単独 | env.rs 単独で -564、残 -936 は連動先 |

*** Doc 83 §6.2 着地 ~6,000-8,000 LOC の内訳推定

Doc 83 §6.2 表で「Phase 2 着地」が ~6,000-8,000 とされていたが、現 21,796
からの削減試算合計 (-3,000+-308+-564 = -3,872) では ~17,924 にしか着地
しない。差分 ~10,000-12,000 LOC は **Phase 3 (= 究極 D-B) で連動削減**
される領域 (= =eval/sexp.rs= 979 + =eval/ffi.rs= 820 + =eval/mod.rs=
848 + =eval/special_forms.rs= 572 + =eval/ffi.rs= 820 + =eval/nlconsbox.rs=
654 + =eval/nlrc.rs= 566 + =eval/rc_primitives.rs= 464 + =image.rs= 1,503
+ =reader/= 1,370 + =bridge.rs= 884 + =jit/= 残 etc.)。

つまり Doc 86 (Phase 2) 単独では ~17,000-19,000 LOC 着地が現実的。
Doc 83 §6.2 の "~6,000-8,000" は Phase 2 + Phase 3 の合算値と解釈すべき。

** §1.2 Phase 2 単独着地点定義

| sub-stage | scope                          | v1 試算 | v2 reconciled (Doc 87/88/89) | 着地 (累積)  |
|-----------+--------------------------------+---------+----------+--------------|
| §86.1.a (SHIPPED 2026-05-10) | Tier 1 predicate / type-of (= 3 arm: type-of / functionp / recordp) | -300 (estimate, see §12.1) | **-34 net actual** (= 既存 batch 6u で 8 arm 先行移管済) | 21,762 |
| §86.1 残 (Doc 87 articulated) | §86.1.b/c/d (Tier 1 残 17 arm) + e/f (Tier 2 13 arm) + g (install_builtins / dispatch trim) | -2,000〜-3,000 | **-1,537 actual remaining** (= -1,571 total - 34 SHIPPED) | 20,225 (projected) |
| §86.2 (SHIPPED 2026-05-10) | error.rs → nelisp-stdlib-error.el | -258〜-278 | **-113 net actual** (= elisp registry 確立 + compact、full variant elision を §86.3 へ deferred) | 21,683 → 21,649 (post §86.1.a) |
| §86.3 (Doc 89 articulated) | env.rs scope chain + 連動 (mod.rs / special_forms.rs / builtins.rs Tier 3、ffi.rs 0) | -1,500 | **-1,214 actual** (= ffi.rs 連動 -100 → 0 corrected by Doc 89 grep) | 18,435 (projected) |
| *合計* | *Phase 2 着地* | *-3,605〜-4,605* | ***~-3,043*** *actual* | ***~18,753 (projected)*** |

Phase 2 reconciled landing は **build-tool/src/ 18,753 LOC** (= 21,796
基線から -3,043 net)、Phase 1 完遂時 21,796 → Phase 2 完遂時 ~18,753 →
Phase 3 D-B 完遂時 ~6,000-8,000 (Doc 83 §6.3 終点) → Phase 47 完遂時 ~0
(Doc 85 spike 終点)。

* §2 builtins.rs 解剖 (= 3,804 LOC, 17.5% of build-tool/src/)

** §2.1 構造

- =install_builtins(env: &mut Env)= line 35 - global env に builtin
  symbol を install する 1 関数 (= 約 420 LOC)
- =dispatch(name: &str, args: &[Sexp], env: &mut Env)= line 458 -
  builtin call 時の large match expression (= ~390 LOC、67 dispatch
  arm)
- =bi_*= helper fns line 850-3,800 - 各 builtin の Rust 実装 (= ~25
  個、各 ~50-200 LOC)

** §2.2 dispatch arm 67 件の categorization

source-grep で発見した 67 個 (= =^\s*"nelisp--= count) の dispatch
arm を Tier 別に分類:

*** §2.2.1 Tier 1: pure elisp 移管可 (= 推定 ~30 arm)

新 ABI mode 不要、既存 =nl-jit-call-out-*= bridge で対応可能、または
elisp で直接実装可能:

- 算術系 (= div / sxhash / equal / ref-eq) ← Doc 84 §84.1 同 pattern
- predicate (= integerp / floatp / stringp / consp / etc.)
- type 系 (= type-of)
- record 系 (= make-record / record-ref / record-set / record-length /
  record-type / recordp)
- intern / make-symbol / symbol-name
- 単純な string 系 (= string-bytes)

*** §2.2.2 Tier 2: 新 ABI mode + bridge primitive 必要 (= 推定 ~25 arm)

Doc 81 trampoline ABI を拡張する必要、または特殊 calling convention:

- =bi_make_mut_string= (= 可変長 byte array)
- =bi_concat_ints= (= variadic byte stream)
- =bi_string_match_p= (= regex match、外部 backend 必要)
- =bi_syscall_canonicalize= (= path normalize、syscall 連携)
- ffi 系 (= libc bridge 経由)

*** §2.2.3 Tier 3: 環境への副作用が深い (= 推定 ~12 arm)

=Env= 内部 state を直接読み書き、§86.3 (= env.rs elisp 化) と統合する
必要:

- special form 関連 (= setq / set / boundp / unbound)
- scope chain manipulation
- dynamic binding stack push/pop

** §2.3 helper fns 削減先

=bi_*= helper fn (= 25 個程度) のうち:
- Tier 1 の bi_* = elisp 直接実装、Rust 関数本体ごと削除
- Tier 2 の bi_* = trampoline 経由で elisp wrapper、Rust shim は ~10
  LOC まで縮減
- Tier 3 の bi_* = §86.3 完了まで残置

平均削減: 1 helper あたり ~50-100 LOC、25 個 = -1,500〜-2,500 LOC。

** §2.4 install_builtins 削減

420 LOC の install_builtins は dispatch arm 数に比例。67 arm のうち:
- Tier 1 移管時: -30 entries × 6 LOC/entry = -180
- Tier 2 移管時: -25 entries × 4 LOC/entry = -100 (= bridge entries 残)
- Tier 3 移管時: -12 entries × 6 LOC/entry = -72

合計 install_builtins 削減: -350 LOC、残 ~70 LOC。

** §2.5 dispatch 関数 削減

390 LOC の dispatch match expression は arm 数 × 5-8 LOC。67 arm 全消滅
で -390 LOC。残るのは関数 signature + comment ~30 LOC。

* §3 error.rs 解剖 (= 308 LOC)

** §3.1 構造

- =SourcePos= struct (line 32-44)
- =ReadError= enum (line 49-101) — 4 variants:
  - =Lex { msg, pos }=
  - =Parse { msg, pos }=
  - =UnexpectedEof { msg, pos }=
  - =NotYetImplemented { feature, pos }=
- =EvalError= enum (推定 line 100-220) — variants:
  - =Internal=, =WrongType=, =ArithError=, =UnboundSymbol=,
    =UndefinedFunction=, =VoidVariable=, =ApplyToNonClosure=, etc.
- =Display= / =From= / helper fn implementations

** §3.2 elisp signal contract への移管

elisp 側には既に =signal= / =error= / =wrong-type-argument= /
=void-variable= 等の condition system が存在 (= Doc 80 pre-stdlib
substrate で Stage 80.1〜80.5 で establish 済)。

移管 surface:
- Rust =EvalError::WrongType= → elisp =(signal 'wrong-type-argument
  args)=
- Rust =EvalError::ArithError= → elisp =(signal 'arith-error args)=
- Rust =ReadError::Lex= → elisp =(signal 'invalid-read-syntax args)=
- etc.

eval loop 内部で =Result<Sexp, EvalError>= を扱う Rust 部分を、elisp の
=condition-case= / =unwind-protect= に切り替える。

** §3.3 nelisp-stdlib-error.el (= 新規作成)

新 file =lisp/nelisp-stdlib-error.el= (= 推定 ~250 elisp LOC):
- condition symbol の =put 'name 'error-conditions= form
- signal data structure の elisp accessor
- =error-message-string=, =define-error= macro

Rust 側に残るのは =EvalError::Internal= 1 variant (= elisp で recover
不能な panic 等価)。残 ~30-50 LOC。

** §3.4 削減試算

- =error.rs= 308 LOC → 30-50 LOC = **-258〜-278 削減**
- 新規 elisp = +250 LOC

net Rust = -258〜-278。

* §4 env.rs 解剖 (= 564 LOC)

** §4.1 構造

- =SymbolEntry= struct (line 47, ~40 LOC) - per-symbol value + plist +
  function slot
- =Env= struct (line 92, ~60 LOC) - global symbol table + lexical
  scope stack + special variable bindings
- impl Env (= 推定 line 150-560) - lookup / set / push-scope /
  pop-scope / bind-special / etc.

** §4.2 elisp 化の工法

Env / SymbolEntry を elisp 側で表現するには:

*** §4.2.1 option A: alist + hash table

- 全 global symbol = =nelisp--global-env= hash-table
- lexical scope stack = elisp =let= form の native stack 利用
- special binding = elisp =defvar= / =setq-default= 利用

trade-off: elisp の native scoping を全活用 = elegant だが、Rust 側で
保持していた C-level efficient lookup を失う

*** §4.2.2 option B: payload pointer 経由 (= Doc 81 ABI mode 拡張)

- Env / SymbolEntry の memory layout を pin
- elisp が payload pointer 経由で直接アクセス
- Tier 1/2 と互換性高い

trade-off: Rust 側に layout pin が残る、純 elisp 度が落ちる

*** §4.2.3 推奨

option A を base、Tier 2 builtin が hot path として hit する symbol
lookup のみ option B (= cache 構造)。

** §4.3 連動削減

env.rs を elisp 化すると、env.rs に依存する周辺 file も連動して縮減:

| file                       | 現 LOC | 削減試算 | 理由                  |
|----------------------------+--------+----------+-----------------------|
| =eval/mod.rs=              | 848    | -200     | dispatch entry の elisp 化で連動 |
| =eval/special_forms.rs=    | 572    | -300     | setq / let / lambda の env 操作が elisp 側に移る |
| =eval/ffi.rs=              | 820    | -100     | env-aware ffi marshalling 縮減 |
| =eval/builtins.rs= 残      | (+86.1 後) | -200 | Tier 3 dispatch arm の最終消滅 |
| *連動小計*                 | 2,240+  | *-800*   | (= env.rs 単独 -564 + 連動 -800 = -1,364〜-1,500) |

** §4.4 削減試算

- =env.rs= 564 LOC → ~50 LOC stub = **-514**
- 連動: 上記 4 file で -800
- 新規 elisp = +700 LOC (= scope chain + lookup + bind helper)

net Rust = -1,314。

* §5 §86.1 builtins dispatch arm migration design

** §5.1 scope (= v2 reconciled with Doc 87)

builtins.rs 3,804 LOC のうち、Doc 87 が **132 dispatch arm** (= v1
"67 arm" の grep が install_builtins entry を conflate していた、Doc
87 §1.6 articulated) を inventory + Tier 分類:

| Tier      | v1 estimate | v2 actual (Doc 87) | 移管先              |
|-----------+-------------+--------------------+---------------------|
| Tier 1    | ~30 arm     | **21 arm**         | §86.1.a-d           |
| Tier 2    | ~25 arm     | **13 arm**         | §86.1.e-f           |
| Tier 3    | ~12 arm     | **22 arm**         | §86.3 hand-off (※ Doc 89 §10.7 で §86.3.c canonical / §86.1 Tier 3 cleanup と articulated) |
| KEEP      | (= unspec)  | **76 arm**         | FFI / system / bridge — 移管しない |
| *合計*    | *~67*       | ***132 arm***      | —                   |

§86.1 単独 削減目標 (v2): **-1,571 Rust net** (= elisp 移管 + 新規
+700 elisp 投資)、Doc 87 §11.3 で articulated。新 ABI mode 必要数は
**0-2 個** (= v1 §2.2.2 が想定したより less aggressive、`:trampoline-
unary-float` + optional `:trampoline-format-float` のみ)。

** §5.2 sub-stage 分割

| sub-stage | scope                         | 工数  | LOC delta |
|-----------+-------------------------------+-------+-----------|
| §86.1.a   | Tier 1 predicate / type-of 移管 (= SHIPPED 2026-05-10、actual -34 net + ~30 elisp) | 2 day | -300 (est) / -34 (actual) |
| §86.1.b   | Tier 1 算術 / equal / ref-eq 移管 | 3 day | -500      |
| §86.1.c   | Tier 1 record 系 移管         | 2 day | -400      |
| §86.1.d   | Tier 1 intern / symbol 移管   | 2 day | -300      |
| §86.1.e   | Tier 2 簡単な arm 移管 (= concat / string-bytes) | 3 day | -400 |
| §86.1.f   | Tier 2 trampoline ABI 拡張 + 残 arm | 4 day | -800 |
| §86.1.g   | install_builtins / dispatch 縮減 | 2 day | -300 |

合計工数: ~18 day、削減 -3,000 LOC、新規 elisp +700。

** §5.3 ERT

- 各 arm の round-trip test (= elisp wrapper → trampoline → result)
- 既存 builtin の callers (= test/ ディレクトリ全体) は無変更で動く
  ことを verify
- perf regression check (= Tier 1/2 移管後の hot path latency)

** §5.4 sub-stage 並列性

§86.1.a-d は独立 = 並列 dispatch 可能、§86.1.e-f は ABI 拡張共有 = 半
並列、§86.1.g は最後に直列。

* §6 §86.2 error.rs migration design

:STATE: SHIPPED 2026-05-10 (= 2 commit、precursor + consumer atomic-with-consumer pattern)
:SHIP_COMMITS: =2532ff04= (precursor: +lisp/nelisp-stdlib-error.el + baker/env wiring + error.rs comment trim、-27 net Rust LOC) → =58911b13= (consumer: error.rs full compact、-78 net Rust LOC) = aggregate **-105 net Rust LOC**
:UNLOCKS: §86.1 (= builtins dispatch arm migration、本 §86.2 と並列実装も可能だが §86.2 先行で error registry が elisp 側に存在する形が cleaner)

** §6.1 scope

- =EvalError= variants (= 12 個) の error_tag / signal_data / Display
  / is_error_subtype 経路を維持、elisp 側に condition registry +
  define-error + accessors を新設
- =ReadError= variants (= 4 個) → =EvalError::Read= 経由で
  =invalid-read-syntax= に流れる経路を維持
- =build-tool/src/eval/error.rs= の compact 化 (= 308 LOC → 193 LOC)
- 新規 =lisp/nelisp-stdlib-error.el= (= 158 LOC elisp)

** §6.2 工数 (SHIPPED 実測)

- 1 sub-stage で完結、0.5 日 atomic ship
- planned: 削減 -258〜-278、新規 elisp +250
- shipped: 削減 -105 net Rust、新規 elisp +158

planned -258 と shipped -105 の差は **error.rs full architectural
elision (= EvalError 全 variant 削除 + Rust→elisp signal bridge 再配線)
を §86.2 single-stage では yield しなかった** ためで、これは構造的に
適切な判断:

- Rust 側 EvalError は =Result<Sexp, EvalError>= 型として 282 callsite
  で control-flow に使われている (= 全 builtins.rs / ffi.rs / mod.rs
  にまたがる)。これら全て elisp signal 経由に組み替えるには env.rs +
  builtins.rs の eval loop 自体の書き換えを伴い、§86.3 (= env.rs
  scope chain elisp 化) と統合した方が atomic性 / regression 検出が
  両立する
- §86.2 の ROI 試算 (= 0.95:1 = ほぼ 1:1) は他 sub-stage と比べ最低、
  full elision を狙うより compact 化 + elisp registry 確立で stop し
  §86.1 (= 0.27:1) / §86.3 (= 0.53:1) の効率投資に資源を回すべき
- elisp 側 condition registry が SHIPPED で確立されたことが §86.1 /
  §86.3 の precondition で、その goal は achieved (= 本 §86.2 の真の
  unlock)

planned 数値 -258 は次の機会 (= §86.3 完遂後に env.rs callsite が
elisp 側で signal を発行できるようになった時) に追加 trim で達成可。

** §6.3 risk

- elisp condition system (= signal / condition-case) が Doc 80
  pre-stdlib substrate に既存、互換性確認のみ — *✅ confirmed*
  =bi_signal' (= eval/builtins.rs:2535) は既に
  arith-error / wrong-type-argument tag を canonical EvalError variant
  に逆変換する path 持ち、互換性問題なし
- backtrace info の loss possibility = 各 signal で SourcePos を
  elisp data として運ぶ必要 — *✅ confirmed*  =SourcePos= struct +
  =EvalError::Read.pos()= 経路は trim 後も unchanged、=ReadError=
  variant 4 件全てで pos accessor が live、reader → eval 間の
  positional info は失われない
- Display impl 簡略化により bridge/loader.rs / bridge/takeover.rs の
  =format!("line {} in {}: {}", ...)' 出力 message が変化する可能性
  — *✅ confirmed* compact 後の Display arms は元の 1:1 文言を保持、
  ERT 2706 全 expected pass

* §7 §86.3 env.rs scope chain elisp 化

** §7.1 scope

- =Env= struct を elisp =nelisp--global-env= hash-table に置換
- =SymbolEntry= struct を elisp plist (= =(:value V :function F :plist
  P)=) に置換
- lexical scope stack を elisp native =let= で実現
- special binding stack を elisp =defvar= で実現
- Tier 3 dispatch arm (= 12 個) を §86.1 完了後に統合

** §7.2 elisp implementation strategy

3-step implementation:

1. *§86.3.a precursor*: =nelisp--global-env= hash-table 作成、空 stub
   (Rust の Env と coexist)
2. *§86.3.b shadow path*: elisp 側で env-lookup を実装、Rust の Env と
   同期 (= dual-path、performance 検証用)
3. *§86.3.c switch-over*: Rust Env 削除、全 lookup を elisp に switch、
   連動 file の env reference を elisp 化

** §7.3 連動削減 (= v2 reconciled with Doc 89)

| file                       | v1 estimate | v2 actual (Doc 89 grep) | 理由 |
|----------------------------+-------------+-------------------------+------|
| =eval/mod.rs=              | -200        | -200                    | bind_formals + apply_lambda_inner + init を elisp 経由に書き換え |
| =eval/special_forms.rs=    | -300        | -300                    | parse_let_binding + sf_let/let* + sf_setq + sf_lambda + sf_condition_case + helpers |
| =eval/ffi.rs=              | -100        | **0** (corrected)       | Doc 89 grep で env callsite 0 確認、v1 estimate 完全に misestimate |
| =eval/builtins.rs= 残 Tier 3 | -200      | -200                    | 22 arm body + dispatch arm 削除 (= v1 では Tier 3 = 12 arm 想定だったが Doc 87 で 22 arm に correct) |
| *連動小計*                 | *-800*      | ***-700***              | (= env.rs 単独 -514 + 連動 -700 = total -1,214) |

Doc 89 §10.5 の追加 finding: ffi.rs が =Env= を全く参照していない実態は
v1 design 時の visual estimation だけで -100 LOC を見積もった結果の
誤り。v2 では grep で実測。

** §7.4 工数

- §86.3.a precursor: 3 day
- §86.3.b shadow path: 5 day
- §86.3.c switch-over: 5 day
- 合計: ~13 day

** §7.5 risk (= self-reference / infinite loop) — v2 reconciled

elisp が自身の env を実装すると、=defvar= macro 自体の expand 中に env
lookup が必要 = chicken-and-egg。

Doc 89 推奨 solution: ***Option C (Hybrid)***
- *Tier 0 permanent Rust stub* (= ~50 LOC): =Env::globals= HashMap を
  canonical に保持 (image.rs wire-format compat + extern_builtins +
  recursion counter)
- *11 slim primitives* (= ~80 LOC Rust): globals lookup/set を
  trampoline 経由で elisp 露出
- *elisp shim* (= =lisp/nelisp-stdlib-env-shim.el=、~700 LOC): user-
  visible API + global-env mirror

Bootstrap order: substrate → syscall-table → jit-strategy → **NEW
env-shim** → eval-special → 残。shim load 中は Tier 0 path 経由で自己
install (= no self-call)、その後 dual-update activates。

Chicken-and-egg risk評価: **MEDIUM, MITIGATED** (Doc 89 §6 articulated)。
hot-path perf regression は Rust 内 callsite を Tier 0 method に保持、
shim は user-visible API のみで mitigate。image.rs wire format は Rust
globals が canonical なので Phase 2 中 unchanged。

* §8 sub-stage 順序 / 並列性

** §8.1 dependency graph

#+begin_src text
                 [Doc 84 SHIPPED]
                       |
                       v
        +------[§86.2 error.rs]-------+
        |                              |
        v                              |
   [§86.1 builtins (Tier 1+2)]         |
        |                              |
        v                              |
        +-----[§86.1 builtins Tier 3]--+
                       |
                       v
              [§86.3 env.rs]
                       |
                       v
              [§86.3 連動削減]
                       |
                       v
              [Phase 2 着地]
#+end_src

並列度: §86.2 と §86.1 (= Tier 1/2) は並列可能。§86.3 は §86.1 (=
Tier 3) の後。最大 2 stream 同時。

** §8.2 milestone

- M1: §86.2 SHIPPED 2026-05-10 (=2532ff04= + =58911b13=) = error.rs compact + elisp condition registry 確立 (-105 net Rust)
- M2: §86.1 Tier 1 SHIPPED = pure elisp 直接移管 30 arm = -1,500
- M3: §86.1 Tier 2 SHIPPED = trampoline 経由 25 arm = -800
- M4: §86.1.g SHIPPED = install_builtins / dispatch 縮減 = -300
- M5: §86.3 SHIPPED = env.rs elisp 化 + 連動削減 = -1,500
- *Phase 2 完遂*: build-tool/src/ ~17,000-19,000 LOC

期間試算: ~2-3 ヶ月 (= 2 並列 stream で 2 ヶ月、1 並列で 3 ヶ月)

* §9 Risk / unknowns

** §9.1 perf regression

builtin dispatch の hot path が elisp に移ると、interpreter overhead +
ABI marshalling cost が積もる。Tier 1 移管前後で micro-bench 必要。

対策:
- nelisp-cc の AOT compile で Tier 1 elisp wrapper も native code 化
- =:trampoline-*= ABI mode 経由で marshalling overhead を最小化
- bench gate (= fib 30x perf) に Tier 1 wrapper を含む

** §9.2 type marshalling overhead

Sexp の box accessor を elisp 経由で叩くと、Rust 直接 access に対して
~10x overhead の可能性。Doc 84 §84.3 で box accessor を elisp 化した
pattern を流用。

** §9.3 Tier 3 → §86.3 cycle

builtins Tier 3 (= 12 arm、env 操作系) を §86.1 で elisp 化しようと
すると env.rs の elisp 化が precondition だが、env.rs elisp 化には
Tier 1 の minimal elisp infrastructure が precondition = chicken-and-egg。

解決策:
- §86.3.a precursor で minimal env を elisp 側に確立
- §86.1 Tier 3 は §86.3.a 完了後に着手 (= 直列依存)

** §9.4 Doc 83 §6.2 試算との乖離

Doc 83 が示した「Phase 2 着地 ~6,000-8,000」は Phase 3 連動削減を含む
値。本 Doc 86 は Phase 2 単独 = ~17,000-19,000 着地。差分 ~10,000 は
Phase 3 (= 究極 D-B / Doc 77c) で別途削減する旨を Doc 83 §6.2 に追記
する update が必要。

** §9.5 ERT coverage

builtins.rs 3,804 LOC の全 arm が elisp 化されると、現 test suite (=
=test/= 配下 ~2,700 ERT) が引き続き green を保つこと、新 elisp wrapper
の round-trip ERT を追加することの両方が必要。

* §10 ROI 試算

** §10.1 LOC 投資 vs 削減 — v2 reconciled

| sub-stage | elisp 投資 | v1 Rust 試算 | v2 Rust actual (Doc 87/88/89) | v2 比率 (elisp:Rust) |
|-----------+------------+--------------+-------------------------------+---------------------|
| §86.1     | ~700       | -2,000〜-3,000 | **-1,571** (Doc 87)         | 0.45:1 (= 良)        |
| §86.2     | +179 (= shipped) | -258〜-278 | **-113** SHIPPED + 残 deferred to §86.3 | (= shipped: 1.58:1; full: 0.95:1 with §86.3 consolidation) |
| §86.3     | ~700       | -1,314       | **-1,214** (Doc 89, ffi.rs 0 corrected) | 0.58:1 (= 良)       |
| *合計*    | *~1,579*   | *-3,572〜-4,592* | ***~-2,898 net*** (= -1,571 + -113 + -1,214) | ***~0.55:1*** (= 1 elisp で 1.8 Rust、当初 0.36-0.46 estimate より下振れ) |

*Phase 2 ROI v2*: **1 elisp 投資で 1.8 Rust 削減** = Phase 1 (= 1.5:1)
と同等水準。当初 estimate 「2-3 Rust 削減」は v1 designer の
optimistic bias、Doc 87/88/89 grep で reconciled。

** §10.2 Phase 1 / Phase 47 比較

| Phase    | 投資 ratio | 期間試算  |
|----------+------------+-----------|
| Phase 1 (= Doc 73-84) | 1.5:1 | ~3 ヶ月 (= 完了) |
| *Phase 2 (= Doc 86)*  | *0.36-0.46:1* | *~2-3 ヶ月* |
| Phase 3 (= Doc 77c D-B) | (= 別 ROI 試算) | ~3 ヶ月 |
| Phase 47 (= Doc 85 spike) | 0.5:1 | ~6-9 ヶ月 (= 保留) |

Phase 2 は ROI 最高 = 並列 dispatch で短期に大削減。

* §11 References

** internal NeLisp design docs

- Doc 73-84 (= Phase 1 全 SHIPPED)
- Doc 80 (= elisp-pre-stdlib-substrate.org) — pre-stdlib elisp infra
  (= signal / condition / cond / not / etc.)、本 Doc 86 §86.2 / §86.3
  の precondition
- Doc 81 (= nelisp-cc-primitive-trampoline-extension.org) — ABI mode
  pattern (= §86.1 Tier 2 拡張で active 利用)
- Doc 83 (= 100pct-pure-elisp-roadmap-consolidated.org) — 本 Doc 86 の
  parent、§6.2 で Phase 2 試算を articulated
- Doc 84 (= strategy-rs-final-elisp-ization.org SHIPPED) — Phase 1
  末端、=strategy.rs= 全消滅 + Doc 80 §7 OOS unlock
- Doc 85 (= phase-47-feasibility-spike.org) — 究極 0 Rust 路線 spike
  (= Phase 2 後、Phase 3 後に再評価)

** 派生候補 design doc (= 本 Doc 86 ship 中に起こす)

- Doc 87 = §86.1 builtins dispatch arm migration detailed (= 約 18
  day 工程の sub-stage articulation)
- Doc 88 = §86.2 error.rs migration detailed (= 2-3 day)
- Doc 89 = §86.3 env.rs scope chain elisp 化 detailed (= ~13 day)
- Doc 90 = Phase 2 連動削減 audit (= mod.rs / special_forms.rs /
  ffi.rs の env reference 削減 articulation)

(注: Doc 85 §9 は元々 "Phase 47 候補 = Doc 86-90" を予約していたが、
Phase 2 が時系列的に先に来るため Phase 47 候補を Doc 91-95 に renumber
する。本 commit で Doc 85 §9 を update。)

** 関連 memory

- =project_anvil_runtime_phase_b6_shipped= (= NeLisp standalone MCP)
- =feedback_nelisp_runtime_vs_elisp_language= (= NeLisp = runtime、
  elisp = 構文)

** external prior art

- SBCL Common Lisp = builtin の多くが Common Lisp 自身で書かれた pattern
- GHC = Haskell builtin の Haskell 移管 pattern
- Emacs = many subr (Rust 側 builtin 等価) を elisp lib で wrap する
  pattern

* §12 SHIPPED status

** §12.1 §86.1.a — Tier 1 predicate / type-of (SHIPPED 2026-05-10)

- =functionp= (= =bi_predicate= closure arm) → elisp =(and (consp x)
  (memq (car x) '(lambda closure builtin)) t)= in
  =lisp/nelisp-stdlib.el=
- =recordp= (= =bi_recordp=) → elisp =condition-case= around the
  existing =nelisp--record-type= primitive (signals
  =wrong-type-argument= on non-records, returns the type_tag symbol
  on records) in =lisp/nelisp-stdlib.el=
- =type-of= (= =bi_type_of=) → JIT trampoline =nl_jit_type_of=
  (=build-tool/src/jit/predicate.rs=) reachable via
  =(nl-jit-call-out-1 "nelisp_jit_type_of" x)= from
  =lisp/nelisp-jit-strategy.el= (installed FIRST so subsequent
  strategy wrappers see the elisp version)
- =bi_predicate= helper deleted (= sole caller was =functionp=)

LOC delta (= actual):
- =build-tool/src/= : -34 net (= 21,796 → 21,762).  =bi_type_of= /
  =bi_predicate= / =bi_recordp= helpers (-70 LOC), dispatch arms +
  install_builtins entries (-15 LOC), comments trimmed (-15 LOC).
  Trampoline + bridge wiring (+45 LOC) + new comments (+21 LOC).
- =lisp/nelisp-stdlib.el= : +24 elisp LOC (functionp + recordp).
- =lisp/nelisp-jit-strategy.el= : +18 elisp LOC (type-of fset + comments).

Estimate vs actual: spec said -300 net Rust; actual is -34.  The
gap is because the pre-Doc-86 baseline had already migrated 8 of
the 10 Tier 1 predicate arms (consp / listp / symbolp / stringp /
numberp / integerp / floatp / vectorp) in batch 6u (2026-05-06) on
top of the existing =bi_type_of= primitive.  §86.1.a as written in
§5.2 predicted ~10 arms × 30 LOC = -300; in reality only 3 arms
(=type-of / functionp / recordp=) remained for §86.1.a, and the
=type-of= trampoline body is structurally identical to =bi_type_of=
(= same Sexp variant → tag mapping logic), so no body-level savings.

The conceptual win is full elisp dispatch for the predicate family —
a downstream user can now =(advice-add 'type-of ...)= or shadow with
=fset= the same way they would in host Emacs; the primitive surface
is exactly =nl_jit_type_of=.

Verification:
- =cargo build --workspace --release --features image-baker= — green
- =cargo test --workspace --release --lib -- --test-threads=1= —
  706 passed / 0 failed (= same as baseline)
- =make test= — 1 pre-existing flake (=apply-fn-dispatches-to-
  builtin=) verified identical on baseline =feat/doc-77-stage2-prep=

Commits (= 2 atomic):
1. =4192489d= — functionp / recordp migration (-17 Rust)
2. =2d2f116f= — type-of migration via JIT trampoline (-17 Rust)

** §12.2 §86.2 — error.rs migration (SHIPPED 2026-05-10)

詳細 design metadata は §6 (= error.rs migration design SHIPPED status
block) に記載。本節は §12 SHIPPED status の sibling normalization。

LOC delta (= actual):
- =build-tool/src/= : -113 net (= 21,796 → 21,683)
- =error.rs= 内: 308 → 185 LOC (-123 inside file)
- =lisp/nelisp-stdlib-error.el= NEW : +179 elisp LOC + 7,654 bytes baked

Estimate vs actual: spec said -258〜-278 net Rust; actual is -113 net.
The gap is intentional architectural choice: full =EvalError= variant
elision requires 282 callsite refactor across all eval/, which is
structurally a §86.3 concern (= eval-loop's =Result<_, EvalError>=
type touched by env.rs migration).  §86.2 stops at compact-rewrite +
elisp condition registry establishment, which is the **real unlock**
for §86.1 / §86.3 to land on top.  Remaining ~-145 LOC will land
during §86.3 when env.rs migration consolidates the Result type.

Commits (= 3 atomic):
1. =2532ff04= — precursor: +lisp/nelisp-stdlib-error.el + baker/env
   wiring + error.rs comment trim (-27 net Rust)
2. =58911b13= — consumer: error.rs full compact (-78 net Rust)
3. =76e10b27= — Doc 86 §86.2 SHIPPED metadata + dead =pos()= removal
   (-8 net Rust)

* §13 v2 reconcile appendix (= 2026-05-10)

Doc 87 / Doc 88 / Doc 89 detail design 起こし時に判明した v1 estimate
からの drift を本 appendix で articulation。reader が §0 / §1.2 / §2 /
§4 / §5 / §7 / §10 を読む時の context として参照すべき。

** §13.1 v1 → v2 主要 drift summary

| 領域 | v1 estimate | v2 actual (Doc) | drift 要因 |
|------+-------------+-----------------+------------|
| dispatch arm 総数 | 67 (§2.2) | **132** (Doc 87 §1.6) | v1 grep =^\s*"nelisp--= が install_builtins entry + dispatch arms を conflate |
| Tier 1 arm 数 | ~30 (§2.2.1) | **21** (Doc 87) | 2026-05-06 batch 6u で 8 predicate arm 先行移管済 |
| Tier 2 arm 数 | ~25 (§2.2.2) | **13** (Doc 87) | v1 過大評価、actual は trampoline 容易 arm に絞る |
| Tier 3 arm 数 | ~12 (§2.2.3) | **22** (Doc 87) | v1 が Stage 7.4.a apply/frame primitives 8 件を見落とし |
| =bi_*= helper 数 | ~25 (§2.3) | **~75** (Doc 87) | v1 が visual estimation、実態は 33 deletion 候補 + 残 KEEP |
| =eval/ffi.rs= env 連動 | -100 (§4.3) | **0** (Doc 89 §10.5) | v1 が visual estimation、Doc 89 grep で env callsite 0 確認 |
| §86.1 net | -2,000〜-3,000 (§5.1) | **-1,571** (Doc 87 §11.3) | 上記 arm 数 reconcile + helper 削減見積もり修正 |
| §86.2 net | -258〜-278 (§3.4) | **-113 SHIPPED** + 残 deferred to §86.3 | full variant elision を §86.2 single-stage では yield せず、§86.3 と統合判断 |
| §86.3 net | -1,500 (§4.4) | **-1,214** (Doc 89 §10) | env.rs -514 + 連動 -700 (= ffi.rs -100→0 correction の影響) |
| Phase 2 合計 | -3,572〜-4,592 (§10.1) | ***-2,898*** (= sum of v2 actual) | 全 sub-stage で下振れ |
| Phase 2 着地 LOC | ~17,000-19,000 | **~18,898** (= 21,796 − 2,898) | upper bound は概ね正しい、lower bound に届かない |

** §13.2 v1 design の bias 分析

v1 estimate が optimistic に振れた要因:
1. *Visual estimation*: §4.3 ffi.rs -100 / §2.3 bi_* helper ~25 は
   grep せずに visual で見積もった結果。Doc 87/88/89 detail 起こし時
   に grep で実測したら大幅に異なった
2. *grep query 不適切*: §2.2 dispatch arm count は =^\s*"nelisp--= で
   carrying install_builtins entries も match していた。Doc 87 が
   dispatch =match= block 内の arm を別 grep で count
3. *先行移管未認識*: §86.1.a = Tier 1 predicate に 10 arm 想定だった
   が、batch 6u (2026-05-06) で 8 arm 先行移管済 = §86.1.a 残 3 arm
4. *architectural cost 軽視*: §86.2 -258 estimate は full variant
   elision を 1 sub-stage で達成する想定だったが、282 callsite refactor
   の architectural cost を visual で過小評価

** §13.3 reconciled landing 数値

Phase 2 完遂時の build-tool/src/ Rust LOC:

| 完遂時点 | LOC | 累積 net delta |
|----------+-----+----------------|
| Phase 1 完遂 (= Doc 84 SHIPPED) | 21,796 | (= base) |
| §86.1.a SHIPPED (2026-05-10) | 21,762 | -34 |
| §86.2 SHIPPED (2026-05-10) | 21,649 | -147 (= cumulative) |
| §86.1 残 完遂 (projected) | 20,212 | -1,584 |
| §86.3 完遂 (projected) | **~18,998** | ***-2,798*** |

Phase 2 reconciled landing = **~18,998 LOC** (= 21,796 - 2,798)。

(註: §13.1 表の "-2,898" は §86.2 v1 estimate -258 と actual -113 の差
-145 を §86.3 完遂時に消化する想定の合算。本表 §13.3 は SHIPPED 経路
ベースの累積。最終差分 ~100 LOC は consolidation 時に着地。)

** §13.4 reader guidance

本 Doc 86 を読む reader の参照優先順:
1. *§0 TL;DR* (= reconciled 列を canonical) — 本 v2 update で更新済
2. *§13.3 reconciled landing 数値* — 累積予測の最新版
3. *§1.2 / §10.1 表* — sub-stage 別の v2 actual 列
4. *§5 / §7 各 sub-stage* — Doc 87/88/89 reference + v2 reconciled
5. *§12 SHIPPED status* — 完了 sub-stage の commit + LOC delta 記録
6. *§2 / §4 解剖* — v1 articulation を保存、v2 update は冒頭 reconcile
   note のみ

v1 と v2 の値が混在する箇所は v2 reconciled 列を canonical とする。v1
の articulation は design 思考プロセスを示すために保存。

** §13.5 follow-up tasks

1. *§86.1 残 sub-stage dispatch* (= §86.1.b/c/d/e/f/g) — Doc 87 §3 で
   articulated 各 sub-stage を順次 ship、合計 -1,537 net (= -1,571 −
   §86.1.a 既 ship -34) 達成
2. *§86.3 implementation dispatch* — Doc 89 推奨 Option C (Hybrid)
   path で precursor / shadow path / switch-over を 3 stage 実装
3. *Doc 86 §6 §86.2 v2 numeric reconcile* — 本 v2 update では §6 内の
   detail numeric は更新せず、§12.2 + §13 で articulation。次 master
   pass で §6 内も整合可
4. *Doc 83 §6.2 update* — Phase 2 着地 ~6,000-8,000 が Phase 2 + Phase
   3 合算であることを Doc 83 側に追記 (= §9.4 で flagged)

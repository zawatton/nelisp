#+TITLE: Doc 115 — env_lexframe_aot_shims pure elisp化 (= the last Rust intermediary)
#+AUTHOR: NeLisp design
#+DATE: 2026-05-17

* Status

- *State*: DRAFT 2026-05-17 — awaiting user approval before
  implementation.
- *Parent*: Doc 114 (= x86_64-linux pivot, SHIPPED main =2e32f16b=).
  Doc 102 Phase 8.b condition (a) literally satisfied (env_mirror.rs /
  env_lexframe.rs deleted), but the 500 LOC of
  `env_lexframe_aot_shims.rs` remains as the last Rust
  intermediary in the lexframe AOT dispatch chain.
- *Dependents*: this is the final step before "真の純 elisp 化"
  (per user 2026-05-17 directive).  After Doc 115 SHIPPED, no Rust
  code participates in env_lexframe / lexical-frame management at
  runtime.
- *Scope*: replace 7 `extern "C" fn nl_*' shims (=
  `nl_frame_stack_ensure_capacity', `nl_frame_push', `nl_frame_pop',
  `nl_frame_bind', `nl_frame_stack_find', `nl_wrap_alist_cells', plus
  the depth read `nl_frame_stack_depth' which is already trivial =
  one `sexp-int-unwrap') with pure-elisp AOT implementations.
  *Additionally extends AOT grammar with `int-logxor' + `int-logand'
  ops* so the last hash-related Rust shim (`mirror_fnv1a' +
  `nl_mirror_fnv1a_sexp' extern) can also be elisp-ified inside Doc 115.
  Delete the shim file outright + delete `mirror_fnv1a' from
  `env_helpers.rs'.  Doc 115 completion = *zero Rust logic in
  lexframe + env_mirror runtime dispatch* = 完全 elisp 化.

* §0 TL;DR

** §0.1 What Doc 114 left unfinished

After Doc 114 Step 5 (main =2e32f16b=), the lexframe dispatch chain is:

#+begin_src
caller (eval::env::Env method)
   ↓
AOT-compiled .o (lisp/nelisp-cc-frame-*.el)
   ↓
nl_frame_* extern (env_lexframe_aot_shims.rs)  ← still Rust
   ↓
NlRecord / NlVector primitive ops (Rust)
#+end_src

The shim layer (= 500 LOC) is "hybrid": elisp dispatches into the
shim, but the shim does the real work in Rust.  Per the repo's
existence purpose (= 純 elisp化, Rust 削減 唯一の評価軸), this is a
contradiction — the elisp `.o' files are thin trampolines back into
Rust.

Doc 115 closes the loop: the `nl_frame_*' implementations move into
elisp, the shim file is deleted, the dispatch chain becomes:

#+begin_src
caller (eval::env::Env method)
   ↓
AOT-compiled .o (lisp/nelisp-cc-frame-*.el)  ← does the actual work
   ↓
NlRecord / NlVector / NlCell / NlConsBox primitive ops (extern "C",
shared with all other AOT callers)
#+end_src

** §0.2 Why this is feasible now

Most required AOT grammar ops + ABI helpers are already SHIPPED
on main =2e32f16b=:

| Op needed                          | Source     | Status   |
|------------------------------------+------------+----------|
| record-slot-ref / -ref-ptr / -set | Doc 111 §111.B | SHIPPED |
| record-make (via `nl_alloc_record') | Doc 111 §111.E #12 | SHIPPED |
| vector-len / -ref / -ref-ptr      | Doc 111 §111.C | SHIPPED |
| vector-make (via `nl_alloc_vector') | Doc 111 §111.E #20 | SHIPPED |
| vector-slot-set (= `nl_vector_set_slot') | Doc 111 §111.E #12 | SHIPPED |
| cell-make / -value / -set-value   | Doc 111 §111.D | SHIPPED |
| cons-make / -car / -cdr / -cdr-raw | Doc 101 §101.B-D | SHIPPED |
| sexp-int-unwrap / -make           | Doc 100        | SHIPPED |
| sexp-tag / -write-{nil,t}         | Doc 101 §101.C | SHIPPED |
| str-eq / symbol-eq                | Doc 101 §101.C | SHIPPED |
| str-len / str-byte-at             | Doc 101 §101.C | SHIPPED |
| while / cond / logic              | AOT core  | SHIPPED |
| arith (add/sub/mul/logior/logand/logxor) | Doc 100 §100.D | SHIPPED |

**Audit finding (2026-05-17, post-DRAFT)**: §115.0 was redundant.
The AOT grammar already accepts `(logxor A B)' / `(logand A B)'
via Doc 100 §100.D's `arith' op set (= `lisp/nelisp-aot-compiler.el'
line 265: `(memq (car sexp) '(+ - * logior logand logxor))').  Both
are MR-form lowerings to `xor rax, r10' / `and rax, r10' (= 3 bytes
each).  The asm helpers `nelisp-asm-x86_64-xor-reg-reg' /
`-and-reg-reg' are also shipped (= `lisp/nelisp-asm-x86_64.el' lines
1010-1016).

**No grammar extension is required**.  Doc 115 §115.0 is *deleted
from scope*; §115.7 pure-elisp FNV-1a uses the existing `(logxor)' /
`(logand)' surface forms directly.

Net consequence: Doc 115 reduces from 9 stages to 8 (= §115.1-6
shim ports, §115.7 FNV pure-elisp, §115.8 file deletion).

* §115.0 [DELETED — already SHIPPED via Doc 100 §100.D]

The 2026-05-17 audit confirmed `(logxor)' / `(logand)' AOT
grammar surface is already shipped via the `arith' op set.  Asm
helpers `nelisp-asm-x86_64-{xor,and}-reg-reg' also exist.  §115.0
removed from Doc 115 scope.  See §0.2 for the corrected table.

The remainder of this section (kept for archival purpose) shows
what the planned grammar extension *would have looked like* before
the audit found the existing implementation.  Skip to §1.

* §115.0-DELETED-ARCHIVE (= prior DRAFT design, no longer in scope)

** §115.0.1 Why

`mirror_fnv1a' (= 32-bit FNV-1a hash in `env_helpers.rs') is the
only Rust intermediary that survives Doc 114 + Doc 115's 7-shim
sweep.  Its body is:

#+begin_src rust
fn mirror_fnv1a(s: &str) -> u32 {
    let mut h: u32 = 2166136261;     // OFFSET_BASIS
    for b in s.bytes() {
        h ^= b as u32;               // <-- needs `int-logxor'
        h = h.wrapping_mul(16777619); // already in AOT arith
    }
    h & 0xFFFFFFFF                   // <-- needs `int-logand'
}
#+end_src

Three primitives missing from AOT grammar:
- *`int-logxor'* — i64 XOR, lowered to `xor rax, rdi' (3 bytes).
- *`int-logand'* — i64 AND, lowered to `and rax, rdi' (3 bytes).
- 32-bit truncation can be done via `int-logand' with `0xFFFFFFFF'.

Multiplication wrapping is already correct (AOT `arith mul' uses
`imul rax, rdi' which is 64-bit two's-complement, matching
Rust's `wrapping_mul' semantics for u32 promoted to i64).

** §115.0.2 Grammar surface

Add to `lisp/nelisp-aot-compiler.el' parse-value clause + emit
dispatch (= same shape as existing `arith' / `shift'):

#+begin_src elisp
;; (int-logxor A B) — i64 bitwise XOR.
((and (consp sexp) (eq (car sexp) 'int-logxor))
 (unless (= (length sexp) 3)
   (signal 'nelisp-aot-compiler-error
           (list :int-logxor-arity sexp)))
 (list :kind 'int-logxor
       :a (nelisp-aot-compiler--parse-value
           (nth 1 sexp) env fenv defuns)
       :b (nelisp-aot-compiler--parse-value
           (nth 2 sexp) env fenv defuns)))

;; (int-logand A B) — i64 bitwise AND.
((and (consp sexp) (eq (car sexp) 'int-logand))
 (unless (= (length sexp) 3)
   (signal 'nelisp-aot-compiler-error
           (list :int-logand-arity sexp)))
 (list :kind 'int-logand
       :a ...
       :b ...))
#+end_src

Emit (= ~15 LOC each):

#+begin_src elisp
(defun nelisp-aot-compiler--emit-int-logxor (node buf)
  "Emit `int-logxor' — i64 bitwise XOR, result in rax."
  (nelisp-aot-compiler--emit-value (plist-get node :a) buf)
  (nelisp-asm-x86_64-push buf 'rax)
  (nelisp-aot-compiler--emit-value (plist-get node :b) buf)
  (nelisp-asm-x86_64-mov-reg-reg buf 'rdi 'rax)
  (nelisp-asm-x86_64-pop buf 'rax)
  (nelisp-asm-x86_64-xor-reg-reg buf 'rax 'rdi))

(defun nelisp-aot-compiler--emit-int-logand (node buf)
  "Emit `int-logand' — i64 bitwise AND, result in rax."
  ;; ... same shape, `and rax, rdi' instead of `xor rax, rdi'.
  )
#+end_src

Plus 2 helper functions in =lisp/nelisp-asm-x86_64.el=:
- =nelisp-asm-x86_64-xor-reg-reg= (= REX.W + 0x31 + ModR/M).
- =nelisp-asm-x86_64-and-reg-reg= (= REX.W + 0x21 + ModR/M).

Each helper is ~10 LOC of elisp emitting 3 bytes.

** §115.0.3 Verification

Probe test = ERT in `lisp/nelisp-aot-compiler.el' (or a new
`elisp_cc_int_logxor_probe.rs' integration test):

| Case             | Expected            |
|------------------+---------------------|
| 0 XOR 0          | 0                   |
| 0xFF XOR 0xAA    | 0x55                |
| 0xFFFFFFFF XOR 0 | 0xFFFFFFFF (= -1 in i64 if interpreted signed) |
| OFFSET_BASIS XOR 0x41 ("A") | matches Rust `2166136261 ^ 0x41' |
| AND mask         | 0xDEADBEEF_CAFEBABE & 0xFFFFFFFF = 0xCAFEBABE |

After §115.0 SHIPPED, FNV-1a can be expressed entirely in elisp
(§115.7 below), and `mirror_fnv1a` + `nl_mirror_fnv1a_sexp` are
deletable.

** §115.0.4 Stage breakdown

| Step      | Content                                          |
|-----------+--------------------------------------------------|
| §115.0.a  | Add =nelisp-asm-x86_64-xor-reg-reg= + =-and-reg-reg= |
| §115.0.b  | Add parse + emit clauses for `int-logxor' / `int-logand' |
| §115.0.c  | ERT + integration probes (≥ 5/5 cases each)      |
| §115.0.d  | Commit message: "feat(doc-115): §115.0 — int-logxor / int-logand grammar ops" |

Estimated LOC delta for §115.0: +60 elisp (grammar + emit + helper),
+~80 probe Rust.  Net Rust: 0 (probes don't count toward削減 target).

* §1 Shim audit (= what gets rewritten)

** §1.1 The 7 shims

Source-of-truth: =build-tool/src/eval/env_lexframe_aot_shims.rs=
@ main =2e32f16b= (501 LOC).

*** nl_frame_stack_ensure_capacity (= shim #20, ~50 LOC Rust)

*Behavior*: grow the backing vector inside `frames_record' to at
least `needed' slots.  Returns the resulting capacity, or 0 on
unbuilt mirror.

*AOT ops needed*: =record-slot-ref-ptr=, =vector-len=,
=vector-slot-set= (= `nl_vector_set_slot'), =nl_alloc_vector=.

*Algorithm*:
1. Read `slots[0]' from frames_record (= backing NlVector) via
   record-slot-ref-ptr.
2. If `vector-len' ≥ needed → return current capacity.
3. Compute new capacity via capacity-doubling.
4. Allocate fresh vector via `nl_alloc_vector(new_cap)'.
5. Copy existing slots via repeated `vector-slot-set' + vector-ref.
6. Overwrite `slots[0]' via record-slot-set.
7. Return new capacity.

LOC estimate: ~60 elisp + 0 new Rust.

*** nl_frame_push (= shim #21, ~60 LOC Rust)

*Behavior*: push a fresh empty `nelisp-lexframe' record onto the
stack.  Returns 1 on push, 0 on unbuilt mirror.

*AOT ops needed*: ensure_capacity (= #20, call sibling helper
via extern-call), =record-slot-ref=, =vector-slot-set=,
=record-slot-set= (for depth increment), =nl_alloc_record=,
=sexp-int-unwrap/make=.

*Algorithm*:
1. Read current `depth' (= slots[1]) and `backing' (= slots[0]).
2. Call `nelisp_frame_stack_ensure_capacity(depth + 1)' via
   extern-call.
3. Allocate fresh frame record via `nl_alloc_record(type_tag,
   slot_count)' with `nelisp-lexframe' type tag.
4. Initialize frame's slots (= 1 fast-hash-table sub-record + 16
   nil bucket vector).
5. `vector-slot-set(backing, depth, new_frame)'.
6. `record-slot-set(frames_record, 1, depth + 1)' to increment.
7. Return 1.

LOC estimate: ~80 elisp.

*** nl_frame_pop (= shim #22, ~30 LOC Rust)

*Behavior*: pop the innermost frame (= write Nil into slot
[depth-1], decrement depth).  Returns 1 on pop, 0 on empty stack.

*AOT ops needed*: =record-slot-ref-ptr= for backing,
=sexp-int-unwrap/make= for depth, =vector-slot-set=,
=record-slot-set=, =sexp-write-nil=.

*Algorithm*:
1. Read current depth.  If 0 → return 0.
2. Read backing pointer.
3. =vector-slot-set(backing, depth-1, Sexp::Nil)= (= refcount drop).
4. =record-slot-set(frames_record, 1, depth - 1)=.
5. Return 1.

LOC estimate: ~30 elisp.

*** nl_frame_bind (= shim #23, ~30 LOC Rust + uses bind_into_frame)

*Behavior*: bind NAME → CELL in the innermost frame's hash table.
Returns 1 on bind, 0 on no-op.

*AOT ops needed*: =record-slot-ref-ptr= (frame stack), =vector-ref-ptr=
(active frame), =record-slot-ref= (hash-table inner record), =vector-ref-ptr=
(bucket vector), *pure-elisp FNV-1a* (= §115.7 helper, no extern),
=cons-make=, =cons-car/-cdr=, =vector-slot-set= (= overwrite bucket head).

*Algorithm*:
1. Read innermost frame from `backing[depth-1]'.
2. Read `fast-hash-table' inner record (frame's slot 0).
3. Read `buckets' vector (= hash-table slot 1).
4. Compute FNV-1a hash of name → bucket index via `vector-len' mod
   (= call elisp-compiled `nelisp_fnv1a' from §115.7 via extern-call).
5. Walk bucket cons-list, replace cell if name matches, else
   cons-prepend new `(name . cell)' pair to bucket head.
6. Return 1.

LOC estimate: ~60 elisp (longest shim).

*** nl_frame_stack_find (= shim #24, ~25 LOC + uses lookup_in_frame)

*Behavior*: walk the lexframe stack innermost-first, return pointer
to the matching pair's CDR (= cell slot), or 0 on miss.

*AOT ops needed*: depth read, =vector-ref-ptr=, *pure-elisp
FNV-1a* (= §115.7 helper, no extern), frame internal walk (= same
hash-table lookup as `nl_frame_bind' minus the cons-prepend).

*Algorithm*:
1. Read depth.  For i in [depth-1, 0]:
   a. Read backing[i] = frame.
   b. Compute FNV-1a hash of name.
   c. Walk corresponding bucket cons-list checking str-eq.
   d. On hit, return pointer to pair's CDR via cons-cdr-raw (=
      `*const Sexp', as `i64' for the extern signature).
2. On miss, return 0.

LOC estimate: ~50 elisp.

*** nl_wrap_alist_cells (= shim #26, ~50 LOC Rust)

*Behavior*: walk a proper-list alist of `(symbol . value)' pairs,
construct a new alist of `(symbol . NlCell(value))', return Sexp
result via output slot.  Returns 1 on success, 0 on malformed input.

*AOT ops needed*: =cons-walk= (= proper-list iteration),
=cons-car=, =cons-cdr=, =cell-make=, =cons-make=.

*Algorithm*: standard list-rebuild fold — for each pair (sym . val)
in input, emit (sym . cell-make(val)) in output.

LOC estimate: ~40 elisp.

*** nl_frame_stack_depth (= shim #19, ALREADY trivial)

Already a single =sexp-int-unwrap= of `slots[1]'.  No Rust shim
exists (= goes direct from `.o' to the Sexp byte at the
known offset).

*Status*: nothing to do; this entry counts as 0 LOC of Rust to
remove.

*** mirror_fnv1a (= NEW §115.7 target, ~25 LOC Rust + 8 LOC extern wrapper)

*Behavior*: 32-bit FNV-1a hash of a Sexp::Symbol's name bytes.
Currently in `env_helpers.rs' as a free fn `mirror_fnv1a' + extern
wrapper `nl_mirror_fnv1a_sexp'.  Called from elisp via extern-call
by all helpers that need bucket-index resolution
(`mirror_lookup_entry', `mirror_install_entry', plus future
§115.5 / §115.6 if FNV stayed extern).

*AOT ops needed* (= §115.0 newly added + existing):
- =int-logxor= (§115.0)
- =int-logand= (§115.0, for the 0xFFFFFFFF truncation)
- =arith mul= (existing, for wrapping multiplication)
- =str-len= + =str-byte-at= (Doc 101 §101.C)
- =while=, =sexp-int-unwrap/make=, =cond=

*Algorithm* (= literal elisp transcription of the Rust body):

#+begin_src elisp
;; lisp/nelisp-cc-fnv1a.el (new source file).
(defun nelisp-fnv1a (str-ptr)
  "Compute 32-bit FNV-1a hash of STR-PTR's bytes.
Returns i64 (= the u32 hash zero-extended).  AOT grammar:
no extern call, pure elisp + Doc 100/101 + §115.0 ops."
  (let ((h 2166136261)
        (n (str-len str-ptr))
        (i 0))
    (while (< i n)
      (setq h (arith-mul (int-logxor h (str-byte-at str-ptr i))
                         16777619))
      (setq h (int-logand h #xFFFFFFFF))
      (setq i (+ i 1)))
    h))
#+end_src

LOC estimate: ~20 elisp (= 12-line body + boilerplate).

After §115.7 SHIPPED, `mirror_fnv1a' (free fn) and
`nl_mirror_fnv1a_sexp' (extern wrapper) are both deletable from
`env_helpers.rs' (~35 LOC removed).

** §1.2 Shared dependencies

- *Pure-elisp FNV-1a* (= §115.7) replaces both `mirror_fnv1a' (free
  fn) and `nl_mirror_fnv1a_sexp' (extern wrapper).  After §115.7
  ships, the `mirror_fnv1a' callers in `env_helpers.rs' (=
  `mirror_lookup_entry' / `mirror_install_entry' fallback paths)
  also dispatch into the new elisp helper, and the Rust hash
  implementation can be deleted.
- `nl_alloc_vector', `nl_alloc_record', `nl_alloc_cell',
  `nl_vector_set_slot', `nl_record_set_slot', `nl_cell_set_value',
  `nl_alloc_consbox' allocators stay in Rust as small atomic
  externs (Doc 111 §111.E §2.4 audit-approved).  These are not
  shims; they are primitive memory operations that the elisp
  compiler legitimately cannot synthesize (= they manipulate
  heap-allocated boxes + refcounts).

* §2 Implementation plan

** §2.1 Per-shim cutover (= 7 single-commit steps)

For each of the 7 shims (Doc 115 §1.1's ordering):

1. *Write the elisp source*: `lisp/nelisp-cc-frame-XXX-impl.el'
   (suffix `-impl' to distinguish from the existing trampoline
   `nelisp-cc-frame-XXX.el' Agent G shipped, which currently
   dispatches into the Rust shim).
2. *Add manifest entry* in `scripts/compile-elisp-objects.el'.
3. *Add build.rs source-list entry*.
4. *Replace the existing trampoline body*: edit
   `lisp/nelisp-cc-frame-XXX.el' to drop the `extern-call' to
   `nl_frame_XXX' and inline (or delegate to) the new impl source.
5. *Delete the Rust shim function* from
   `env_lexframe_aot_shims.rs'.
6. *Add probe test*: extend or add
   `build-tool/tests/elisp_cc_frame_XXX_probe.rs' to verify the
   new pure-elisp path round-trips correctly.
7. *Build + verify gates*: cargo build, =make compile=, lib tests
   732+, integration tests 911+.

Repeat per shim; one commit per shim with message:

#+begin_src
feat(doc-115): §115.N — elisp化 nl_frame_XXX shim

Replaces the ~XXX LOC Rust shim with a AOT-compiled elisp
implementation.  ABI ops: <list>.  Probe coverage: <count> cases
including <key invariant>.

Rust deletion: -XX LOC from env_lexframe_aot_shims.rs.

Co-Authored-By: ...
EOF
#+end_src

** §2.2 §115.7 cutover — pure-elisp FNV-1a + delete mirror_fnv1a

After §115.0 grammar extension SHIPPED (provides `int-logxor' /
`int-logand'):

1. Write `lisp/nelisp-cc-fnv1a.el' (= ~20 LOC, see §1.1.7 body).
2. Add manifest entry + build.rs source list.
3. Rewire callers in `env_helpers.rs' (= `mirror_lookup_entry' /
   `mirror_install_entry' fallback paths): replace
   `mirror_fnv1a(name)' with `crate::elisp_cc_spike::nelisp_fnv1a(
   name_sym_ptr)'.
4. Rewire `lisp/nelisp-cc-frame-bind-impl.el' (§115.5) and
   `lisp/nelisp-cc-frame-stack-find-impl.el' (§115.6) to call
   `(nelisp-fnv1a name-ptr)' via extern-call (= compose-on-elisp
   pattern, no Rust).
5. Delete `mirror_fnv1a' free fn + `nl_mirror_fnv1a_sexp' extern
   wrapper from `env_helpers.rs'.
6. ERT + integration probe: bit-equality of elisp vs. Rust hash on
   ≥ 20 sample strings (= shake-out test to confirm zero
   semantic drift).

Commit message:

#+begin_src
feat(doc-115): §115.7 — pure-elisp FNV-1a, delete mirror_fnv1a

The last Rust intermediary in the env_mirror / env_lexframe AOT
dispatch chain is gone.  All hash-related callsites now dispatch
through the elisp-compiled `nelisp_fnv1a' .o.

Rust deletion: ~35 LOC from env_helpers.rs (mirror_fnv1a free fn +
nl_mirror_fnv1a_sexp extern wrapper).

Co-Authored-By: ...
#+end_src

** §2.3 Final commit — file deletion

After all 7 shims + FNV-1a are elisp-ified:

- `env_lexframe_aot_shims.rs' contains only the file header
  doc-comment + `use' statements + the `frame_stack_view_from_ptr'
  private helper.
- Inline `frame_stack_view_from_ptr' usages into env_helpers.rs (=
  it's only used internally by the shims).
- `git rm build-tool/src/eval/env_lexframe_aot_shims.rs'.
- Remove `pub mod env_lexframe_aot_shims;' from `eval/mod.rs'.

Final commit message:

#+begin_src
feat(doc-115): SHIPPED — delete env_lexframe_aot_shims.rs

All 7 nl_frame_* shims + mirror_fnv1a are now pure elisp.  AOT
grammar gained `int-logxor' / `int-logand' (§115.0) to enable the
FNV-1a transcription.

完全 elisp 化 milestone achieved:
- caller → AOT `.o` → primitive extern (alloc-only)
- *zero Rust logic* participates in lexframe / env_mirror runtime
  dispatch at this layer
- only memory primitives (= `nl_alloc_*`, `nl_*_set_slot`,
  `nl_sexp_clone_into`) remain as small atomic externs

Cumulative Rust deletion (Doc 114 + Doc 115):
- Doc 114: env_mirror.rs + env_lexframe.rs files deleted (= -300 LOC
  net after env_helpers.rs inline, +0 dispatch overhead).
- Doc 115: -501 LOC shim file + -35 LOC FNV = -536 LOC.
- Total: ≈ -836 LOC of Rust logic across both docs.

Co-Authored-By: ...
#+end_src

* §3 Verification gates (per shim + cumulative)

** Per shim
- `cargo build` clean
- `make compile` no warnings
- =find lisp -name "*.elc" -delete= before testing
- `cargo test --release --lib -- --test-threads=1` shows 732 pass
- The shim's probe test ≥ 3/3 cases
- Integration test suite all green (911+)

** Cumulative (post-Doc-115 SHIPPED)
- `env_lexframe_aot_shims.rs` does not exist
- `grep -rn "nl_frame_" build-tool/src/` returns ZERO hits
- `grep -rn "extern \"C\" fn nl_frame" build-tool/` returns ZERO hits
- `grep -rn "fn mirror_fnv1a\|nl_mirror_fnv1a_sexp" build-tool/src/`
  returns ZERO hits (= §115.7 deletion gate)
- Rust LOC delta: net −536 LOC (= -501 shim file + -35 FNV in
  env_helpers.rs)
- Elisp LOC delta: net +340 to +420 LOC (= +320-400 shims +20 FNV)
- New AOT grammar surface: *none* (= `(logxor)' / `(logand)'
  audit-confirmed already shipped via Doc 100 §100.D)
- *Net total LOC*: ≈ −116 to −196 LOC
- Doc 102 Phase 8.b condition (a) was satisfied by Doc 114; Doc 115
  adds the deeper "*zero Rust logic in lexframe + env_mirror runtime
  path*" property = 完全 elisp 化.

* §4 Risk + open questions

** §4.1 FNV-1a hash performance after elisp化

After §115.7, FNV-1a runs as AOT-compiled `.o' (= native x86_64
code).  The inner loop is:

#+begin_src text
load byte at str-bytes-ptr + i      ; movzx eax, [rdi + rsi]
xor with h                          ; xor rcx, rax        (§115.0)
imul h by FNV_PRIME                 ; imul rcx, r11
and h with 0xFFFFFFFF               ; and rcx, r9         (§115.0)
inc i, jmp loop                     ; add rsi, 1 / jmp
#+end_src

Performance is *identical or better* than the Rust version, because
the Rust loop is also lowered to this exact sequence by =rustc=.
The only divergence is the function-call boundary: the Rust path
has Rust ABI call overhead, the elisp path has the AOT extern-call
overhead (= ~3-5 cycles for cdecl prologue).  Both are negligible vs.
the loop body.

*Decision*: §115.7 is *not* a perf regression.  Sample probe
includes ≥ 1 micro-benchmark (= 100-byte string hashed 10000×) to
confirm.

** §4.2 Probe coverage drift

When the per-shim probe was first written (Agent G's work), it
exercised the Rust shim.  After elisp化, the same probe exercises
the elisp path — but the probe's assertions may have implicitly
depended on Rust behaviour.

*Mitigation*: each per-shim commit must run the existing probe
unchanged (= same input, same expected output).  If the probe
fails after elisp化, that's a bug in the elisp port, not a probe
update.

** §4.3 Frame structure layout assumptions

The shims hardcode:
- `nelisp-lexframe' is a Record with `fast-hash-table' in slot 0.
- `fast-hash-table' is a Record with `bucket-count' (slot 0),
  `buckets' (slot 1, Vector), `size' (slot 2).
- `frames_record' is a Record with `backing' (slot 0, Vector),
  `depth' (slot 1, Int).

Pure-elisp versions must match exactly.  Source-of-truth is the
current shim file + `lisp/nelisp-lexframe.el'.

*Decision*: each elisp impl source includes a top-of-file comment
listing the layout assumptions (= maintenance footprint), and a
=defconst= for the slot indices.

** §4.4 Refcount discipline

`vector-slot-set' / `record-slot-set' / `cell-set-value' all
delegate to refcount-aware Rust externs (= `nl_vector_set_slot'
clones+drops, etc.).  Pure-elisp impls do not need to manage
refcounts explicitly — that's already encapsulated.

* §5 Schedule

- ~~§115.0~~ — *DELETED from scope* (= already shipped via Doc 100
  §100.D, see §0.2 audit finding).
- §115.1 (= nl_frame_stack_ensure_capacity) — simplest, no FNV hash,
  ~60 LOC elisp.  Pattern-establishing; can ship first.
- §115.2 (= nl_frame_pop) — second simplest, ~30 LOC.
- §115.3 (= nl_frame_push) — uses §115.1 via extern-call.
- §115.4 (= nl_wrap_alist_cells) — independent of stack ops, ~40
  LOC.
- §115.5 (= nl_frame_bind) — uses pure-elisp FNV (= depends on §115.0
  but FNV impl itself lands in §115.7).  For §115.5 ship, FNV still
  Rust extern; rewires to elisp helper in §115.7.
- §115.6 (= nl_frame_stack_find) — same as §115.5.
- *§115.7* (= pure-elisp FNV-1a + delete mirror_fnv1a + rewire
  §115.5/6 to elisp FNV).  Depends on §115.0 grammar.
- §115.8 (= file deletion + final cleanup, = delete
  env_lexframe_aot_shims.rs).

*8 commits total* (= §115.1-6 shim ports + §115.7 FNV cutover +
§115.8 file deletion).  Per-shim Agent dispatch is fine because
each shim is independent (the only shared dep is FNV — initially
Rust extern, finally pure-elisp helper).  §115.5/6 ship first with
Rust FNV extern, then §115.7 rewires them to elisp FNV + deletes
the Rust extern.  §115.1-§115.4 + §115.5-§115.6 can
parallel-dispatch (= 6 concurrent Agents).

* §6 References

- =/dev/nelisp/CLAUDE.md= — repo existence purpose.
- =docs/design/114-x86_64-linux-pivot.org= — Doc 114 SHIPPED main
  =2e32f16b=.
- =docs/design/111-phase-47-sexp-record-vector-cell-abi.org= — Doc
  111 §111.E (= 18 AOT helpers, all SHIPPED).  Group C/D
  classification grounds the §4.1 keep-FNV-extern decision.
- =build-tool/src/eval/env_lexframe_aot_shims.rs= @ main
  =2e32f16b= — the 501 LOC target file.
- =build-tool/src/eval/env_helpers.rs= — current home of FNV-1a +
  primitive helpers Doc 115 reuses (= retained per Group C/D).
- =lisp/nelisp-cc-frame-*.el= — current trampolines (Agent G ship
  on main `c60c1f96', now to be repurposed as full
  implementations).

Memory cross-refs:

- =feedback_anvil_uses_nelisp_architecture= — dual-target Emacs /
  NeLisp; Doc 115 advances the NeLisp side toward "Emacs
  primitives gone".
- =feedback_nelisp_core_vs_layer2_boundary= — language rule = core,
  I/O primitive = layer 2.  FNV-1a hash crosses this boundary; the
  extern stays Layer 2.
- =project_anvil_runtime_phase_b_complete= — precedent: anvil-runtime
  crate deleted when elisp equivalent was sufficient.  Doc 115
  applies the same pattern to env_lexframe_aot_shims.

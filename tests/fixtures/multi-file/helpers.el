;;; helpers.el --- math helpers loaded by main.el  -*- lexical-binding: t; -*-

(defun helpers-square (n)
  (* n n))

(defun helpers-add (a b)
  (+ a b))

(defun helpers-incr (n)
  (1+ n))

(provide 'helpers)

;;; helpers.el ends here

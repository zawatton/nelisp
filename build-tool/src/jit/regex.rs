//! `string-match-p` trampoline — Rust body deleted.
//!
//! `nl_jit_string_match_p' migrated to Phase 47 elisp body in
//! `lisp/nelisp-cc-jit-string-match-p.el'.  The compiled `.o'
//! is linked via `build-tool/build.rs' manifest_sources and
//! anchored in `bridge.rs::_ELISP_ARCHIVE_ANCHOR'.

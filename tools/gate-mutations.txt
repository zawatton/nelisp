# Known-defect injections (tools/nelisp-gate-mutation.sh).
#
# Each row is: GATE | FILE | SED-EXPRESSION | WHAT IT BREAKS [| TARGET]
#
# The row is applied with `sed -i', the gate is run, and the file is
# restored.  The gate MUST fail.  A gate that stays green with a real defect
# in front of it is not a gate, and on 2026-08-19 three of them were in
# exactly that state while printing PASS.
#
# TARGET is optional and is one exact standalone target name (for example
# `linux-x86_64').  With no fifth field the row applies to every target, which
# preserves every row written before target scoping existed.  An out-of-scope
# row is SKIPPED before its file is touched: it did not test its gate and is
# therefore neither a pass nor a failure.  The harness reports passed, failed,
# and skipped separately; counting a skipped row as checked would recreate the
# same "green while seeing nothing" failure this table exists to prevent.
# Malformed scopes and any field count other than four or five are errors, so
# a typo cannot quietly widen or narrow a row.  As before, `|' cannot appear
# inside SED-EXPRESSION or WHAT because it is the field separator.
#
# Keep the injections small and obviously wrong.  A subtle one that a gate
# legitimately does not cover teaches nothing except to delete the row.
#
# ACCEPTANCE CRITERIA FOR A NEW ROW.  Three rows have shipped here without
# meeting these, and each cost a full CI round to discover.  Prove the row with
# `make gate-mutation-verify GATE=<gate-name>' BEFORE committing it -- that
# target exists so the proof is cheap enough that skipping it is never worth
# it.  The three failure modes, named so they can be recognised:
#
#   1. UNREACHABLE -- the injection sits on a path the gate never walks, so the
#      gate stays green and reads as broken when it is fine.  A `garbage-
#      collect' smoke was once used to police a chunk-return guard that only
#      the mid-form collector reaches; the row was removed with its reason
#      recorded rather than the gate being called weak.  ASK FIRST: does this
#      gate's own execution actually go through the line I am mutating?
#
#   2. NON-LETHAL AFTER AN IMPROVEMENT -- the row was lethal when written, and
#      a later change made the defect harmless.  Doc 199's park barrier did
#      this to an inhibit-begin row.  That is the improvement WORKING; re-point
#      or retire the row and say why.  It is not a regression.
#
#   3. ENVIRONMENT-SENSITIVE -- RED locally, green in CI.  A park-bypass row
#      went RED 10 times out of 10 on the author's machine and green in CI, and
#      was removed.  PREFER A ROW WHOSE RED-NESS IS STRUCTURAL: the check is
#      gone, a constant is wrong, a name no longer resolves.  Avoid rows whose
#      failure depends on timing, scheduling, load, or memory pressure -- those
#      are the ones that pass locally and lie in CI.
#
# Record the evidence in the row's own comment: what you injected, what the
# gate printed when RED, and what it prints when restored.
# 2026-09-14: returning zero on failed result validation fails the host-only
# ProjectResultContract; restoring it passes (1 case, 0 findings). No binary
# build is needed for either side of this sensitivity check.
project-cli-contract|tools/nelisp-project.py|s/^        return 1$/        return 0/|failed or incomplete test run is reported as success|linux-x86_64
emacs-parity|scripts/nelisp-stdlib-prelude.el|s/^(defun regexp-quote (s)/(defun regexp-quote (s) (setq s (concat s "@"))/|regexp-quote appends a stray character
ns-gate|scripts/nelisp-stdlib-prelude.el|s/^(defun round (x \&optional div)/(defun round (x \&optional div) (setq x x)/|round diverges from its lisp/ mirror
# The replacement is GLOBAL on purpose.  The first version changed one
# occurrence of a name that appears ten times, so the name stayed covered and
# the gate correctly stayed green -- a weak injection reads as a broken gate.
# The injection has to remove a name the corpus mentions ONCE.  An earlier
# version renamed `regexp-quote', which the corpus has grown many uses of --
# the coverage count stayed above the baseline and the gate correctly stayed
# green, which reads as a broken gate.  `nbutlast' is mentioned once.
# The name has to be one the compat table LISTS and the corpus mentions
# ONCE.  Two earlier versions failed that: `regexp-quote' is mentioned many
# times (the count stayed above the floor), and `nbutlast' is not in the
# generated table at all (so it was never counted, and removing it changed
# nothing).  Both read as a broken gate when the gate was fine.
parity-coverage|test/nelisp-shadow-differential-cases.el|s/generate-new-buffer/ignore-me-not-a-name/g|a covered name stops being mentioned
prelude-toplevel-check|scripts/nelisp-stdlib-prelude.el|s/^(defun string-empty-p (s) (string-equal s ""))$/(defun string-empty-p (s) (string-equal s ""))\n  (nelisp--check-string s)/|a check lands outside a one-line defun
# The sed expression cannot use `|' -- it is this file's field separator.
prelude-toplevel-check|scripts/nelisp-stdlib-prelude.el|/^  (defun seq-length (seq) (length seq)))$/a\    (unless (sequencep seq) (signal (quote wrong-type-argument) (list (quote sequencep) seq)))|a check lands outside a one-line defun INSIDE an unless wrapper
#
# 2026-08-21 (this session): the five rows below cover the identity-pinning
# added to unsafe-inventory, ns-inventory, fallback-inventory and pkg-graph,
# plus generated-source-parse, which had no row at all.  Each is chosen so a
# bare COUNT would not have moved: the pinned identity changes (a new one
# appears, or an already-divergent pair's shape drifts) while the aggregate
# stays where it was, which is exactly the blind spot `parity-coverage' and
# `ns-gate' were caught in on 2026-08-21.
unsafe-inventory|lisp/nelisp-cc-alloc-dealloc.el|/^(provide 'nelisp-cc-alloc-dealloc)$/i\(defun nl-check-inventory--gm-injected () (ptr-read-u64 nil 0))|a kernel file with no `pinned-kernel-call' line starts calling an unsafe primitive
ns-inventory|lisp/nelisp-stdlib.el|s/^  (if (nelisp--num-lt2 x 0) (nelisp--sub2 0 x) x))$/  (progn (setq x x) (if (nelisp--num-lt2 x 0) (nelisp--sub2 0 x) x)))/|a pinned ns-collision-divergent pair's definition drifts to a new shape, subject and file list unchanged
fallback-inventory|lisp/nelisp-artifact.el|s/^               (error nil)))$/               (error (progn nil))))/|a pinned bare-handler site's exact shape drifts while its file and toplevel stay the same
pkg-graph|src/nelisp-file-notify.el|s/(require 'filenotify)/(require 'ert-x)/|a hard host require is swapped for a different unresolved one, count unchanged
generated-source-parse|scripts/nelisp-standalone-build.el|s/"(defvar features nil)\\n"/"(defvar features nil\\n"/|drops a closing paren inside a generated string literal, invisible to parens-check
# doc200-census proof (2026-08-26): changing the sole direct tag-5 test in
# `nl_jit_length' to 50 made the live key vanish.  RED printed
# `STALE-LEDGER-ROW ... tag-test-sexp-tag 0' and
# `GATE-COUNT checked=630 findings=169'; restored GREEN printed PASS and the
# same GATE-COUNT.  `make gate-mutation-verify GATE=doc200-census' exercised
# the injection, observed RED, restored the file, and observed GREEN.
doc200-census|lisp/nelisp-cc-jit-length.el|s/(= (sexp-tag arg) 5)/(= (sexp-tag arg) 50)/|the sole direct Str-tag test in nl_jit_length vanishes from the live census
#
# 2026-08-22 (this session): ert-full had never been mutation-proven --
# `nelisp-ai.sh test' is not a raw `make' target, so it could not go
# through this file's plain `make "$gate"' runner unmodified; fixed in
# `tools/nelisp-gate-mutation.sh' with a small `ert-full'-specific branch
# that calls `nelisp-ai.sh test' instead (see its comment for both rows'
# routing and why each one's report is scratched to a throwaway
# NELISP_GATE_DIR rather than overwriting the tree's real ert-full
# report).  Two rows, the two cases named in the survey this session
# closed gaps from:
#
#   the corrupted-helper row targets `nelisp-eval' (src/nelisp-eval.el),
#   the single most widely-used repo-defined function in the whole test
#   tree (the interpreter entry point nearly every `(should (equal
#   (nelisp-eval FORM) ...))' assertion depends on) -- made to always
#   return nil regardless of FORM.  Running the FULL suite to prove this
#   costs about 75s per injection, too slow for `gate-mutation' inside
#   `nelisp-ai.sh check''s fast lane; scoped via
#   NELISP_GATE_MUTATION_TEST_FILES to `test/nelisp-eval-test.el' alone,
#   which calls `nelisp-eval' directly and fails within a couple of
#   seconds once corrupted -- the SAME `run_ert_gate' path `ert-full'
#   uses for real, exercised on fewer files, not a different mechanism.
#
#   the empty-glob row targets `test_files()' itself
#   (tools/ai/nelisp-ai.sh), the function `cmd_test' calls to build
#   ert-full's file list -- corrupted so its output is filtered to
#   nothing regardless of what matched, the same "glob defined no test"
#   shape AI.md's rule 1 already documents.  This one must run UNSCOPED
#   (no NELISP_GATE_MUTATION_TEST_FILES): scoping would skip the very
#   line being tested.  `run_ert_gate' already had the guard that catches
#   this -- an empty file list reports GATE-COUNT ran=0 failed=1 and
#   exits 1 before spawning Emacs at all -- so this row is also the
#   cheapest of the two, proving a pre-existing guard rather than
#   exercising the interpreter.
ert-full|src/nelisp-eval.el|s/^  (nelisp-eval-form form nil))$/  (progn (nelisp-eval-form form nil) nil))/|nelisp-eval always returns nil, corrupting every should that reads its value
ert-full|tools/ai/nelisp-ai.sh|s/'nelisp-worker-soak-test\\\.el\$'/'.'/|test_files() output gets filtered to nothing regardless of what matched; "no test file matched" must still fail ert-full
# Doc 200 P1 representation proof.  These rows are scoped by the mutation
# harness to the hand-built tag-14/tag-15 executable test.  Each injection
# removes one production consumer arm while leaving the raw layouts intact.
# Verified 2026-08-26: all six rows printed `went red as it should'; the
# restored targeted ERT printed `Ran 2 tests, 2 results as expected, 0
# unexpected', and the complete ert-full mutation verification finished
# `GATE-COUNT checked=12 findings=0'.  The first clone injection removed the
# tag-14 fast path but stayed green because the generic rc path still copied
# the same four words; it was replaced by the lethal tag-15 refcount row.
ert-full|lisp/nelisp-aot-compiler.el|/(defun nelisp-aot-compiler--aot-direct-tag-predicate-form/,/(defconst nelisp-aot-compiler--aot-builtinn-delegation-symbols/s/(= (sexp-tag ,arg) ,nelisp-sexp-layout-tag-unibyte-str)/(= (sexp-tag ,arg) 140)/|the direct AOT stringp expansion stops accepting tag 14
ert-full|scripts/nelisp-standalone-build.el|/(defun bf_arrayp_raw/,/(defun bf_wrong_type_hash_table/s/(= tg 14)/(= tg 140)/|arrayp stops accepting a hand-built tag-14 string
ert-full|lisp/nelisp-cc-jit-type-of.el|/(defun nl_jit_type_of_tag/,/(defun nl_jit_type_of_inner/s/((= tag 14)/((= tag 140)/|type-of stops mapping tag 14 to string
ert-full|scripts/nelisp-standalone-build.el|/(defun nl_gc_mark_slot/,/(defun nl_gc_poison_fill/s/(or (= tag 5) (= tag 14))/(or (= tag 5) (= tag 140))/|the precise GC marker stops marking a reachable tag-14 byte buffer
ert-full|lisp/nelisp-cc-sexp-clone-into.el|/(defun nl_sci_bump/,/(defun nl_sci_rc/s/(or (= tag 6) (= tag 15))/(or (= tag 6) (= tag 150))/|the cloner stops refcounting the NlStr box of a hand-built tag-15 string
ert-full|scripts/nelisp-standalone-build.el|/(defun nl_cli_value_to_buf/,/(defun nl_cli_write_value/s/((= tag 14)/((= tag 140)/|CLI value output stops rendering tag-14 bytes with raw octal escapes
# Doc 200 P4 key-equality proof.  The range replacement restores the exact
# outer tag-identity gate that rejected a tag-5/tag-14 ASCII pair before the
# shared character-count/byte-count/bytes comparison could run.  The mutation
# harness rebuilds target/nelisp for standalone-build-script ert-full rows so
# the binary-backed alist/member ERT cannot accidentally exercise stale code.
# Verified 2026-08-27: `make gate-mutation-verify GATE=ert-full' printed this
# row's `went red as it should', then `GATE-COUNT checked=13 findings=0'; after
# restoration the focused file printed `Ran 11 tests, 11 results as expected,
# 0 unexpected'.
ert-full|scripts/nelisp-standalone-build.el|/^                    (if (or (= ta tb)$/,/^                                 (= (m5_string_tag_p tb) 1)))$/c\                    (if (= ta tb)|wf_key_eq_depth again rejects equal ASCII string keys when one is tag 5 and the other tag 14
#
# 2026-08-22 (this session): doc-claims, added the same session as the
# Definition of Done it enforces.  Case (i) of its own verification (a
# SHIPPED doc with no #+VERIFIED-BY, not in the baseline) is the natural
# injection: renaming a compliant doc's #+VERIFIED-BY: line to anything
# else reproduces exactly that shape without inventing a synthetic file.
# Doc 160 (docs/design/160-stdlib-breadth.org) is chosen for the single,
# short #+VERIFIED-BY: line the sed can match unambiguously.
doc-claims|docs/design/160-stdlib-breadth.org|s/^#+VERIFIED-BY: reader-surface-audit$/#+VERIFIED-BY-REMOVED: reader-surface-audit/|a compliant SHIPPED doc loses its #+VERIFIED-BY: line and is not in the baseline
#
# 2026-08-23 (this session): gate-selfcheck itself had no row -- fixed while
# making it skip-aware (fix/gate-selfcheck-skip-aware), which changed how it
# tells a reasoned `GATE-SKIP' apart from a gate that prints NEITHER a count
# NOR a skip reason, i.e. the exact "PASS while seeing nothing" shape this
# gate exists to catch.  `bootstrap-contract' is the injection target: its
# GATE-COUNT print is a single line, so the sed match is unambiguous, and
# dropping it (replaced with `nil', paren-balanced -- the form it replaces
# opens and closes exactly two parens) leaves the rest of the gate's checks
# running and printing "ok"/"PASS" as before -- a silent, fully-fake pass,
# not a crash.  Confirmed by hand before this row was added: mutated,
# `make bootstrap-contract' exits 0 with no GATE-COUNT line, and
# `make gate-selfcheck' (real Emacs 30.x, no skip in play) correctly goes
# red on it.
gate-selfcheck|tools/nelisp-bootstrap-contract.el|s/^    (princ (format "GATE-COUNT checked=%d findings=%d.*missing))$/    nil/|bootstrap-contract fake-passes (prints ok/PASS, no GATE-COUNT line); gate-selfcheck must still flag it as a finding, not confuse it with a reasoned skip
# 2026-08-26 (Doc 199 Tier 3a/3b mirror guard): confirmed by hand before
# adding this row.  With a fresh binary, replacing the reached registry lookup
# key with zero made nelisp-thread-mirror-guard-standalone-smoke exit nonzero:
# "worker hit-path mirror write was not catchably refused".  Restored and
# rebuilt clean immediately afterwards.  The mutation harness rebuild list
# above includes this conditional-prerequisite standalone gate, so the row
# cannot accidentally exercise a stale clean target/nelisp.
nelisp-thread-mirror-guard-standalone-smoke|lisp/nelisp-cc-rootstack.el|s/(nl_thread_registry_find mirror-ptr)/(nl_thread_registry_find 0)/|worker registry lookup uses a non-worker key, so mirror/intern writes are admitted instead of raising the named condition|linux-x86_64
#
# 2026-08-23 (Doc 188 P1, buffer unification): standalone-reader-buffer-
# smoke is new this session.  Confirmed by hand before this row was
# added: mutated, `make standalone-reader' (forced -- the gate's own
# conditional prerequisite only builds when target/nelisp is absent, so
# a stale binary silently re-passes an already-mutated source the first
# time), then `./target/nelisp --load test/nelisp-buffer-unification-
# standalone-smoke.el' went from `6 passed, 0 failed' to `2 passed, 4
# failed' (exit 1); reverted and rebuilt clean before committing.
standalone-reader-buffer-smoke|scripts/nelisp-stdlib-prelude.el|s/(nelisp-insert s nelisp--current-buffer)/(ignore s nelisp--current-buffer)/|insert becomes a silent no-op again (Doc 188 §1.3's original bug shape); buffer-string never reflects it
#
# 2026-08-23 (Doc 188 P2, buffer unification): the smoke grew from 6 to
# 15 cases (`current-buffer'/`set-buffer'/`buffer-substring'/
# `erase-buffer', plus the nelisp-textprop-hook attach test).  This row
# proves the grown smoke actually catches a P2-specific regression, not
# only the pre-existing P1 `insert' row above.  Confirmed by hand before
# this row was added: mutated, `make standalone-reader' (forced, same
# reason as the P1 row's own note), then `./target/nelisp --load
# test/nelisp-buffer-unification-standalone-smoke.el' went from `15
# passed, 0 failed' to `14 passed, 1 failed' (exit 1) -- exactly
# `nelisp-buffer/erase-buffer-clears-and-resets-point', the one test
# whose assertions this call site backs; reverted and rebuilt clean
# before committing.
standalone-reader-buffer-smoke|scripts/nelisp-stdlib-prelude.el|s/(nelisp-erase-buffer nelisp--current-buffer)/(ignore nelisp--current-buffer)/|erase-buffer becomes a silent no-op; buffer-string/point never reflect the clear
#
# 2026-08-23 (docs/design/185-cl-generic-subset.org): the two rows below
# cover this doc's own loud-failure discipline -- exercised through
# `test/nelisp-cl-generic-test.el' (`nelisp-gate-mutation.sh' scopes
# `ert-full' mutations on `lisp/nelisp-cl-macros.el' to that one file; see
# the case in `run_gate' there).  Both confirmed by hand before landing:
# each sed hits its target exactly once, and `NELISP_GATE_MUTATION_TEST_FILES=
# test/nelisp-cl-generic-test.el tools/ai/nelisp-ai.sh test' goes red on the
# corresponding test, clean on the unmutated file.
ert-full|lisp/nelisp-cl-macros.el|s/(nelisp-cl-macros--struct-isa (nelisp--record-type val) type))/t)/|every struct instance matches every struct-type specializer regardless of ancestry, so struct-based dispatch order silently collapses (a dog instance would satisfy an unrelated struct's method too)
ert-full|lisp/nelisp-cl-macros.el|s/(signal 'cl-no-applicable-method (cons name args))/nil/|an unmatched generic-function call silently returns nil instead of signaling cl-no-applicable-method
# 2026-08-23 (this session, Doc 187 layer-1 fix): standalone-reader-test
# now covers fixnum-overflow signalling for the native +/-/* dispatch
# (wf_sum/wf_prod in scripts/nelisp-standalone-build.el, checks 11/13-18
# of nelisp-standalone--reader-stdlib-completion-smoke) -- these two rows
# prove the check itself, not just its presence.  Row 1 deletes the
# positive-overflow half of wf_sum's sign check (unique substring: only
# wf_sum's own literal-bound comparison shape has this exact text); row 2
# deletes wf_prod's div-round-trip check (unique substring: `/' makes it
# safe to use `#' as the sed delimiter instead of `/', since this file's
# field separator is `|').  Both require `standalone-reader-test' to
# rebuild target/nelisp (it does so inline, unlike emacs-parity, so no
# `rebuild_checked' special-case is needed here).
#
# 2026-08-23 UPDATE (Doc 190 Phase B, feat/bignum-p2-arith): the SAME two
# literal-bound/div-round-trip checks still exist verbatim in the rewritten
# wf_sum/wf_prod (the detection logic is unchanged from Doc 187 -- only the
# RESPONSE changed, signal -> promote), so both sed patterns below still
# match exactly once and both rows still prove real defects -- deleting
# either check now makes the overflow case wrap silently again instead of
# PROMOTING to a bignum (not "instead of signalling", since signalling is
# no longer this phase's correct behavior for these three ops; checks
# 11/13-16 of the same smoke now assert the promoted value instead).
standalone-reader-test|scripts/nelisp-standalone-build.el|s/(and (> v 0) (> acc (- 2305843009213693951 v)))/(and (> v 0) nil)/|wf_sum's positive-overflow half of the literal-bound check is deleted, (+ most-positive-fixnum 1) wraps silently again instead of promoting to a bignum
standalone-reader-test|scripts/nelisp-standalone-build.el|s#(/= (/ prod acc) v)#nil#|wf_prod's div-round-trip overflow check is deleted, (* 100000000000 100000000000) wraps silently again instead of promoting to a bignum
standalone-reader-test|lisp/nelisp-cc-reader-parser.el|s/(if (= data 0) n$/(if (= data 1) n/|the NULL guard in nelisp_reader_p_hash_pair_count is defeated, so an empty-table literal with no data key -- '#s(hash-table test equal), what Emacs 30 writes and savehist restores -- reads the tag word at address 0 and the binary faults
standalone-reader-test|scripts/nelisp-standalone-build.el|s/(if (if (= data_slot 0) 1 (= (ptr-read-u64 data_slot 0) 0))/(if (= data_slot 0)/|nl_ht_put stops treating a materialised nil cdr as an absent data slot, so puthash into an empty reader-built table writes into a scratch view and the table stays empty
# 2026-08-26 (Doc 187 P4 continuous cost gate): the exact flag-controlled
# guard call is made unconditional-false.  The gate compiles its one-add loop
# twice from the same source; this mutation makes the checked and unchecked
# native-object SHA-256 values identical, so it goes red before timing.  The
# failure is structural and does not depend on runner speed or load.  Proved
# manually while adding the row: injected => identical hashes,
# `GATE-COUNT checked=1 findings=1', exit nonzero; restored => distinct hashes,
# ratio below 1.15x, `GATE-COUNT checked=1 findings=0', exit zero.
bench-aot-checked-arith|lisp/nelisp-aot-compiler.el|s/^  (when nelisp-aot-compiler--checked-arith$/  (when nil/|the --checked-arith guard call is disabled, making the opt-in flag emit the same native object as the default unchecked path
# 2026-08-23 (this session): Doc 180 Phase 1 (form-located errors) row.
# `(setq form_index (+ form_index 1))' appears twice in `nl_eval_source_all'
# (the successful-parse branch and the reader-error branch); the sed has no
# `/g' but matches the whole line so both occurrences (one per line) are
# replaced -- FORM_INDEX is pinned to 1 everywhere, so an error in the
# fixture's second top-level form is reported as "top-level form #1", not
# "#2".  `standalone-reader-form-location-smoke''s "load form #2 signals"
# row (`nelisp-standalone--reader-form-location-smoke') asserts the literal
# substring "top-level form #2 (byte ...", so this must fail it.
standalone-reader-form-location-smoke|scripts/nelisp-standalone-build.el|s/(setq form_index (+ form_index 1))/(setq form_index 1)/|every uncaught error reports "top-level form #1" regardless of which form actually failed
# nl-actor-standalone-smoke: added 2026-08-23 (Doc 193 §4.4 Phase 1).
# The injection reverts nelisp-actor.el's soft `(require 'generator nil
# t)' back to the hard `(require 'generator)' it replaced -- i.e. it
# reproduces, verbatim, the exact defect this whole phase fixed
# (`nelisp: uncaught error: file-missing: generator', measured before
# any change here and again with this exact injection applied).  Chosen
# over mutating the generated CPS file or the scheduler/mailbox logic
# after two of those (`(setf (nelisp-actor-status actor) actor)' and
# `nelisp-actor--mailbox-pop' returning `(cdr box)' instead of `(car
# box)') were tried by hand and BOTH hung target/nelisp instead of
# failing cleanly (one grew to 15GB RSS before being killed) -- a
# corrupted status or mailbox value defeats `nelisp-actor-run-until-
# idle''s termination on this substrate rather than erroring the way
# host Emacs does, which is a real, separate, un-investigated
# divergence this row deliberately does not depend on.  This injection
# fails at the very first `load' statement, before any scheduling code
# runs, so it cannot hit that class of problem.
nl-actor-standalone-smoke|packages/nelisp-actor/src/nelisp-actor.el|s/^(require 'generator nil t)$/(require 'generator)/|nelisp-actor becomes unloadable standalone again (file-missing: generator), the exact pre-Doc-193-§4 defect
# standalone-reader-bignum-smoke: added 2026-08-23 (Doc 190 Phase A,
# feat/bignum-p1).  The injection flips the tag byte `nl_bignum_write'
# stamps into a freshly-constructed Bignum Sexp from 13 (Bignum) to 2
# (Int) -- every bignum this session's reader/`nl--read-int' constructs
# is thereafter mis-tagged as a plain Int whose "value" is actually the
# sign word (0 or 1), so `bignump' answers nil, `prin1' prints "0"/"1"
# instead of the real digits, and comparisons read garbage.  Caught by
# this smoke's own fencepost/printing/GC-stress checks, not by any
# pre-existing gate (no prior gate constructs a tag-13 Sexp at all).
standalone-reader-bignum-smoke|scripts/nelisp-standalone-build.el|s/(seq (ptr-write-u8  result_slot 0  13)/(seq (ptr-write-u8  result_slot 0  2)/|a constructed bignum is mis-tagged as a plain Int (its sign word read back as the "value"), corrupting bignump/prin1/comparison for every bignum the reader produces

# standalone-reader-bignum-smoke: added 2026-08-23 (Doc 190 Phase B,
# feat/bignum-p2-arith).  New correctness property this phase introduces:
# an arithmetic result that re-fits the fixnum bound must DEMOTE back to
# a plain Sexp::Int, not stay boxed as a Bignum that merely prints/
# compares correctly (Doc 190 §2's contraction rule, §4's own "verified
# by tag inspection, not just value equality" requirement).  The
# injection forces `nl_bignum_finish''s bound check to always take the
# box (Bignum) branch, so EVERY arithmetic result -- including ones that
# obviously fit a fixnum, e.g. `(- (+ most-positive-fixnum 1) 1)' -- stays
# a Bignum forever.  Caught by this smoke's own demotion-fencepost checks
# (`(not (bignump r))'), which would flip to `t' (a Bignum) under this
# mutation; not caught by any value-only check, since the mutated value
# still prints/compares as the right number.
standalone-reader-bignum-smoke|scripts/nelisp-standalone-build.el|s/(if (= (nl_bignum_mag_le_bound limb_ptr limb_count bound_hi bound_lo) 1)/(if 0/|an arithmetic result that should demote back to a fixnum stays boxed as a Bignum forever (right value, wrong tag) -- Doc 190 §2's contraction rule silently stops holding
# nl-num build-first phases (Doc 196, 2026-08-24).  Phase 0's row changes
# the doubling bound from inclusive to exclusive, so an exact divisor
# multiple is omitted and divmod leaves remainder == denominator.  Phase 1's
# row disables gcd reduction at the constructor choke point.  Both use the
# ert-full mechanism scoped above to nl-num-test.el.  Phase 2 must run on the
# standalone: host Emacs handles Bignum+Float natively, while target/nelisp's
# float fold misreads tag 13's sign word; bypassing nl-num's safe promotion
# therefore reproduces the measured silent wrong-value defect only there.
ert-full|packages/nl-num/src/nl-num-core.el|s/(while (<= multiple numerator)/(while (< multiple numerator)/|nl-num doubling division omits an exact top multiple, leaving a remainder equal to the denominator instead of canonical Euclidean divmod
ert-full|packages/nl-num/src/nl-num-rational.el|s/(divisor (nl-num--gcd numerator denominator))/(divisor 1)/|nl-rational stops reducing numerator and denominator by their gcd, violating the canonical lowest-terms invariant
nl-num-standalone-smoke|packages/nl-num/src/nl-num-rational.el|s/(+ (nl-num--real-to-float a) (nl-num--real-to-float b))/(+ a b)/|nl-num mixed addition hands a Bignum and Float to the native standalone fold, which reads the bignum sign word as its magnitude
# nelisp-thread-standalone-smoke: Doc 199 Tier 2's result-publication property.
# The worker still bumps the done counter, so join completes normally, but the
# distinct result slots remain zero and the interpreter-driven reduction is 0
# instead of 42.  This isolates a missing/torn result publication from a lost
# completion increment (which would correctly keep the join waiting).
nelisp-thread-standalone-smoke|scripts/nelisp-standalone-build.el|s/(ptr-write-u64 result 0 sum)/(ptr-write-u64 result 0 0)/|a GC-free worker signals completion without publishing its partial sum, so join returns but the interpreter observes 0 instead of 42|linux-x86_64
# nelisp-thread-allocating-standalone-smoke: Doc 199's bounded-section active
# publication.  Parked collection and both reclaim guards key off this bit;
# forcing the unique begin store to zero makes the existing section-exit
# assertion fail deterministically before a false-safe section can pass.
# The old inhibit-begin mutation stopped going red once the park barrier
# landed: with workers parked the collector marks their private roots
# correctly, so disabling the inhibit no longer corrupts anything -- that is
# the improvement working, not a weak gate.  The reachable defect now is
# bypassing the park itself, which is pinned on the percpu-roots gate below.
# Stage 4c/Tier 3a regression: bypass both inline worker-live checks while
# leaving Linux empty-chunk reclaim enabled.  The ordinary unlimited smoke can
# pass this defect; the same target's second `ulimit -v 4000000' run
# deterministically exposes a worker-visible arena chunk being unmapped.
# NOTE (2026-08-25): the historical release-guard mutation was removed before
# Tier 3b because the then-inhibited smoke could not reach it.  Tier 3b still
# leaves reclaim disabled under active workers; the real parked-collection
# proof below deliberately exercises mark/sweep, not munmap.
# nelisp-thread-percpu-roots-smoke: Doc 199 Tier 3b's real parked-collection
# checkpoint.  The clean gate reaches the main-thread garbage-collect path,
# parks all three workers, executes mark/sweep, and only then reads index 18.
# Corrupting that exact reached observation to zero makes the mandatory
# last-parked=3 assertion fail.  This is intentionally an observer-boundary
# mutation: the native park path still executes, so no stale-binary rebuild can
# accidentally turn an unreachable mutation into a false green result.
nelisp-thread-percpu-roots-smoke|tools/nelisp-thread-percpu-roots-smoke.el|s/(nth 18 (nelisp--debug-switch 0))/0/|the reached post-mark/sweep diagnostic reports zero parked workers, so the mandatory last-parked=3 assertion fails|linux-x86_64
# A second percpu row bypassing nl_thread_park_request_begin was tried and
# REMOVED: it reproduces 10/10 locally but CI round 25 reported it as
# 'STAYED GREEN with a real defect in front of it', i.e. the defect it injects
# is environment-sensitive and the gate cannot be relied on to catch it there.
# The remaining percpu row (forcing the parked-worker diagnostic to read 0) is
# the one CI itself confirms goes 'red as it should', so park coverage rests on
# that assertion rather than on a flaky second injection.
# standalone-reader-frame-stack-pop-desync-smoke: added 2026-08-23 (Doc 180
# Phase 2 item 1).  The injection reverts `nelisp_eval_call_recorded_done''s
# pop back to unconditional -- the exact pre-fix defect -- by deleting the
# `pushed' guard the fix added.  The identical substring appears a second
# time in `nl_driver_eval_with_recorded_roots' (the same fix, same shape,
# applied there too); the sed has no `/g' but matches the whole line so
# both occurrences (one per line) are reverted.  Only the first matters for
# this smoke (it goes through `nelisp_eval_call', not the driver-level
# wrapper), and reverting the second is harmless here since nothing in this
# smoke exercises it.  Measured RED with this exact injection applied:
# depth=5 at the checkpoint (want 1024); GREEN with it removed.
standalone-reader-frame-stack-pop-desync-smoke|scripts/nelisp-standalone-build.el|s/(if (= pushed 1) (nl_gc_ctx_pop) 0)/(nl_gc_ctx_pop)/|the recorded frame-stack depth counter under-counts during unwind past the ring's capacity (the pop/depth-cap desync this smoke exists to catch)
# standalone-reader-bounded-backtrace-smoke: added 2026-08-23 (Doc 180
# Phase 2 item 3).  The injection reverses `nl_bt_capture_frames_from''s
# write index, so buffer slot 0 receives the INNERMOST ring frame instead
# of the outermost one of the captured window -- the captured window's
# names come out reversed (outermost-first: level-a, level-b, level-c,
# error) instead of innermost-first.  Measured RED with this exact
# injection applied: "nelisp-doc180-level-c" not found after "error"'s
# position (the smoke's own ordered-position check); GREEN with it
# removed.
standalone-reader-bounded-backtrace-smoke|scripts/nelisp-standalone-build.el|s/(ptr-write-u64 (+ (data-addr nl_bt_snapshot) (+ 16 (\* i 8))) 0/(ptr-write-u64 (+ (data-addr nl_bt_snapshot) (+ 16 (* (- count (+ i 1)) 8))) 0/|the bounded backtrace prints outermost-first instead of innermost-first (the capture write index is reversed)
# standalone-midform-gc-bounded: Doc 152 Stage 4c/Stage 5's against-the-bug row.
# Disabling the Linux-only capability leaves ordinary sweep sound but restores
# the measured ~262 B/iteration RSS growth; the 500k->1M plateau and positive
# reclaimed-chunk assertions must both go red.  Stage 5 removed the gate's
# explicit debug-switch arm, so this row now reaches reclamation through the
# production default boot state.  The mutation harness rebuilds the injected
# and restored binaries because this row changes generated native code, not
# the host-side checker.
standalone-midform-gc-bounded|scripts/nelisp-standalone-build.el|s/(defun nl_os_empty_chunk_reclaim_p () 1)/(defun nl_os_empty_chunk_reclaim_p () 0)/|Linux sweep stops releasing proven-empty growth chunks, restoring linear default-loop RSS growth|linux-x86_64
# precise-root-coverage: the row this gate was built around.  Deleting the
# executing form's own root arm is the defect shape any change to the recorded
# root set risks, and in the SHIPPED configuration it is invisible: measured
# 2026-08-29, with the arm gone the reader-smoke suite (41 smokes) and the full
# anvil module load are both still green, because the conservative native-stack
# scan holds the form up.  With the scan off the same binary dies immediately.
# RED prints: `precise_root_fail label=no-conservative-scan reason=nonzero-exit
# rc=139' and `... label=no-conservative-scan-no-pool-walk ... rc=139', with
# `label=shipped acc=1750000' still passing above them -- that contrast is the
# point of the row.  GREEN prints three `precise_root_result' lines,
# `acc=1750000 collections=6' each.  Structural, not timing-dependent: the arm
# is gone, so the form has no precise root at all.
precise-root-coverage|scripts/nelisp-standalone-build.el|s/(nl_seq2 (nl_gc_mark_recorded_slot result) ; executing form . subform/(nl_seq2 0 ; MUTATION: executing form is not a root/|the executing top-level form is no longer a precise root, so only the conservative stack scan keeps it alive
# precise-root-coverage gained two `setq' cases when that construct was closed.
# They carry NO row here, deliberately: the defect they cover is an ordering
# inside one CPS chain (the cdr taken before the eval instead of after), which
# no single `sed -i' expression restores, and a row that cannot be written
# honestly is worse than none.  Their red-ness was still measured the same way
# a row proves itself -- built at the parent commit and run:
#   precise_root_fail label=setq-collects-in-value reason=nonzero-exit rc=1
#   nelisp: form aborted without signal (rc=1)
# and green after: `label=setq-collects-in-value marker=7'.  The row above
# already holds the gate itself lethal.
# standalone-reader-socket-smoke: added 2026-08-24 (socket primitives,
# Doc 184 follow-on, feat/socket-primitives-p1).  The injection changes
# accept4's raw syscall number from 288 (the real Linux x86_64 number)
# to 289 (unused/EINVAL territory) in `nl_socket_accept_impl'.  Manually
# verified before adding this row: RED with the injection applied
# (`[standalone-reader] FAIL: socket smoke: socket smoke: round-trip
# failed, stdout="ROUNDTRIP=ERR:(nelisp-socket-error -22)...'` --
# `nelisp-socket-connect' + `nelisp-socket-accept' still run, but
# accept4 now fails with EINVAL(-22) instead of returning the queued
# connection), GREEN again with the injection reverted.  Chosen over
# corrupting the sockaddr byte layout or the connect/listen syscall
# numbers because those either segfault (wrong offsets) or fail at
# `nelisp-socket-listen'/`-connect' before ever reaching accept, giving
# a less specific signal than "this exact syscall number is what
# matters, and this gate reads it, not just \"a socket call failed
# somewhere\"".
# Row text UPDATED 2026-08-24 (Doc 194 P3, feat/network-process-p345):
# `nl_socket_accept_impl' gained a NOWAIT/`flags' argument (P3's own
# arity extension, S3.3 item 1), changing the 5th `syscall-direct'
# argument from a literal `0' to the `flags' variable -- the OLD row's
# exact-text pattern went stale (matched nothing, caught by this exact
# phase's own gate-mutation run) without this update.  Same defect,
# same target, new surrounding text; re-verified RED-with-injection
# (accept4 fails every connection with EINVAL) / GREEN-with-injection-
# reverted before updating.
standalone-reader-socket-smoke|scripts/nelisp-standalone-build.el|s/(cfd (syscall-direct 288 (wf_argval args 0) 0 0 flags 0 0)))/(cfd (syscall-direct 289 (wf_argval args 0) 0 0 flags 0 0)))/|accept4 issues the wrong raw syscall number, so nelisp-socket-accept fails on every connection instead of returning the queued one|linux-x86_64
# Doc 194 P0 (feat/network-process-p0): manually verified RED-with-
# injection / GREEN-with-injection-reverted before adding -- the
# synchronous client's initial status flips from `open' to `closed' at
# construction time -- `process-status' would read `closed' on a
# connection that is, in fact, live, which is exactly the
# "process-status reads open the instant make-network-process returns"
# contract Doc 194 S1.3 measured against real Emacs 30.1 and this gate's
# own `roundtrip' case checks.
standalone-reader-network-process-smoke|packages/nelisp-process-adapter/src/nelisp-process-adapter.el|s/(proc (nelisp--make-network-process-object name 'open fd)))/(proc (nelisp--make-network-process-object name 'closed fd)))/|a freshly-connected client process reports status closed instead of open, breaking Doc 194 S1.3's measured "open the instant make-network-process returns" contract
# Doc 194 P1 (feat/network-process-p0): manually verified RED-with-
# injection / GREEN-with-injection-reverted before adding --
# `nelisp--hosts-file-lookup' always answers nil, as if `/etc/hosts' (or
# the fixture) were never consulted at all -- the P1 positive case (a
# hostname resolved purely from the fixture, no DNS round trip) fails.
standalone-reader-hosts-file-smoke|packages/nelisp-process-adapter/src/nelisp-process-adapter.el|s/(cdr (assoc name (nelisp--hosts-file-table file))))/(and nil (cdr (assoc name (nelisp--hosts-file-table file)))))/|/etc/hosts lookup always answers nil, as if the file (or its fixture) were never consulted
# Doc 194 P2 (feat/network-process-p0): manually verified RED-with-
# injection / GREEN-with-injection-reverted before adding --
# `nelisp--dns-byte''s own out-of-bounds guard is disabled -- a
# truncated/malformed DNS response no longer signals the catchable,
# DNS-specific `nelisp-dns-error'; the parse instead falls through to the
# raw, unguarded native `string-byte' primitive this parser is built on,
# which (measured, Doc 194 P2) has NO bounds check of its own and
# silently returns a wrong value with no error at all -- exactly the
# length-prefix/RDATA parsing defect class Doc 194's own P2 exit
# criterion asks this gate to catch.
standalone-reader-dns-smoke|packages/nelisp-process-adapter/src/nelisp-process-adapter.el|s/(if (or (< i 0) (>= i (string-bytes buf)))/(if (or nil nil)/|the DNS response parser's own bounds check never fires, so a truncated/malformed response silently reads a wrong value from the unguarded native string-byte primitive instead of signalling the catchable nelisp-dns-error
# Doc 194 P3 (feat/network-process-p345): manually verified RED-with-
# injection / GREEN-with-injection-reverted before adding -- this is the
# EXACT defect this phase's own implementation hit and fixed (see
# `nl_socket_poll_impl''s own comment and this smoke's Makefile comment):
# `230' is `clock_nanosleep' on the Linux x86_64 ABI, not `poll' (`7').
# Re-injecting it reproduces the original silent-wrong-answer bug --
# `nelisp-socket-poll' always answers not-ready, no hang, no signal --
# measured RED at `connect-ok=(t t nil t 0) connect-refused=(t t nil t
# t)' (the two `nil's are the `ready' checks, both wrongly false), GREEN
# again with `7' restored.
standalone-reader-nonblocking-socket-smoke|scripts/nelisp-standalone-build.el|s/(let\* ((rc (syscall-direct 7 pfd 1 timeout 0 0 0))/(let* ((rc (syscall-direct 230 pfd 1 timeout 0 0 0))/|nelisp-socket-poll issues clock_nanosleep(230) instead of poll(7), so it silently always answers not-ready regardless of true socket readiness|linux-x86_64
# feat/windows-socket-arm: Windows-only refusal-bound proof from the gate's
# Makefile comment.  `events' 0 makes WSAPoll wait out its full timeout.
# Clean: PASS, elapsed2=2.040.  Injected: FAIL, elapsed2=5.009, beyond the
# relaxed 3.0s refusal bound.  Restored: PASS again.
standalone-reader-nonblocking-socket-smoke|scripts/nelisp-standalone-build.el|s/(events (if (= want_write 1) 16 768))/(events 0)/|the Windows WSAPoll arm requests no events, so refusal readiness waits the full 5s timeout and violates the deliberately relaxed 3s bound|windows-x86_64
# Schannel slice 3: structural carry-over defect.  The first live Cloudflare
# read enters SEC_I_RENEGOTIATE; throwing away its SECBUFFER_EXTRA suffix
# removes the following application record, so the gate cannot observe HTTP.
# This row rebuilds because the injected helper is baked into nelisp.exe.
# Proved 2026-09-03 on windows-x86_64: the restricted harness printed "went
# red as it should" for this row, restored/rebuilt, then reported
# `checked=1 findings=0 passed=1 failed=0 skipped=0`.
standalone-reader-tls-smoke|scripts/nelisp-standalone-build.el|s/(ptr-write-u64 ctx 56 extra)/(ptr-write-u64 ctx 56 0)/|Schannel discards every SECBUFFER_EXTRA encrypted suffix instead of carrying it into the next decrypt, so the live HTTPS response loses the HTTP record|windows-x86_64
# Doc 194 P4 (feat/network-process-p345): manually verified RED-with-
# injection / GREEN-with-injection-reverted before adding -- the async
# connect-completion status machine's own success/failure branch is
# inverted (a real SO_ERROR=0 success is misread as errno 999, a value
# `nelisp-socket-connect-error' can never actually return), so a
# genuinely successful nonblocking connect fires the sentinel with
# "failed with code 0\n" and transitions to `failed' instead of `open' --
# measured RED at `mixed=(failed ("failed with code 0\n") nil
# ("finished\n"))' (the "ok" case's own network status, expected `open').
standalone-reader-network-process-nowait-smoke|packages/nelisp-process-adapter/src/nelisp-process-adapter.el|s/(if (= err 0)/(if (= err 999)/|a successful nonblocking connect (real SO_ERROR 0) is misclassified as failed, firing "failed with code 0\n" instead of transitioning to open
# windows-x86_64 async-process fixture bound: suppress the shared-loop pump
# entirely, so the child can start and write but its output can never reach
# the filter.  The first fixture-dependent accept-process-output must exhaust
# its relaxed 3.0s bound and the gate must fail with filter=nil; this proves
# that the bound still bites instead of masking a genuine delivery hang.
# Measured 2026-09-02 on windows-x86_64: the restricted harness reported
# "went red as it should" for this row (checked=1, failed=0); after its trap
# restored the file, the clean gate passed with filter=("first-" "second")
# and the live-child sentinel "terminated\n".
standalone-reader-process-adapter-smoke|packages/nelisp-process-adapter/src/nelisp-process-adapter.el|s/(when (nelisp-process-adapter--drain-and-fire p)/(when (and nil (nelisp-process-adapter--drain-and-fire p))/|the shared process pump never fires, so fixture output cannot be delivered and the relaxed Windows wait must expire red|windows-x86_64
# Doc 194 P5 (feat/network-process-p345): manually verified RED-with-
# injection / GREEN-with-injection-reverted before adding -- an accepted
# child's `:filter' is dropped (set to nil) instead of copied from the
# listener, so a client's data never reaches its own filter and the
# smoke's own `filters-shared'/`both-received' checks both flip false --
# measured RED at `(listen 5 2 t nil nil t listen closed t)' (positions
# 4 and 5, expected both `t'), GREEN again with the real copy restored.
standalone-reader-network-process-server-smoke|packages/nelisp-process-adapter/src/nelisp-process-adapter.el|s/(process-put child :filter (process-get proc :filter))/(process-put child :filter nil)/|an auto-accepted child's filter is dropped instead of inherited from the :server t listener, so per-connection data never reaches the caller's filter
# Doc 194 P7 (feat/socket-ipv6): manually verified RED-with-injection /
# GREEN-with-injection-reverted before adding.  `nl_socket_build_
# sockaddr6' writes sin6_family = AF_INET(2) instead of AF_INET6(10) --
# the constructed 28-byte struct is used for a socket the caller already
# created with `domain = AF_INET6' (`nl_socket_listen_impl'/-connect_impl
# decide the syscall's own domain argument independently, upstream of
# this call, so that part stays correct), but the struct's OWN family
# field now disagrees with it, and the kernel rejects the mismatch --
# measured RED: every IPv6-literal listen/connect in the smoke
# (::1 loopback round trip, the bracketed/full-form check, `:family
# 'ipv6'/open-network-stream over ::1) fails with `(nelisp-socket-error
# -97)' (EAFNOSUPPORT), while the parts needing no live IPv6 socket
# (the pure-elisp literal table, the AAAA DNS fixture, the /etc/hosts
# typed lookup) still pass -- exactly the shape a sockaddr_in6 family-byte
# defect should have, not a wholesale crash.  GREEN again with `10'
# restored.
standalone-reader-ipv6-socket-smoke|scripts/nelisp-standalone-build.el|s/(ptr-write-u8 out_buf 0 10)/(ptr-write-u8 out_buf 0 2)/|nl_socket_build_sockaddr6 writes AF_INET(2) instead of AF_INET6(10) into sin6_family, so every real IPv6 socket call fails with EAFNOSUPPORT even though domain=AF_INET6 was still passed to socket(2) correctly
# nl-clj-async repeated-resumption gate: manually injected before this
# row was added.  Replacing the reached successful-take unwrap with a
# sentinel made `make nl-clj-async-standalone-smoke' exit 2 through its
# blocking result check instead of printing a green GATE-COUNT; restored,
# the widened smoke reports `GATE-COUNT checked=10 findings=0'.
nl-clj-async-standalone-smoke|packages/nl-clj/src/nl-clj-async.el|s/^  (if (eq (car v) :closed) nil (cadr v)))$/  (if (eq (car v) :closed) nil :gate-mutated))/|successful nl-clj-<! takes unwrap to a sentinel instead of their payload, so the repeated-take fixture and wrapper-based ping/pong cannot complete correctly
# The version drift that took stage-d red on all four platforms on
# 2026-08-26: the tarball was built as v1.0.1 and the installer, whose
# default is a self-contained copy, still said v0.6.0.  Reproduce that
# exact drift and require the gate to catch it.
version-consistency|release/stage-d-v3.0/install-v3.sh|s/RELEASE_VERSION:-v[0-9][0-9.]*/RELEASE_VERSION:-v0.0.0/|installer default left behind at a stale release version
# The Windows half of the same drift: install-v3.ps1 and the two tarball
# scripts were self-consistent at v0.6.0 while the POSIX side had moved to
# v1.0.1, so CI shipped v0.6.0-labelled tarballs for a v1.0.1 release and
# nothing complained.  Found on 2026-08-26 by the macOS lane, not by CI.
version-consistency|release/stage-d-v3.0/install-v3.ps1|s/\[string\]\$Version = "v[0-9][0-9.]*"/[string]$Version = "v0.0.0"/|windows installer default left behind at a stale release version
# Doc 201 §5.2/§4 item 1 (cold-start hand-off, 2026-08-30): the
# literal-separator fast path `nlre-split-string' added over the old
# always-regexp-engine `split-string' behavior.  Disabling the fast-path
# guard forces every split-string call back through `nlre-string-match',
# which was measured at 3.2-3.4s for a single real PATH split vs 0.4s
# fast-path -- the new `standalone-reader-splitstring-perf-smoke' gate
# exists to catch exactly this regression.  Verified BY HAND (inject via
# Edit, `make standalone-reader`, run the gate, restore, rebuild, run the
# gate again): RED at elapsed=2.707s pass=nil with this injection, GREEN
# at elapsed=0.369s pass=t restored.  NOT yet proven through `make
# gate-mutation-verify GATE=standalone-reader-splitstring-perf-smoke'
# itself -- that run did not respect NELISP_GATE_MUTATION_ONLY and swept
# unrelated rows (one, standalone-midform-gc-bounded, was killed mid-row
# and left its own injection in scripts/nelisp-standalone-build.el,
# restored via `git checkout`), taking 20+ minutes before being aborted.
# Root cause not diagnosed; re-run `gate-mutation-verify` on this row
# before trusting it as the row's own proof.
# RE-POINTED 2026-08-30 (Doc 201 §6.6 item 4).  The row used to disable the
# literal-separator fast path alone, and that stopped being lethal -- the
# harness reported "STAYED GREEN with a real defect in front of it" on the
# first run after Doc 201 §6.5 landed.  Not a weak gate: the improvement
# working, which is failure mode 2 in this file's header.  §6.5 gave
# `nlre-string-match' a leading-character filter, and a literal separator is
# exactly the shape that filter helps most, so the regexp path this row
# forces `split-string' down is no longer slow enough to breach the gate's
# bound.  Measured on the gate's own 60-entry synthetic PATH,
# windows-x86_64:
#
#   as-is                                  0.375s
#   literal fast path off (the old row)    0.736s   <- under the 1.0s bound
#   both off (this row)                    3.525s
#
# A self-baselined ratio bound was measured as an alternative and rejected:
# split-string against `nelisp--split-on-char' on the same input in the same
# process gives 2.72-2.80 as-is and 4.21-4.58 with the old injection, a
# separation of only ~1.55x -- failure mode 3 territory, not worth pinning a
# CI gate to.
#
# So the row now removes BOTH of the defences that keep a literal separator
# off the backtracking scan.  The claim it proves is weaker than the old
# one by exactly that much, and is stated rather than implied: the gate
# catches the loss of literal-separator splitting, but no longer the loss of
# either single defence on its own.  The lead filter has its own gate and
# its own two rows (standalone-reader-regexp-lead-filter-smoke).
#
# Evidence, windows-x86_64, 2026-08-30, through the real harness with the
# target pinned: `NELISP_STANDALONE_TARGET=windows-x86_64
# NELISP_GATE_MUTATION_ONLY=standalone-reader-splitstring-perf-smoke
# tools/nelisp-gate-mutation.sh' -> "PASS (1 gates caught their injected
# defect)".
#
# REPAIRED 2026-09-03 (failure mode 3).  The absolute 1.0s verdict disagreed
# across hosts.  WSL Emacs 30.1 measured 0.043-0.045s clean and 0.596-0.653s
# with this injection: ~14x slower, but still 0.347-0.404s inside the bound,
# so the gate stayed green.  The smoke now prints elapsed time only as a
# diagnostic and structurally requires `nlre--string-match-calls' to remain
# zero.  This injection is therefore proved by entering the regexp engine,
# independent of how quickly that host executes it.
standalone-reader-splitstring-perf-smoke|lisp/nelisp-stdlib-regexp.el|s/(if (and separators/(if (and nil separators/; s/^(defun nlre--leading-lit-char (nodes)$/(defun nlre--leading-lit-char (nodes) (setq nodes nil)/|disables BOTH literal-separator defences (the split fast path and the leading-character filter), forcing every split-string call through the full backtracking scan
# Doc 201 §4 item 1.  `standalone-reader-winpath-smoke' polices the three
# windows-native path defects Doc 201 §1/§2 and its §4 follow-up fixed:
# `path-separator' vs `system-type', `file-exists-p' answering yes/no at all,
# and `expand-file-name' keeping a drive designator instead of anchoring it
# onto "/".  The injection puts a bogus component into every expanded name,
# which is structural (the constant is wrong; nothing depends on timing or
# on which platform runs it) and lethal on Linux and Windows alike -- the
# gate's `exp-ok' and `find-ok' assertions both fail.
#
# Evidence, windows-x86_64, 2026-08-30.  RED with the injection:
#   [winpath-smoke] FAIL: sep=";" abs-ok=t exp-ok=nil yes-ok=t no-ok=t
#     found=nil pass=nil
# GREEN restored:
#   [winpath-smoke] PASS: sep=";" abs-ok=t exp-ok=t yes-ok=t no-ok=t
#     found=".../target/tmp/winpath-smoke/nelisp-doc201-marker" pass=t
standalone-reader-winpath-smoke|scripts/nelisp-stdlib-prelude.el|s#(concat (or drive "") "/" (mapconcat#(concat (or drive "") "/zz/" (mapconcat#|expand-file-name injects a bogus component into every name it expands
# Doc 201 §5.3.  `standalone-reader-defvar-alloc-smoke' polices §5.1's
# replacement of `nl_sf_defvar'/`nl_sf_defconst''s per-call AST synthesis
# with direct primitives.  The row targets the half of that fix with a
# structural failure mode: the `boundp' gate the synthetic
# `(if (boundp ...) nil (set ...))' form used to carry had to be
# re-implemented by hand, and losing it makes `defvar' clobber an
# already-bound value.  Forcing that branch to nil is a wrong constant, not
# a timing question, and reads the same on every platform.
#
# The row deliberately does NOT try to re-create the ~25x allocation
# regression the gate's other assertion bounds: the pre-fix body was a
# 12-line AST synthesis plus a recursive `nelisp_eval_call', which no small
# `sed' expression reproduces, and this file's own header rules out subtle
# injections.  That assertion is held by its own budget (200 allocations per
# form, against 65 measured after the fix and ~1629 before it), read from
# the allocator's exact counters rather than a clock.
#
# Evidence, windows-x86_64, 2026-08-30.  RED with the injection:
#   [defvar-alloc-smoke] FAIL: per-form=63 budget=200 keep-ok=nil
#     const-ok=t pass=nil
# GREEN restored:
#   [defvar-alloc-smoke] PASS: per-form=65 budget=200 keep-ok=t
#     const-ok=t pass=t
#
# RETARGETED 2026-09-17 (gate-wiring audit).  The row above stopped matching:
# `bf_defvar_init_mode' (scripts/nelisp-standalone-build.el) renamed its
# parameter from `name_ptr' to `sym' and `name_ptr' now occurs zero times in
# the file, so `sed' matched nothing and the row proved nothing since the
# rename.  Its literal boundp-gate defect is not available to re-point at
# either: the `special-variables' row (below) already mutates that exact
# call site (`(if (= (nelisp_mirror_is_bound (+ env 0) sym (+ env 64)) 1) 0
# 1)') for the identical defect (`defvar' clobbers an already-bound value) --
# re-pointing here would make the two rows duplicates of the same defect at
# the same line, not two distinct ones, which is exactly what the brief that
# ordered this retarget said to avoid.
#
# Retargeted instead at this smoke's OTHER semantic assertion: `defconst'
# MUST overwrite an already-bound value (`const-ok'), the opposite polarity
# of defvar's guard.  First attempt (changing `bf_defconst_install''s
# `(bf_set_defvar_value env sym val out 2)' mode literal from 2 to 0) was
# tried and measured NON-LETHAL: `bf_set_defvar_value' only special-cases
# `(= mode 1)' (the declared-default path); every other mode, 0 or 2 alike,
# falls through to the same unconditional `bf_set_dynamic_value' call, so
# the mode argument there is decorative and const-ok stayed t.  Recorded
# here rather than silently discarded, per this file's own header.
#
# The real boundp gate defvar gets lives one level up, in `nl_sf_defvar'
# (the caller): `(let* ((mode (bf_defvar_init_mode env name_ptr out))) (if
# (= mode 0) (seq (nl_sexp_clone_into name_ptr out) 0) ...))' -- an early
# return BEFORE `bf_set_defvar_value' is ever reached.  `nl_sf_defconst'
# has no such check; it calls `(bf_defconst_install env name_ptr val_slot
# out)' unconditionally.  The lethal injection copies defvar's guard onto
# that call site: if NAME is already bound, yield it unchanged instead of
# installing.  Same defect shape as the original row (defvar's boundp gate
# migrating somewhere it does not belong), different call site and
# different assertion than `special-variables'.
#
# Evidence, linux-x86_64, 2026-09-17 (gate-wiring audit).  RED with the
# injection:
#   [defvar-alloc-smoke] FAIL: per-form=108 budget=200 keep-ok=t
#     const-ok=nil pass=nil (expected pass=t -- defvar/defconst regressed
#     to AST synthesis, or lost their boundp gate)
# GREEN restored:
#   [defvar-alloc-smoke] PASS: per-form=108 budget=200 keep-ok=t
#     const-ok=t pass=t
standalone-reader-defvar-alloc-smoke|scripts/nelisp-standalone-build.el|s/(bf_defconst_install env name_ptr val_slot out)/(if (= (nelisp_mirror_is_bound (+ env 0) name_ptr (+ env 64)) 1) (seq (nl_sexp_clone_into name_ptr out) 0) (bf_defconst_install env name_ptr val_slot out))/|defconst adopts defvar's boundp gate and stops overwriting an already-bound value
# Doc 201 §4 item 2.  `standalone-reader-fileattrs-smoke' polices the
# windows-native `stat'/`readdir' shims and the `nelisp--syscall-stat'
# classification that moved onto st_mode with them.  The injection changes
# the S_IFDIR constant the classifier compares against, so no directory is
# ever recognised as one -- a wrong constant, lethal on every platform,
# with no timing or load component.
#
# Evidence, windows-x86_64, 2026-08-30.  RED with the injection:
#   [fileattrs-smoke] FAIL: size-ok=t mtime=1788083979 reg-ok=t dir-ok=nil
#     names=("sub" "ten-bytes.txt") absent-ok=t pass=nil
# GREEN restored:
#   [fileattrs-smoke] PASS: size-ok=t mtime=1788084019 reg-ok=t dir-ok=t
#     names=("sub" "ten-bytes.txt") absent-ok=t pass=t
standalone-reader-fileattrs-smoke|scripts/nelisp-stdlib-prelude.el|s/(= (logand mode 61440) 16384)/(= (logand mode 61440) 16385)/|nelisp--syscall-stat compares st_mode's file-type bits against a wrong S_IFDIR, so no directory is recognised
# Doc 201 §5.4, row 1 of 2 (the CORRECTNESS half).  A leading-character
# filter that fires where it must not skips real matches, and no timing
# assertion can see that -- so this row breaks the filter's own case
# handling: it folds the required character with `nlre--flip-case' instead
# of `nlre--fold-char', so the character a pattern demands is never the one
# the string can offer and every leading-literal pattern stops matching.
# A wrong function, not a wrong duration.
#
# Evidence, windows-x86_64, 2026-08-30.  RED with the injection (note the
# first `got' element, `(string-match "abc" "xxabcyy")', turning nil):
#   [regexp-lead-filter-smoke] FAIL: got=(nil nil 2 nil 2 0 nil 0 nil) ...
#     ratio=0.041 pass=nil
# GREEN restored:
#   [regexp-lead-filter-smoke] PASS: got=(2 nil 2 nil 2 0 nil 0 nil) ...
#     ratio=0.047 pass=t
standalone-reader-regexp-lead-filter-smoke|lisp/nelisp-stdlib-regexp.el|s/(lead (and lead (nlre--fold-char lead)))/(lead (and lead (nlre--flip-case lead)))/|the leading-character filter folds the required character the wrong way, so no leading-literal pattern matches at all
# Doc 201 §5.4, row 2 of 2 (the PATH-SELECTION half).  Makes
# `nlre--leading-lit-char' answer nil for every pattern, so the filter never
# fires and the scan is exactly what it was before the fix.  Correctness is
# untouched.  The smoke now counts calls that actually select the filter and
# requires exactly 42 for its 42 leading-literal cases.
#
# The original verdict was a ratio of two sweeps run back to back in one
# process on the same strings: measured 0.531 injected vs 0.048 restored
# against a 0.25 threshold.  CI later disproved the assumption that this
# self-baseline made the timing row portable; the replacement below is why
# the row remains admissible now.
#
# Evidence, windows-x86_64, 2026-08-30.  RED with the injection:
#   [regexp-lead-filter-smoke] FAIL: got=(2 nil 2 nil 2 0 nil 0 nil) ...
#     lead=2.488 nolead=4.682 ratio=0.531 pass=nil
# GREEN restored:
#   [regexp-lead-filter-smoke] PASS: got=(2 nil 2 nil 2 0 nil 0 nil) ...
#     lead=0.281 nolead=5.899 ratio=0.048 pass=t
#
# REPAIRED 2026-09-03 (failure mode 3).  Despite the same-process ratio, CI's
# runner stayed green with this injection.  WSL Emacs 30.1 measured ratios
# 0.065-0.071 clean against the <0.25 threshold and 0.272-0.301 injected --
# only 0.022-0.051 across the threshold.  Ratio and times remain diagnostics,
# but the verdict now uses the exact path-selection count: 42 clean, 0 when
# this injection makes the filter unreachable.
standalone-reader-regexp-lead-filter-smoke|lisp/nelisp-stdlib-regexp.el|s/^(defun nlre--leading-lit-char (nodes)$/(defun nlre--leading-lit-char (nodes) (setq nodes nil)/|the leading-character filter never fires, putting the scan back to full work at every start position
# Stage D1 (2026-09-03).  The Windows-scoped FFI gate must execute the PE
# import-backed arm, not merely find the builtin name.  Replacing the mapped
# system DLL with a nonexistent name leaves the generated dispatch intact but
# makes the PE loader reject the binary, a structural failure independent of
# timing or installed developer tools.  Evidence, windows-x86_64, 2026-09-03:
# the filtered harness reported "went red as it should" and
# `GATE-COUNT checked=1 findings=0 passed=1 failed=0 skipped=0`; restored GREEN
# is `[standalone-reader-ffi-smoke] PASS` with toupper, s32 and f64 checks green.
standalone-reader-ffi-smoke|scripts/nelisp-standalone-build.el|s/("libm.so.6" . "ucrtbase.dll")/("libm.so.6" . "ucrtbase-broken.dll")/|the Windows libm mapping names a nonexistent DLL, so the PE-import-backed FFI binary cannot load|windows-x86_64
# v1.2.1 host-parity gate.  Two rows, one per half of what it holds: the
# `load' semantics (gap 1) and the coding pair (gap 6).  Both defects are the
# state v1.2.0 shipped in, so each row restores that release exactly -- which
# is the point: the gate has to notice a REGRESSION to the previous release,
# not just an arbitrary edit.
standalone-reader-host-parity-smoke|scripts/nelisp-standalone-build.el|s/(bf_load_with_lfn args env out)/(bf_load_readable args env out)/|`load' stops binding `load-file-name', as in v1.2.0 -- every (or load-file-name buffer-file-name) idiom silently sees nil
standalone-reader-host-parity-smoke|scripts/nelisp-stdlib-prelude.el|s/(if (fboundp .string-as-multibyte) (string-as-multibyte str) str)))/str))/|`decode-coding-string' is the identity again, as in v1.2.0 -- decoded text stays unibyte and `length' counts bytes
# 2026-09-05 print-large-sexp smoke (tools/print-large-sexp-smoke.el), added
# with the nelisp-llm crash investigation (both consumer faults were the
# e240485bb conservative-pin class; the printer was never the faulting frame,
# this smoke pins its contract on the shipped binary anyway).  The row drops
# the element separator in `nelisp--prn-list-body' -- the ONE line in the
# prelude with that exact text (the vector and record printers spell theirs
# differently), so the injection lands once.  Proven with `make
# gate-mutation-verify GATE=standalone-reader-print-large-sexp-smoke' before
# landing: mutated, the smoke prints `FAIL: 10k-int list printed length ->
# 60002' (exit 1); restored, `PASS: 10k-int list 70001 bytes exact, ...'.
standalone-reader-print-large-sexp-smoke|scripts/nelisp-stdlib-prelude.el|s/(unless first (nelisp--prn-chunks-add chunks " "))/(unless first (ignore chunks " "))/|list elements print with no separator between them: a 10k-int list comes out 60002 bytes instead of 70001, text no reader could take back
#
# native-unit-repl-smoke (WS-F, 2026-09-12): widening the raw-v1 argument
# ceiling by one lets a seven-argument export compile and publish, which
# the smoke's requirement-3 boundary check expects refused.  Verified
# red-then-green by hand and via `make gate-mutation-verify
# GATE=native-unit-repl-smoke' before landing: mutated, `make
# native-unit-repl-smoke' failed with `nelisp: uncaught error: ...
# error: ("smoke check failed: seven-arg-export-refused-at-compile")' and
# `GATE-COUNT checked=33 findings=1' (exit 2); restored (confirmed
# byte-identical via `git diff --stat'), `nelisp-native-unit-repl-smoke:
# PASS' and `GATE-COUNT checked=88 findings=0' (exit 0).
native-unit-repl-smoke|lisp/nelisp-native-load.el|s/^(defconst nelisp-native-load-raw-max-arity 6)$/(defconst nelisp-native-load-raw-max-arity 7)/|the declared six-argument ceiling widens to seven, so the refused-seven-argument-export assertion silently stops being true
lisp-byte-compile|lisp/nelisp-native-unit.el|s/^(require 'nelisp-native-load)$/(require 'nelisp-native-load)\n(defun nelisp-native-unit--mutation-probe () (let ((unused 1)) nil))/|a defun with an unused lexical binding in a lisp\/ file the baseline records as clean
# RETIRED 2026-09-18 -- failure mode #2 above, NON-LETHAL AFTER AN IMPROVEMENT.
# The injection below was lethal when written (2026-09-12) and is not any more,
# and that is `f25ec9af1' working, not a regression.  Measured with the gate's
# own reproducer plus a denser sweep of the same knobs, same instrument on both
# sides:
#
#   ffc862a66 (= f25ec9af1^) + injection : 41 crashes of 160 configurations
#   382f8d1cc (tip)          + injection :  0 crashes of 160
#
# The hit bands at the control are regular, not sparse -- varname 8 crashes at
# feature-name lengths 17-24 and 73-80, varname 9 at 10-16 and 65-72, and every
# leading-form count 0-8 -- so the dense sweep crosses them repeatedly and
# cannot have missed them by sampling.  216 configurations in total were silent
# at the tip across all three knobs the reproducer can turn (variable-name
# length, feature-symbol length, leading top-level form count).
#
# Cause, read from the diff rather than guessed: f25ec9af1 rewrites every
# `nl_bind_frame_fast' call into `bf_bind_frame_declared', which runs an extra
# `bf_mark_declared_special' per binding.  The reclaim side is untouched --
# `nl_boundary_reclaim', `nl_alloc_zero_fill' and `head-reclaimed' have zero
# changed lines in that commit, and `alloc-bytes 128' appears nowhere in it --
# so the hazard still exists; the 128-byte bind scratch simply no longer lands
# in the un-zeroed rewound span.  It is placement, not size.
#
# The gate itself STAYS REQUIRED (tools/ai/gates.expected): it still guards the
# regression it was written for.  What is retired is only the claim that this
# injection can prove it, which the measurements above refute.  A future row for
# this gate needs a program whose binding sequence lands back in the rewound
# span under the post-f25ec9af1 allocation, and `make gate-mutation-verify
# GATE=nelisp-sexp-clone-bind-smoke' is how to prove it before committing.
# nelisp-sexp-clone-bind-smoke|scripts/nelisp-standalone-build.el|s/(if (> head-reclaimed 0)/(if (> head-reclaimed 999999999)/|the reclaimed head span stops being zeroed, so a rewound bump cursor hands out memory the reader had already written to -- the original defect
#
# Contract suites (2026-09-16).  Each row below was run RED through this
# harness, and the gate was GREEN on the restored tree.
#
# builtin-arity: the fixed-arity contract table
# (`nelisp--builtin-fixed-arities' in scripts/nelisp-stdlib-prelude.el) is
# read TWICE -- once by the build driver, baked into the binary's native
# arity guards, and once by the test itself (fresh host-Emacs read) to
# compute the expected wrong-number-of-arguments boundary.  Moving `car'
# from the arity-1 group to the arity-2 group needs no rebuild: the test
# now expects a 1-argument `(apply '(builtin car) '(nil))' to be a wrong-
# arity call, but the untouched binary still accepts exactly one argument
# and returns `nil'.  Verified: mutated, `AssertionError: Lists differ...
# First differing element 59: 'nil' != '(wrong-number-of-arguments car 1)''
# (`GATE-COUNT checked=2 findings=1'); restored, `GATE-COUNT checked=2
# findings=0'.
builtin-arity|scripts/nelisp-stdlib-prelude.el|s/^  '((1 car cdr car-safe/  '((1 cdr car-safe/;s/^    (2 cons eq eql equal setcar setcdr nth nthcdr elt aref rassoc string=$/    (2 car cons eq eql equal setcar setcdr nth nthcdr elt aref rassoc string=/|car's declared fixed arity drifts from 1 to 2 in the build-time contract table while the already-built binary still enforces arity 1, so the test's independently recomputed expectation disagrees with the binary's real behavior
#
# special-variables: `bf_defvar_init_mode' (scripts/nelisp-standalone-
# build.el) decides whether `defvar' may skip re-initializing an
# already-bound global (mode 0) or must evaluate VALUE (mode 1) -- the
# override-protection Emacs `defvar' is defined by.  Forcing mode 0's
# branch to also answer 1 makes every `(defvar NAME VALUE)' re-evaluate
# VALUE even when NAME is already bound.  Baked into target/nelisp, so the
# gate is listed in `gate_needs_rebuild'.  Verified: mutated + rebuilt,
# `nelisp: uncaught error: ... error: ("must not run")' from the case that
# defvars an already-bound name with
# an error-signalling VALUE form (`GATE-COUNT checked=6 findings=2');
# restored + rebuilt, `GATE-COUNT checked=6 findings=0'.
special-variables|scripts/nelisp-standalone-build.el|s/(if (= (nelisp_mirror_is_bound (+ env 0) sym (+ env 64)) 1) 0 1)/(if (= (nelisp_mirror_is_bound (+ env 0) sym (+ env 64)) 1) 1 1)/|defvar's override-protection is defeated: an already-bound global is reinitialized, and its VALUE form re-evaluated, on every later (defvar NAME VALUE)
#
# symbol-identity: `nl_next_symbol_identity' (lisp/nelisp-cc-nlstr-direct-
# ops.el) is the atomic counter that gives every `make-symbol' call a
# distinct identity, which `nl_uninterned_symbol_equal' reads to tell two
# uninterned symbols apart under `equal'.  Pinning it to a constant makes
# every uninterned symbol answer the same identity.  Baked into
# target/nelisp, so the gate is listed in `gate_needs_rebuild'.  Verified:
# mutated + rebuilt, the 5 tests reported 9 subtest failures,
# including `equal-table-identities' overwriting the first key's slot
# (`GATE-COUNT checked=5 findings=9'); restored + rebuilt, `GATE-COUNT
# checked=5 findings=0'.
symbol-identity|lisp/nelisp-cc-nlstr-direct-ops.el|s/(+ (atomic-fetch-add 268435544 1) 1)/1/|every make-symbol call receives the same identity number, so two uninterned symbols sharing a name collide under an equal-test hash table instead of remaining distinct
#
# reference-parity: `tools/nelisp-reference-parity.py' is itself the
# checker `substrate-parity-smoke' and the CI "Emacs parity" step build
# their evidence on, so this gate's own suite (test/nelisp-reference-
# parity-test.py) is the only thing that can prove the checker refuses a
# false-positive comparison.  Disabling its reference-Emacs-major-version
# guard needs no rebuild (pure host Python).  Verified: mutated, the
# `{'version': 'GNU Emacs 31.1'}' false-positive case returned 0 instead
# of the required 1 (`GATE-COUNT checked=2 findings=1'); restored,
# `GATE-COUNT checked=2 findings=0'.
reference-parity|tools/nelisp-reference-parity.py|s/if not re.match(r'GNU Emacs 30\\.', report\['emacs_version'\]):/if False:/|the reference-Emacs major-version guard never fires, so a differential recorded against the wrong Emacs major version is accepted as a valid parity pass
#
# lsp-server: `WorkspaceIndex.search' (tools/nelisp_workspace.py) is what
# `candidates'/`references'/definition-navigation all route through for
# an exact, same-namespace lookup.  Dropping the name-equality half of its
# filter (keeping only the namespace half) needs no rebuild (pure host
# Python, loaded in-process via `runpy'). Verified: mutated, a 2-symbol
# `car'/1000-`carN' fixture returned 1001 candidates for the exact query
# `car' instead of 1, and a cross-file definition lookup returned 2
# locations instead of 1 (`GATE-COUNT checked=37 findings=2'); restored,
# `GATE-COUNT checked=37 findings=0'.
lsp-server|tools/nelisp_workspace.py|s/matches = \[item for item in plan\['symbols'\] if item\['symbol'\] == query/matches = [item for item in plan['symbols'] if True/|exact-name filtering is dropped from workspace candidate search, so every symbol in a namespace is returned as a candidate instead of only the ones actually named by the query
#
# native-symbol-test: `nl_gc_mark_slot' (scripts/nelisp-standalone-
# build.el) marks a Symbol's name-buffer block for both tag 4 (interned)
# and tag 16 (uninterned, `make-symbol'); this is the ONE gate that can
# observe a tag-16-specific marking regression, since the public API
# (symbol-identity above) has no way to read a GC mark bit directly.
# Dropping tag 16 from that check needs no separate rebuild entry: this
# gate's own Makefile recipe always builds its private fixture fresh (see
# the recipe's comment), so the mutated source is picked up on the very
# next invocation. Verified: mutated + the fixture's own fresh build,
# `symbol-test-mark' read back 0 instead of 1 both immediately after
# construction and after a `dump-runtime-image'/`eval-runtime-image'
# round trip (`GATE-COUNT checked=11 findings=2'); restored + rebuilt,
# `GATE-COUNT checked=11 findings=0'.
native-symbol-test|scripts/nelisp-standalone-build.el|/(if (or (= tag 4) (= tag 16))/{N;s/(if (or (= tag 4) (= tag 16))\n\(.*Symbol name; +8 may be an ID.*\)/(if (or (= tag 4) (= tag 999))\n\1/}|the precise GC marker stops marking an uninterned (tag-16) symbol's name buffer, so a reachable uninterned symbol's storage can be swept as garbage
#
# Project-toolchain contract suites. None mutates code baked into
# target/nelisp, so none needs an entry in gate_needs_rebuild.
#
# package-resolver: satisfies() decides whether a candidate release is
# inside a caret/bare requirement's [lower, upper) window. Excluding the
# lower bound makes a release exactly at that bound (the common case for a
# freshly published dependency) fail its own requirement.
package-resolver|tools/nelisp_packages.py|s/lower <= actual < upper/lower < actual < upper/|the inclusive lower bound of a caret or bare version requirement becomes exclusive, so a release exactly at that bound no longer satisfies its own requirement
#
# project-doc: render() escapes every field it interpolates into generated
# HTML, including declaration documentation strings pulled verbatim from
# source. Dropping the documentation field's escape call reopens the
# injection this suite exists to close.
project-doc|tools/nelisp_project_docs.py|s/escape(symbol\["documentation"\])/symbol["documentation"]/|declaration documentation text is no longer HTML-escaped before being embedded in the generated API page, so a docstring containing markup renders as live HTML instead of literal text
#
# project-manifest: publish_pair() gives the staged rollback copy of the
# manifest (original.toml) the same file mode as the live file before
# either replaces the other, so a failed lock publish can restore the
# manifest without narrowing its permissions. Chmod'ing the wrong staged
# file leaves the rollback copy at the stage directory's default mode.
project-manifest|tools/nelisp_project_manifest.py|s/original\.chmod(mode)/candidate.chmod(mode)/|the staged rollback copy of the manifest is never chmod'ed to match the live file; a failed lock publish then restores a manifest with the wrong permissions
#
# package-store: source_bytes() hashes every artifact, cached or freshly
# fetched, against its locked digest before returning it. Skipping that
# call lets a corrupted cache entry or a hash-mismatched download through
# unnoticed.
package-store|tools/nelisp_package_store.py|s/verify_artifact(locked, content)/pass  # verify_artifact(locked, content)/|the SHA-256 integrity check against the locked digest is skipped, so corrupted or hash-mismatched artifact bytes are cached and returned undetected
#
# artifact-service: _classify() maps nelisp_package_store's "missing cached
# package" message to the "missing" identity, so a caller can tell an
# offline cache miss apart from a corrupted or tampered artifact. Pointing
# that same message at "digest-mismatch" instead collapses the two cases.
artifact-service|tools/nelisp_artifact_service.py|s/("missing", re.compile(r"^missing cached package ")),/("digest-mismatch", re.compile(r"^missing cached package ")),/|an offline cache miss is classified as "digest-mismatch" instead of "missing", so a caller can no longer tell a missing artifact from a corrupted one or match it to the "run nelisp fetch" guidance
#
# package-registry: search_index() matches a query against package names
# case-insensitively. Comparing the raw query instead of its lowercased
# form makes the search reject any query whose case does not match the
# stored name.
package-registry|tools/nelisp_registry.py|s/if query\.lower() in name and available:/if query in name and available:/|registry search becomes case-sensitive, so a query that does not match a package name's exact case returns no results even when the package exists
#
# project-build: a release build strips native symbols down to the entry
# point; every other profile keeps them all. The build metadata's
# native_symbols field is what a debugger or profiler downstream trusts to
# tell which case it is looking at.
project-build|tools/nelisp-project.py|s/"native_symbols": "entry-only" if release else "all",/"native_symbols": "all",/|the build metadata always reports native_symbols as "all", so a release build's own record no longer reflects that it stripped symbols down to the entry point
#
# project-cli-integration: the full nelisp-project-cli-test.py suite,
# including ProjectCLI's real run/build/clean coverage that needs
# target/nelisp already built; project-cli-contract above stays scoped to
# ProjectResultContract, which needs no runtime build and can run earlier
# in CI. clean_project() removes each build profile's own named debug
# sidecar file; comparing against the wrong loop variable makes it check a
# directory the sidecar is never written to.
project-cli-integration|tools/nelisp-project.py|s/if directory == debug:/if directory == release:/|nelisp clean compares against the wrong build-profile directory, so a project's own target\/debug\/NAME.debug.json file survives cleaning instead of being removed
#
# project-test-runner: repeating the same --exact selector must not
# double-count that test in the reported total. The deduplication call
# around the selected-name list is what enforces that.
project-test-runner|tools/nelisp-project.py|s/selected = list(dict\.fromkeys(test_filter if isinstance(test_filter, list) else \[test_filter\])) if exact else None/selected = list(test_filter if isinstance(test_filter, list) else [test_filter]) if exact else None/|duplicate --exact selections are no longer deduplicated, so selecting the same test twice inflates its reported total instead of running it once
#
# project-test-runner: `--jobs N` shards the statically discovered test list
# across N processes and relies on comparing each shard's actual
# `nelisp-ert--tests` registrations back to that static list to catch a test
# static discovery could not see (declared through a macro or `eval`).
# Disabling that comparison lets such a shard run silently under-covered
# instead of failing the whole run with status "error".
project-test-runner|tools/nelisp-project.py|s/if any(r\["registered_ok"\] and set(r\["registered"\]) != static_name_set for r in results):/if any(False for r in results):/|--jobs no longer detects a shard that registered a test static discovery did not list, so a macro\/eval-defined test can be silently dropped from a sharded run instead of failing it
#
# project-bench: the reported median must be the statistical median of the
# recorded per-process samples, not an arbitrary one of them.
project-bench|tools/nelisp_project_bench.py|s/"median_ns": statistics\.median(durations),/"median_ns": min(durations),/|the reported median is the fastest sample instead of the statistical median, so the benchmark summary misrepresents typical process latency
#
# project-packages: nelisp remove resolves the reduced dependency graph
# from a synthetic catalog built out of the currently locked packages, with
# every field but the package's own name kept. Dropping a different field
# from that synthetic release produces one that fails the same release
# schema check every other release in the system is held to.
project-packages|tools/nelisp-project.py|s/if key != "name"/if key != "sha256"/|the synthetic catalog nelisp remove builds from the current lock strips each entry's sha256 field instead of its name field, so every remove operation fails release validation on a malformed synthetic entry
#
# project-profile-build: a profiled function's completed counter must
# reflect calls that actually returned, not merely calls that started, so a
# function that raised or the process exited mid-call is reported as
# incomplete rather than done.
project-profile-build|lisp/nelisp-project-profile.el|s/(when completed (aset row 2 (1+ (aref row 2))))/(aset row 2 (1+ (aref row 2)))/|the completed counter increments unconditionally on every call instead of only when the call actually returned, so a function that raised or the process exited mid-call still reports as completed
#
# project-debug: debug-info's symbol lookup returns exactly the declarations
# matching the requested name, filtering out the rest. Inverting that
# comparison returns everything except the requested symbol.
project-debug|tools/nelisp_project_debug.py|s/if name is not None and symbol\["symbol"\] != name:/if name is not None and symbol["symbol"] == name:/|debug-info's symbol name filter is inverted, so looking up one declared symbol returns every other declared symbol instead of the one actually requested
#
# project-arguments: application arguments must reach the running project
# as inert data, never as flags the runtime itself interprets. The `--`
# separator before the forwarded argument list is what keeps an argument
# like `--help` from being read as a flag to the runtime.
project-arguments|tools/nelisp-project.py|s/args = \[str(binary), "--load", str(bundle), "--", \*arguments\]/args = [str(binary), "--load", str(bundle), *arguments]/|the argv separator before forwarded application arguments is dropped, so a data argument that looks like a flag (for example --help) is parsed by the runtime instead of being handed to the project unchanged
#
# project-distribution: the installed bundle's README documents the path
# its own installed runtime actually lives at, so a relocated checkout's
# documented REPL command works as written.
project-distribution|tools/build-standalone-tarball.sh|s#STAGED_RUNTIME_PATH="libexec/nelisp-runtime"#STAGED_RUNTIME_PATH="bin/nelisp-runtime"#|the bundled README documents the runtime at the wrong relative path, so the documented NELISP_BIN=... command in a relocated installation points at a file that is not there
#
# ffi-dsl: turning nl-ffi-call's own "name not in the fixed extern table"
# fallback (a raw Lisp nil) into the named nl-ffi-unresolved-symbol
# condition is this package's whole reason to exist. Deleting the check
# reintroduces exactly the silent-nil bug ffi:defun/ffi-call are supposed
# to remove: an unresolved C symbol call quietly returns nil instead of
# signalling. Proven 2026-09-16: GATE-COUNT checked=7 findings=1 (RED,
# ffi-dsl-unresolved-symbol: no error signaled) with the mutation in
# place; checked=8 findings=0 (GREEN) restored.
ffi-dsl|packages/nl-ffi/src/nl-ffi.el|s/(when (null result)/(when nil/|nl-ffi--invoke stops turning an unresolved FFI symbol's raw nil into nl-ffi-unresolved-symbol, so a typo'd or unlinked C symbol name silently returns nil from the generated wrapper instead of signalling
#
# ffi-dsl (step 2, dlopen/dlsym): `nl-ffi--ptr-call-invoke' resolves a
# symbol the build-time table does not carry via `dlsym', then calls it
# through `ptr-call' -- but only after checking the resolved address is
# not zero (`dlsym'\='s own "not found" answer).  Dropping that check
# means an unresolved dlsym-path symbol is called through `ptr-call' at
# address 0 -- a call through a null function pointer, undefined
# behaviour that crashes the reader process rather than signalling
# `nl-ffi-unresolved-symbol'.  Proven 2026-09-16: with the mutation in
# place, `make ffi-dsl' dies with "Segmentation fault" on
# `ffi-dsl-dlsym-unresolved-symbol' before ever printing its GATE-COUNT
# line, so `tools/ai/nelisp-ai.sh gate ffi-dsl -- make ffi-dsl' reports
# status fail, ran=0, reason "no GATE-COUNT line; the command did not
# report what it checked (exit 2)" -- RED by a crash, not a clean
# finding count, which is itself the point: this check is what stands
# between an unresolved symbol and undefined behaviour.  Restored:
# GATE-COUNT checked=16 findings=0 skipped=0, status pass, ran=16
# (GREEN).
ffi-dsl|packages/nl-ffi/src/nl-ffi.el|s/(when (zerop addr)/(when nil/|nl-ffi--ptr-call-invoke stops checking a dlsym-resolved address for zero before calling it through ptr-call, so a symbol no declared library exports is called at address 0 (a null function pointer, undefined behaviour) instead of signalling nl-ffi-unresolved-symbol
#
# ffi-dsl (ptr-call-typed, the f64-capable sibling of ptr-call): the
# generated dispatch's f64-return arm reboxes the raw i64 bit pattern
# `extern-call-ptr-f64'/`f64-bits' produced back into a real Lisp float
# with `sexp-write-float'/`bits-to-f64' -- this row makes it box the raw
# bits as a plain integer instead, so every dlsym-resolved double-
# returning ffi:defun call (all 16 of the 32 generated SIG arms with an
# f64 return, not just one) returns a huge raw-bits integer instead of
# the real floating-point result.  Proven 2026-09-17: GATE-COUNT
# checked=17 findings=3 (RED -- ffi-dsl-ptr-call-typed-f64-arg-f64-
# return, -mixed-f64-int-args, and -int-args-f64-return, its three
# f64-return-using cases, all fail their `should' assertion; the
# f64-arg-int-return case, whose return is plain integer, is unaffected)
# with the mutation in place; checked=20 findings=0 (GREEN) restored.
ffi-dsl|scripts/nelisp-standalone-build.el|s/(seq (sexp-write-float out (bits-to-f64 pcret)) 0))/(wf_write_int out pcret))/|nelisp-standalone--build-ptr-call-typed-dispatch's f64-return arm boxes the raw i64 bit pattern as an integer instead of a real Lisp float, so a dlsym-resolved double-returning ffi:defun call returns nonsense instead of the actual floating-point result
#
# ffi-loader (FFI step 3 increment 2, pure-elisp ELF loader): the row moved
# from increment 1's line (`(when (= st-shndx 0)' in
# `nl-ffi-loader--apply-one-relocation') to increment 2's own equivalent
# guard, since increment 2 restructured undefined-symbol handling into
# `nl-ffi-loader--resolve-relocation-symbol' (a graph-wide search plus an
# STB_WEAK-only "resolve to 0" carve-out -- see that file's Commentary, "A
# real defect found while implementing").  Widening the `STB_WEAK'-only
# branch to match EVERY undefined symbol reproduces the exact same defect
# shape as increment 1's own row: a symbol this loader cannot resolve
# anywhere in the dependency graph (not just a legitimately-absent weak
# one) is silently treated as resolving to address 0 instead of refusing
# with `nl-ffi-loader-unsupported'. Proven 2026-09-16: GATE-COUNT
# checked=23 findings=1 (RED, ffi-loader-glob-dat-refuses-undefined-symbol:
# "no error signaled by (nl-ffi-loader--apply-one-relocation ...)") with
# the mutation in place; checked=24 findings=0 (GREEN) restored.  Row
# UNCHANGED through increment 3 (TLS): the targeted line is still exactly
# `((= l-bind nl-ffi-loader--stb-weak) 0)' in the same function, untouched
# by increment 3's new, separate `nl-ffi-loader--resolve-tpoff64'.  The
# test COUNT moved only because increment 3 added tests elsewhere.
# Re-proven 2026-09-17: GATE-COUNT checked=31 findings=1 (RED,
# ffi-loader-glob-dat-refuses-undefined-symbol: "no error signaled by
# (nl-ffi-loader--apply-one-relocation ...)") with the mutation in place;
# checked=32 findings=0 (GREEN) restored.
ffi-loader|packages/nl-ffi/src/nl-ffi-loader.el|s/((= l-bind nl-ffi-loader--stb-weak) 0)/(t 0)/|nl-ffi-loader--resolve-relocation-symbol stops refusing an R_X86_64_GLOB_DAT/R_X86_64_JUMP_SLOT relocation whose symbol is undefined and unresolved anywhere in the dependency graph, so a dependency this loader cannot load is silently treated as resolving to a null pointer instead of signalling nl-ffi-loader-unsupported|linux-x86_64
# nelisp-toml-standalone-smoke: added 2026-09-16 for packages/nelisp-toml.
# nelisp-toml--parse-bare-value's integer branch always returns 0 instead of
# the parsed token, so every TOML integer value silently becomes 0. A first
# attempt widening the leading-zero guard instead (`(> (length digits) 1)' ->
# `(>= (length digits) 0)') shipped UNREACHABLE per this file's own
# acceptance criteria: it only changes behavior for a bare "0", which no
# fixture or test in this package's corpus ever parses, and was caught by
# `make gate-mutation-verify' itself staying green -- replaced with this row
# rather than committed. Proven 2026-09-16 via
# `make gate-mutation-verify GATE=nelisp-toml-standalone-smoke': RED with the
# mutation in place -- "GATE-COUNT checked=37 findings=3" and
# "nelisp-toml-standalone-smoke: 3 failure(s), 37 passed" (a nonzero exit,
# since the standalone smoke `error's on any failure), naming
# nelisp-toml-parses-a-lock-with-packages and
# nelisp-toml-parses-empty-packages-array (both asserting `schema_version'
# reads back as 1, not 0) and nelisp-toml-parses-integer-array-across-lines-
# with-comment (expecting `(1 2 3)', getting `(0 0 0)'); restored to
# "GATE-COUNT checked=40 findings=0" / "PASS (40 tests)" (GREEN).
nelisp-toml-standalone-smoke|packages/nelisp-toml/src/nelisp-toml.el|s/(cons (string-to-number token) next)/(cons 0 next)/|nelisp-toml's integer branch returns a constant 0 instead of the parsed value, so every TOML integer (schema_version, array elements, etc.) silently becomes 0
# standalone-eval-test: had no row at all until 2026-09-17 (the
# standalone-eval-test link fix session). The generated driver writes the
# embedded form's two integer operands (NELISP_FORM_A/_B, default 1/2) into
# int1/int2 before building `(OP A B)'; feeding int2 the SAME value as int1
# instead of B makes the embedded form silently evaluate as `(OP A A)'
# instead of `(OP A B)' -- lethal whenever A != B, which the gate's own
# defaults (1 and 2) always are. Reachable every run (this line always
# executes to build the test form) and structural (a literal `,a' in place
# of `,b', not a timing-dependent effect). Proven 2026-09-17 via
# `make gate-mutation-verify GATE=standalone-eval-test': RED with the
# mutation in place -- "GATE-COUNT checked=1 findings=1" and
# "[standalone] FAIL: ... -> exit 2 (expected 3)" ((+ 1 1) instead of
# (+ 1 2)); restored to "GATE-COUNT checked=1 findings=0" / "[standalone]
# PASS: ... -> exit 3 (expected 3)" (GREEN).
standalone-eval-test|scripts/nelisp-standalone-build.el|s/(ptr-write-u64 int2 8 ,b)/(ptr-write-u64 int2 8 ,a)/|the driver writes operand A into int2 instead of B, so the embedded (OP A B) form silently evaluates as (OP A A) and the exit code no longer matches NELISP_FORM_A/_B

# parity-fuzz and emacs-compat: added 2026-09-17 (gate-wiring audit).  Both
# were real gates -- each prints a genuine `GATE-COUNT checked=N findings=M'
# line -- with no `gates.expected' entry, no mutation row, and no CI step;
# see tools/ai/gates.expected for the corresponding registration and
# target/ai/gate-wiring-report.md for the full audit.
#
# parity-fuzz's own generator draws from `docs/emacs-compat-table.txt' via a
# regex in `nelisp-parity-fuzz--names' that recognises lines starting
# "shared-shadowing " / "shared-deferring ".  If that regex stops matching
# (a table reformat, a typo in a refactor), `names' comes back empty and the
# fuzzer would otherwise report "0 names" as if that were a clean run --
# exactly the "checked=0 read as clean" failure this repository's other
# gates were built to rule out.  The tool already guards this itself
# (`(when (null names) ... (kill-emacs 1))'), so the injection proves that
# guard is still wired to the real parser rather than adding a new one.
# Proven 2026-09-17: clean, `make parity-fuzz' prints
# "parity-fuzz: seed=1 budget=1500 names=285" and "GATE-COUNT checked=1500
# findings=6" (exit 0 -- divergence findings are a reading job, not a
# failure; see the Makefile's own comment above this target). RED with the
# mutation (`shared-' -> `xshared-' in the regex, so it matches nothing):
# "parity-fuzz: FAIL (no shared names selected -- is
# docs/emacs-compat-table.txt present?)", exit 1. GREEN restored: identical
# to the clean run above, findings=6, exit 0.
parity-fuzz|tools/nelisp-parity-fuzz.el|s/"\^shared-/"\^xshared-/|the shared-name parser's regex stops matching the compat table, so the fuzzer silently draws from zero names instead of refusing to run

# emacs-compat: the tree-wide ratchet on how many stock Emacs names this
# repository shadows unconditionally (`tools/emacs-compat-baseline.txt').
# The injection adds one new unconditional top-level definition of a real
# stock Emacs name -- `xor' (`fboundp' t on host Emacs 30.1; confirmed
# absent as a top-level `defun'/`defalias' anywhere under the tool's own
# scan scope, `lisp/*.el src/*.el scripts/*.el packages/*/src/*.el', before
# this row existed) -- into an existing scanned file, right before its
# `provide' form.  A genuinely new unconditional shadow is exactly the
# defect class `shared-shadowing' exists to catch.
#
# CAVEAT of 2026-09-17, RESOLVED 2026-09-18.  Kept rather than deleted
# because the workaround it describes is no longer needed, and a reader who
# finds the isolation machinery referenced elsewhere should be able to see
# why it existed and why it is gone.
#
# When this row was written the tree was ALREADY `shared-shadowing 276'
# against baseline 268 (8 over, undocumented -- see
# target/ai/gate-wiring-report.md, which lives under the generated target/
# and will not survive), so `make emacs-compat' exited nonzero on a clean
# checkout for a reason unrelated to this row.  The harness's plain
# before/after exit-code check cannot separate this row's own contribution
# from a pre-existing failure, so the row was proven in ISOLATION instead:
# with `tools/emacs-compat-baseline.txt''s `shared-shadowing' line
# temporarily raised from 268 to 276 for the duration of the proof only,
# restored to 268 immediately after.
#
# Proven 2026-09-17, baseline temporarily 276: clean tree --
# "GATE-COUNT checked=8732 findings=276" / "emacs-compat: PASS" (GREEN,
# exit 0). RED with the injection -- "GATE-COUNT checked=8733
# findings=277" / "emacs-compat: FAIL (shared-shadowing 277 over baseline
# 276 -- ...)" (exit 1). Restored -- back to findings=276 / PASS (GREEN).
#
# The 8 are now gone: the eight `bool-vector-*' operations they were are
# wrapped in `(unless (fboundp ...))' in scripts/nelisp-stdlib-prelude.el,
# so a clean tree measures `shared-shadowing 268' against baseline 268 and
# `make emacs-compat' PASSES.  The isolation workaround is therefore
# obsolete and this row proves the ordinary way, against the real permanent
# baseline with nothing temporarily edited: `make gate-mutation-verify
# GATE=emacs-compat' on 2026-09-18 reported "emacs-compat: went red as it
# should", `passed=1 skipped=0', with `tools/emacs-compat-baseline.txt'
# byte-identical to HEAD afterwards and no injection left in the tree.
emacs-compat|lisp/nelisp-cc-alloc-dealloc.el|/^(provide 'nelisp-cc-alloc-dealloc)$/i\(defun xor (nl-gm-a nl-gm-b) (if nl-gm-a (not nl-gm-b) nl-gm-b))|a new unconditional definition shadows a stock Emacs name, pushing shared-shadowing over its baseline

# binary-size-ratchet (mutation coverage, 2026-09-17): `grep -c
# '^binary-size-ratchet|' tools/gate-mutations.txt' was 0 before this row --
# tools/ai/gates.expected records only a one-time hand check (a temporarily
# deflated `size' in the baseline file), nothing durable re-checked that the
# comparison still catches an oversized binary.  A row cannot mutate
# `tools/nelisp-binary-size-baseline.txt' (pinned to CI's toolchain; the
# brief that added this row forbids touching it) and, unlike unsafe-
# inventory/ns-inventory, cannot cheaply mutate the linked SOURCE either --
# a small source edit does not reliably move a linked binary's size across
# the 2% slack window, and would need a full rebuild besides.  The
# injection instead corrupts the CEILING ARITHMETIC in the Makefile recipe
# itself (`baseline + baseline * slack / 100' -> `baseline - baseline *
# slack / 100'), deflating the ceiling to roughly 98% of baseline.  This is
# lethal independent of the real binary's size: any binary at or above the
# pinned baseline -- the normal case, since the baseline IS the last real
# measurement -- now reads as over budget.  No rebuild needed: `Makefile'
# is not baked into `target/nelisp' (`gate_needs_rebuild' correctly says no
# for this row).
#
# CAVEAT, recorded rather than hidden, same shape as `emacs-compat' above:
# a genuinely FRESH local build in the worktree that authored this row
# already exceeded the pinned ceiling by itself (7,832,544 bytes against
# ceiling 7,786,916 -- the local/CI toolchain skew `tools/ai/preflight.sh'
# documents at length, not a regression; see
# target/ai/ratchet-freshness-report.md).  Both the clean and the mutated
# recipe therefore reported FAIL on that machine -- the mutation could not
# be told apart from the pre-existing skew by exit code alone there.  It
# WAS visible in the reported ceiling number (see below).
# `tools/nelisp-gate-mutation.sh' does not require a clean PASS before
# mutating a row (only `nl-num-standalone-smoke' gets that treatment) --
# it only requires the MUTATED run to exit nonzero -- which on THIS host
# it would have done with or without the injection, since the clean run
# already failed.  The harness's red is therefore not itself evidence
# for this row here.  The row is still lethal, but over a window rather
# than unconditionally: the injection deflates the ceiling from 7,786,916
# to 7,481,548, so a binary anywhere in [7,481,549, 7,786,916] flips
# PASS to FAIL -- 305,368 bytes, the whole range in which this gate
# legitimately passes.  Below 7,481,549 the mutated recipe passes too.
# Re-prove by running the harness on a host whose clean run passes.
#
# Proven 2026-09-17: `NELISP_GATE_MUTATION_ONLY=binary-size-ratchet
# tools/nelisp-gate-mutation.sh' reported the row going red, then its own
# "GATE-COUNT checked=1 findings=0 passed=1 failed=0 skipped=0" /
# "gate-mutation: PASS (1 passed, 0 skipped)".  By hand around the same
# command, on the fresh 7,832,544-byte binary above: clean -- "GATE-COUNT
# checked=1 findings=1" / "[binary-size-ratchet] FAIL: ./target/nelisp is
# 7832544 bytes, 198312 over baseline 7634232 -- exceeds ceiling 7786916
# (2% slack)."; mutated -- "GATE-COUNT checked=1 findings=1" / "... exceeds
# ceiling 7481548 (2% slack)." (the deflated ceiling, the row's visible
# effect); restored -- byte-identical to the pre-mutation Makefile (`git
# status` clean, `diff' empty).
binary-size-ratchet|Makefile|s#ceiling=\$\$(( baseline + baseline \* slack / 100 ))#ceiling=\$\$(( baseline - baseline \* slack / 100 ))#|the ceiling arithmetic subtracts the slack instead of adding it, deflating the ceiling below the pinned baseline

# standalone-reader-bignum-smoke: added this session (Doc 190 Phase C,
# feat/bignum-bitwise): `ash`/`logand`/`logior`/`logxor`/`lognot` accept a
# Bignum (tag 13) operand, and `/`/`%`/`mod` accept a Bignum DIVIDEND
# paired with a small fixnum divisor.  Seven rows, one per new code path,
# each proven RED-then-GREEN with `make gate-mutation-verify
# GATE=standalone-reader-bignum-smoke` before being committed here.
standalone-reader-bignum-smoke|scripts/nelisp-standalone-build.el|s/(if (= n0 2) 1 (if (= n0 13) 1 0))/(if (= n0 2) 1 0)/|`ash`'s dispatch entry stops accepting a Bignum N, so every Bignum-operand `ash` case reports `wrong-type-argument integerp` again instead of shifting
standalone-reader-bignum-smoke|scripts/nelisp-standalone-build.el|s/(nextcarry (if (= bitshift 0) 0 (shr v (- 32 bitshift))))/(nextcarry 0)/|`nl_bignum_shl_bits_loop` stops propagating the intra-limb carry on a left shift, so any `ash` left shift whose result crosses a 32-bit limb boundary silently drops the carried-out bits instead of promoting an exact wider value
standalone-reader-bignum-smoke|scripts/nelisp-standalone-build.el|s/(if (= tg 2) 1 (if (= tg 13) 1 0))/(if (= tg 2) 1 0)/|`bf_first_non_integer_or_bignum` stops treating a Bignum as an integer, so `logand`/`logior`/`logxor` with a Bignum operand report `wrong-type-argument integer-or-marker-p` again instead of computing the bitwise result
standalone-reader-bignum-smoke|scripts/nelisp-standalone-build.el|s/(bf_lognot_general (wf_arg_ptr args 0) out)/(bf_wrong_type_integerp (wf_arg_ptr args 0))/|`lognot`'s Bignum-operand arm is replaced with the fixnum-only type error, so `(lognot BIGNUM)` signals `wrong-type-argument integerp` again instead of computing -X-1
standalone-reader-bignum-smoke|scripts/nelisp-standalone-build.el|s/(logxor (if (< i dec_count) (ptr-read-u32 dec_ptr (\* i 4)) 0) 4294967295)/(logxor (if (< i dec_count) (ptr-read-u32 dec_ptr (* i 4)) 0) 4294967294)/|`nl_bignum_twos_fill_neg`'s bit-complement mask is off by one bit, so every AND/OR/XOR with a NEGATIVE Bignum operand computes a wrong two's-complement window and returns the wrong value instead of signalling anything
standalone-reader-bignum-smoke|scripts/nelisp-standalone-build.el|s/(if (<= mag 2147483648) 1 0)/(if (<= mag 0) 1 0)/|`nl_bignum_small_divisor_p` stops accepting any nonzero divisor, so every Bignum-dividend `/`/`%`/`mod` case (even a tiny divisor like 256) signals `nelisp-bignum-division-unsupported` instead of computing the exact answer
standalone-reader-bignum-smoke|scripts/nelisp-standalone-build.el|s/(nl_alloc_symbol sbuf 34 268435480)/(nl_alloc_symbol sbuf 0 268435480)/|`bf_signal_bignum_division_unsupported` writes a zero-length condition symbol, so `condition-case` handlers keyed on `nelisp-bignum-division-unsupported` (a genuine Bignum divisor to `/`/`%`/`mod`) no longer catch it
